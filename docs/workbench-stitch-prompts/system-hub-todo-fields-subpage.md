[Context]
UNIVIEW 固定资产管理系统，System Operation Hub / 系统运营中枢。请复刻 IMAGE2 产品图 `flow-platform-subpage-06-todo-fields-v2.png` 的待办字段显示配置台。该页用于配置流程待办列表和钉钉 H5 待办可显示字段。

目标：用户可以新建字段方案、维护显示字段、配置排序/筛选、桌面布局、H5 布局，保存草稿并提交校验。

[Layout]
Desktop 1586x992。顶部深色导航，顶部导航为“固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢”，仅系统运营中枢高亮。左侧系统运营中枢导航展开流程平台，待办字段配置高亮。顶部导航和左侧导航都不要搜索，搜索只出现在当前页面内容区。

主区标题“待办字段显示配置台”，操作条包含：新建方案、新增字段、保存草稿、预览效果、提交校验。下方显示操作反馈条：预览已生成 · 桌面 8 字段 / H5 5 字段 · 字段权限与脱敏通过。

核心区域三栏：
左栏：字段方案列表，顶部有页面内搜索“搜索方案 / 流程 / 责任人”，包含“资产入账待办 / CIP 转固待办 / 资产报废待办 / 转移交接待办”，显示业务类型、负责人、字段数和状态。
中栏：字段方案工作台。上方摘要显示“方案字段 8 / 桌面显示 8 / H5显示 5 / 待补权限 1”。中部主表“待办字段清单”列包含字段名称、字段编码、来源、桌面、H5、固定列、宽度、筛选、脱敏、状态、操作；选中“金额 amount”行并提示待补权限。下方并排显示桌面待办列表预览和钉钉 H5 待办卡片预览，明确一套字段定义、两端不同布局。
右栏：字段属性配置，当前字段为“金额 amount”，包含字段名称、字段编码、来源字段、显示名称、桌面显示、H5显示、固定列、宽度、排序、筛选方式、权限范围、脱敏规则。底部发布校验 5/6，权限范围待确认用橙色提示。

[Components]
后台操作台，表格主导，紧凑行高，浅色面板、细边框、蓝色主按钮、绿色通过态、橙色待补充态。不要做报表 KPI 首页。

易用性要求：
- 桌面待办和 H5 待办预览必须同屏，体现“一套字段定义，两端布局”。
- 可以看到 CIP 项目编号、转固金额、风险等级、SLA 到期时间等字段。
- 发布门禁 checklist 覆盖字段顺序、桌面/H5 布局、权限范围、敏感字段脱敏、宽度不溢出和默认排序。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
