[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 消息与通知 / 通知渠道。请复刻 IMAGE2 产品图 `notification-subpage-06-notification-channels-v2.png` 的后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝导航中「系统运营中枢」高亮。左侧系统配置导航展开「消息与通知」，选中「通知渠道」；左侧不放搜索。

主区标题「通知渠道配置台」。右上角按钮：新建渠道、保存草稿、提交校验、发送测试。标题下有测试结果反馈条和校验状态反馈条，显示钉钉 H5 工作通知发送测试成功、12 项校验通过。

主体上半区左右分栏。左侧「通知渠道列表」带页面内搜索和筛选，表格列为渠道名称、渠道类型、触达范围、限流策略、降级策略、状态、操作；行包含钉钉 H5 工作通知、站内通知、邮件通知渠道、短信备用通知。右侧「渠道参数编辑区」字段：渠道名称、渠道编码、渠道类型、Webhook/接口地址、认证方式、签名密钥状态、限流阈值、失败降级、负责人、审计要求。

底部四块：渠道健康矩阵、强制通知策略、降级路由、发布校验清单。降级路由从钉钉 H5 到站内消息渠道再到邮件通知渠道，标注成功/失败路径。发布校验清单保留绿色通过项，并以橙色提示「短信备用未配置」。

[Components]
强调渠道可测试、可降级、可审计。白色高密后台、蓝色主按钮、绿色成功、橙色注意、红色异常。圆角不超过 8px。不要装饰图，不要大 KPI。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 查看触达审计必须在当前页生成流程引用、变量快照、渠道降级、偏好继承和发送日志记录，不得跳到通用预览弹窗。
