[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 系统参数 / 文件存储配置。请复刻 IMAGE2 产品图 `system-params-subpage-03-file-storage-v2.png` 的后台配置页。

[Layout]
Desktop 1595x986。顶部深蓝工作台导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的存储策略列表筛选栏。左侧系统配置导航展开「系统参数」，选中「文件存储配置」；左侧导航不放搜索。

主区标题「文件存储与缩略图配置台」。标题右侧是「新建存储策略」「保存草稿」「提交校验」「连接测试」。下方四个摘要卡：存储策略、缩略图队列、附件容量、异常任务。

主体上半区左右分栏。左侧大表格「文件存储策略列表」带页面内搜索和筛选，列为策略名称、策略键、文件范围、存储值、风险等级、状态、操作；行包含资产附件主桶、CIP 验收附件归档桶、缩略图生成策略、Office 预览策略、安全扫描规则。右侧「文件存储策略编排」包含存储后端、附件目录、缩略图策略、预览格式、归档生命周期、访问权限、安全检查、失败回退，并展示完成度 8/8 与保存草稿、提交校验、测试连接。

底部四块：存储拓扑、缩略图队列、预览格式与安全扫描、归档与失败回退。每块都要像真实可操作后台，含连接状态、进度、支持格式、安全扫描、归档策略、失败回退统计。

[Components]
后台配置页、表格主导、右侧编辑。浅色页面、蓝色主操作、绿色成功、橙色警告、红色异常。不要全局搜索框，不要大面积渐变，不要报表 KPI 大屏。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 查看发布审计必须在当前页生成参数变更、影响预览、发布门禁、回滚记录和操作审计记录，不得跳到通用预览弹窗。
