[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 系统参数 / 基础参数。请复刻 IMAGE2 产品图 `system-params-subpage-01-base-params-v2.png` 的后台配置页。

[Layout]
Desktop 1603x981。顶部深蓝工作台导航中「系统运营中枢」高亮，文案必须准确，不要写成「系统运营中板」或其他相近错字。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的列表筛选栏。左侧系统配置导航展开「系统参数」，选中「基础参数」；左侧导航不放搜索。

主区标题「基础参数运行配置台」。标题右侧是「新建参数」「保存草稿」「提交校验」「影响预览」。下方四个摘要卡：参数项 64、待复核 6、回滚点全部开启、最近发布 06-16 12:26。

主体上半区左右分栏。左侧大表格「基础参数列表」有页面内搜索「搜索参数名称/参数键」和参数域、状态、风险筛选；列为参数名称、参数键、参数域、参数值、风险等级、状态、操作；行包含附件大小与格式、编号预览默认策略、工作台表格密度、盘点周期默认值、CIP 转固附件上限。右侧「基础参数编辑区」包含参数名称、参数域、参数键、影响范围、默认值、回滚方案、允许值、发布门禁、负责人复核、审计说明，并提供「保存草稿」「提交校验」。

底部四块：影响拓扑、发布校验清单、参数版本记录、回滚预案。影响拓扑用节点串起 Workbench、CIP、导入导出、钉钉 H5；校验清单显示唯一性、格式、依赖关系、影响范围和风险规则；版本记录展示 V1.2.0、V1.1.0、V1.0.0；回滚预案提供最近稳定版本和自定义版本。

[Components]
表格主导，右侧编辑区可快速保存/提交。现代中文 B2B 后台，白色面板、细分割线、克制蓝/青/绿/橙状态，圆角不超过 8px。不要营销页，不要 KPI 大屏，不要渐变大背景。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 查看发布审计必须在当前页生成参数变更、影响预览、发布门禁、回滚记录和操作审计记录，不得跳到通用预览弹窗。
