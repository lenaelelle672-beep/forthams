[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 组织权限 / 工作交接。请基于当前 Workbench 信息架构和 IMAGE2 产品图 `org-permission-subpage-05-handover-v2.png` 生成高保真桌面后台页。这个页面用于离职、调岗、代理、资产责任人、流程待办、岗位权限、角色权限和数据权限交接，是专用工作台，不能是通用配置模板。

不要做 KPI 大屏，不要做营销页，不要使用制造管控语义。所有可见文案必须为中文。

[Layout]
Desktop 1586x992。顶部深蓝导航：资产运营总览 / 数据监控中心 / 资产运维中心 / 风险预警中心 / 系统运营中枢，系统运营中枢高亮。左侧为系统运营中枢导航，组织权限分组展开，工作交接高亮；相邻入口包含用户管理、角色权限、菜单权限、数据权限。左侧导航上方不要放搜索框，搜索只放在页面专属筛选栏。

主区域标题「工作交接配置与模拟」。标题右侧操作：新建交接批次、扫描待办、保存草稿、提交模拟、提交生效。

标题下方必须有搜索筛选栏：
- 搜索：转出人、接收人、批次、资产编号、流程
- 筛选：交接类型、状态、来源、截止时间

主体是交接操作工作台：
1. 左侧「交接批次列表」：高志明离职交接批次、李敏调岗交接批次、临时代理批次、CIP 项目负责人交接。每项显示转出人、接收人、状态、风险。
2. 中央上方「交接对象明细」表格，列：交接对象、类型、当前责任人、接收人、数量、风险、状态、操作。行包含资产责任人、流程待办、岗位权限、角色权限包、数据权限范围、审批代理、CIP 项目负责人。
3. 中央下方「扫描影响概览 + 交接模拟流程」：扫描组织/EHR状态、汇总资产责任、汇总流程待办、汇总权限回收、CIP 项目负责人变更、跳过转出人环节、接收人确认、生效发布。显示进度、阻断项、风险门禁和提示项。
4. 右侧「高志明离职交接批次」属性面板，字段：转出人、接收人、交接原因、生效日期、跳过转出人、交接范围、通知方式、审计说明。按钮：保存批次、运行模拟、提交生效、查看审计。
5. 右下「模拟结果汇总」：可交接、阻断、跳过、生效门禁；并显示最近审计记录。
6. 底部「交接审计记录」台账：点击任意「查看审计」后必须在当前页追加范围审计或批次审计，记录扫描条件、接收确认、跳过原因、生效门禁和审计冻结证据，不允许打开通用占位弹窗。

[Components]
视觉保持高保真企业后台：深蓝顶部、白色/浅蓝灰面板、1px 细边框、8px 以内圆角、紧凑表格、流程时间线、状态 pill、蓝色主按钮、橙/红风险提示。

必须体现易用性：
- 交接批次、交接对象、模拟流程和属性编辑必须同屏联动
- 离职/调岗场景必须支持跳过转出人环节
- 阻断项和生效门禁必须明显，避免错误交接
- 保存草稿、提交模拟、提交生效路径清楚
- 不出现英文菜单，不出现泛泛的“配置项模板”

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 「查看审计」必须在当前工作交接页面生成交接审计记录，至少包含扫描条件、接收确认、跳过原因、生效门禁和审计冻结证据。
