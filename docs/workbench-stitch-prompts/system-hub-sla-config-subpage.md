[Context]
UNIVIEW 固定资产管理系统，System Operation Hub / 系统运营中枢。请复刻 IMAGE2 产品图 `flow-platform-subpage-07-sla-config-v2.png` 的 SLA 时限策略配置台。页面用于固定资产审批时限、超时提醒和升级规则。

目标：用户能新建 SLA、配置审批时限、提醒策略、升级路径、豁免条件、模拟升级、保存草稿、提交校验。

[Layout]
Desktop 1586x992。顶部深色导航，顶部导航为“固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢”，仅系统运营中枢高亮。左侧系统运营中枢导航展开流程平台，SLA 配置高亮。顶部导航和左侧导航都不要搜索，搜索只出现在当前页面内容区。

主区标题“SLA 时限策略配置台”，操作条包含：新建 SLA、保存草稿、模拟升级、查看超时、提交校验。下方显示操作反馈条：SLA 升级模拟已完成 · CIP 专员审核 24 小时升级至 CIP 负责人 · 通知渠道 3/4 已启用。

核心区域三栏：
左栏：SLA 规则列表，顶部有页面内搜索“搜索规则 / 流程 / 节点”，包含“资产入账审批 SLA / CIP 专员审核 SLA / CIP 财务复核 SLA / 报废赔偿 SLA / 合同变更 SLA / 新建 SLA 规则”，显示适用流程、节点、状态、首响和处理时限。
中栏：SLA 控制台。上方摘要显示“SLA 总数 6 / 启用规则 5 / 待复核 2 / 超时待处理 9”。主表“SLA 规则明细”列包含规则名称、流程、节点、启用、首响、处理时限、提醒策略、升级路径、豁免、状态、操作。中部是超时升级路径：节点到达 -> 首响提醒 6h -> 处理到期 24h -> 升级闭环 CIP 负责人，并并排显示当前超时模拟队列。底部是通知渠道矩阵和发布门禁矩阵。
右栏：SLA 属性配置，当前为“CIP 专员审核 SLA”，包含 SLA 名称、适用流程、适用节点、首响时限、处理时限、提醒策略、升级路径、豁免条件、启用 SLA、允许豁免，以及保存规则、模拟升级、查看审计操作。

[Components]
企业后台高保真，表格和配置面板为主。使用深色顶部栏、白色面板、浅蓝灰底、蓝色主按钮、橙色超时、绿色通过。圆角不超过 8px，避免大装饰。

易用性要求：
- 时限、提醒、升级、豁免在同一面板完成。
- 超时升级路径可视化但不抢占表格主导地位。
- 发布门禁显示“规则启用 / 流程节点 / 通知渠道 / 升级路径 / 豁免条件 / 审计留痕”，其中审计留痕待确认用橙色提示。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
