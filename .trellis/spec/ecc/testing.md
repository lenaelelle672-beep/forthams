# Testing Rules (ECC distilled)

## Coverage

- Minimum 80% line coverage on service/domain logic
- Skip trivial getters/config classes; focus effort on critical paths and complex logic
- Three levels, all expected for real features: unit, integration, E2E (critical flows)

## TDD loop (used with the `tdd` skill)

1. Write the failing test first (RED) — confirm it fails
2. Write minimal implementation (GREEN) — confirm it passes
3. Refactor belongs to the review stage, not the red-green cycle
4. One seam, one test, one minimal implementation per cycle — vertical slices,
   never "all tests first, then all code"

## What a good test is

- Verifies behavior through public interfaces, not implementation details
- Expected values come from an independent source of truth (spec, worked example),
  never recomputed the same way the code computes them
- Survives refactors: if a test breaks when behavior didn't change, it's coupled
  to implementation — fix the test

## Structure and naming

- Arrange-Act-Assert structure
- Names describe behavior: `returns empty array when no markets match query`,
  `throws error when API key is missing`

## When tests fail

- Check test isolation and mock correctness first
- Fix the implementation, not the test — unless the test itself is wrong
- Never delete or skip a failing test to make the suite green

## Start the stack (do not wait for the human)

See `.trellis/spec/dwk/dev-runtime.md`. Probe health, start missing frontend/backend from README, then test. Browser visual paths may use Browser Harness; Playwright stays the CI contract.

## Playwright E2E (forthAMS)

- `frontend/playwright.config.ts` collects `src/e2e` and `tests/e2e`. Loadability (`playwright test --list`) is not the same as a green suite.
- Specs that cannot parse, mix vitest with Playwright, or import missing/Graphify modules go in `testIgnore`. Do not add fake modules to satisfy imports.
- Anchor selectors to routed pages (`src/router/index.tsx` → `src/pages/**`), not unmounted `src/app/pages/**`.
- Auth seed must write `auth_token` / `user_info` (`src/utils/auth.ts`). Seeding only `ams_auth_token` leaves the guard on `/login`.
- Login: heading `欢迎回来` (desktop) / `登录系统` (narrow); username `#username`; submit `登录系统`. Never `getByLabel('用户名')`.
- Post-login land is `/fixed-assets/workbench?menu=home`, not `/dashboard`. Approval list is `/approvals` with h1 `审批中心`. Asset list h1 is `资产台账`.
- Workbench 真页 heading 合同（`workbench-platform-entry.browser-regression-smoke.spec.ts`）：home `运营首页`、todo `审批中心`、asset `/资产/`、device `重要设备管理`、orders `资产处置管理`、inspection `检验/年检管理`、spares `备品备件管理`、energy `能耗管理`、report `报表中心`、alarm `通知中心`、policy `风险矩阵`、settings `资产分类管理`。禁止再断言 mock 列表/抽屉文案。
- 该 spec 用 `--workers=1`。多 worker 会触发 Vite `Failed to fetch dynamically imported module`（尤其 `EnergyDashboardPage`）。
- `mockApi` 默认 `paged([])` 不能喂给：`/energy/dashboard`（对象 `{byType,trend,assetRanking,total}`）、`/risk-assessments/matrix`（数组）、`/categories/tree`（数组）。否则真页无 heading 或 `forEach` 崩溃。`assetRanking`、备件详情 `usages`、检验详情 `history` 必须 `Array.isArray` 再 `.map`。能耗 `byType`/`trend` 喂数组时当 `{}`。风险矩阵、自定义字段 `fieldOptions`、转移单 `depts/tree` 纯对象不崩。TCO mock 用 `**/api/**/tco/**`，勿拦 Vite `TcoPage` chunk。
- AppLayout 列表补测 `src/e2e/app-pages-smoke.spec.ts`：下列路径必须是 **数组** 不是 paged：`/contracts/expiring`、`/stocktaking/cycles`、`/menus/admin/tree`、`/fault-codes/tree`、`/reliability/trend|ranking`、`/asset-health/unhealthy`、`/gis/assets`、`/manufacturers/options`、`/system/custom-fieldsets/all`、`/maintenance/upcoming`。`/reliability/summary`、`/gis/stats`、`/licenses/summary` 必须是对象（喂数组不崩）。`/fault-codes/tree`、`/reliability/trend|ranking`、`/asset-health/unhealthy`、`/licenses/expiring` 喂纯对象不崩。报表构建器 stats `.slice/.map` 前 `Array.isArray`。ReportsPage `/reports/depreciation-stats|maintenance-stats|retirement-stats` 与 `/workorders/status-distribution|dept-pending` 喂纯对象不崩（`.map` 前 `Array.isArray`）。AnalyticsPage `/dashboard/maintenance-stats` 期望对象，喂数组当 undefined。`useGisAssets`、RFID `surplusItems`/`deficitItems`、审计 `changes`、工单 `attachments` 非数组时先 `Array.isArray`。资产详情 `log.changes`、转移单 `assignees/preview` 的 `nodes`/`missingFields`、设备页 `/maintenance/upcoming` 同理。测转移单 preview 须同时 mock `start-availability` `canStart: true`，否则 query 不发。风险矩阵编辑 `probabilityDimension`/`severityDimension`/`levelMapping` 与导入导出 `asset-categories/tree`、`asset-locations/cascade` 的 `children` 喂对象不崩。个人资料 `roles`/`permissions`、平面图 `/floor-plans/:id/assets` 喂对象不崩。`/workorders/1`、`/budgets/1` 的 `page.route` 只用 `**/api/**`，勿拦文档导航。
- 空态/403/失败态：`src/e2e/app-pages-empty-error.spec.ts`。失败态只测页面会渲染「加载失败」的路径；Dashboard KPI 失败不显示该文案，不要硬断言。桌面 `/inspection-templates`、`/inventory/cycle-count`、`/inventory/abc-classification` 空表文案分别为「暂无检验模板」「暂无循环盘点规则」「暂无资产分类数据」（antd `locale.emptyText`）。`/sam` 另有「暂无扫描历史」。`/contracts` 须点 tab「即将到期」才见「暂无即将到期合同」，点「时间轴视图」才见「暂无时间线数据」；`/budgets` 须点「超支告警」才见「暂无超支告警」，点「执行率」才见「暂无执行率数据」。`/inventory/smart-report/:id` 空态为「暂无差异资产，盘点结果正常」。`/inventory/tasks/*/summary` 必须喂对象 `{surplusCount,deficitCount,surplusItems,deficitItems}`，默认 `paged([])` 会让 DonutChart 收到 NaN（console warning）。`/assets/:id` 「暂无 TCO 数据」须把 `/tco/asset/:id` mock 成 `null`（paged 对象为 truthy 会走成本构成而非该空态）；TCO 路由用 `**/api/**/tco/**` 勿拦 Vite `TcoPage` chunk。`/approvals` 须点「发起申请」才见「暂无可发起的流程」。`/audit/:id` 默认 paged 详情仍渲染「暂无变更记录」（normalizeAuditLog 不带 changes）。采购无独立 `/purchase-orders/:id` 路由：列表 mock 一条再点行，详情须 `{order, items:[]}` 才见「暂无明细」。检验历史在 `/inspection-records`（不是 `/inspections`）点「历史」，列表须有 `assetId`。`/disposals/clearance/new` 默认「暂未选择资产」，点「添加资产」才见「暂无匹配资产」。`/purchase-orders` 须 mock 一条订单再点行，详情 `{order, items:[]}` 才见「暂无明细」。`/inspection-records` 须 mock 一条含 `assetId` 的检验再点「历史」才见「暂无历史记录」。`/gis` 点「资产定位管理」默认关联模式见「暂无可关联的资产」。`/sam` 图表区另有「暂无数据」。DonutChart 计数须 `Number(x) || 0`。`/assets/:id` 默认 paged TCO 为 truthy 时趋势列见「暂无趋势数据」。`/workflows` 预定义流程无 server 时详情见「暂无发布快照」。`/reports` 点「资产分类统计」（role=button）才见「暂无图表数据」；「资产汇总表」在 summary 为 paged 对象时会拼出 4 个点，不是空态。`/intake/:id` 默认 paged 详情可见「暂无入库资产」。`/system/custom-fieldsets` 须 mock 一条再点「查看字段」才见「该字段集暂无字段」。`/audit` 须点「筛选」才见「暂无筛选项」。`/vendor-portal` 是独立登录非 AppLayout，admin 会话看不到「暂无合同数据」。`/audit` 趋势按 7 天补零，`trendData.length` 恒为 7，「暂无趋势数据」不可达。`/maintenance/plans` 「暂无维保计划数据」须点日历视图。`/disposals` 默认清退 tab；点「资产调拨」「报废转让」才见对应 emptyText。`/compensation/new` 在 `/depts/tree` 为空时见「暂无可选部门」。`/sam` 须 mock 一条 `/sam/history` 再点「查看详情」，`/sam/:id/details` 喂 `{details:[]}` 才见「暂无详情」。`/system/roles` 须 mock `/roles/list` 一条再点「菜单权限」才见「暂无菜单数据」，点「数据权限」才见「暂无部门数据」（DialogTitle console 忽略）。`/assets/:id/timeline` 另有「该资产暂无任何履历事件」。`/floorplans` 另有「请新建平面图」。领用/借用新建「请选择资产」是 hidden option，不可见。`/assets` EmptyState 另有「未找到符合条件的资产记录，请调整筛选条件或新建资产」。`/approvals` EmptyState 另有「当前没有待处理的审批事项」。`/notifications` 全部 tab 另有「所有通知都会显示在这里」；点「系统通知」见「当前筛选条件下没有通知」。`/gis` 另有「没有已定位的资产可在地图上显示」。`/energy` dashboard 空对象时另有「尚未采集到能耗数据」。`/sam` 图表描述另有「执行合规扫描后显示许可类型分布」「执行合规扫描后显示席位使用率」「所有许可合规运行」。`/fault-codes` 另有「点击「新增根节点」创建第一级故障现象」。`/categories` 另有「点击上方按钮添加根分类」。`/reports` 点资产分类统计另有「当前报表暂无可用数据」；资产状态分布有图点不是空态。`/report-builder` 未选字段见「将字段拖拽到此处，或点击左侧字段添加」。`/assets/import-export` 点导出 tab 见「请选择资产分类」；TreeSelect 用 `treeCheckable` 不是 `treeCheck`。`/locations` 空树可见「新增顶级位置」。`/assets/import-export` 导出 tab 另有「请选择资产状态（可多选）」「请选择存放位置」。`/system/depts` 空树可见「新增部门」。`/assets/import-export` 导入 tab 见「将 .xlsx 文件拖到此处，或点击选择文件」「支持 .xlsx 格式，文件大小不超过 10MB」。`/system/posts` 空表可见「新增岗位」。`/assets/import-export` 导入 tab 另有「下载导入模板」。`/system/menus` 空树可见「新增菜单」。`/system/depts` 另有「请选择一个部门查看详情」。`/spare-parts` 可见「新增备件」。`/insurances` 可见「新增保险」。`/borrows` 可见「新建借用单」。`/assignments` 可见「新建领用单」。`/intake` 可见「新建验收单」。`/assets` EmptyState 可见「新建资产」。`/budgets` 可见「新增预算」。`/revaluations` 可见「新增减值/重估」。`/retirement` 可见「新建退役申请」。`/inspections` 可见「新增检验」。`/maintenance` 可见「新增维保」。`/stocktaking-cycles` 可见「新建周期」。`/maintenance/plans` 可见「新建计划」。`/vendors` 可见「新增供应商」。`/purchase-orders` 可见「新增采购单」。`/manufacturers` 可见「新增制造商」。`/contracts` 可见「新增合同」。`/asset-models` 可见「新增模型」。`/licenses` 可见「新增许可证」。`/fault-codes` 可见「新增根节点」。`/floorplans` 可见按钮「新建」。`/inventory` 可见「新建任务」。`/inventory/cycle-count` 可见「新增规则」。`/categories` 可见「添加根分类」（EmptyState 内「添加分类」因 action 形状不对不可达）。`/inspection-templates` 可见「新增模板」。`/reports/scheduled` 可见「新建定时报表」。`/inspection-records` 可见「新增检验」。`/disposals` 默认清退 tab 可见「新建资产清退」。`/risk-assessments` 可见「新增评估」。`/safety-checklists/config` 可见「新增模板」。`/disposals` 点「资产调拨」见「新建资产调拨」，点「报废转让」见「新建报废转让」，点「资产赔偿」见「新建资产赔偿」，点「工单管理」见「新建工单管理」。`/workflows` 可见「新建流程」。`/equipment` 可见「新建维保」；点后 dialog 见「新建维保记录」（DialogTitle console 忽略）。`/sam` 可见「触发合规扫描」。`/reports/scheduled` 另有「点击"新建定时报表"开始创建」。`/gis` 点「资产定位管理」可见「新建资产定位」「关联已有资产」。`/notifications` 可见「全部已读」。`/gis` 定位弹窗关联模式见台账提示与搜索 placeholder；点「新建资产定位」见临时数据提示与名称/编号/纬度/经度/位置 placeholder 及「确认创建」。关联模式见「标注坐标」「取消」。`/depreciation` 可见「批量计算折旧」。`/inventory/abc-classification` 可见「批量重新分类」。`/idle` 可见副标题「闲置资产公告发布与认领流程管理」。`/inventory/abc-classification` 另有「根据资产原值和分类规则自动分类。」。`/depreciation` 另有「待计算资产」「刷新」。`/inventory/abc-classification` 另有「未分类资产」「导出报告」「未匹配任何规则的资产会标记为未分类。」。空表无法点「批量计算折旧」确认框。`/inventory/abc-classification` 另有「A 类资产」；点「批量重新分类」见「确认批量重新分类」。`/idle` 可见「闲置总量」。`/inventory/abc-classification` 另有「B 类资产」「C 类资产」；确认框正文「此操作将根据当前的循环盘点规则重新分类所有资产。」。ABC 确认框另有「操作可能需要较长时间，请确认是否继续？」；卡片可见「总价值」。`/idle` 列可见「闲置天数」。`/bigscreen` 可见「资产运营分析平台」「值班领导」「值班经理」「资产运行分析」「今日资产信息」「资产异常分析」「今日不正常情况明细」「收入运力信息」「人员信息」「承运情况分析」「油量信息」「重点关注航班信息」（WebGL console 忽略）。`/bigscreen-3d` 可见「资产规模指标」「资产分类结构」「价值趋势预测」「设备在线总览」「城市资产TOP5」「风险异常队列」（WebGL console 忽略）。`/categories` 未选节点见「选择分类」「请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类」。`/floorplans` 空列表见「请选择平面图」。`/intake/new` 见「填写验收信息和入库资产」。`/spare-parts/new` 见「按维保工单、库存下限和供应商交期创建备件保障记录」。`/workflows` 搜索 placeholder「搜索流程名称、编码、说明或业务对象」。`/budgets/new` 见「创建新的预算记录」。`/revaluations/new` 见「资产价值调整申请」。`/analytics/reliability` 见「MTBF/MTTR/可用性/故障率分析」。`/analytics/tco` 见「全生命周期成本总览与分析」。`/disposals/scrap/new` 见「发起资产报废处置流程，提交后将进入审批环节。」。`/disposals/transfer/new` 见「发起部门或位置之间的正式资产转移申请。」。`/analytics/tco` 查询 placeholder「输入资产ID」「输入部门ID」「输入分类ID」。`/manufacturers` 搜索「搜索名称/编码」。`/contracts` 搜索「搜索合同名称/编号」。`/insurances` 搜索「保单号/保险名称/保险公司」。`/asset-models` 搜索「搜索名称 / 型号」。`/assignments` 搜索「搜索编号、名称...」。`/borrows` 搜索「搜索用途/备注...」。`/approvals` 搜索「搜索编号、标题或发起人」。`/maintenance` 搜索「按资产ID搜索...」。`/maintenance/plans` 搜索「搜索计划名称...」。`/licenses` 搜索「搜索软件名称/厂商」。`/spare-parts` 搜索「搜索备件编码/名称...」。`/audit` 搜索「搜索操作记录...」。`/equipment` 搜索「搜索设备名称或编号...」。`/system/posts` 搜索「搜索岗位编码、名称或备注」。`/system/custom-fields` 搜索「搜索字段名或显示名...」。`/system/depts` 搜索「搜索部门名称、编码、负责人...」。`/system/custom-fieldsets` 搜索「搜索字段集名称」。`/vendors` 搜索「搜索供应商名称、编码、联系人...」。`/budgets/new`「采购预算」是 native `<select>` 的 hidden `<option>`，`getByText('采购预算').toBeVisible()` 会超时；用 `locator('select').filter({ hasText: '采购预算' })` 断言 select 可见。
- 补真实 `/:resource/:id/edit` 路由后，必须从 `app-pages-empty-error`「未测路由 / 404」未挂载列表移除该 path，否则已认证访问会误断言落到 /404（Q1812：`/budgets/1/edit`）。
- antd 双汉字按钮 accessible name 中间有空格（「更 新」「取 消」「扫 码」）。空态用 `getByRole('button', { name: /更\s*新/ })`，禁止 `name: '更新'`。
- native `<select>` 的 `<option>` 对 Playwright `toBeVisible` 为 hidden（Q1897：`/stocktaking-cycles/new`「全盘点」）。测可见 label / placeholder / 按钮，不要测 option 文本。
- antd `Select` 的 placeholder 不是 input placeholder（Q1914：`getByPlaceholder('请选择')` 找不到）。用 `InputNumber`/`TextArea` 真 placeholder，或 `getByText`。
- 登录变体：`src/e2e/login-variants-smoke.spec.ts`。`/login5` 走 `Login4Page` 且 h1 可能不是 role=heading，用 `region` landmark「UNIVIEW 固定资产平台登录」。
- `locations`/`departments` 树展平必须 `Array.isArray` 再 `for...of`（`InventoryDetailPage`）；`paged({})` 当树会 `items is not iterable`。桌面 `/locations`、`/system/menus`、`/system/depts` tree 与 `/stocktaking/cycles` 喂纯对象时 heading 仍在、无 pageerror；`/gis/stats` 喂数组时同理。树节点 `children` 为对象时（locations/categories/fault-codes/depts/assets）须先 `Array.isArray` 再递归。预算 `alerts`/`execRates` 同理。
- 列表页 `data.records` 必须 `Array.isArray` 后再给表格（许可证/设备/备件/供应商/领用/借用/入库/采购/制造商/检验/折旧/合同/报表/菜单树/部门树）。`records` 为对象时 heading 仍在、无 pageerror。桌面 e2e：`app-pages-smoke` grep `records 非数组`（不含 `/m`）。`/asset-models` 的 `manufacturers/options` 与 `categories/all`、入库详情 `checkItems`/`intakeAssets`、采购详情 `items` 同样先 `Array.isArray`。
- `/analytics/health` 空态不渲染表头（`unhealthyList` 空只显示「暂无数据」）；饼图三档 value 全 0 时 Pie label 不可靠。Q1978 切 `/analytics/reliability` 测「可靠性分析」「MTBF」「MTTR」。排名表空同样无列头。
- textarea 的 `aria-label` 对 `getByText` 不可见（Q2015：`/workflow-designer`「业务数据 JSON」）。用 `getByLabel`。
- `/energy` 默认 mock 对象在 `!effectiveData` 时只渲染 EmptyState「暂无能耗数据／尚未采集到能耗数据」，主界面「能耗监控与数据分析」「总能耗」不可见（Q2026）。
- workbenchv3 桌面空态 Q3261+：流程控制台用 `mockCommandCenterEmpty`（`/workflows` 必须数组）；流程设计器用 `mockFlowDesignerEmpty`（`/workflows` 与 `*/versions` 必须数组）；表单配置用 `mockFormDefinitionsEmpty`（`/form-definitions` 必须数组）；资产分类可用默认 `mockApi`（`/categories/tree` 已是 `[]`）。部门组织用 `mockDeptsListEmpty`（`/depts/list` 必须数组，否则 flatten 崩进 V3 error boundary）；位置用 `mockLocationsListEmpty`；供应商用 `mockVendorsListEmpty`。通知偏好用 `mockNotificationPreferencesEmpty`；通知渠道用 `mockNotificationChannelsEmpty`（`channelTypes` 必须数组）。编号规则另拦 `**/numbering-rules*` 用 `mockNumberingRulesEmpty`。SLA 用 `mockSlaConfigEmptyCatalog`。Q3271–Q3474 覆盖控制台至交接/技术支持边角。Q3475–Q3479 覆盖用户管理未用角色。Q3480–Q3502 覆盖部门组织/位置/供应商/岗位余项与基础参数未用角色。Q3503–Q3523 覆盖邮件模板/邮件日志/通知模板未用角色。Q3524–Q3539 覆盖通知偏好/渠道/自定义字段余项。Q3540–Q3544 覆盖字段集未用角色。Q3545–Q3552 覆盖编号规则/SLA 余项（已绿）。Q3553–Q3556 覆盖邮件网关 TLS/landmark/表单余项（已绿）。Q3557–Q3564 覆盖安全策略 landmark/hint.key/边界余项（已绿）。Q3565–Q3572 覆盖文件存储/基础参数/邮件网关/资产分类余项（已绿）。Q3573–Q3584 覆盖审批规则表达式边界与六域侧栏描述（已绿；审批规则须 `mockApprovalRulesEmpty`，接口须 `mockSystemInterfacesEmpty`）。Q3585–Q3596 覆盖六域侧栏剩余描述（已绿）。Q3597–Q3608 覆盖侧栏预览旗标、页头与页脚模块编码（已绿）。Q3609–Q3617 覆盖页脚剩余模块编码与六域已接入计数（已绿）。Q3618 覆盖子项导航 complementary 与同步规则说明余项（已绿）。Q3619–Q3626 覆盖表单存储/待办字段/审批规则闭环提示与通知开关/审计/偏好/编号规则/岗位未用 landmark（已绿）。Q3627–Q3634 覆盖通知模板/渠道/邮件模板/邮件日志/自定义字段/字段集/角色/菜单未用 note（已绿）。Q3635–Q3642 覆盖审计 note 与编号规则/渠道/岗位/SLA/邮件日志/自定义字段/字段集预览字段余项（已绿）。Q3643–Q3650 覆盖同步规则失败计数、SLA 脱敏余项与渠道/偏好/Webhook 未用角色（已绿；Webhook 须 `mockWebhookConfigsEmpty`）。Q3651–Q3658 覆盖运行监控只读边界、表单存储/待办字段/审批规则余项（已绿）。Q3659–Q3666 覆盖交接/文档中心/技术支持只读边界余项（已绿）。Q3667–Q3674 覆盖租户/数据权限只读与写入边界余项（已绿）。Q3675–Q3682 覆盖导入导出/用户管理检索与写操作边界余项（已绿）。Q3683–Q3690 覆盖位置/供应商检索说明余项（已绿；位置须 `mockLocationsListEmpty`，供应商须 `mockVendorsListEmpty`）。Q3691–Q3698 覆盖部门组织 list/tree 检索说明余项（已绿；须 `mockDeptsListEmpty`）。Q3699–Q3706 覆盖外部系统目录/掩码余项与流程设计器处理人预览未用角色（已绿；外部系统须 `mockExternalSystemsEmpty`）。Q3707–Q3714 覆盖文件存储 catalog/上传下载边界余项（已绿；须 `mockFileStorageCatalogEmpty`）。Q3715–Q3722 覆盖审批规则表达式白名单/禁止引擎余项（已绿；须 `mockApprovalRulesEmpty`）。Q3723–Q3730 覆盖表单存储 uploads/脱敏/归档余项（已绿；须 `mockFormStorageEmpty`）。Q3731–Q3738 覆盖待办字段预览/审计/租户隔离余项（已绿；须 `mockTodoFieldsEmpty`）。Q3739–Q3746 覆盖字段映射转换白名单、接口管理元数据校验与菜单权限 catalog 余项（已绿；字段映射须 `mockSystemFieldMappingsEmpty`，接口须 `mockSystemInterfacesEmpty`，菜单须 `mockRolePermissionsEmpty`）。Q3747–Q3754 覆盖角色权限 catalog、数据权限 dataScope、运行监控 SLA 路径与审计导出端点余项（已绿；角色须 `mockRolePermissionsEmpty`，数据权限须 `mockDataPermissionsEmpty`，运行监控须 `mockRuntimeMonitorEmpty`，审计须 `mockAuditLogsEmpty`）。Q3755–Q3762 覆盖运行监控 timeout-records/审批实例摘要、审计 stats/trends/meta/GET 族与岗位 posts 端点/metadata-only 标志（已绿；运行监控须 `mockRuntimeMonitorEmpty`，审计须 `mockAuditLogsEmpty`，岗位可用默认 `mockApi`）。Q3763–Q3770 覆盖运行监控整句、审计 {id}/导出限制、岗位 preview 字段、缓存白名单与流程控制台只读聚合（已绿；缓存须 `mockCacheNamespacesEmpty`，控制台须 `mockCommandCenterEmpty`）。Q3771–Q3778 覆盖控制台长路径、租户整句、技术支持/导入导出/交接/文档/流程邮件说明余项（已绿）。Q3779–Q3786 覆盖用户检索整句、基础参数 DEGRADED、资产分类 list/tree、安全策略 no-login-chain、控制台整段与交接写边界（已绿）。Q3787–Q3794 覆盖 SLA 端点/安全边界、审批规则白名单整句、文档/技术支持写边界与流程邮件零调用风险（已绿；SLA 须 `mockSlaConfigEmptyCatalog`，审批规则须 `mockApprovalRulesEmpty`）。Q3795–Q3802 覆盖通知偏好/渠道端点与编号规则 catalog 边界（已绿；偏好须 `mockNotificationPreferencesEmpty`，渠道须 `mockNotificationChannelsEmpty`，编号规则须双 route `mockNumberingRulesEmpty`）。Q3803–Q3810 覆盖邮件/通知模板端点、流程通知开关 biz-type 与邮件网关 metadata-only（已绿；开关须 `mockNotificationSwitchesEmpty`）。Q3811–Q3818 覆盖邮件日志端点/管线、自定义字段与字段集端点族（已绿；自定义字段须 `mockCustomFieldsEmptyCatalog`）。Q3819–Q3826 覆盖字段集 preview 诊断与自定义字段加密/schema 边界（已绿）。Q3827–Q3834 覆盖表单配置端点/schema 白名单、流程设计器 draft/fail-closed、Webhook config-only、同步规则队列摘要、流程定义 /workflows 目录与文件存储生命周期闭环（已绿；表单配置须 `mockFormDefinitionsEmpty`，设计器须 `mockFlowDesignerEmpty`，Webhook 须 `mockWebhookConfigsEmpty`，同步规则须 `mockSystemSyncRulesEmpty`，流程定义须 `mockWorkflowDefinitionsEmpty`，文件存储须 `mockFileStorageCatalogEmpty`）。Q3835–Q3842 覆盖表单配置同屏/H5、流程设计器画布、控制台只读风险、SLA 安全边界与交接未闭环整句（已绿）。Q3843–Q3850 覆盖安全策略 no-login-chain、流程邮件/文档写边界、控制台 SLA 9/9、部门/位置/供应商检索整句（已绿）。Q3851–Q3858 覆盖技术支持/租户/导入导出写边界、编号规则端点/preview、缓存白名单与基础参数 SYSTEM/DEGRADED 整句（已绿）。Q3859–Q3866 覆盖岗位/邮件模板/通知偏好/渠道/开关端点与边界整句（已绿）。Q3867–Q3874 覆盖邮件网关/邮件日志/通知模板端点与边界整句（已绿）。Q3875–Q3882 覆盖邮件日志端点+管线、自定义字段/字段集边界、审计 GET 族与只读导出边界（已绿；自定义字段须 `mockCustomFieldsEmptyCatalog`，审计须 `mockAuditLogsEmpty`）。Q3883–Q3890 覆盖邮件日志/偏好/字段集/通知开关/渠道未用 getByLabel 与 coverage 整句（已绿；偏好须 `mockNotificationPreferencesEmpty`，开关须 `mockNotificationSwitchesEmpty`，渠道须 `mockNotificationChannelsEmpty`，编号规则须双 route `mockNumberingRulesEmpty`）。Q3891–Q3898 覆盖邮件日志 redaction、自定义字段 preview、邮件/通知模板 previewPolicy 整句（已绿；自定义字段须 `mockCustomFieldsEmptyCatalog`）。Q3899–Q3906 覆盖审计采集/导出整句与表单配置/流程设计器 fail-closed 整句（已绿；审计须 `mockAuditLogsEmpty`，表单配置须 `mockFormDefinitionsEmpty`，设计器须 `mockFlowDesignerEmpty`）。Q3907–Q3914 覆盖工单/退役/验收/风险/处置新建未用 textbox/button/link/heading（已绿；可用默认 `mockApi`）。Q3915–Q3918 覆盖借用 heading 与退役/报废/清退/转移未用余句（已绿；报废默认 sell 为 medium 风险条，勿测 low 文案）。Q3919–Q3926 覆盖工单/资产/退役未用角色、报废/转移空资产整句与金额标签（已绿；可用默认 `mockApi`）。Q3927–Q3928 覆盖退役列表未用指标余句与借用/领用/验收未用 button（已绿；可用默认 `mockApi`）。Q3929 覆盖保险/备件未用 button（已绿；可用默认 `mockApi`）。Q3930–Q3937 覆盖 GIS/重估/风险/平面图/许可证/制造商/故障/ABC/转移/工单/循环盘点/位置/SAM 未用 heading/button/整句（已绿；位置须 `mockLocationsListEmpty`，其余可用默认 `mockApi`）。Q3938–Q3943 覆盖 ABC/SAM/安全检查/报表构建器/导入导出/循环盘点未用 heading/button/余句（已绿；可用默认 `mockApi`）。Q3944–Q3948 覆盖设备/分类/TCO/健康评分/安全历史/风险矩阵/报表未用 heading/button（已绿；勿测 `/dashboard` 默认 mock 会因 trends 非数组崩；其余可用默认 `mockApi`）。Q3949 覆盖定时报表/可靠性未用 button/heading（已绿；可用默认 `mockApi`）。Q3950 覆盖 SAM/健康评分未用 heading（已绿；可用默认 `mockApi`）。Q3951 覆盖 GIS/能耗 EmptyState 与预算年度未用 heading（已绿；可用默认 `mockApi`）。Q3952–Q3953 覆盖平面图/SAM/导入导出/分类未用 heading/button（已绿；可用默认 `mockApi`；EmptyState JSX action 不渲染「添加分类」）。Q3954–Q3955 覆盖资产列表 EmptyState 与报表中心未用卡片 heading（已绿；可用默认 `mockApi`）。Q3956 覆盖报表中心未用 heading/button（已绿；可用默认 `mockApi`）。Q3957 覆盖合同未用类型筛选 button（已绿；可用默认 `mockApi`；Radix tab 不是 button）。Q3958–Q3960 覆盖合同/闲置/采购未用筛选 button（已绿；可用默认 `mockApi`）。Q3961–Q3965 覆盖采购/领用/借用/维保未用筛选 button（已绿；可用默认 `mockApi`）。Q3966–Q3969 覆盖退役/重估/折旧/处置未用 heading/button（已绿；可用默认 `mockApi`）。Q3970–Q3981 覆盖保险/设备/合同/处置/审批/盘点周期/检验/备件/预算/验收未用 heading/button（已绿；保险路由是 `/insurances`；可用默认 `mockApi`）。Q3982–Q3989 覆盖资产/维保计划/通知/盘点/许可证/检验记录/审计/TCO/GIS 未用 heading/button（已绿；可用默认 `mockApi`）。Q3990–Q4001 覆盖履历/盘点报告/个人/盘点周期/健康/维保/流程中心/审批/风险/通知未用 heading/button（已绿；可用默认 `mockApi`；盘点报告 MissingTaskId 与个人页空 payload 回退已修）。Q4002–Q4005 覆盖流程分页/KPI 副文案与工单详情/验收未用 heading/button（待跑；可用默认 `mockApi`；工单详情空审批改为 h3）。Q4006–Q4007 覆盖安全检查禁用筛选、借用/领用详情 heading 与 forbidden/个人未用角色（已绿；个人页 CardTitle 补中文 fallback）。Q4008 覆盖个人角色权限、流程当前选中与退役详情 heading（已绿；流程「当前选中流程」改为 h3）。Q4009–Q4010 覆盖退役详情分区 heading（已绿；可用默认 `mockApi`）。Q4011 覆盖退役详情空态 heading「暂无关联资产/暂无审批历史」（已绿）。Q4012–Q4024 覆盖盘点报告/任务/RFID/处置详情/流程表单/领用空态未用 heading/button/link（已绿）。Q4025–Q4030 覆盖审计详情/资产详情未用 heading/button/link（已绿）。Q4031–Q4033 覆盖检验/保险详情未用 heading（已绿）。Q4034 覆盖健康评分/测试结果未用空态 heading（已绿）。Q4035 覆盖备件详情未用空态 heading（已绿；可用默认 `mockApi`；暂无领用记录改为 heading）。Q4036–Q4043 覆盖 TCO/验收/健康/资产详情/可靠性 KPI/盘点盘盈亏/通知/流程表单未用空态 heading（已绿；可用默认 `mockApi`；TCO/验收/健康/资产/可靠性/盘点/通知空态改为 heading；KPI MTBF/MTTR 须 exact 以免命中「MTBF/MTTR 趋势」）。Q4044–Q4049 覆盖资产新建/检验上传/通知/资产成本分区/位置空态与流程表单未用 heading（已绿；位置须 `mockLocationsListEmpty`，其余可用默认 `mockApi`）。Q4050–Q4055 覆盖定时报表/风险矩阵/盘点周期/供应商/报废/审计/分析图表空态与流程表单未用 heading（已绿；可用默认 `mockApi`；`/analytics` 默认 mock 的 summary 为 truthy 对象，处置图不走「暂无统计数据」）。Q4056–Q4059 覆盖检验上传/资产趋势分区与流程表单未用 heading/button（已绿；资产「趋势 (近12个月)」须 exact 以免命中已测 TCO 趋势 heading）。Q4060–Q4062 覆盖检验上传分区 heading 与流程表单未用编码（已绿；可用默认 `mockApi`）。Q4063 覆盖报废/调拨未用空态 heading（已绿；可用默认 `mockApi`；「暂无已选资产」已测，改测报废/调拨整句后半段）。Q4064–Q4068 覆盖盘点报告差异空态、故障代码提示、平面图未选、检验选择照片、调拨已选 0 项、清退暂未选择资产与流程表单未用编码（已绿；可用默认 `mockApi`）。Q4069–Q4072 覆盖报表构建器预览空态/生成预览 button 与分析 KPI heading（已绿；可用默认 `mockApi`）。Q4073–Q4074 覆盖分析页副文案/处置统计/数据范围与报表构建器保存/已选字段（已绿；可用默认 `mockApi`）。Q4075–Q4082 覆盖报表构建器配置 heading/图表与字段 button、导入导出拖拽空态 heading 与流程表单未用编码（已绿；可用默认 `mockApi`）。Q4083–Q4090 覆盖报表构建器说明/字段分组、分析 KPI 副文案与值、部门管理空态 heading 与流程表单未用编码（已绿；可用默认 `mockApi`）。Q4091–Q4098 覆盖位置/供应商未用 heading、报表字段 button、位置/部门工作台根空态与流程表单未用编码（已绿；位置须 `mockLocationsListEmpty`，部门工作台须 `mockDeptsListEmpty`）。Q4099–Q4106 覆盖制造商/许可证/合同/分类/型号副文案与工作台「请选择」空态 heading（已绿；自定义字段须 `mockCustomFieldsEmptyCatalog`，审计须 `mockAuditLogsEmpty`）。Q4107–Q4114 覆盖 SLA/审批规则/流程定义/待办字段/表单存储/角色权限/运行监控/分类工作台「暂无」空态 heading（已绿；须对应专用 mock）。Q4115–Q4122 覆盖设计器/表单配置/邮件通知模板/同步规则/接口「暂无」空态 heading（已绿）。Q4123–Q4130 覆盖通知偏好/编号规则/Webhook/通知渠道/外部系统/字段映射/缓存「暂无」空态 heading（已绿；编号规则须双 route `mockNumberingRulesEmpty`）。Q4131–Q4138 覆盖许可证/合同/制造商统计 heading 与自定义字段/字段集/基础参数/分类/邮件日志/岗位/网关/通知开关空态 heading（已绿；自定义字段须 `mockCustomFieldsEmptyCatalog`，通知开关须 `mockNotificationSwitchesEmpty`）。Q4139–Q4146 覆盖分类/安全检查/风险评估统计 heading 与控制台/运行监控/审计空态及流程表单未用编码（已绿；勿测表单配置「暂无可配置字段」——默认 schema 有字段，属交互后区域）。Q4147–Q4154 覆盖控制台/运行监控/安全检查/风险/审计/SLA 未用统计 heading（已绿；SLA 须 `mockSlaConfigEmptyCatalog`；短名「通过/已启用/SLA 超时」须 exact）。Q4155–Q4162 覆盖角色/数据权限/文件存储/租户/技术支持/文档/导入导出/交接/流程邮件未用统计 heading（已绿；履历空列表提前返回，勿测全部/错误/警告/信息事件 KPI）。Q4163–Q4170 覆盖菜单权限/SAM/采购/型号/数据权限边界/租户只读提示/表单存储摘要与流程表单未用编码（已绿）。Q4171–Q4178 覆盖重要设备统计/筛选 heading、表单存储「暂无字段摘要/附件引用」与流程表单未用编码（已绿；表单存储须 `mockFormStorageEmpty`；勿把设备「即将到期」改成 heading，已绿合同 exact）。Q4179–Q4186 覆盖闲置资产统计/副文案、安全检查/风险/设备/SAM/采购/GIS/平面图/表单配置预览与通知计数 heading（已绿；表单配置须 `mockFormDefinitionsEmpty`；GIS 只测页头副文案）。Q4187–Q4194 覆盖验收/维保计划/定时报表/健康评分/资产健康/审计/风险矩阵配置/流程中心/盘点周期/退役/报表中心/检验副文案与 KPI heading（已绿；可用默认 `mockApi`；「健康资产」须 exact 以免命中「不健康资产 Top 20」）。Q4195–Q4202 覆盖履历空事件、表单配置同屏说明与工作台只读副文案 heading（已绿）。Q4203–Q4210 覆盖运行监控/SLA/审计/同步规则/审批规则/流程定义/待办字段/位置/部门/用户/供应商/控制台/表单存储/设计器/安全策略副文案 heading（已绿；须对应专用 mock）。Q4211–Q4218 覆盖邮件网关/日志/通知开关/自定义字段/岗位/字段集/渠道/偏好/编号规则/邮件模板/通知模板/基础参数副文案 heading（已绿）。Q4219–Q4226 覆盖保险/维保/折旧/借用/领用未用统计 heading（已绿）。Q4227–Q4234 覆盖备件/处置/故障/部门/领用已签收/折旧徽标 heading（已绿；部门须 `mockDeptsListEmpty`；「备件/折旧/已签收」须 exact；勿再测已绿「已启用」）。Q4235–Q4242 覆盖重估/盘点/预算/审批/故障/处置/领用/资产未用统计 heading（已绿；「审批/领用/RFID/已通过/已拒绝/已驳回/已使用/已承诺/执行率」须 exact；勿再测已绿「任务总数/待审批/已完成/总记录」）。Q4243–Q4250 覆盖资产/领用/部门/岗位/用户/角色/检验/菜单/盘点周期未用统计 heading（已绿；部门须 `mockDeptsListEmpty`，角色/菜单须 `mockRolePermissionsEmpty`；「闲置率/待处理/已归还/角色数/岗位数/当前页/总页数/待检验/目录/菜单/按钮/进行中/已计划」须 exact；勿再测已绿「角色总数/通过/不通过/已过期/已完成/正常/停用」）。Q4251–Q4258 覆盖菜单/角色/岗位/用户徽标与重估处置预算副文案、借用统计及流程表单未用编码（已绿；角色/菜单须 `mockRolePermissionsEmpty`；「总计/树形/岗位/用户/已借出/已逾期」须 exact；勿再测已绿「暂无数据/待审批」）。Q4259–Q4266 覆盖列表眉题与用户/菜单/岗位/角色分区 heading、预算占比及流程表单未用编码（已绿；角色/菜单须 `mockRolePermissionsEmpty`；占比空态是 0.0%；勿再测已绿「用户列表/资产列表/故障代码」）。Q4267–Q4274 覆盖处置默认页资产清退 heading 与流程表单未用编码（已绿；默认 tab 是 CLEARANCE；「资产清退」须 exact；勿再测已绿「资产清退列表」）。Q4275–Q4282 覆盖重估/预算/盘点周期/检验/折旧/清退/维保/保险/借用/角色/岗位/备件/用户/审批表格空态 heading 与流程表单未用编码（已绿；角色须 `mockRolePermissionsEmpty`；勿再测已绿「暂无领用记录/暂无资产/暂无故障代码/暂无盘点任务」）。Q4283–Q4290 覆盖退役/验收/设备/SAM/部门/菜单空态 heading 与流程表单未用编码（已绿；部门须 `mockDeptsListEmpty`，菜单须 `mockRolePermissionsEmpty`；「暂无维保记录」须 exact；勿再测已绿「暂无维保记录，点击「新增」开始创建/扫描历史/最近维保记录」）。Q4291–Q4298 覆盖 EmptyState 描述/自定义字段/部门空态提示 heading 与流程表单未用编码（已绿；部门须 `mockDeptsListEmpty`；勿再测已绿「暂无资产/暂无平面图/暂无分类/选择分类/暂无数据/暂无高风险项」）。Q4299–Q4306 覆盖退役统计/空态副文案 heading 与流程表单未用编码（已绿；「待审核」须 exact；勿再测已绿「管理资产生命周期终止、处置流程和退役记录。」）。Q4307–Q4314 覆盖 GIS/能耗空态描述、角色菜单空态 heading 与流程表单未用编码（已绿；「暂无菜单数据」须 exact；勿再测已绿「暂无资产定位数据/暂无能耗数据/暂无部门数据」）。Q4315–Q4322 覆盖工作台空表 emptyMessage heading 与流程表单未用编码（已绿；供应商须 `mockVendorsListEmpty`，位置须 `mockLocationsListEmpty`，部门须 `mockDeptsListEmpty`，文件存储须 `mockFileStorageCatalogEmpty`，流程定义须 `mockWorkflowDefinitionsEmpty`，角色/菜单须 `mockRolePermissionsEmpty`，数据权限须 `mockDataPermissionsEmpty`）。Q4323–Q4330 覆盖流程表单未用编码 heading（已绿）。Q4331–Q4338 覆盖流程表单未用编码 heading（已绿）。Q4339–Q4346 覆盖流程表单未用编码 heading（已绿）。Q4347–Q4354 覆盖流程表单未用编码 heading（已绿）。Q4355–Q4370 覆盖流程表单未用编码 heading（已绿）。Q4371–Q4394 覆盖流程表单未用编码 heading（已绿）。Q4395–Q4426 覆盖流程表单未用编码 heading（已绿）。Q4427–Q4458 覆盖流程表单未用编码 heading（已绿）。Q4459–Q4490 覆盖流程表单未用编码 heading（已绿）。Q4491–Q4522 覆盖流程表单未用编码 heading（已绿）。Q4523–Q4554 覆盖流程表单未用编码 heading（已绿）。Q4555–Q4586 覆盖流程表单未用编码 heading（已绿）。Q4587–Q4618 覆盖流程表单未用编码 heading（已绿）。Q4619–Q4650 覆盖流程表单未用编码 heading（已绿）。Q4651–Q4682 覆盖流程表单未用编码 heading（已绿）。Q4683–Q4714 覆盖流程表单未用编码 heading（已绿）。Q4715–Q4746 覆盖流程表单未用编码 heading（已绿）。Q4747–Q4778 覆盖流程表单未用编码 heading（已绿）。Q4779–Q4810 覆盖流程表单未用编码 heading（已绿）。Q4811–Q4842 覆盖流程表单未用编码 heading（已绿）。Q4843–Q4874 覆盖流程表单未用编码 heading（已绿）。Q4875–Q4906 覆盖流程表单未用编码 heading（已绿）。Q4907–Q4938 覆盖流程表单未用编码 heading（已绿）。Q4939–Q4970 覆盖流程表单未用编码 heading（已绿）。Q4971–Q5002 覆盖流程表单未用编码 heading（已绿）。Q5003–Q5034 覆盖流程表单未用编码 heading（已绿）。Q5035–Q5066 覆盖流程表单未用编码 heading（已绿）。Q5067–Q5098 覆盖流程表单未用编码 heading（已绿）。Q5099–Q5130 覆盖流程表单未用编码 heading（已绿）。Q5131–Q5162 覆盖流程表单未用编码 heading（已绿）。Q5163–Q5194 覆盖流程表单未用编码 heading（已绿）。Q5195–Q5226 覆盖流程表单未用编码 heading（已绿）。Q5227–Q5258 覆盖流程表单未用编码 heading（已绿）。Q5259–Q5290 覆盖流程表单未用编码 heading（已绿）。Q5291–Q5322 覆盖流程表单未用编码 heading（已绿）。Q5323–Q5354 覆盖流程表单未用编码 heading（已绿）。Q5355–Q5386 覆盖流程表单未用编码 heading（已绿）。Q5387–Q5418 覆盖流程表单未用编码 heading（已绿）。Q5419–Q5450 覆盖流程表单未用编码 heading（已绿）。Q5451–Q5482 覆盖流程表单未用编码 heading（已绿）。Q5483–Q5514 覆盖流程表单未用编码 heading（已绿）。Q5515–Q5546 覆盖流程表单未用编码 heading（已绿）。Q5547–Q5578 覆盖流程表单未用编码 heading（已绿）。Q5579–Q5610 覆盖流程表单未用编码 heading（已绿）。Q5611–Q5642 覆盖流程表单未用编码 heading（已绿）。Q5643–Q5674 覆盖流程表单未用编码 heading（已绿）。Q5675–Q5706 覆盖流程表单未用编码 heading（已绿）。Q5707–Q5738 覆盖流程表单未用编码 heading（已绿）。Q5739–Q5770 覆盖流程表单未用编码 heading（已绿）。Q5771–Q5802 覆盖流程表单未用编码 heading（已绿）。Q5803–Q5834 覆盖流程表单未用编码 heading（已绿）。Q5835–Q5866 覆盖流程表单未用编码 heading（已绿）。Q5867–Q5898 覆盖流程表单未用编码 heading（已绿）。Q5899–Q5930 覆盖流程表单未用编码 heading（已绿）。Q5931–Q5962 覆盖流程表单未用编码 heading（已绿）。Q5963–Q5994 覆盖流程表单未用编码 heading（已绿）。Q5995–Q6026 覆盖流程表单未用编码 heading（已绿）。Q6027–Q6058 覆盖流程表单未用编码 heading（已绿）。Q6059–Q6090 覆盖流程表单未用编码 heading（已绿）。Q6091–Q6122 覆盖流程表单未用编码 heading（已绿）。Q6123–Q6154 覆盖流程表单未用编码 heading（已绿）。Q6155–Q6186 覆盖流程表单未用编码 heading（已绿）。Q6187–Q6218 覆盖流程表单未用编码 heading（已绿）。Q6219–Q6250 覆盖流程表单未用编码 heading（已绿）。Q6251–Q6282 覆盖流程表单未用编码 heading（已绿）。Q6283–Q6314 覆盖流程表单未用编码 heading（已绿）。Q6315–Q6346 覆盖流程表单未用编码 heading（已绿）。Q6347–Q6378 覆盖流程表单未用编码 heading（已绿）。Q6379–Q6410 覆盖流程表单未用编码 heading（已绿）。Q6411–Q6442 覆盖流程表单未用编码 heading（已绿）。Q6443–Q6474 覆盖流程表单未用编码 heading（已绿）。Q6475–Q6506 覆盖流程表单未用编码 heading（已绿）。Q6507–Q6538 覆盖流程表单未用编码 heading（已绿）。Q6539–Q6570 覆盖流程表单未用编码 heading（已绿）。Q6571–Q6602 覆盖流程表单未用编码 heading（已绿）。Q6603–Q6634 覆盖流程表单未用编码 heading（已绿）。Q6635–Q6666 覆盖流程表单未用编码 heading（已绿）。Q6667–Q6698 覆盖流程表单未用编码 heading（已绿）。Q6699–Q6730 覆盖流程表单未用编码 heading（已绿）。Q6731–Q6762 覆盖流程表单未用编码 heading（已绿）。Q6763–Q6794 覆盖流程表单未用编码 heading（已绿）。Q6795–Q6826 覆盖流程表单未用编码 heading（已绿）。Q6827–Q6858 覆盖流程表单未用编码 heading（已绿）。Q6859–Q6890 覆盖流程表单未用编码 heading（已绿）。Q6891–Q6922 覆盖流程表单未用编码 heading（已绿）。Q6923–Q6954 覆盖流程表单未用编码 heading（已绿）。Q6955–Q6986 覆盖流程表单未用编码 heading（已绿）。Q6987–Q7018 覆盖流程表单未用编码 heading（已绿）。Q7019–Q7050 覆盖流程表单未用编码 heading（已绿）。Q7051–Q7082 覆盖流程表单未用编码 heading（已绿）。Q7083–Q7114 覆盖流程表单未用编码 heading（已绿）。Q7115–Q7146 覆盖流程表单未用编码 heading（已绿）。Q7147–Q7178 覆盖流程表单未用编码 heading（已绿）。Q7179–Q7210 覆盖流程表单未用编码 heading（已绿）。Q7211–Q7242 覆盖流程表单未用编码 heading（已绿）。Q7243–Q7274 覆盖流程表单未用编码 heading（已绿）。Q7275–Q7306 覆盖流程表单未用编码 heading（已绿）。Q7307–Q7338 覆盖流程表单未用编码 heading（已绿）。Q7339–Q7370 覆盖流程表单未用编码 heading（已绿）。Q7371–Q7402 覆盖流程表单未用编码 heading（已绿）。Q7403–Q7434 覆盖流程表单未用编码 heading（已绿）。Q7435–Q7466 覆盖流程表单未用编码 heading（已绿）。Q7467–Q7498 覆盖流程表单未用编码 heading（已绿）。Q7499–Q7530 覆盖流程表单未用编码 heading（已绿）。Q7531–Q7562 覆盖流程表单未用编码 heading（已绿）。Q7563–Q7594 覆盖流程表单未用编码 heading（已绿）。Q7595–Q7626 覆盖流程表单未用编码 heading（待跑）。禁 `/m`。下一票 Q7627。
