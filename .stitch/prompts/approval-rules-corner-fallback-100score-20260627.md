Edit the selected uploaded IMAGE screen into one persistent Stitch DESIGN/HTML screen.

This is a 100/100 pixel-fidelity transcription task, not a redesign task.
The uploaded IMAGE2/product screenshot is the only source of truth.
Any visible mismatch loses points.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use your own default admin template.
Do not change business data.
Preserve the exact source-visible brand mark and lockup.
Generate a persistent DESIGN/HTML screen with a real downloadable htmlCode output.

Fallback scope:
- This is a small single-screen/local-crop Stitch draft.
- Canvas size: exactly 605 x 248.
- Transcribe only the visible upper-left navigation intersection shown in the uploaded crop.
- No browser chrome, no red annotations, no extra content outside the crop.
- No page-level scroll.

Visible geometry and content:
- Dark navy top header fills the top band, from x=0 y=0 to about y=154.
- Brand lockup at x about 44 y about 64:
  - Large white text `UNIVIEW`, bold, softly glowing and slightly blurred like the source.
  - Thin vertical divider after the brand.
  - Gray-blue text `系统设置中心`.
- At the right side of this crop, the header/left-sidebar intersection has the key notched connector:
  - Vertical cyan line at about x=536 from top down to the connector.
  - Cyan horizontal/angled trace around the notch.
  - Dark blue hanging square/rounded block at about x=485 y=111, about 52 x 70.
  - 3x3 cyan dotted grid inside the block.
  - The block casts a small dark shadow and cyan glow.
- A bright cyan horizontal hairline runs at y about 153 across the full crop.
- Below the header is a pale blue/white sidebar background.
- The top of the left sidebar title is visible at lower left: `流程平台`, black/navy bold, cropped by the bottom edge just like the source.
- At far right bottom, only the left edge of the white main card is visible. Do not invent the whole card.

Final self-check:
- The connector block, cyan traces, brand lockup, and crop boundaries must visually match the uploaded image.
- Keep all visible text exact.
- The exported HTML must render at 605 x 248 with no scroll.
