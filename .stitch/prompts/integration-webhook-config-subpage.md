[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 集成配置 / Webhook 配置。请复刻 IMAGE2 产品图 `integration-subpage-05-webhook-config-v2.png` 的 Webhook 后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝导航系统运营中枢高亮。左侧系统配置导航中集成配置展开，Webhook 配置高亮；左侧无搜索。

主区标题「Webhook 配置工作台」。顶部操作包含新建 Webhook、发送测试事件、签名校验、重试队列、保存草稿、提交校验。顶部页面专属筛选：Webhook名称、外部系统、事件类型、签名状态、状态，并有查询和重置。对象卡片：ERP 回执接收、MES 设备事件回调、EHR 扣款回执、PO/合同变更通知、新建 Webhook。

主体左侧表格「Webhook 订阅清单」列为 Webhook名称、外部系统、事件类型、回调地址、签名算法、重试策略、最近状态、操作。右侧「Webhook 配置编辑区」含配置名称、外部系统、事件类型、回调地址、签名算法、密钥引用、幂等键、重试次数、超时设置、审计要求，并有保存草稿、提交校验、发送测试事件。中部展示「事件订阅与回调链路」：事件源、签名校验、幂等性校验、业务处理、归档/异常队列。底部展示测试事件 Payload、签名校验日志、重试队列、发布门禁；发布门禁必须包含 HTTPS地址、签名算法已配置、密钥引用有效、样例事件通过、审计字段完整，以及「失败回调负责人待确认」的警示。

[Components]
后台可用性优先：订阅清单、签名校验、幂等键、测试事件、回调日志、失败重放和发布门禁同屏可见。Webhook 是通用接入方式之一，不替代外部系统档案；密钥只显示引用状态，不展示明文密钥。不要营销页、不要移动端、不要顶部或左侧导航搜索。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 「发送测试事件」「失败重放」「重试队列」必须在当前 Webhook 配置页生成操作留痕，展示签名摘要、幂等键、回调响应和人工重放人，不得打开通用预览页。
