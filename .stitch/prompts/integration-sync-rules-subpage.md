[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 集成配置 / 同步规则。请复刻 IMAGE2 产品图 `integration-subpage-04-sync-rules-v2.png` 的同步规则后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝导航为「固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢」，系统运营中枢高亮。左侧系统运营中枢导航中集成配置展开，同步规则高亮；左侧导航区不放搜索。

主区标题「同步规则工作台」。顶部操作为新建规则、试跑规则、异常队列、人工补发、保存草稿、提交校验。标题下方是页面内筛选：规则名称、外部系统、触发事件、失败处理、状态。对象卡片：MES 状态定时拉取、ERP 报废推送、EHR 赔偿扣款、PO/合同金额变更同步、新建同步规则。注意 PO/合同金额变更同步的外部系统必须是 ERP/采购合同系统，不能写成 MES。

主体左侧大表格「同步规则视图」列为规则名称、外部系统、触发事件、触发条件、重试策略、幂等键、失败处理、状态、操作。中间展示「事件流程时间线」：审批通过 -> 条件校验 -> 调用接口 -> 接收回执 -> 归档/异常队列，并显示重试计数和调用耗时。右侧「同步规则配置编辑区」含配置名称、外部系统、接口端点、触发事件、触发条件、重试次数、重试间隔、幂等键、失败处理、审计要求，并有保存草稿、提交校验、试跑规则。底部展示异常队列与人工补发、幂等与重试监控、发布门禁。发布门禁必须体现字段映射已确认、核销状态只读保护、样例试跑通过、审计字段完整，以及「金额差异阈值待复核」警告，发布按钮禁用。

[Components]
强调规则可配置、失败可追踪、人工补发可审计。表格主导，状态清楚，蓝色主按钮，8px 以内圆角，不做大屏 KPI。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 「试跑规则」「异常队列」「人工补发」必须在当前同步规则页生成操作留痕，展示差异预览、异常定位、幂等键和审计摘要，不得打开通用预览页。
