[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 消息与通知 / 流程邮件配置。请复刻 IMAGE2 产品图 `notification-subpage-02-workflow-mail-v2.png` 的后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝工作台导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的列表筛选栏。左侧系统配置导航展开「消息与通知」，选中「流程邮件配置」；左侧导航不放搜索。

主区标题「流程邮件配置台」。标题右侧是「新建规则」「复制规则」「发送预演」「保存草稿」「提交校验」。标题下方必须是页面内搜索筛选栏：搜索流程/节点/模板/收件人、流程类型、邮件模板、发送状态、校验状态。下方反馈条展示流程邮件规则待配置、草稿保存数、校验通过/警告/错误。

主体上半区三栏。左侧大表格「流程邮件规则」列为流程、节点事件、邮件模板、引用网关、收件人规则、静默条件、状态、操作；行包含 CIP 转固流程、资产入账流程、资产处置流程、赔偿扣款流程、SLA 超时升级。中间「CIP 转固流程邮件规则」展示流程节点条：提交 -> 资产管理员审批 -> 财务复核 -> 转固完成，每个节点都有发送邮件、站内通知、钉钉 H5 开关，并展示邮件模板、邮件网关、收件人规则、抄送规则、静默条件、失败重试、附件策略、审计要求。右侧「规则字段维护」包含规则名称、流程类型、节点事件、邮件模板、邮件网关、收件人规则、抄送规则、静默条件、状态、启用、保存草稿、发送预演、提交校验。

底部三块：邮件预览、变量映射、发送预演与发布校验。预览中显示主题、正文和资产变量；变量映射用 chip 展示 `${receiver_name}`、`${asset_name}`、`${asset_code}`、`${capitalization_date}`、`${project_code}`；校验清单显示主题渲染、正文渲染、附件生成、网关连通性、认证状态、TLS 安全、策略校验、审计记录和最近试发记录。

[Components]
表格主导，右侧编辑区可快速保存/提交。现代中文 B2B 后台，白色面板、细分割线、克制蓝/青/绿/橙状态，圆角不超过 8px。不要营销页，不要 KPI 大屏，不要渐变大背景。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 查看触达审计必须在当前页生成流程引用、变量快照、渠道降级、偏好继承和发送日志记录，不得跳到通用预览弹窗。
