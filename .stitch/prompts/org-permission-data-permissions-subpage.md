[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 组织权限 / 数据权限。请基于当前 Workbench 信息架构和 IMAGE2 产品图 `org-permission-subpage-04-data-permissions-v2.png` 生成高保真桌面后台页。这个页面是专用的数据范围授权与越权校验配置台，不能是通用配置模板。

不要做 KPI 大屏，不要做营销页，不要使用制造管控语义。所有可见文案必须为中文。

[Layout]
Desktop 1586x992。顶部深蓝导航：资产运营总览 / 数据监控中心 / 资产运维中心 / 风险预警中心 / 系统运营中枢，系统运营中枢高亮。左侧为系统运营中枢导航，组织权限分组展开，数据权限高亮；相邻入口包含用户管理、角色权限、菜单权限、工作交接。左侧导航上方不要放搜索框，搜索必须只出现在页面专属筛选栏。

主区域标题「数据权限范围配置」。标题右侧操作：新建数据权限、越权校验、保存草稿、提交校验。

标题下方必须有搜索筛选栏：
- 搜索：规则、组织、位置、资产类别、CIP项目
- 筛选：范围维度、风险等级、授权状态、有效期

主体是数据权限工作台：
1. 左侧「数据权限规则列表」：CIP 项目数据权限、A厂区资产范围、全量导出临时授权、机房服务器范围。每项显示状态、风险等级、最近更新时间。
2. 中央「组合范围矩阵」表格，列：规则名称、组织范围、位置范围、资产分类、CIP 项目、敏感字段、有效期、越权策略、状态、操作。使用勾选和 chip 展示范围维度是否启用。
3. 右侧「CIP 项目数据权限」属性面板，字段：规则名称、组织范围、位置范围、资产分类、CIP 项目、敏感字段、授权有效期、越权策略、数据脱敏、所有者。按钮：保存数据权限、运行越权校验、查看审计。
4. 底部「访问模拟」面板：测试用户张敏、角色资产会计、请求资产/CIP记录；结果分为可见数据、被拦截数据、脱敏字段。右侧显示发布校验清单：范围维度完整、敏感字段脱敏、临时授权有效期、安全管理员复核。

点击「访问模拟」后，不打开占位弹窗，而是在当前页操作反馈区显示「数据权限访问模拟已生成」，并把模拟场景、规则、结果、证据和时间追加到「访问模拟」。点击「查看审计」后，在当前页显示「数据权限审计轨迹已展开」，并追加「审计追踪」记录。

[Components]
视觉保持高保真企业后台：深蓝顶部、白色/浅蓝灰面板、1px 细边框、8px 以内圆角、紧凑矩阵表、状态 pill、维度 chip、蓝色主按钮、橙/红风险提示。

必须体现易用性：
- 数据范围不能只展示摘要，必须用矩阵表现组织/位置/分类/CIP/敏感字段组合关系
- 越权校验和访问模拟必须同屏可见
- 临时授权和全量导出必须突出风险
- 右侧属性保存和提交校验路径清楚
- 不出现英文菜单，不出现泛泛的“配置项模板”

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 访问模拟和查看审计必须可真人模拟，并写入本页「访问模拟」记录区，不得只弹通用预览弹窗。
