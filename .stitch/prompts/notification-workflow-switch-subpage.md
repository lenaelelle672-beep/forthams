[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 消息与通知 / 流程通知开关。请复刻 IMAGE2 产品图 `notification-subpage-08-workflow-notification-switch-v2.png` 的后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能放在当前页面的列表筛选区。左侧系统配置导航展开「消息与通知」，选中「流程通知开关」；左侧导航不放搜索。

主区标题「流程通知开关配置台」。右上角按钮：新建通知开关、保存草稿、提交校验、通知试算。顶部反馈条展示：试算结果命中 7 条通知、草稿状态已保存、校验状态通过、最近试算时间。

主体上半区为三块。左侧「流程模板树」带页面内搜索，包含 CIP 转固流程、待建资产入账流程、资产处置流程、赔偿扣款流程、PO/合同变更流程，并显示规则数和启停状态。中间「节点事件通知矩阵」列为流程节点、事件、站内、钉钉 H5、邮件、强制触达、静默规则、关闭审批、状态、操作，用开关、锁定标签、禁止关闭和待复核标签体现控制状态。右侧「开关规则编辑区」字段：规则名称、开关编码、流程模板、节点事件、渠道组合、强制触达、静默时段、关闭审批人、失败兜底、审计要求、生效范围。

底部四块：通知试算面板、审计检查清单、风险与兜底提示、处理时序链路。通知试算展示实例 CIP-2025-0008，从事件触发、规则匹配、渠道发送到日志归档；审计清单保留通过项和待复核项。

[Components]
表格主导，管理员可快速配置开关并试算结果。现代中文 B2B 后台，白色面板、细线、克制蓝/绿/橙/红状态，圆角不超过 8px。不要大屏 KPI，不要营销页。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 通知试算和查看审计必须在当前页生成可见记录：试算记录展示发送预演、实例试算、关闭复核、日志归档；审计记录展示审计轨迹、关闭复核和发布门禁，不得跳到通用预览弹窗。
