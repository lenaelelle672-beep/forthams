[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 系统参数 / 操作审计。请复刻 IMAGE2 产品图 `system-params-subpage-06-audit-log-v2.png` 的后台配置页。

[Layout]
Desktop 1595x986。顶部深蓝工作台导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的审计事件筛选栏。左侧系统配置导航展开「系统参数」，选中「操作审计」；左侧导航不放搜索。

主区标题「系统操作审计配置台」。标题右侧是「新建审计策略」「保存草稿」「提交校验」「审计查询」「导出取证包」。下方四个摘要卡：审计事件 9842、高危操作 42、取证包 18、留存策略 3年。

主体上半区左右分栏。左侧大表格「审计事件查询工作台」列为时间、操作人、操作对象、操作类型、来源模块、风险等级、结果、操作；列表上方有页面内筛选：操作人、对象范围、风险等级、时间范围、审计查询，并显示「已查询 1/4 条命中」。右侧「审计策略编排」包含策略名称、采集范围、留存周期、高危标记、导出水印、脱敏规则、不可删除策略、取证审批、告警通知。

底部四块：导出取证包预览、留存保护策略、风险事件趋势、审计链路追溯。取证包预览展示编号、事件数量、完整性哈希、水印信息、下载审批状态和「下载取证包」按钮；审计链路追溯展示从作业台发起、工作流审批、ERP/EHR/MES 回执到审计归档的时间线。

[Components]
高密度审计后台，核心是检索、表格、策略编辑和取证。白色面板、细分割线、蓝色主按钮、红橙风险曲线。不要顶部/左侧全局搜索，不要营销页，不要 KPI 大屏。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 审计事件查询必须按操作人、对象范围、风险等级、时间范围过滤「审计事件查询工作台」，并显示命中数量，例如「1/4 条命中」。
- 「导出取证包」「下载取证包」「追溯详情」都必须在当前页面更新操作反馈和取证包/链路区域，不得打开通用弹窗或跳转到占位页。
- 取证包区域必须展示事件数量、完整性哈希、水印信息、审批单号、下载人和不可删除/留存保护策略，支撑真人模拟审计取证。
