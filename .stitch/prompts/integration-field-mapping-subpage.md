[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 集成配置 / 字段映射。请复刻 IMAGE2 产品图 `integration-subpage-03-field-mapping-v2.png` 的字段映射工具页。

[Layout]
Desktop 1586x992。顶部深蓝导航为「固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢」，系统运营中枢高亮。左侧系统运营中枢导航中「集成配置」展开，「字段映射」高亮；左侧导航区不出现搜索。

主区标题「字段映射工作台」。顶部操作为维护映射、样例校验、冲突检测、保存草稿、提交校验。标题下方是页面内搜索筛选：源字段、目标字段、外部系统、业务对象、发布状态。对象卡片：MES 设备绑定、ERP 转固回写、EHR 人员同步、PO/合同金额变更、新建映射配置。

主体左侧表格「字段映射视图」列为源字段、目标字段、字段类型、转换规则、必填策略、异常处理、状态、操作。中间为映射关系画布，左侧 MES/ERP/EHR 源字段，右侧固定资产目标字段，中间曲线连线和校验点。右侧「字段映射配置编辑区」含配置名称、外部系统、接口端点、源字段、目标字段、转换规则、默认值、必填策略、异常处理、审计要求，并有保存草稿、提交校验、样例校验。底部展示样例数据预览、缺失字段异常、冲突检测、发布门禁。

[Components]
必须像后台工具而非看板：高密度表格、可视化映射画布、右侧配置表单、发布门禁 checklist。包含 asset_class、资产小类、emp_no、工号等字段示例。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
