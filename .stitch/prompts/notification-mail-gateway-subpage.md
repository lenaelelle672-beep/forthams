[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 消息与通知 / 邮件网关配置。请复刻 IMAGE2 产品图 `notification-subpage-01-mail-gateway-v2.png` 的后台配置页。

[Layout]
Desktop 1586x992。顶部深蓝工作台导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的列表筛选栏。左侧系统配置导航展开「消息与通知」，选中「邮件网关配置」；左侧导航不放搜索。

主区标题「邮件网关配置台」。标题右侧是「新建邮件网关」「测试连接」「安全策略」「发送日志」。标题下方有页面内搜索栏「搜索网关、编码、负责人、端点、认证或场景」，显示筛选结果数；搜索只能在本页内容区出现，不能放到顶部导航或左侧导航。

主体上半区左右分栏。左侧「邮件网关列表」列出主邮件网关、CIP 转固流程邮件、审计超时提醒、备用邮件网关、钉钉 H5 通知渠道；每行展示网关编码、状态、负责人和发送成功率，搜索无结果时显示空状态和清空搜索按钮。中间是「邮件网关草稿」编辑区，包含网关名称、网关编码、传输方式、认证方式、发件身份、负责人、网关端点、测试收件人、安全策略、启用开关。右侧「网关策略详情」展示 SMTP/TLS、OAuth2/应用密码、白名单、凭据轮换、失败切换和审计要求。

底部四个步骤卡：网关、模板、触发、日志。右侧保留「流程邮件引用」和「发送日志」小表，包含 CIP 转固流程、SLA 超时升级、ERP 回执失败通知。

[Components]
后台操作要方便快捷，表格和编辑区主导，管理员能在同屏完成新建、测试连接、保存草稿、提交校验。现代中文 B2B 后台，白底浅蓝灰面板、蓝色主按钮、绿色正常、橙色待处理、细边框，圆角不超过 8px。不要营销页，不要 KPI 大屏，不要人物或制造产线图片。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 「安全策略」点击后必须在当前页生成 TLS/白名单/认证轮换/敏感脱敏检查记录；「发送日志」和日志重试必须在当前页生成日志处理记录，不得跳到通用预览弹窗。
