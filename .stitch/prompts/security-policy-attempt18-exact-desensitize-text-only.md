Repair the selected Stitch DESIGN screen into a new persistent full DESIGN screen.

This is a text-only exactness repair for `system-params-subpage-02-security-policy-v2.png`.
Do not redesign the page. Do not change geometry, spacing, rows, cards, links, buttons, or scroll behavior.
Return a persistent DESIGN screen with downloadable HTML and screenshot; do not return DOM operations only.

Preserve all currently passing gates from the selected screen:
- viewport/document/body exactly 1609 x 977.
- document title `登录会话与敏感字段策略`.
- forbidden strings absent: `Security Policy Console`, `安全策略编辑`, ASCII/split trend title variants.
- all five table rows visible.
- right editor bottom visible.
- bottom links visible.
- exact trend title `异常登录趋势（最近 7 天）` visible.
- search placeholder visible.
- real internal scroller count 0.

Only make this exact text substitution wherever it appears:
- Replace `按角色分级脱敏（3级）` with `按角色脱敏（3级）`.

Do not leave the old string anywhere in the exported HTML.
Do not change `按角色脱敏（3级）` into ASCII punctuation.
Do not change any other business text.

Hard final self-check:
- `按角色脱敏（3级）` is present in body text or form/control text.
- `按角色分级脱敏（3级）` is absent.
- `missing=[]` under the existing security-policy verifier.
- all existing visibility gates remain true.
- `realScrollerCount=0`.
