Create a NEW DESIGN screen. Do not return DOM operations against the existing file.
The response must include outputComponents.design.screens with a new DESIGN screen id.

This is a 100/100 pixel-fidelity transcription task, not a redesign task.
The uploaded IMAGE2 v2 screenshot is the only source of truth.
Any visible mismatch loses points.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use your own default admin template.
Do not change business data.

Page name: 操作审计
Menu id: system-audit-log
Reference image: system-params-subpage-06-audit-log-v2.png
Reference URL: http://127.0.0.1:5173/mock/workspace-preview/asset-kit-v8/system-hub/subpages/system-params-subpage-06-audit-log-v2.png
Workbench URL: http://127.0.0.1:5173/fixed-assets/workbench?menu=system-audit-log
Source viewport: 1595 x 986.

Recreate the IMAGE2 v2 source as one static desktop HTML page at exactly 1595 x 986.
Keep page-level content inside the viewport with no body scroll.

Absolute source locks:
- Top-left brand: white `UNIVÍEW`, vertical divider, and adjacent `固定资产管理系统`, positioned like the source.
- Top nav active tab: `系统运营中枢`.
- Page title: `系统操作审计配置台`.
- Header buttons: `新建审计策略`, `保存草稿`, `提交校验`, `审计查询`, `导出取证包`.
- KPI row values: `审计事件 9,842`, `高危操作 42`, `取证包 18`, `留存策略 3年`.
- KPI icons must be the source-style large colored rounded/circular badges, not pale square tiles.
- Main left card title: `审计事件查询工作台`.
- Audit table must show four rows and readable columns: `时间`, `操作人`, `操作对象`, `操作类型`, `来源模块`, `风险等级`, `结果`, `操作`.
- Include exact visible row data such as `2025-05-21 10:23:45`, `ERP凭证VCH-20250521-001`, and `缓存键：AssetCacheAll`.
- Right card title must be `审计策略编排`.
- Bottom cards must be fully visible: `导出取证包预览`, `留存保护策略`, `风险事件趋势（近 7 天）`, `审计链路追溯（示例）`.
- The `下载取证包` button must sit inside the first bottom card, not outside the viewport.
- The right editor must not overlap the bottom cards.

Use dense, source-matching spacing:
- Shell top bar about 52px high.
- Left sidebar about 195px wide.
- Main content starts at x about 218 and y about 65.
- KPI row ends around y=200.
- Middle table/editor section ends around y=625.
- Bottom cards span roughly y=634 to y=933.

Use self-contained HTML/CSS only. Do not add external font or icon CDN links.
