[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 系统参数 / 导入导出配置。请复刻 IMAGE2 产品图 `system-params-subpage-04-import-export-v2.png` 的后台配置页。

[Layout]
Desktop 1595x986。顶部深蓝工作台导航中「系统运营中枢」高亮。顶部导航、页面头部、左侧导航都不要出现任何全局搜索框；搜索只能出现在当前页面内容区的模板列表筛选栏。左侧系统配置导航展开「系统参数」，选中「导入导出配置」；左侧导航不放搜索。

主区标题「导入导出模板与队列配置台」。标题右侧是「新建模板」「保存草稿」「提交校验」「试运行」。下方四个摘要卡：模板数、待发布、导入队列、导出权限。

主体上半区左右分栏。左侧大表格「导入导出模板列表」带页面内搜索和筛选，列为模板名称、模板键、适用对象、字段映射、校验规则、队列策略、状态、操作；行包含待建资产导入、CIP 费用归集导入、资产台账导出、审计取证导出、供应商基础资料导入。右侧「导入导出策略编排」包含模板版本、文件格式、批量上限、字段校验、重复处理、错误报告、导出控制、队列策略，并展示完成度 8/8 与保存草稿、提交校验、试运行。

底部四块：字段映射预览、队列运行情况、校验错误样例、发布校验清单。字段映射用左右字段 chip 对齐并用细线连接；队列展示等待、处理中、失败、完成；错误样例展示行号、字段、原因和处理建议；发布清单展示版本锁定、权限门禁、字段映射、校验规则、水印策略和审计留痕。

[Components]
以操作效率为主，一屏看清模板列表、编辑配置和试运行结果。现代中文后台，白底、细线、紧凑表格、蓝色主按钮。不要顶部/左侧全局搜索，不要营销页，不要 KPI 大屏。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 查看发布审计必须在当前页生成参数变更、影响预览、发布门禁、回滚记录和操作审计记录，不得跳到通用预览弹窗。
