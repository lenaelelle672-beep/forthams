Create one persistent Stitch DESIGN/HTML screen for a crop-only UI image.

Task type:
This is a 100/100 pixel-fidelity transcription of a small product screenshot crop, with exactly one user correction.
The source reference is the uploaded project image screen:
`审批规则左上角方案 IMAGE2 fallback reference 2026-06-27`
screen id `14518017655974171815`.

Canvas:
- Width: 605px.
- Height: 248px.
- Fixed canvas, no scroll, no browser chrome, no annotations.

Visible composition to preserve:
- Top band: dark navy header, y=0..153.
- Left brand lockup: large white glowing `UNIVIEW` at x=44 y=64, vertical divider near x=277, gray-blue `系统设置中心` to the right.
- A bright cyan horizontal line runs across the full crop at y=153.
- Lower band: pale blue/white sidebar area.
- Lower-left text `流程平台` is partially visible and cropped by the bottom edge.
- Lower-right: only the left edge of a large white rounded content card is visible.

Critical corrected detail:
- At the header/sidebar intersection, create a small hanging block around x=485 y=110, size about 51px wide and 70px high.
- The block is a dark navy rounded rectangle.
- All four visible corners of this small block must be identical blunt/rounded corners.
- Top-left corner, top-right corner, bottom-right corner, and bottom-left corner must all use the same radius, about 6px.
- The top-left corner must not be special.
- Do not cut, notch, bevel, chamfer, angle, clip, mask, polygon-cut, stair-step, or diagonal-trim the top-left corner.
- Do not draw any separate overlay that changes the top-left corner silhouette.
- The block outline must read as a normal rounded rectangle when zoomed in.
- Place a centered 3x3 cyan dotted grid inside the block.
- Keep subtle cyan glow and dark shadow.

Cyan connector lines:
- Draw a vertical cyan line near x=536 from the top edge down to the block.
- Draw the cyan horizontal line at y=153 behind or beside the block.
- The connector lines may touch the block edge, but they must not alter or cover the block's rounded-rectangle silhouette.

Quality gates:
- The crop must remain 605 x 248.
- The small hanging block must have four matching blunt/rounded corners.
- No diagonal or angular geometry may appear on the block body.
- Generate a persistent DESIGN/HTML screen with real downloadable htmlCode output.
