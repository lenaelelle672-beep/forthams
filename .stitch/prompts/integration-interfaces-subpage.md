[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 集成配置 / 接口配置。请复刻 IMAGE2 产品图 `integration-subpage-02-interfaces-v2.png` 的接口端点配置工作台。

[Layout]
Desktop 1586x992。顶部深蓝导航固定为：固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢，系统运营中枢高亮。左侧是系统运营中枢导航，集成配置展开，接口配置高亮；顶部和左侧导航都不要搜索框。

主区标题「接口配置工作台」。副标题说明在外部系统档案之下维护入站、出站、定时任务和流程触发端点，认证策略只引用系统配置，不暴露密钥。右上角操作为「新建接口」「健康检查」「试跑配置」「保存草稿」「提交校验」。

标题下方有页面专属筛选：接口名称、外部系统、方向、认证引用、状态，并有「重置」「查询」。对象卡片显示 MES 设备状态源、ERP 固资推送接口、EHR 扣款接口、PO/合同同步、异常队列 Webhook，选中 ERP 固资推送接口。

主体左侧大表格「接口端点视图」列为接口名称、方法、方向、触发方式、认证引用、超时、状态、操作，选中 ERP 报废结果推送。右侧「接口配置编辑区」含配置名称、外部系统、接口端点、方法、方向、认证引用、触发事件、失败处理、审计要求，并有保存草稿、提交校验、试跑。底部展示健康检查结果、认证引用策略、异常队列、最近调用日志。

[Components]
后台操作必须快速清晰：紧凑表格、状态 pill、行内编辑/复制/更多、蓝色主按钮、细分割线。不要做 KPI 大屏或通用空模板。

[Strict Fill Requirements]
- 端点表格不少于 9 行，必须包含固资信息推送、资产变更推送、报废结果推送、EHR 扣款字段同步。
- 右侧编辑区必须展示发布门禁或校验清单。
- 认证引用策略只能展示引用名、scope、有效期，不展示密钥。
- 底部必须有健康检查结果、认证引用策略、异常队列、最近调用日志。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 「试跑配置」「异常队列」必须在当前接口配置页生成操作留痕，展示样例试跑、异常定位、幂等重放和审计摘要，不得打开通用预览页。
