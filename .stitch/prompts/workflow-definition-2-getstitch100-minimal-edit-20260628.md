Create a new persistent DESIGN/HTML screen by cloning the selected uploaded HTML screen.

This is a minimal Stitch fallback edit. Keep all geometry, colors, layout, panels, rows, density, and visual style exactly as the selected screen. Do not redesign.

Only make these visible text updates:
- Page title: `流程定义 2`
- Active sidebar item: `流程定义 2`
- Add a small header pill: `/workflows-v2`
- Add a small header pill: `正式入口未切换`
- Add small note: `正式 /workflows 保持不变`
- Ensure actions visible: `保存草稿`, `提交校验`, `发布流程`

Output requirement:
- Must create/export a persistent DESIGN/HTML screen with real `htmlCode.downloadUrl`.
- Do not return only suggestions, DOM operations, or text.
