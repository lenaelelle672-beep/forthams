[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 组织权限 / 用户管理。请基于项目当前 Workbench 页面和 IMAGE2 产品图 `org-permission-subpage-01-user-management-v2.png` 生成高保真桌面后台页。这个页面不能是通用配置模板，必须是可直接用于维护用户账号、钉钉 H5 身份、EHR 状态、组织归属、角色包和工作交接的专用页面。

不要做 KPI 大屏，不要做营销页，不要使用制造管控语义。所有可见文案必须为中文。

[Layout]
Desktop 1586x992。顶部深蓝导航，入口为：资产运营总览 / 数据监控中心 / 资产运维中心 / 风险预警中心 / 系统运营中枢。系统运营中枢高亮。

左侧为系统运营中枢导航，组织权限分组展开，用户管理高亮。左侧导航不放全局搜索框；常用入口包含用户管理、角色权限、工作交接、数据权限。左侧必须体现当前系统运营中枢分组结构，而不是旧版“系统管理 / 字典管理 / 参数配置”导航。

主区域标题「用户账号与外部身份配置」。标题旁有按钮：新建用户、批量同步、发起交接、导出。

标题下方是高密度搜索筛选栏：
- 关键词搜索：姓名 / 工号 / 钉钉 userId / EHR 编号
- 账号状态：启用、待交接、停用
- 组织范围：资产管理部、财务共享、IT 运维
- 角色包：资产管理员、资产会计、系统运维
- 同步状态：EHR 正常、钉钉异常、待补齐

中央主体必须以用户数据表为主导，表格列包含：
姓名、工号、部门、岗位、账号状态、钉钉 userId、EHR 状态、角色包、待交接事项、最近同步、操作。
表格示例行要包含真实业务数据：张敏、李伟、王佳、陈立；状态包含启用、待交接、停用保护、同步异常。

右侧为选中用户详情编辑面板：
- 基础身份：姓名、工号、部门、岗位、手机号
- 外部身份：钉钉 userId、EHR 人员编号、同步结果
- 权限与角色：角色包、数据权限范围、临时授权到期日
- 工作交接：资产责任人、流程待办、岗位权限三类交接状态
- 操作按钮：保存用户、同步外部身份、发起工作交接、停用账号

下半区需要一个「交接与审计记录」表格，展示交接批次、来源用户、接收人、交接对象、状态、审批单、更新时间。
点击「处理差异」和「发起工作交接」后，必须在当前页操作反馈区显示结果，并把差异处理批次或工作交接批次追加到「交接与审计记录」表格，不得只弹占位弹窗。

[Components]
视觉保持项目已确定的高保真企业后台质感：深蓝顶部栏、白底/浅蓝灰面板、细边框、紧凑行高、状态 pill、清晰按钮、8px 以内圆角。表格必须可读，筛选栏必须明显，右侧编辑面板必须像真实业务页面。

必须体现易用性：
- 页面能快速搜索用户，不能让用户找不到数据
- 用户列表、详情、交接记录必须同屏联动
- 停用和交接是高风险操作，要有橙色/红色风险标识
- 钉钉 H5 只是身份/待办承载，不做独立移动端模块
- 不出现英文菜单，不出现泛泛的“配置项模板”

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 处理差异和发起工作交接必须可真人模拟，并写入本页交接与审计记录。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
