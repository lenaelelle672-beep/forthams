Edit the selected uploaded IMAGE2 screen into a new persistent Stitch DESIGN/HTML screen.

This is a 100/100 pixel-fidelity transcription task, not a redesign task.
Source of truth: `notification-subpage-02-workflow-mail-v2.png`.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use a generic admin template. Do not change business data.

Canvas: exactly 1586 x 992. No page scroll. No internal visible scroll containers.
Top-left brand must match the IMAGE2 source: `UNIVIEW | 固定资产管理系统`.
Active page/menu: `流程邮件配置台`, `system-workflow-mail`.

Critical visible text that must exist exactly:
`流程邮件配置台`
`流程邮件规则`
`规则字段维护`
`CIP 转固流程邮件规则`
`CIP 转固流程`
`资产入账流程`
`资产处置流程`
`隐患扣款流程`
`SLA 超时升级`
`CIP 转固通知`
`入账完成通知`
`处置完成通知`
`扣款提醒`
`SLA升级通知`
`MAIL-GW-001`
`固定资产管理员`
`CIP 转固流程-转固完成通知`
`抄送规则`
`失败重试`
`附件策略`
`审计要求`
`邮件预览`
`变量映射`
`发送预演与发布校验`
`发送预演（节点：转固完成）`
`查看完整日志`

Layout priority:
- Keep the source shell, sidebar, header, KPI row, left table, middle/right editor, and bottom cards.
- The left `流程邮件规则` table must show all five rule rows horizontally, not vertical/stacked.
- The bottom `邮件预览`, `变量映射`, and `发送预演与发布校验` cards must be visible above the viewport bottom.
- Do not use emoji icons such as ✉, ✈, 👁, ⚙, 🚀.
- Do not output `发送预演 （节点：转固完成）`; no extra space before the full-width parentheses.
