Edit only the top-left brand lockup of this screen.

This remains a 100/100 pixel-fidelity transcription of `master-data-subpage-04-vendor-management-v2.png`.

Keep the entire page layout, tables, right panel, bottom cards, text, row visibility, and sizing from the current candidate unchanged.

Only fix this visible mismatch:
- Current top-left brand icon is a generic circular/exclamation-style mark.
- Source screenshot top-left brand mark is a small blue rounded-square product icon followed by `固定资产管理系统`.
- Replace the generic icon with a source-like blue rounded-square mark with small white dot/leaf shapes, aligned at the far left of the navy top bar.
- Keep `固定资产管理系统` text immediately to the right, white, same baseline as source.
- Do not add `UNIVIEW` if the source screenshot for this page does not show it.

Hard constraints:
- Do not change any business text or table content.
- Do not change the right `供应商详情` / `风险提示` / `发布门禁` visibility.
- Do not change the bottom `交易反查台` six visible rows.
- Do not introduce emoji.
- No document scroll or horizontal overflow.
