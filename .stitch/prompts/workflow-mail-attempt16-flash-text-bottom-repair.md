Edit the selected Stitch DESIGN screen only enough to fix source text fidelity.

This remains a 100/100 pixel-fidelity transcription task, not a redesign task.
Keep the current layout, coordinates, sizing, shell, sidebar, cards, and no-scroll state from the selected screen.
Do not redesign or regenerate a different page.

Source of truth: `notification-subpage-02-workflow-mail-v2.png`.
Canvas must remain exactly 1586 x 992.
No page scroll. No internal visible scroll containers. No emoji icons.

Make these exact text repairs:

1. In the left `流程邮件规则` table, show all five rows as horizontal text:
   - Row 1: `CIP 转固流程` and `CIP 转固通知`
   - Row 2: `资产入账流程` and `入账完成通知`
   - Row 3: `资产处置流程` and `处置完成通知`
   - Row 4: `隐患扣款流程` and `扣款提醒`
   - Row 5: `SLA 超时升级` and `SLA升级通知`

2. Preserve exact spaces:
   - Use `CIP 转固通知`, not `CIP转固通知`.
   - Use `隐患扣款流程`, not `赔偿扣款流程` or any other wording.

3. In the bottom-right `发送预演与发布校验` card, add/show the exact source heading:
   - `发送预演（节点：转固完成）`
   - No extra space before the full-width parenthesis.
   - Do not use `发送预演 （节点：转固完成）`.

4. Keep these bottom cards visible above the viewport bottom:
   - `邮件预览`
   - `变量映射`
   - `发送预演与发布校验`
   - `发送预演（节点：转固完成）`
   - `查看完整日志`

Forbidden:
- `发送预演 （节点：转固完成）`
- `赔偿扣款流程`
- `CIP转固通知`
- emoji characters such as ✉, ✈, 👁, ⚙, 🚀
- page scroll or internal scrollbars
