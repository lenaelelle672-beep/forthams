This is a 100/100 pixel-fidelity transcription repair task, not a redesign task.
The IMAGE2 v2 source screenshot remains the only source of truth.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use your own default admin template.
Do not change business data except to restore exact source-visible text.

Page name: 操作审计
Menu id: system-audit-log
Reference image: system-params-subpage-06-audit-log-v2.png
Reference URL: http://127.0.0.1:5173/mock/workspace-preview/asset-kit-v8/system-hub/subpages/system-params-subpage-06-audit-log-v2.png
Workbench URL: http://127.0.0.1:5173/fixed-assets/workbench?menu=system-audit-log

Start from the current candidate layout because the vertical density is close.
Repair only the visible mismatches below:

1. Top-left brand lockup must match the IMAGE2 source.
   - Use the source-visible white wordmark text exactly as `UNIVÍEW`.
   - Preserve the vertical divider and adjacent `固定资产管理系统` subtitle placement.
   - Do not render a generic `UNIVIEW` wordmark without the accent.

2. Right-side panel title must match the IMAGE2 source exactly.
   - Change `审计策略编辑` to `审计策略编排`.
   - Keep the same card position, width, row spacing, inputs, toggles, and bottom alignment from the current candidate.

3. KPI card icons must match the source style.
   - The four KPI icons are large colored rounded/circular badges, not small pale square tiles.
   - First KPI: blue badge with white clipboard icon.
   - Second KPI: red badge with white shield warning icon.
   - Third KPI: green badge with white package/download icon.
   - Fourth KPI: blue badge with white shield/clock style icon.
   - Keep the KPI labels and values unchanged: `审计事件 9,842`, `高危操作 42`, `取证包 18`, `留存策略 3年`.

4. Preserve the current candidate's improvements.
   - Keep bottom four cards fully visible inside 1595 x 986.
   - Keep the right panel from overlapping the bottom cards.
   - Keep the audit table horizontal and readable with four rows.
   - Keep `导出取证包预览`, `留存保护策略`, `风险事件趋势（近 7 天）`, and `审计链路追溯（示例）` visible.

Any visible mismatch loses points. The output must be a single desktop HTML page that fits the source viewport without page-level scrolling.
