Edit the selected Stitch DESIGN screen into a new persistent DESIGN/HTML screen.

This is a strict 100/100 pixel-fidelity transcription repair for `notification-subpage-02-workflow-mail-v2.png`.
Do not redesign, resize, reflow, or regenerate the page. Keep the exact current 1586 x 992 fixed canvas, all shell/navigation/table/right-editor geometry, text, colors, and no-scroll state.

Only adjust the vertical position of these bottom-card headings by moving them down about 6-8px so they sit inside the source-visible bottom card header band:

- `邮件预览`
- `变量映射`
- `发送预演与发布校验`
- `发送预演（节点：转固完成）`

Coordinate target:
- The three outer bottom-card titles (`邮件预览`, `变量映射`, `发送预演与发布校验`) must have visible text top y >= 650 and bottom <= 992.
- The inner heading `发送预演（节点：转固完成）` must have visible text top y >= 680 and bottom <= 992.

Do not change any other text. Preserve:
- `处置完成通知`
- `扣款提醒`
- `CIP 转固通知`
- `发送预演（节点：转固完成）`
- `查看完整日志`

Forbidden:
- `处置结果通知`
- `扣款结果通知`
- `发送预演 （节点：转固完成）`
- emoji characters
- page scroll or internal visible scroll containers

The exported HTML must keep documentElement/body exactly 1586 x 992 with no real overflow containers.
