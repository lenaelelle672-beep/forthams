Edit the selected Stitch DESIGN screen into a new persistent DESIGN/HTML screen.

This is a 100/100 pixel-fidelity transcription task, not a redesign task.
The uploaded IMAGE2 v2 screenshot `system-params-subpage-04-import-export-v2.png` is the only source of truth.
Any visible mismatch loses points.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use your own default admin template.
Do not change business data.

Preserve the exact top-left IMAGE2 brand mark for the current source screenshot:
white `UNIVIEW`, the source-visible divider/lockup, and the adjacent product subtitle exactly as visible.
Do not redraw, distort, split, re-space, or replace the UNIVIEW wordmark.

Repair the current candidate to fit the source screenshot geometry.

Hard gates:
- Canvas, html, body, documentElement: exactly 1595 x 986 CSS pixels.
- No page scroll. No internal scrollbars. No content below y=986.
- Keep the source dark top shell and source dark left sidebar.
- Active sidebar item remains `导入导出配置`.
- Title remains `导入导出模板与队列配置台`.

Use these source bands:
- KPI cards: y=132..231.
- Left table card: x=219..959, y=246..606.
- Right editor card: x=972..1584, y=246..606.
- Four bottom cards: y=640..946.

The left table must show five horizontal rows inside y=246..606:
`待建资产导入`
`CIP 费用归集导入`
`资产台账导出`
`审计取证导出`
`供应商基础资料导入`
Do not stack characters vertically. Do not wrap table row names into tall cells.

The right editor title must be exactly:
`导入导出策略编排`

The right editor must keep these exact source values and three buttons inside y=246..606:
`v2.3.0（草稿）`
`Excel（.xlsx）`
`启用（必填 + 格式 + 业务规则）`
`生成错误报告（分工作表输出）`
`脱敏 + 水印（公司名 + 时间戳）`
`导入默认队列（并发 3，失败重试 2 次）`
`保存草稿`
`提交校验`
`试运行`

The bottom row must show the four source cards and links:
`字段映射预览`
`队列运行情况`
`校验错误样例`
`发布校验清单`
`导入队列（3）`
`导出队列（2）`
`查看队列详情`
`查看全部错误（2）`
`查看校验报告`

Use compact 11-12px lower-card rows if needed. Preserve all full-width Chinese punctuation exactly.
