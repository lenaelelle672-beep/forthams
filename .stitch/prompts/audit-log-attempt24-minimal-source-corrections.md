Create a NEW DESIGN screen. Do not return DOM operations.

This is a 100/100 pixel-fidelity transcription repair task, not a redesign task.
The IMAGE2 v2 source screenshot is the only source of truth.
Any visible mismatch loses points.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the page.
Do not use your own default admin template.
Do not change business data.

Page name: 操作审计
Menu id: system-audit-log
Reference image: system-params-subpage-06-audit-log-v2.png
Reference URL: http://127.0.0.1:5173/mock/workspace-preview/asset-kit-v8/system-hub/subpages/system-params-subpage-06-audit-log-v2.png
Workbench URL: http://127.0.0.1:5173/fixed-assets/workbench?menu=system-audit-log
Source viewport: 1595 x 986.

Important repair strategy:
- Keep the current candidate's overall geometry, y-bands, card positions, row heights, and bottom-card fit because that geometry is already closest to the IMAGE2 source.
- Do not re-layout the page.
- Do not change the table density, bottom card heights, chart placement, or right form vertical budget.
- Only correct source-visible mismatches listed below.

Required minimal corrections:

1. Top-left brand:
   - Must be the source-visible white wordmark `UNIVÍEW`, with the accented `Í`.
   - Keep the vertical divider and `固定资产管理系统` subtitle.
   - Do not render plain `UNIVIEW`.

2. Right middle card title:
   - Must be exact source text `审计策略编排`.
   - Do not use `审计策略编辑`.
   - Preserve the current card position and form spacing.

3. KPI icons:
   - Keep KPI card layout and text unchanged.
   - Replace only the icon tiles to match the source's large colored badges:
     - `审计事件`: blue large badge with white clipboard mark.
     - `高危操作`: red large badge with white shield/warning mark.
     - `取证包`: green large badge with white package/download mark.
     - `留存策略`: blue large badge with white shield/clock mark.
   - The badges must be saturated source-style, not pale square tiles.

4. Preserve these current-candidate successes exactly:
   - Four audit table rows visible.
   - Table action links stay inside the card.
   - Right form does not overlap bottom cards.
   - Bottom four cards fully visible inside 1595 x 986.
   - `导出取证包预览` shows full `下载取证包` button inside the card.
   - `审计链路追溯（示例）` shows four steps ending with `审计归档入库`.

No emoji. Avoid external icon fonts if possible; use inline SVG/CSS icons if needed.
