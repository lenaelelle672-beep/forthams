Edit the selected Stitch DESIGN screen into a new persistent DESIGN/HTML screen.

This remains a strict 100/100 pixel-fidelity transcription of `notification-subpage-02-workflow-mail-v2.png`.
Do not redesign, reflow, resize, modernize, or replace the current layout.
Keep the current 1586 x 992 fixed canvas, top shell, sidebar, cards, table geometry, right editor, bottom cards, no-scroll state, and line-icon style.

Only repair these exact visible text issues:

1. In the left `流程邮件规则` table, keep all five rows horizontal and single-line. Set the notification/template names exactly:
   - Row 1 notification/template: `CIP 转固通知`
   - Row 2 notification/template: `入账完成通知`
   - Row 3 notification/template: `处置完成通知`
   - Row 4 notification/template: `扣款提醒`
   - Row 5 notification/template: `SLA升级通知`

2. In the bottom-right card `发送预演与发布校验`, show a visible inner heading exactly:
   - `发送预演（节点：转固完成）`
   The text must be within the 1586 x 992 viewport, above the bottom card content, and not clipped.

3. Preserve exact punctuation and spacing:
   - Use `发送预演（节点：转固完成）`.
   - Do not use `发送预演 （节点：转固完成）`.
   - Do not use half-width parentheses.
   - Use `CIP 转固通知`, not `CIP转固通知`.

Forbidden:
- `处置结果通知`
- `扣款结果通知`
- `发送预演 （节点：转固完成）`
- emoji characters such as ✉, ✈, 👁, ⚙, 🚀
- page scroll or internal visible scroll containers

The result must export as a persistent Stitch DESIGN screen with documentElement/body exactly 1586 x 992 and all current bottom cards still visible.
