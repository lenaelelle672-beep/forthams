[Context]
UNIVIEW 固定资产管理系统，System Operation Hub / 系统运营中枢。请复刻 IMAGE2 产品图 `flow-platform-subpage-05-approval-rules-v2.png` 的审批规则配置页。页面是后台规则配置，不是流程图页面，也不是 KPI 大屏。

目标：用户能在同一页维护重复处理人跳过、离职交接承接、代理审批、节点权限等规则，并能新建规则、模拟命中、保存草稿、提交校验。

[Layout]
Desktop 1586x992。顶部深色导航含“固定资产工作台 / 资产全景视图 / 大屏态势 / 流程协同 / 系统运营中枢”，系统运营中枢高亮。顶部导航和左侧导航都不要搜索图标、搜索按钮、搜索输入框。左侧系统配置导航展开流程平台，审批规则高亮。

主区标题“审批规则配置台”，操作条包含：新建规则、保存草稿、模拟命中、提交校验。标题下有绿色操作反馈条“规则命中模拟已完成 · CIP 转固流程 / 财务审核 · 已生成审计链路”。

核心区域三栏：
左栏：规则集，不放搜索框，包含分类 chip 和规则卡片：“重复处理人跳过 / 离职交接承接 / 财务节点代理 / 节点权限校验”，显示规则类型、状态、优先级。
中栏：规则命中工作台。上方是摘要卡：规则总数、启用规则、待复核、发布完成度。中部是命中模拟场景，包含流程、节点、处理人、上下文和“模拟命中”按钮。再下方是规则执行编排画布：入口流程 -> 命中条件 -> 处理路由 -> 审计归档。下方左右两块分别是优先级队列和发布门禁矩阵，底部是规则明细表格。
右栏：规则属性配置，包含规则名称、规则类型、适用流程、触发条件、处理动作、审计要求、启用规则、审计留痕；下方是模拟结果卡和操作按钮：保存规则、查看审计、提交校验。

[Components]
精致企业后台风格，使用状态 pill、规则优先级条、路径小画布、checklist、紧凑表格。圆角不超过 8px，避免大阴影。

易用性要求：
- 规则输入、命中模拟、执行编排、优先级队列和发布门禁同屏。
- 发布门禁显示“规则启用 / 字段完整 / 模拟场景 / 审计留痕 / 冲突检查”。
- 必须体现离职交接、重复处理人跳过、代理审批这些固定资产流程规则。

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
