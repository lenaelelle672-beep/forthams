Create one persistent Stitch DESIGN/HTML screen.

This is a fallback Stitch generation based on the uploaded project reference image:
`审批规则左上角方案 IMAGE2 fallback reference 2026-06-27`, screen id `14518017655974171815`.

This is a 100/100 pixel-fidelity transcription task, not a redesign task.
The uploaded IMAGE2/product screenshot is the only source of truth.
Any visible mismatch loses points.
Do not improve, simplify, reinterpret, modernize, normalize, or redesign the crop.
Do not use your own default admin template.
Preserve the exact source-visible brand mark and lockup.
Generate a persistent DESIGN/HTML screen with a real downloadable htmlCode output.

Canvas:
- Exactly 605 x 248.
- Fixed viewport.
- No scroll.
- No browser chrome.
- No red annotations.

Visible crop transcription:
- Dark navy header occupies y=0 to about y=153.
- Large glowing white `UNIVIEW` at x about 44 y about 64.
- Thin vertical divider after `UNIVIEW`.
- Gray-blue `系统设置中心` to the right of the divider.
- At the right edge of the crop, reproduce the exact header/sidebar intersection detail:
  - a vertical cyan line at x about 536,
  - an angular cyan notch,
  - a hanging dark-blue connector block around x=485 y=111, about 52 x 70,
  - a 3x3 cyan dotted grid inside the block,
  - cyan glow and small dark drop shadow.
- A bright cyan horizontal line runs across the crop at about y=153.
- Below that line is a pale blue/white sidebar area.
- Lower-left visible sidebar title: `流程平台`, cropped by the bottom edge just like the source.
- At lower-right, only the left edge of a white rounded main card is visible. Do not draw the full card.

Final output must be a real Stitch DESIGN/HTML screen, not a description.
