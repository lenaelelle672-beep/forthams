[Context]
UNIVIEW 固定资产管理系统，系统运营中枢 / 组织权限 / 角色权限。请基于项目当前 Workbench 页面和 IMAGE2 产品图 `org-permission-subpage-02-role-permissions-v2.png` 生成高保真桌面后台页。这个页面不能是通用配置模板，必须是可直接用于维护角色包、菜单权限、按钮权限、数据范围、成员来源和敏感权限复核的专用页面。

不要做 KPI 大屏，不要做营销页，不要使用制造管控语义。所有可见文案必须为中文。

[Layout]
Desktop 1586x992。顶部深蓝导航，入口为：资产运营总览 / 数据监控中心 / 资产运维中心 / 风险预警中心 / 系统运营中枢。系统运营中枢高亮。

左侧为系统运营中枢导航，组织权限分组展开，角色权限高亮。左侧导航不放全局搜索框；常用入口包含用户管理、角色权限、菜单权限、数据权限、工作交接。左侧必须体现当前系统运营中枢分组结构，而不是旧版“系统管理 / 字典管理 / 参数配置”导航。

主区域标题「角色权限包配置」。标题旁有按钮：新建角色包、复制角色、权限对比、提交复核。

标题下方是搜索筛选栏：
- 关键词搜索：角色名称 / 编码 / 权限点
- 风险等级：全部、敏感权限、待复核
- 成员来源：岗位继承、用户例外授权、临时授权
- 业务范围：资产台账、盘点、CIP、系统运营中枢

页面主体采用三栏高密度工作台：
左栏是角色包列表，显示角色名称、编码、成员数、状态、风险等级。示例：资产管理员、资产会计、CIP 专员、系统运维、安全管理员。
中栏是权限矩阵表格，行是权限模块，列包含查看、新建、编辑、删除、导出、发布、审批、敏感标识。模块包含资产台账、待建资产池、盘点策略、CIP 转固、流程平台、集成配置、系统参数。
右栏是选中角色详情与影响预览，包含成员来源、数据权限范围、敏感权限清单、发布影响、审计要求、最近变更记录。

中栏下方必须有「权限变更影响预览」：
- 影响用户数
- 影响菜单数
- 影响按钮/API 数
- 需要二次复核的敏感权限
- 发布后写入操作审计

页面还必须有「权限审计轨迹」记录区。点击「影响预览」后，不打开占位弹窗，而是在当前页操作反馈区显示「角色权限影响预演已生成」，并追加一条「影响预演」审计记录。点击右侧「查看审计」后，在当前页显示「角色权限审计轨迹已展开」，并追加一条「审计轨迹」记录。

[Components]
视觉保持高保真企业后台：深蓝顶部、白色/浅蓝灰面板、1px 细边框、8px 以内圆角、紧凑矩阵、开关/复选框、状态 pill、蓝色主按钮、橙色风险提示。页面必须像真实权限配置工具，而不是普通表格模板。

必须体现易用性：
- 角色搜索和风险筛选必须醒目
- 权限矩阵要一屏可操作，不能只有摘要卡
- 敏感权限和待复核状态要突出
- 右侧必须展示发布影响，避免权限误放大
- 不出现英文菜单，不出现泛泛的“配置项模板”

## 硬性复刻要求
- 顶部业务视角和左侧导航不得放搜索框；搜索/筛选只能出现在当前专属页面内容区。
- 页面必须包含可真人模拟的保存草稿动作，并在操作反馈区显示保存结果。
- 页面必须包含可真人模拟的提交校验/发布复核动作，并在操作反馈区显示校验或复核结果。
- 新建、编辑、搜索、详情预览、保存、提交必须围绕当前业务对象完成，不得复用通用占位模板。
- 影响预览和查看审计必须可真人模拟，并写入本页「权限审计轨迹」，不得只弹通用预览弹窗。
