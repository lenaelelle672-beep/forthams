<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ams.mapper.InspectionTemplateMapper">

    <!-- 按模板类型查询 -->
    <select id="selectTemplatesByType" resultType="com.ams.entity.InspectionTemplate">
        SELECT * FROM inspection_template
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          AND type = #{type}
        ORDER BY create_time DESC
    </select>

    <!-- 按状态查询 -->
    <select id="selectTemplatesByStatus" resultType="com.ams.entity.InspectionTemplate">
        SELECT * FROM inspection_template
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          AND status = #{status}
        ORDER BY create_time DESC
    </select>

    <!-- 查询模板详情（包含关联的检验记录数） -->
    <select id="selectTemplateDetail" resultType="java.util.HashMap">
        SELECT
            t.*,
            COUNT(DISTINCT ir.id) as recordCount
        FROM inspection_template t
        LEFT JOIN inspection ir ON t.id = ir.template_id AND ir.deleted = 0
        WHERE t.deleted = 0
          AND t.tenant_id = #{tenantId}
          AND t.id = #{id}
        GROUP BY t.id
    </select>

</mapper>