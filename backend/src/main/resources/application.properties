# Application Configuration
# 所有配置已统一到 application.yml（单一事实源）。
# 此前此文件与 application.yml 存在 4 处冲突（jwt.secret、datasource.url、
# idle-timeout、mapper-locations），.properties 优先级高于 .yml 导致 yml 值被静默覆盖。
# 合并后此文件保留为空；如需环境覆盖请使用环境变量（DB_URL/JWT_SECRET 等）或 profile。
