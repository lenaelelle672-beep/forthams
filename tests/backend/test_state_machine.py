[{'DRAFT': ['PENDING'], 'PENDING': ['APPROVED', 'REJECTED'], 'APPROVED': ['CLOSED'], 'REJECTED': ['PENDING'], 'CLOSED': []}, [], {'action}': 'self.current_state = next_state\n        return Mock(current_state=next_state)\n\n@pytest.fixture\ndef state_machine():\n    return StateMachine()\n\nclass TestStateMachineRules:', 'ATB-2: 验证状态机流转规则的合法性"': 'pytest.mark.parametrize(', 'PENDING", "approve", "APPROVED"),\n        ("PENDING", "reject", "REJECTED"),\n        ("APPROVED", "close", "CLOSED"),\n        ("REJECTED", "reopen", "PENDING': '允许重新打开\n    ])\n    def test_valid_transition(self', 'expected_next)': '', '步骤5': '合法状态流转应成功', '\n        result = state_machine.transition("TK-001", from_state, action)\n        assert result.current_state == expected_next\n\n    @pytest.mark.parametrize("invalid_combo", [\n        ("PENDING", "close"),      # 跳过审批直接关闭\n        ("REJECTED", "approve"),   # 拒绝后不能直接批准\n        ("CLOSED", "reopen': '终态不能重新打开\n    ])\n    def test_invalid_transition_raises_error(self', 'invalid_combo)': '', '步骤6': '非法状态流转应抛出 InvalidStateTransitionError', '\n        from_state, action = invalid_combo\n        with pytest.raises(InvalidStateTransitionError):\n            state_machine.transition("TK-002': 'from_state', 'test_state_machine_initialization(self)': '', '测试状态机初始化"': "sm = StateMachine()\n        assert sm.current_state is None\n        assert 'PENDING' in sm.states\n        assert 'APPROVED' in sm.states\n        assert 'REJECTED' in sm.states\n        assert 'CLOSED' in sm.states\n\n    def test_state_machine_state_tracking(self", 'state_machine)': '', '测试状态机状态跟踪"': '初始状态应为None\n        assert state_machine.current_state is None\n        \n        # 执行一次转换\n        result = state_machine.transition('}]