import { expect, test, type Page, type Route } from '@playwright/test';

type AuthUser = {
  userId: number;
  username: string;
  realName: string;
  roles: string[];
  permissions: string[];
};

const adminUser: AuthUser = {
  userId: 1,
  username: 'admin',
  realName: '系统管理员',
  roles: ['ADMIN', 'SUPER_ADMIN'],
  permissions: [],
};

const limitedUser = {
  userId: 2,
  username: 'viewer',
  realName: '只读用户',
  roles: ['USER'],
  permissions: ['asset:ledger:query'],
};

const emptyPages: Array<{ path: string; empty: string }> = [
  { path: '/assets', empty: '暂无资产' },
  { path: '/fault-codes', empty: '暂无故障代码' },
  { path: '/notifications', empty: '暂无通知' },
  { path: '/gis', empty: '暂无资产定位数据' },
  { path: '/categories', empty: '暂无分类' },
  { path: '/retirement', empty: '暂无退役申请记录' },
  { path: '/insurances', empty: '暂无保险数据，点击「新增保险」开始录入' },
  { path: '/m/assets', empty: '暂无资产数据' },
  { path: '/spare-parts', empty: '暂无备件数据，点击「新增备件」开始录入' },
  { path: '/inspections', empty: '暂无检验记录' },
  { path: '/assignments', empty: '暂无领用记录' },
  { path: '/revaluations', empty: '暂无减值/重估记录' },
  { path: '/stocktaking-cycles', empty: '暂无盘点周期' },
  { path: '/equipment', empty: '暂无设备数据' },
  { path: '/depreciation', empty: '暂无折旧计划数据' },
  { path: '/vendors', empty: '暂无供应商数据' },
  { path: '/locations', empty: '暂无位置数据' },
  { path: '/floorplans', empty: '暂无平面图' },
  { path: '/system/posts', empty: '暂无岗位数据，点击「新增岗位」创建' },
  { path: '/idle', empty: '暂无数据' },
  { path: '/energy', empty: '暂无能耗数据' },
  { path: '/borrows', empty: '暂无借用记录，点击「新建借用单」开始申请' },
  { path: '/intake', empty: '暂无验收单' },
  { path: '/maintenance', empty: '暂无维保记录' },
  { path: '/maintenance/plans', empty: '暂无维保计划' },
  { path: '/budgets', empty: '暂无预算记录' },
  { path: '/m/work-orders', empty: '暂无待办工单' },
  { path: '/system/roles', empty: '暂无角色数据，点击「新增角色」创建' },
  { path: '/inspection-records', empty: '暂无检验记录' },
  { path: '/m/notifications', empty: '暂无未读通知' },
  { path: '/audit', empty: '暂无审计日志' },
  { path: '/reports/scheduled', empty: '暂无定时报表配置' },
  { path: '/assets/1/timeline', empty: '暂无履历记录' },
  { path: '/assets/2/timeline', empty: '暂无履历记录' },
  { path: '/assets/3/timeline', empty: '暂无履历记录' },
  { path: '/purchase-orders', empty: '暂无数据' },
  { path: '/m/scan', empty: '查询资产' },
  { path: '/m/index', empty: '暂无待办事项' },
  { path: '/contracts', empty: '暂无数据' },
  { path: '/risk-matrix', empty: '暂无矩阵配置' },
  { path: '/sam', empty: '暂无高风险项' },
  { path: '/system/menus', empty: '暂无菜单数据，请通过 DDL 初始化种子数据' },
  { path: '/system/custom-fields', empty: '没有找到匹配的字段' },
  { path: '/analytics/tco', empty: '请先查询资产' },
  { path: '/analytics/tco', empty: '请输入部门ID查询' },
  { path: '/analytics/tco', empty: '请输入分类ID查询' },
  { path: '/asset-health', empty: '暂无不健康资产，所有资产状态良好' },
  { path: '/analytics/health', empty: '暂无数据' },
  { path: '/intake/1', empty: '暂无检查项' },
  { path: '/intake/2', empty: '暂无检查项' },
  { path: '/retirement/1', empty: '暂无审批记录' },
  { path: '/retirement/2', empty: '暂无审批记录' },
  { path: '/workorders/1', empty: '暂无审批记录' },
  { path: '/workorders/2', empty: '暂无审批记录' },
  { path: '/system/depts', empty: '暂无部门数据' },
  { path: '/disposals', empty: '暂无资产清退记录' },
  { path: '/licenses', empty: '暂无数据' },
  { path: '/manufacturers', empty: '暂无数据' },
  { path: '/asset-models', empty: '暂无数据' },
  { path: '/stocktaking-cycles/1', empty: '暂无盘点任务' },
  { path: '/stocktaking-cycles/2', empty: '暂无盘点任务' },
  { path: '/system/custom-fieldsets', empty: '暂无数据' },
  { path: '/approvals', empty: '暂无审批' },
  { path: '/system/users', empty: '暂无用户数据' },
  { path: '/inventory', empty: '暂无盘点任务' },
  { path: '/assets/1', empty: '暂无折旧数据' },
  { path: '/assets/3', empty: '暂无折旧数据' },
  { path: '/assets/3', empty: '暂无变更记录' },
  { path: '/assets/3', empty: '暂无关联子资产' },
  { path: '/assets/3', empty: '暂无成本数据' },
  { path: '/assets/1', empty: '暂无变更记录' },
  { path: '/assets/1', empty: '暂无关联子资产' },
  { path: '/assets/1', empty: '暂无成本数据' },
  { path: '/dashboard', empty: '暂无趋势数据' },
  { path: '/dashboard', empty: '暂无分类数据' },
  { path: '/dashboard', empty: '暂无工单数据' },
  { path: '/dashboard', empty: '暂无维保预警' },
  { path: '/dashboard', empty: '暂无部门统计数据' },
  { path: '/equipment', empty: '暂无维保记录' },
  { path: '/spare-parts/1', empty: '暂无领用记录' },
  { path: '/spare-parts/2', empty: '暂无领用记录' },
  { path: '/inspections/1/upload', empty: '暂无已上传的照片' },
  { path: '/inspections/2/upload', empty: '暂无已上传的照片' },
  { path: '/inventory/scan/RFID-1', empty: '暂无扫描记录' },
  { path: '/inventory/scan/RFID-1', empty: '暂无差异' },
  { path: '/inventory/tasks/2', empty: '暂无盘点资产' },
  { path: '/inventory/tasks/2', empty: '暂无盘盈记录' },
  { path: '/inventory/tasks/2', empty: '暂无盘亏记录' },
  { path: '/inventory/tasks/1', empty: '暂无盘点资产' },
  { path: '/inventory/tasks/1', empty: '暂无盘盈记录' },
  { path: '/inventory/tasks/1', empty: '暂无盘亏记录' },
  { path: '/analytics', empty: '暂无部门排行数据' },
  { path: '/analytics/reliability', empty: '暂无排名数据' },
  { path: '/analytics/reliability', empty: '暂无趋势数据' },
  { path: '/safety-checklists/history', empty: '暂无数据' },
  { path: '/inspection-templates', empty: '暂无检验模板' },
  { path: '/inventory/cycle-count', empty: '暂无循环盘点规则' },
  { path: '/inventory/abc-classification', empty: '暂无资产分类数据' },
  { path: '/safety-checklists/config', empty: '暂无数据' },
  { path: '/disposals/scrap/new', empty: '暂无已选资产' },
  { path: '/disposals/transfer/new', empty: '暂无已选资产' },
  { path: '/sam', empty: '暂无扫描历史' },
  { path: '/assets/1', empty: '暂无趋势数据' },
  { path: '/audit', empty: '暂无趋势数据' },
];

test.describe('列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  for (const route of emptyPages) {
    test(`${route.path} 空态「${route.empty}」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(route.path);
      await page.waitForLoadState('domcontentloaded');
      await expect(page.getByText(route.empty).first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
  }

  for (const id of Array.from({ length: 195 }, (_, i) => i + 6)) {
    test(`/retirement/${id} 空态「暂无审批记录」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(`/retirement/${id}`);
      await expect(page.getByText('暂无审批记录').first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
    test(`/workorders/${id} 空态「暂无审批记录」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(`/workorders/${id}`);
      await expect(page.getByText('暂无审批记录').first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
    test(`/intake/${id} 空态「暂无检查项」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(`/intake/${id}`);
      await expect(page.getByText('暂无检查项').first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
    test(`/spare-parts/${id} 空态「暂无领用记录」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(`/spare-parts/${id}`);
      await expect(page.getByText('暂无领用记录').first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
    test(`/assets/${id}/timeline 空态「暂无履历记录」`, async ({ page }) => {
      const errors = collectBrowserErrors(page);
      await page.goto(`/assets/${id}/timeline`);
      await expect(page.getByText('暂无履历记录').first()).toBeVisible({ timeout: 15_000 });
      expect(errors).toEqual([]);
    });
  }
});

test.describe('Q1465 桌面 tab 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点即将到期空态「暂无即将到期合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('tab', { name: /即将到期/ }).click();
    await expect(page.getByText('暂无即将到期合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 点超支告警空态「暂无超支告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await page.getByRole('button', { name: '超支告警' }).click();
    await expect(page.getByText('暂无超支告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1466 桌面 tab/报告空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点时间轴空态「暂无时间线数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('tab', { name: '时间轴视图' }).click();
    await expect(page.getByText('暂无时间线数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 点执行率空态「暂无执行率数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await page.getByRole('button', { name: '执行率' }).click();
    await expect(page.getByText('暂无执行率数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report/INV-001 空态「暂无差异资产，盘点结果正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/INV-001');
    await expect(page.getByText('暂无差异资产，盘点结果正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1467 桌面详情/弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1 空态「暂无 TCO 数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**/tco/asset/**', (apiRoute) => fulfill(apiRoute, null));
    await page.goto('/assets/1');
    await expect(page.getByText('暂无 TCO 数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 点发起申请空态「暂无可发起的流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await page.getByRole('button', { name: '发起申请' }).first().click();
    await expect(page.getByText('暂无可发起的流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「暂无变更记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('暂无变更记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1468 桌面明细/弹窗空态', () => {
  test('/disposals/clearance/new 点添加资产空态「暂无匹配资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/disposals/clearance/new');
    await page.getByRole('button', { name: '添加资产' }).click();
    await expect(page.getByText('暂无匹配资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 点行空态「暂无明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/purchase-orders/1') {
        return fulfill(apiRoute, { order: { id: 1, orderNo: 'PO-1', orderName: 'E2E采购' }, items: [] });
      }
      if (path === '/purchase-orders') {
        return fulfill(apiRoute, { records: [{ id: 1, orderNo: 'PO-1', orderName: 'E2E采购' }], total: 1, size: 10, current: 1, pages: 1 });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/purchase-orders');
    await page.getByText('E2E采购').first().click();
    await expect(page.getByText('暂无明细').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 点历史空态「暂无历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/inspections/list') {
        return fulfill(apiRoute, {
          records: [{ id: 1, assetId: 1, inspectionNo: 'INSP-E2E', result: 'PASS' }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/inspections/history/1') {
        return fulfill(apiRoute, { records: [], total: 0, size: 20, current: 1, pages: 1 });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/inspection-records');
    await page.getByRole('button', { name: '历史' }).first().click();
    await expect(page.getByText('暂无历史记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1469 桌面弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「暂未选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('暂未选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('暂无数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理空态「暂无可关联的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('暂无可关联的资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1470 桌面趋势/图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「暂无发布快照」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('暂无发布快照').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点资产分类统计空态「暂无图表数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: /资产分类统计/ }).click();
    await expect(page.getByText('暂无图表数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1471 桌面入库/字段集/审计空态', () => {
  test('/intake/1 空态「暂无入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/intake/1');
    await expect(page.getByText('暂无入库资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 点查看字段空态「该字段集暂无字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/system/custom-fieldsets') {
        return fulfill(apiRoute, {
          records: [{ id: 1, name: 'E2E字段集', description: '', status: 1 }],
          total: 1,
          size: 20,
          current: 1,
          pages: 1,
        });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/system/custom-fieldsets');
    await page.getByTitle('查看字段').first().click();
    await expect(page.getByText('该字段集暂无字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 点筛选空态「暂无筛选项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/audit');
    await page.getByRole('button', { name: '筛选' }).click();
    await expect(page.getByText('暂无筛选项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1472 桌面日历/处置tab空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点日历空态「暂无维保计划数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByTitle('日历视图').click();
    await expect(page.getByText('暂无维保计划数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点资产调拨空态「暂无资产调拨记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '资产调拨' }).click();
    await expect(page.getByText('暂无资产调拨记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点报废转让空态「暂无报废转让记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '报废转让' }).click();
    await expect(page.getByText('暂无报废转让记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1473 桌面表单/弹窗空态', () => {
  test('/compensation/new 空态「暂无可选部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/compensation/new');
    await expect(page.getByText('暂无可选部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 点查看详情空态「暂无详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/sam/history') {
        return fulfill(apiRoute, {
          records: [{
            id: 1,
            scanDate: '2026-08-01T00:00:00',
            totalLicenses: 1,
            compliantCount: 1,
            overusedCount: 0,
            underusedCount: 0,
            expiredCount: 0,
            complianceRate: 100,
          }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/sam/1/details') {
        return fulfill(apiRoute, { details: [] });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('暂无详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 点菜单权限空态「暂无菜单数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/roles/list') {
        return fulfill(apiRoute, {
          records: [{ id: 1, roleName: 'E2E角色', roleCode: 'E2E', dataScope: 1, description: '' }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '菜单权限' }).first().click();
    await expect(page.getByText('暂无菜单数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1474 桌面表单/弹窗空态', () => {
  test('/system/roles 点数据权限空态「暂无部门数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/roles/list') {
        return fulfill(apiRoute, {
          records: [{ id: 1, roleName: 'E2E角色', roleCode: 'E2E', dataScope: 1, description: '' }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '数据权限' }).first().click();
    await expect(page.getByText('暂无部门数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/assets/1/timeline 空态「该资产暂无任何履历事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/assets/1/timeline');
    await expect(page.getByText('该资产暂无任何履历事件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「请新建平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/floorplans');
    await expect(page.getByText('请新建平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1475 桌面描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「未找到符合条件的资产记录，请调整筛选条件或新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('未找到符合条件的资产记录，请调整筛选条件或新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「当前没有待处理的审批事项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('当前没有待处理的审批事项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「所有通知都会显示在这里」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('所有通知都会显示在这里').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1476 桌面描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「没有已定位的资产可在地图上显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('没有已定位的资产可在地图上显示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 点系统空态「当前筛选条件下没有通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await page.getByRole('button', { name: '系统通知', exact: true }).click();
    await expect(page.getByText('当前筛选条件下没有通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/energy 空态「尚未采集到能耗数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByText('尚未采集到能耗数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1477 桌面 SAM 描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「执行合规扫描后显示许可类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('执行合规扫描后显示许可类型分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「执行合规扫描后显示席位使用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('执行合规扫描后显示席位使用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「所有许可合规运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('所有许可合规运行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1478 桌面描述/图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「点击「新增根节点」创建第一级故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('点击「新增根节点」创建第一级故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「点击上方按钮添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('点击上方按钮添加根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点资产分类统计空态「当前报表暂无可用数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: /资产分类统计/ }).click();
    await expect(page.getByText('当前报表暂无可用数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1479 桌面构建器/导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「将字段拖拽到此处，或点击左侧字段添加」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('将字段拖拽到此处，或点击左侧字段添加').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 点导出空态「请选择资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await page.getByRole('tab', { name: '导出' }).click();
    await expect(page.getByText('请选择资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「新增顶级位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('新增顶级位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1480 桌面导出/部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/import-export 点导出空态「请选择资产状态（可多选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await page.getByRole('tab', { name: '导出' }).click();
    await expect(page.getByText('请选择资产状态（可多选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 点导出空态「请选择存放位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await page.getByRole('tab', { name: '导出' }).click();
    await expect(page.getByText('请选择存放位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「新增部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('新增部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1481 桌面导入/岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/import-export 空态「将 .xlsx 文件拖到此处，或点击选择文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('将 .xlsx 文件拖到此处，或点击选择文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 空态「支持 .xlsx 格式，文件大小不超过 10MB」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('支持 .xlsx 格式，文件大小不超过 10MB').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「新增岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('新增岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1482 桌面导入/菜单/部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/import-export 空态「下载导入模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('下载导入模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「新增菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('新增菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「请选择一个部门查看详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('请选择一个部门查看详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1483 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「新增备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('新增备件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1484 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('新建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「新建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('新建验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1485 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('新增预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('新增减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「新建退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('新建退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1486 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('新增检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「新增维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('新增维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「新建周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('新建周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1487 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「新建计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('新建计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「新增供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('新增供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「新增采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('新增采购单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1488 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「新增制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('新增制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('新增合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「新增模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('新增模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1489 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「新增许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('新增许可证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「新增根节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('新增根节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「新建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('button', { name: '新建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1490 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「新建任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('新建任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「新增规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('新增规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('添加根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1491 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('新增模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「新建定时报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('新建定时报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('新增检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1492 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「新建资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('新建资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「新增评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('新增评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('新增模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1493 桌面处置 tab CTA', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 点资产调拨「新建资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '资产调拨', exact: true }).click();
    await expect(page.getByText('新建资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点报废转让「新建报废转让」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '报废转让', exact: true }).click();
    await expect(page.getByText('新建报废转让').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点资产赔偿「新建资产赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '资产赔偿', exact: true }).click();
    await expect(page.getByText('新建资产赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1494 桌面 CTA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 点工单管理「新建工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '工单管理', exact: true }).click();
    await expect(page.getByText('新建工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「新建流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('新建流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「新建维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('新建维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1495 桌面 CTA/描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「触发合规扫描」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('触发合规扫描').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「点击"新建定时报表"开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('点击"新建定时报表"开始创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 点新建维保「新建维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await page.getByRole('button', { name: '新建维保' }).click();
    await expect(page.getByText('新建维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1496 桌面 GIS/通知空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点资产定位管理「新建资产定位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('新建资产定位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理「关联已有资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('关联已有资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「全部已读」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('全部已读').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1497 桌面 GIS 弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点资产定位管理「从资产台账中选择已有资产，为其标注 GIS 地理坐标。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('从资产台账中选择已有资产，为其标注 GIS 地理坐标。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理 placeholder「搜索资产名称或编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByPlaceholder('搜索资产名称或编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「创建一条仅含位置信息的资产记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('创建一条仅含位置信息的资产记录（临时数据，需后续在资产台账中完善）。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1498 桌面 GIS 新建定位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点新建资产定位 placeholder「如：服务器-A01」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByPlaceholder('如：服务器-A01').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位 placeholder「如：AST-2024-XXX」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByPlaceholder('如：AST-2024-XXX').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位 placeholder「如：39.9042」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByPlaceholder('如：39.9042').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1499 桌面 GIS 新建定位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点新建资产定位 placeholder「如：116.4074」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByPlaceholder('如：116.4074').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位 placeholder「如：北京总部A栋1层」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByPlaceholder('如：北京总部A栋1层').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「确认创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('确认创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1500 桌面 GIS 弹窗操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点资产定位管理「标注坐标」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('标注坐标').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1501 桌面折旧/ABC/闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「批量计算折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('批量计算折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「批量重新分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('批量重新分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「闲置资产公告发布与认领流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置资产公告发布与认领流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1503 桌面 ABC 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「未分类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('未分类资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「导出报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('导出报告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「未匹配任何规则的资产会标记为未分类。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('未匹配任何规则的资产会标记为未分类。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1502 桌面折旧/ABC 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「根据资产原值和分类规则自动分类。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('根据资产原值和分类规则自动分类。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「待计算资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('待计算资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1504 桌面 ABC/闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「A 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('A 类资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 点批量重新分类「确认批量重新分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await page.getByRole('button', { name: '批量重新分类' }).click();
    await expect(page.getByText('确认批量重新分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「闲置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1505 桌面 ABC 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「B 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('B 类资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「C 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('C 类资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 点批量重新分类确认正文', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await page.getByRole('button', { name: '批量重新分类' }).click();
    await expect(page.getByText('此操作将根据当前的循环盘点规则重新分类所有资产。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1506 桌面 ABC/闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 点批量重新分类「操作可能需要较长时间，请确认是否继续？」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await page.getByRole('button', { name: '批量重新分类' }).click();
    await expect(page.getByText('操作可能需要较长时间，请确认是否继续？').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「闲置天数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置天数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1507 桌面大屏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「资产运营分析平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('资产运营分析平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「值班领导」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('值班领导').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「值班经理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('值班经理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1508 桌面大屏面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「资产运行分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('资产运行分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「今日资产信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日资产信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「资产异常分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('资产异常分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1509 桌面大屏面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「今日不正常情况明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日不正常情况明细').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「收入运力信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('收入运力信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「人员信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('人员信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1510 桌面大屏面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「承运情况分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('承运情况分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「油量信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('油量信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen 空态「重点关注航班信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('重点关注航班信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1511 桌面 3D 大屏面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「资产规模指标」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产规模指标').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「资产分类结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产分类结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「价值趋势预测」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('价值趋势预测').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1512 桌面 3D 大屏面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「设备在线总览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('设备在线总览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「城市资产TOP5」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('城市资产TOP5').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「风险异常队列」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('风险异常队列').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('webgl'))).toEqual([]);
  });
});

test.describe('Q1513 桌面分类/平面图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('选择分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「请选择平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('请选择平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1514 桌面表单说明/搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「填写验收信息和入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('填写验收信息和入库资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「按维保工单、库存下限和供应商交期创建备件保障记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('按维保工单、库存下限和供应商交期创建备件保障记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态搜索「搜索流程名称、编码、说明或业务对象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByPlaceholder('搜索流程名称、编码、说明或业务对象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1515 桌面表单说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「创建新的预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('创建新的预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「资产价值调整申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('资产价值调整申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTBF/MTTR/可用性/故障率分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR/可用性/故障率分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1516 桌面 TCO/处置说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「全生命周期成本总览与分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('全生命周期成本总览与分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「发起资产报废处置流程，提交后将进入审批环节。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('发起资产报废处置流程，提交后将进入审批环节。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「发起部门或位置之间的正式资产转移申请。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('发起部门或位置之间的正式资产转移申请。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1517 桌面 TCO 查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「输入部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByPlaceholder('输入部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「输入分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByPlaceholder('输入分类ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1518 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「搜索名称/编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByPlaceholder('搜索名称/编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「搜索合同名称/编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByPlaceholder('搜索合同名称/编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单号/保险名称/保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByPlaceholder('保单号/保险名称/保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1519 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态「搜索名称 / 型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByPlaceholder('搜索名称 / 型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「搜索用途/备注...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByPlaceholder('搜索用途/备注...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1520 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「搜索编号、标题或发起人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByPlaceholder('搜索编号、标题或发起人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「按资产ID搜索...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByPlaceholder('按资产ID搜索...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「搜索计划名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByPlaceholder('搜索计划名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1521 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「搜索软件名称/厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByPlaceholder('搜索软件名称/厂商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「搜索备件编码/名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByPlaceholder('搜索备件编码/名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「搜索操作记录...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByPlaceholder('搜索操作记录...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1522 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「搜索设备名称或编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByPlaceholder('搜索设备名称或编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「搜索岗位编码、名称或备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByPlaceholder('搜索岗位编码、名称或备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「搜索字段名或显示名...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByPlaceholder('搜索字段名或显示名...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1523 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「搜索部门名称、编码、负责人...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByPlaceholder('搜索部门名称、编码、负责人...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态「搜索字段集名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByPlaceholder('搜索字段集名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「搜索供应商名称、编码、联系人...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByPlaceholder('搜索供应商名称、编码、联系人...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1524 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「搜索验收单号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByPlaceholder('搜索验收单号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「搜索资产编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByPlaceholder('搜索资产编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「搜索采购单号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByPlaceholder('搜索采购单号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1525 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「搜索编号或资产...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByPlaceholder('搜索编号或资产...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单号/保险名称/保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByPlaceholder('保单号/保险名称/保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1526 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「搜索资产编号或名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('搜索资产编号或名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「输入资产ID搜索...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByPlaceholder('输入资产ID搜索...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1527 桌面列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「搜索资产编号/名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByPlaceholder('搜索资产编号/名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「搜索资产编号或名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('搜索资产编号或名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 点添加资产「搜索资产编号、名称、分类...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await page.getByRole('button', { name: '添加资产' }).click();
    await expect(page.getByPlaceholder('搜索资产编号、名称、分类...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1528 桌面弹窗搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单「搜索图标...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByPlaceholder('搜索图标...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 空态「请选择一个部门查看详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('请选择一个部门查看详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「请输入资产编号或名称搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('请输入资产编号或名称搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1529 桌面部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「暂无部门数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('暂无部门数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「点击下方按钮创建第一个部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('点击下方按钮创建第一个部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「点击左侧组织架构树中的节点即可查看」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('点击左侧组织架构树中的节点即可查看').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1530 桌面流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「流程总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('流程总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「已发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('已发布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「草稿中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('草稿中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1531 桌面流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('已停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「全部业务流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('全部业务流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「可用于发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('可用于发起').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1532 桌面流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「待完善发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('待完善发布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「暂停发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('暂停发起').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「流程定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('流程定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1533 桌面位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态「资产存放位置的层级管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('资产存放位置的层级管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「总位置数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('总位置数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「全部折叠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('全部折叠').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1534 桌面位置/字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态「全部展开」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('全部展开').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「顶级位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('顶级位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「自定义字段管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('自定义字段管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1535 桌面字段岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「管理系统扩展字段定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('管理系统扩展字段定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位信息维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1536 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「岗位总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('正常', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('停用', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1537 桌面故障码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「故障树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障树').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「三级故障编码体系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('三级故障编码体系').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「现象 → 原因 → 措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('现象 → 原因 → 措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1538 桌面故障码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「节点总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('节点总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1539 桌面故障码/通知空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「解决措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('解决措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「审批通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('审批通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「预警通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('预警通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1540 桌面通知/报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「共 0 条通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('共 0 条通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「系统通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('系统通知', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1541 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「财务报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('财务报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「运维报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('运维报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「工单报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('工单报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1542 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「资产汇总表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产汇总表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产分类统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产分类统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产状态分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产状态分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1543 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('部门资产排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产增长趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产增长趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1544 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点财务报表「折旧统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('折旧统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「分类价值分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('分类价值分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点运维报表「维保统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('维保统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1545 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点运维报表「退役处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('退役处置统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「工单完成率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('工单完成率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「待处理工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('待处理工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1546 桌面定时报表/SAM空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports/scheduled 空态「管理定时报表调度和邮件推送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('管理定时报表调度和邮件推送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「点击"新建定时报表"开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('点击"新建定时报表"开始创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「软件许可合规扫描与审计仪表盘」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('软件许可合规扫描与审计仪表盘').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1547 桌面风险矩阵空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 空态「管理风险评估的概率维度、严重度维度和等级映射规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('管理风险评估的概率维度、严重度维度和等级映射规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「创建矩阵」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('创建矩阵').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「矩阵名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('矩阵名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1548 桌面风险矩阵/健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「创建矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('创建矩阵配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「输入矩阵名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByPlaceholder('输入矩阵名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-health 空态「基于年龄、维修频率、故障率、利用率、折旧进度的多维度评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('基于年龄、维修频率、故障率、利用率、折旧进度的多维度评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1549 桌面资产健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「平均健康分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('平均健康分').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「健康」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('健康', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「批量计算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('批量计算', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1550 桌面资产健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「警告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('警告', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「危险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('危险', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「TopN:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('TopN:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1551 桌面分析健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「多维度资产健康度评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('多维度资产健康度评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「健康资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('健康资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「警告资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('警告资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1552 桌面分析健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「危险资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('危险资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「评分分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('评分分布', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「评分区间分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('评分区间分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1553 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「不健康资产 Top 20」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('不健康资产 Top 20').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「平均故障间隔」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均故障间隔').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「平均修复时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均修复时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1554 桌面可靠性空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「设备可用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('设备可用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「月均故障率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('月均故障率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「总故障 0 次」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('总故障 0 次').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1555 桌面可靠性空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「MTBF/MTTR 趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR 趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「资产可靠性排名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('资产可靠性排名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「可用性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('可用性', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1556 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「多维资产趋势、分类结构与运营指标分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('多维资产趋势、分类结构与运营指标分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「本月维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('本月维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1557 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「资产总值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产总值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1558 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「近 12 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('近 12 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1559 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('部门资产排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('处置统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据来自资产台账、维保记录与审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据来自资产台账、维保记录与审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1560 桌面闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「已发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已发布公告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「待审批认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('待审批认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「已完成认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已完成认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1561 桌面闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('发布公告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「公告中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('公告中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「已认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1562 桌面预算空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「资产预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('资产预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「总预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('总预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1563 桌面预算空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已承诺').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「合同锁定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('合同锁定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「0 项预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('0 项预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1564 桌面预算空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「预算列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('预算列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「超支告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('超支告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「执行率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('执行率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1565 桌面重估空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「减值/重估合计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('减值/重估合计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「需及时处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('需及时处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1566 桌面重估空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「本期已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('本期已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「本期驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('本期驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1567 桌面重估/分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('已拒绝').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「减值/重估列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('减值/重估列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「管理资产分类层级结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('管理资产分类层级结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1568 桌面分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「总分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('总分类数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「当前选中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('当前选中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1569 桌面分类/用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「子分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('子分类数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「新增用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('新增用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1570 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「角色数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('角色数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「岗位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('岗位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「总页数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('总页数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1571 桌面用户角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「用户列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色与权限管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色与权限管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1572 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「角色总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「当前页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('当前页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「RBAC」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('RBAC').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1573 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「树形」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('树形').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('目录', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「总计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('总计', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1574 桌面资产台账空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「资产台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产台账管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「资产总净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产总净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1575 桌面资产台账空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「待处理维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('待处理维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「闲置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('闲置率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1576 桌面资产台账空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「导出全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('导出全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('导出 PDF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1577 桌面资产台账空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「共 0 条资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText(/共\s*0\s*条资产/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('导入', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产管理', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1578 桌面运营首页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1579 桌面运营首页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1580 桌面运营首页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1581 桌面运营首页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1582 桌面处置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「全周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('全周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「本月处置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('本月处置总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产回收价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产回收价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1583 桌面处置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「清退操作将资产移出当前库存，需确认资产状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('清退操作将资产移出当前库存，需确认资产状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1584 桌面处置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 点资产调拨「调拨涉及资产归属变更，请核实转入方信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '资产调拨', exact: true }).click();
    await expect(page.getByText('调拨涉及资产归属变更，请核实转入方信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点报废转让「报废操作不可逆，资产将被永久处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '报废转让', exact: true }).click();
    await expect(page.getByText('报废操作不可逆，资产将被永久处置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 点资产赔偿「赔偿涉及财务责任认定，请核实损失金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await page.getByRole('button', { name: '资产赔偿', exact: true }).click();
    await expect(page.getByText('赔偿涉及财务责任认定，请核实损失金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1585 桌面维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「设备维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('设备维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「维保列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('维保列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「维保记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('维保记录管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1586 桌面维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「计划中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('计划中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「执行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('执行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('逾期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1587 桌面维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 点新增维保「新增维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByText('新增维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance 点新增维保「填写以下信息创建新的维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByText('填写以下信息创建新的维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance 点新增维保「请描述维保内容...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByPlaceholder('请描述维保内容...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1588 桌面维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 点新增维保「维保结果描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByPlaceholder('维保结果描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance 点新增维保「维保类型 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByText('维保类型 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance 点新增维保「维保内容 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await page.getByRole('button', { name: '新增维保' }).click();
    await expect(page.getByText('维保内容 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1589 桌面维保计划空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「周期性维保计划配置与管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('周期性维保计划配置与管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「表格视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByTitle('表格视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「卡片视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByTitle('卡片视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1590 桌面维保计划空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「日历视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByTitle('日历视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 点新建计划「新建维保计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByText('新建维保计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划「如 服务器季度维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByPlaceholder('如 服务器季度维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1591 桌面维保计划空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划「关联资产 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByText('关联资产 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划「请选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByText('请选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划「计划名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByText('计划名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1592 桌面盘点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「实时同步」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('实时同步').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('任务总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「平均进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('平均进度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1593 桌面盘点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「已盘资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('已盘资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「盘亏预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('盘亏预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「RFID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('RFID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1594 桌面盘点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('盘点任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「资产盘点管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('资产盘点管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「决策摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('决策摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1595 桌面盘点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「进度趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('进度趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('筛选', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 点筛选「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await page.getByText('筛选', { exact: true }).first().click();
    await expect(page.getByText('重置', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1596 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「备件总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件库存管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件库存管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1597 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「库存告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('库存告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「已启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('已启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「库存总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('库存总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1598 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同信息维护与到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同信息维护与到期预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「全部合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('全部合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1599 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「30天内到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('30天内到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「时间轴视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('时间轴视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('即将到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1600 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「填写以下信息以创建新合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByText('填写以下信息以创建新合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「请输入合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByPlaceholder('请输入合同名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「合同名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByText('合同名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1601 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「合同类型 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByText('合同类型 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByText('合同编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同弹窗「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByText('新增合同').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1602 桌面许可空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「许可证席位跟踪与到期管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('许可证席位跟踪与到期管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「总许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('总许可证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「有效许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('有效许可证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1603 桌面许可空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「即将到期(30天)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('即将到期(30天)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('到期预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 点新增许可证「填写以下信息以创建新许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).click();
    await expect(page.getByText('填写以下信息以创建新许可证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1604 桌面审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「按最近 7 天汇总操作趋势、风险事件和操作人分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('按最近 7 天汇总操作趋势、风险事件和操作人分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近7天」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近7天').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「总操作数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总操作数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1605 桌面审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「今日操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('今日操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「活跃用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('活跃用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「风险事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('风险事件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1606 桌面审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作趋势（近7天）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作类型分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1607 桌面审批空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待我审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('我发起的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1608 桌面审批空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('审批', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1609 桌面借用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「借用总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用记录管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1610 桌面借用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「已借出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已借出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「已逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已逾期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1611 桌面领用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「领用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('领用列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「领用归还管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('领用归还管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('长期领用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1612 桌面领用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「短期借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('短期借用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「归还入库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('归还入库').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「调拨转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('调拨转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1613 桌面领用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「已签收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('已签收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「已归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('已归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1614 桌面验收空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「管理资产入库验收全流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('管理资产入库验收全流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「待质检」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('待质检').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「部分验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('部分验收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1615 桌面验收空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「质检中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('质检中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「已验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('已验收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1616 桌面设备空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「关键生产设备维保追踪与状态监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('关键生产设备维保追踪与状态监控').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「总设备数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('总设备数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「设备列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('设备列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1617 桌面设备空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「维保中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维保中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「正常运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('正常运行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「设备状态:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('设备状态:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1618 桌面设备空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「维修中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维修中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('即将到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1619 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「设备制造商信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('设备制造商信息维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「全部制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('全部制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「有官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('有官网').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1620 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「填写以下信息以创建新制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('填写以下信息以创建新制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入制造商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入制造商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商弹窗「新增制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('新增制造商').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1621 桌面资产模型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态「管理资产品类的标准模板与规格定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('管理资产品类的标准模板与规格定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「全部模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('全部模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「全部分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('全部分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1622 桌面资产模型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型「新增资产模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('新增资产模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「定义一个新的资产模型模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('定义一个新的资产模型模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「如：Dell Latitude 5540」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByPlaceholder('如：Dell Latitude 5540').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1623 桌面资产模型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型「产品型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByPlaceholder('产品型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「模型名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('模型名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「分类 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('分类 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1624 桌面折旧空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「本月折旧总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('本月折旧总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧计划', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「资产折旧计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('资产折旧计划管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1625 桌面折旧空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「直线0 / 双倍0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('直线0 / 双倍0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1626 桌面采购空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「采购订单创建、审批与收货全流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购订单创建、审批与收货全流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「总采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('总采购单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1627 桌面采购空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「新增采购订单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByText('新增采购订单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「如 PO-2024-001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByPlaceholder('如 PO-2024-001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「请输入采购名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByPlaceholder('请输入采购名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1628 桌面采购空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「采购明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByText('采购明细').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「采购单号 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByText('采购单号 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「采购名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByText('采购名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1629 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「保险台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保险台账管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1630 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「总保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('总保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「重置筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('重置筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1631 桌面通知空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 点系统通知「当前筛选条件下没有通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await page.getByRole('button', { name: '系统通知', exact: true }).click();
    await expect(page.getByText('当前筛选条件下没有通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('全部', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「暂无通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('暂无通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1632 桌面平面图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 空态「2D/3D 平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('2D/3D 平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「平面图列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('平面图列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「请从左侧选择一个平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('请从左侧选择一个平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1633 桌面平面图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 空态「资产位置可视化 · 空间联动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('资产位置可视化 · 空间联动').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「前往 GIS 地图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('前往 GIS 地图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 空态「请新建平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByText('请新建平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1634 桌面平面图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建「新建平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByText('新建平面图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建「填写平面图名称、楼栋、楼层和图片地址后创建新的空间视图。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByText('填写平面图名称、楼栋、楼层和图片地址后创建新的空间视图。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建「平面图名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByPlaceholder('平面图名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1635 桌面平面图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建「楼栋」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByPlaceholder('楼栋').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建「楼层」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByPlaceholder('楼层').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建「图片URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByPlaceholder('图片URL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1636 桌面 GIS 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「资产地理位置分布可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('资产地理位置分布可视化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「GIS 定位数据来源于资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('GIS 定位数据来源于资产台账').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「没有已定位的资产可在地图上显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('没有已定位的资产可在地图上显示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1637 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「全量资产数量、总价值、净值等核心指标概览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('全量资产数量、总价值、净值等核心指标概览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「按资产分类统计数量和总价值的分布情况」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('按资产分类统计数量和总价值的分布情况').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「各状态（在用/闲置/退役/待审批）资产数量占比」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('各状态（在用/闲置/退役/待审批）资产数量占比').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1638 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「各部门资产数量排名与价值对比」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('各部门资产数量排名与价值对比').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「月度资产价值与净值变化趋势分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('月度资产价值与净值变化趋势分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「月度/年度折旧金额汇总统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('月度/年度折旧金额汇总统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1639 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点财务报表「各分类资产总价值占比分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('各分类资产总价值占比分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点运维报表「月度维保次数与维保费用统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('月度维保次数与维保费用统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点运维报表「月度退役资产数量及处置方式分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('月度退役资产数量及处置方式分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1640 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点工单报表「工单完成率、按时完成率、平均处理时长」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('工单完成率、按时完成率、平均处理时长').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「各部门待处理工单数量与超时工单统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('各部门待处理工单数量与超时工单统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「资产总价值与净值的月度变化趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('资产总价值与净值的月度变化趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1641 桌面风险矩阵空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「概率维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('概率维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「严重度维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('严重度维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「等级映射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('等级映射').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1642 桌面风险矩阵空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「添加维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('添加维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「维度名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByPlaceholder('维度名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「概率维度配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await expect(page.getByText('概率维度配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1643 桌面风险矩阵空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点严重度维度「严重度维度配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await page.getByRole('tab', { name: '严重度维度' }).click();
    await expect(page.getByText('严重度维度配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射「添加规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByText('添加规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射「风险等级映射规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: '创建矩阵' }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByText('风险等级映射规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1644 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 空态「合作供应商信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('合作供应商信息维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「全部供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('全部供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「合作中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('合作中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1645 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 空态「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('已停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 点新增供应商「填写以下信息以创建新供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('填写以下信息以创建新供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「请输入供应商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入供应商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1646 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「供应商名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('供应商名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商弹窗「新增供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('新增供应商').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1647 桌面字段集空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fieldsets 空态「字段集名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('字段集名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1648 桌面自定义字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「字段名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('字段名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「显示名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('显示名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1649 桌面自定义字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「选项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('选项', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('必填', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('排序', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1650 桌面自定义字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「共 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('共 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1651 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「岗位编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1652 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 点新增岗位「岗位编码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('岗位编码 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「如：CEO、CTO」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByPlaceholder('如：CEO、CTO').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1653 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位「岗位名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('岗位名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「如：董事长、首席执行官」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByPlaceholder('如：董事长、首席执行官').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「岗位描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByPlaceholder('岗位描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1654 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「邮箱/手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('邮箱/手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('部门', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1655 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「数据权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('数据权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1656 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「新增角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('新增角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 点新增角色「请输入角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByPlaceholder('请输入角色名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「如：ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByPlaceholder('如：ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1657 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色「数据权限范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('数据权限范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「全部数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('全部数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「角色描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByPlaceholder('角色描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1658 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「菜单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「系统菜单目录与按钮权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('系统菜单目录与按钮权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「菜单名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1659 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「权限标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('权限标识').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「路由」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('路由', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「按钮」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('按钮', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1660 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 点新增菜单「菜单显示名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByPlaceholder('菜单显示名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「菜单类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('菜单类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1661 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单「菜单图标」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('菜单图标').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「如：system/user」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByPlaceholder('如：system/user').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「父级菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('父级菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1662 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单「如：system/user/index」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByPlaceholder('如：system/user/index').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「可见状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('可见状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「菜单状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('菜单状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1663 桌面部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「组织架构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('组织架构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 点新增部门「部门名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「部门编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1664 桌面部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门「上级部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('上级部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「部门类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「部门领导」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门领导').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1665 桌面部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门「秘书」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('秘书').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('联系电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('邮箱').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1666 桌面部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「支持搜索过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('支持搜索过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「支持展开/收起子级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('支持展开/收起子级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('button', { name: '新增', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1667 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「用户名 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('用户名 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「初始密码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('初始密码 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「真实姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('真实姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1668 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('邮箱', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「所属部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('所属部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1669 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「分配角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('分配角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「分配岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('分配岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1670 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「暂无角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('暂无角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「暂无岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('暂无岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1671 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「账号与权限管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('账号与权限管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「搜索用户名、姓名、邮箱或手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByPlaceholder('搜索用户名、姓名、邮箱或手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 点新增用户「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1672 桌面字段集空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fieldsets 空态「自定义字段集管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('自定义字段集管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态「ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1673 桌面资料与字段集空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「当前套餐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('当前套餐').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('ADMIN', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1674 桌面资料空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「SUPER_ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('SUPER_ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 点新增部门「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('排序', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1675 桌面部门菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门「部门描述、职责说明等」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByPlaceholder('部门描述、职责说明等').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「搜索用户...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('搜索用户...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「排序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('排序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1676 桌面菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单「路由路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('路由路径').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「组件路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('组件路径').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('button', { name: '保存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1677 桌面首页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1678 桌面首页与测试结果空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/test-results 空态「加载测试结果失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/test-results');
    await expect(page.getByText('加载测试结果失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1679 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('排序', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「数字越小越靠前」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByPlaceholder('数字越小越靠前').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1680 桌面字段与菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「管理系统扩展字段定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('管理系统扩展字段定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「共 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('共 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 点新增菜单「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1681 桌面403空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/403 空态「您没有访问此页面的权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('您没有访问此页面的权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('返回上一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('返回首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1682 桌面403空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/403 空态「请联系管理员获取相应角色权限后再尝试访问」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('请联系管理员获取相应角色权限后再尝试访问').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403?reason=roles_missing 空态「用户信息不完整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403?reason=roles_missing');
    await expect(page.getByText('用户信息不完整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403?reason=roles_missing 空态「重新登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403?reason=roles_missing');
    await expect(page.getByText('重新登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1683 桌面403空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/403?reason=roles_missing 空态「当前用户角色信息缺失，无法验证访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403?reason=roles_missing');
    await expect(page.getByText('当前用户角色信息缺失，无法验证访问权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403?reason=roles_missing 空态「请重新登录后重试，如问题持续请联系管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403?reason=roles_missing');
    await expect(page.getByText('请重新登录后重试，如问题持续请联系管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('无访问权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1684 桌面forbidden空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「您没有访问此页面的权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('您没有访问此页面的权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('返回上一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('返回首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1685 桌面forbidden空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「请联系管理员获取相应角色权限后再尝试访问」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('请联系管理员获取相应角色权限后再尝试访问').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden?reason=roles_missing 空态「用户信息不完整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('用户信息不完整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden?reason=roles_missing 空态「重新登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('重新登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1686 桌面forbidden与分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden?reason=roles_missing 空态「当前用户角色信息缺失，无法验证访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('当前用户角色信息缺失，无法验证访问权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden?reason=roles_missing 空态「请重新登录后重试，如问题持续请联系管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('请重新登录后重试，如问题持续请联系管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1687 桌面分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「暂无部门排行数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无部门排行数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「暂无分类数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无分类数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1688 桌面系统空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「编辑请在 V3 工作台自定义字段页操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('编辑请在 V3 工作台自定义字段页操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 点新增菜单「菜单名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('菜单名称', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1689 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('角色名称', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('角色编码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1690 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('button', { name: '保存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 空态「暂无角色数据，点击「新增角色」创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('暂无角色数据，点击「新增角色」创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1691 桌面角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色「自定义部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await page.getByText('全部数据').first().click();
    await expect(page.getByText('自定义部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「本部门数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await page.getByText('全部数据').first().click();
    await expect(page.getByText('本部门数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「本部门及以下」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await page.getByText('全部数据').first().click();
    await expect(page.getByText('本部门及以下').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1692 桌面角色部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色「仅本人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await page.getByText('全部数据').first().click();
    await expect(page.getByText('仅本人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1693 桌面岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「新增岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).click();
    await expect(page.getByText('新增岗位').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1694 桌面能耗用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/energy 空态「暂无能耗数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByText('暂无能耗数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1695 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('重置', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1696 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1697 桌面用户空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 空态「暂无用户数据，可调整筛选或新建用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('暂无用户数据，可调整筛选或新建用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1698 桌面菜单部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单「无匹配图标」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await page.getByPlaceholder('搜索图标...').fill('zzzzz');
    await expect(page.getByText('无匹配图标').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单选按钮「如：system:user:add」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await page.locator('select').first().selectOption('F');
    await expect(page.getByPlaceholder('如：system:user:add').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1699 桌面菜单通知空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单选按钮「权限标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await page.locator('select').first().selectOption('F');
    await expect(page.getByText('权限标识').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/notifications 空态「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('通知中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「全部已读」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('全部已读').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1700 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点财务报表「折旧统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('折旧统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「分类价值分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('分类价值分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1701 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点运维报表「维保统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('维保统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点运维报表「退役处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('退役处置统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「工单完成率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('工单完成率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1702 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点工单报表「待处理工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('待处理工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「月度/年度折旧金额汇总统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('月度/年度折旧金额汇总统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点财务报表「各分类资产总价值占比分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('各分类资产总价值占比分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1703 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点财务报表「资产总价值与净值的月度变化趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '财务报表' }).click();
    await expect(page.getByText('资产总价值与净值的月度变化趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点运维报表「月度维保次数与维保费用统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('月度维保次数与维保费用统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「工单完成率、按时完成率、平均处理时长」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('工单完成率、按时完成率、平均处理时长').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1704 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 点运维报表「月度退役资产数量及处置方式分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '运维报表' }).click();
    await expect(page.getByText('月度退役资产数量及处置方式分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点工单报表「各部门待处理工单数量与超时工单统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByRole('button', { name: '工单报表' }).click();
    await expect(page.getByText('各部门待处理工单数量与超时工单统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「导出 CSV」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('导出 CSV').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1705 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('导出 PDF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「报表中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('报表中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「预定义报表查看与数据可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('预定义报表查看与数据可视化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1706 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「近 12 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('近 12 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点周期「近 6 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByText('近 12 个月').first().click();
    await expect(page.getByText('近 6 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 点周期「近 24 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await page.getByText('近 12 个月').first().click();
    await expect(page.getByText('近 24 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1707 桌面报表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「2026-05-23」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('2026-05-23').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「2026-05-22」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('2026-05-22').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「2026-05-21」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('2026-05-21').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1708 桌面登录空态', () => {
  test('/login 空态「资产全生命周期管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资产全生命周期管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「智能审批 · 实时盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('智能审批 · 实时盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「固定资产平台的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('固定资产平台的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1709 桌面登录空态', () => {
  test('/login 空态「智能运维入口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('智能运维入口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「从采购到报废，一个平台全覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('从采购到报废，一个平台全覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「流程闭环，数据驱动决策」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('流程闭环，数据驱动决策').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1710 桌面登录空态', () => {
  test('/login 空态「运行稳定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('运行稳定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW · 宇视科技」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW · 宇视科技').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「使用组织账号登录，进入固定资产工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('使用组织账号登录，进入固定资产工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1711 桌面登录空态', () => {
  test('/login 空态「全息版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全息版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「流星版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('流星版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「浙江宇视科技有限公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('浙江宇视科技有限公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1712 桌面登录空态', () => {
  test('/login 空态「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1713 桌面登录空态', () => {
  test('/login 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('button', { name: '登录系统' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「MaxKey 单点登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('MaxKey 单点登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1714 桌面登录空态', () => {
  test('/login 空态「使用组织统一身份认证登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('使用组织统一身份认证登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW 固定资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW 固定资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('固定资产平台', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1715 桌面登录空态', () => {
  test('/login 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('用户名', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('密码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW 固定资产连接资产台账、流程审批、盘点巡检和运营分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW 固定资产连接资产台账、流程审批、盘点巡检和运营分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1716 桌面登录空态', () => {
  test('/login 空态「帮助团队高效进入固定资产工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('帮助团队高效进入固定资产工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1717 桌面登录空态', () => {
  test('/login 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1718 桌面登录空态', () => {
  test('/login 空态「全生命周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全生命周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1719 桌面登录空态', () => {
  test('/login5 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('登录系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「使用组织账号进入固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('使用组织账号进入固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「开发环境快速体验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('开发环境快速体验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1720 桌面登录空态', () => {
  test('/login 点登录系统「请输入用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await page.getByRole('button', { name: '登录系统' }).click();
    await expect(page.getByText('请输入用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 点登录系统「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await page.getByRole('button', { name: '登录系统' }).click();
    await expect(page.getByText('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1721 桌面登录空态', () => {
  test('/login4 空态「连接 MES、设备状态、维保工单与资产全生命周期。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('连接 MES、设备状态、维保工单与资产全生命周期。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「安全单点登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('安全单点登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「设备健康预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('设备健康预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1722 桌面登录空态', () => {
  test('/login4 空态「运维闭环协同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('运维闭环协同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「开发环境快速体验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('开发环境快速体验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 固定资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 固定资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1723 桌面登录空态', () => {
  test('/login4 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('登录系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「使用组织账号进入固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('使用组织账号进入固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1724 桌面登录空态', () => {
  test('/login4 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('用户名', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('密码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1725 桌面登录空态', () => {
  test('/login4 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('登录并进入工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 统一身份认证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1726 桌面登录空态', () => {
  test('/login4 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「设计稿版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('设计稿版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「当前版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('当前版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1727 桌面登录空态', () => {
  test('/login4 空态「标准版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('标准版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全息版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全息版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「流星版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('流星版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1728 桌面登录空态', () => {
  test('/login4 空态「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 点登录「请输入用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await page.getByText('登录并进入工作台').click();
    await expect(page.getByText('请输入用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1729 桌面供应商门户空态', () => {
  test('/vendor-portal 空态「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('button', { name: '登录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1730 桌面门户登录空态', () => {
  test('/vendor-portal 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1731 桌面登录空态', () => {
  test('/login4 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1732 桌面登录空态', () => {
  test('/login4 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全生命周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全生命周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1733 桌面登录空态', () => {
  test('/login4 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1734 桌面登录空态', () => {
  test('/login5 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1735 桌面登录空态', () => {
  test('/login5 空态「全生命周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('全生命周期', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1736 桌面登录空态', () => {
  test('/login5 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('登录并进入工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1737 桌面登录空态', () => {
  test('/login5 空态「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('UNIVIEW 统一身份认证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('用户名', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('密码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1738 桌面登录空态', () => {
  test('/login5 空态「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 点登录「请输入用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await page.getByText('登录并进入工作台').click();
    await expect(page.getByText('请输入用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1739 桌面登录分析空态', () => {
  test('/login5 点登录「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await page.getByText('登录并进入工作台').click();
    await expect(page.getByText('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 点数据范围「近 6 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/analytics');
    await page.getByText('近 12 个月').first().click();
    await expect(page.getByText('近 6 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 点登录「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await page.getByText('登录并进入工作台').click();
    await expect(page.getByText('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1740 桌面SSO空态', () => {
  test('/sso-callback 空态「Token 缺失，SSO 登录失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「返回登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('返回登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1741 桌面分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「暂无分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('暂无分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「请输入分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByPlaceholder('请输入分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1742 桌面分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类「分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('分类编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「请输入分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByPlaceholder('请输入分类编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「创建一个新的根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('创建一个新的根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1743 桌面分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByRole('button', { name: '创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类空提交「分类名称不能为空」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await page.getByRole('button', { name: '创建' }).click();
    await expect(page.getByText('分类名称不能为空').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1744 桌面分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类填名称「分类编码不能为空」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await page.getByPlaceholder('请输入分类名称').fill('测试分类');
    await page.getByRole('button', { name: '创建' }).click();
    await expect(page.getByText('分类编码不能为空').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 空态「资产分类管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('资产分类管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('添加根分类').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1745 桌面位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态「位置管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('位置管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「暂无位置数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('暂无位置数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 点新增顶级位置「新增位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByText('新增位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1746 桌面位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 点新增顶级位置「位置名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByText('位置名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「如 A栋3层」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByPlaceholder('如 A栋3层').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1747 桌面位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 点新增顶级位置「位置编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByText('位置编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「如 RD-A-3F」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByPlaceholder('如 RD-A-3F').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1748 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「填写以下信息以创建新制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('填写以下信息以创建新制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入制造商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入制造商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1749 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('编码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('联系人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1750 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('邮箱').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1751 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('官网').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入官网').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「国家」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('国家').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1752 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByText('地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1753 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「请输入联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入联系人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入邮箱').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「请输入备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入备注信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1754 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「请输入国家」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByPlaceholder('请输入国家').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 空态「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('名称', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1755 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('编码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('联系人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1756 桌面制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「国家」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('国家').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1757 桌面制造商供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「制造商管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('制造商管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 点新增供应商「供应商名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('供应商名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「请输入供应商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入供应商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1758 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「如 V001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('如 V001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('联系人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1759 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('联系电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「请输入联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入联系电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('邮箱').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1760 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「请输入地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「请输入邮箱地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入邮箱地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1761 桌面供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「请输入联系人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入联系人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 空态「供应商管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('供应商管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1762 桌面供应商合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「合同名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('合同名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「请输入合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('请输入合同名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1763 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('合同编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「如 C001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('如 C001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「合同类型 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('合同类型 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1764 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('金额', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「货币」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('货币').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「请输入备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('请输入备注信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1765 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1766 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「0.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('0.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1767 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 点新增合同「CNY」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('CNY').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1768 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「维保合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').first().click();
    await expect(page.getByText('维保合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「采购合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').first().click();
    await expect(page.getByText('采购合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「租赁合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').first().click();
    await expect(page.getByText('租赁合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1769 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「服务合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').first().click();
    await expect(page.getByText('服务合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').nth(1).click();
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').nth(1).click();
    await expect(page.getByText('草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1770 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「已到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').nth(1).click();
    await expect(page.getByText('已到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('请选择').nth(1).click();
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「USD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByText('CNY').first().click();
    await expect(page.getByRole('option', { name: 'USD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1771 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同名称', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1772 桌面合同空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('金额', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 点新增空提交「请输入合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await page.getByRole('button', { name: '确认新增' }).click();
    await expect(page.getByText('请输入合同名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1773 桌面采购单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「供应商 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('供应商 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「采购日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('采购日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「预计到货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('预计到货').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1774 桌面采购单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「请输入备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByPlaceholder('请输入备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByPlaceholder('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByPlaceholder('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1775 桌面采购单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「资产名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('资产名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('数量', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「添加行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByRole('button', { name: '添加行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1776 桌面采购单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('单价').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1777 桌面采购单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「采购订单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购订单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('金额', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1778 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「软件名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('软件名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请输入软件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入软件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('厂商', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1779 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「请输入厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入厂商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「软件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('软件类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请输入软件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入软件类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1780 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('版本', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请输入版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「授权类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('授权类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1781 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「总席位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('总席位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请输入总席位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入总席位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「购买价格」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('购买价格').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1782 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「0.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('0.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「采购单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('采购单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请输入采购单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入采购单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1783 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「请输入备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入备注信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('button', { name: '确认新增' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1784 桌面许可证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「软件许可证管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('软件许可证管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 点新增许可证「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「请选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('请选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1785 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「请输入保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1786 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「请输入保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1787 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「请输入保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保费', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「请输入保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1788 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保额', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「请输入保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('免赔额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1789 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「请输入免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入免赔额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1790 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1791 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「请输入备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「财产险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('财产险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1792 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 点类型「责任险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByText('财产险').first().click();
    await expect(page.getByText('责任险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 点类型「车险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByText('财产险').first().click();
    await expect(page.getByText('车险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1793 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: '保存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 点状态「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByText('生效中').first().click();
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 点状态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByText('生效中').first().click();
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1794 桌面保险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 点保存「请输入保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByRole('button', { name: '保存' }).click();
    await expect(page.getByText('请输入保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 点保存「请输入保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByRole('button', { name: '保存' }).click();
    await expect(page.getByText('请输入保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 点保存「请输入保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await page.getByRole('button', { name: '保存' }).click();
    await expect(page.getByText('请输入保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1795 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「请输入备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1796 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「请输入备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「请输入规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1797 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「计量单位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('计量单位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('当前库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1798 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('安全库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('单价').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「申请信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('申请信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1799 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「建议补足缺口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('建议补足缺口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「待确认」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('待确认').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「预计到货时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('预计到货时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1800 桌面备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「备件申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: '提交申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1801 桌面备件预算空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「按维保工单、库存下限和供应商交期创建备件保障记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('按维保工单、库存下限和供应商交期创建备件保障记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算年度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1802 桌面预算新建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「预算类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「采购预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const typeSelect = page.locator('select').filter({ hasText: '采购预算' }).first();
    await typeSelect.click();
    await expect(typeSelect).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「创建预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '创建预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1803 桌面预算新建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('分类ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1804 桌面预算新建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const statusSelect = page.locator('select').filter({ hasText: '草稿' }).first();
    await statusSelect.click();
    await expect(statusSelect).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1805 桌面预算新建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「维保预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const sel = page.locator('select').filter({ hasText: '维保预算' }).first();
    await sel.click();
    await expect(sel).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「运营预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const sel = page.locator('select').filter({ hasText: '运营预算' }).first();
    await sel.click();
    await expect(sel).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const sel = page.locator('select').filter({ hasText: '已审批' }).first();
    await sel.click();
    await expect(sel).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1806 桌面预算空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「已关闭」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    const sel = page.locator('select').filter({ hasText: '已关闭' }).first();
    await sel.click();
    await expect(sel).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「预算执行进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByRole('heading', { name: '预算执行进度' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「审批人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('审批人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1807 桌面预算详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 空态「预算详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByRole('heading', { name: '预算详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1808 桌面预算详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 空态「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('已使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('已承诺').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「剩余」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('剩余').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1809 桌面预算详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 空态「执行率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByText('执行率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByRole('button', { name: '编辑' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 空态「删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await expect(page.getByRole('button', { name: '删除' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1810 桌面预算删除确认空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 点删除「确认删除预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await page.getByRole('button', { name: '删除' }).click();
    await expect(page.getByRole('heading', { name: '确认删除预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 点删除「此操作不可撤销。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await page.getByRole('button', { name: '删除' }).click();
    await expect(page.getByText('此操作不可撤销。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 点删除「确认删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await page.getByRole('button', { name: '删除' }).click();
    await expect(page.getByRole('button', { name: '确认删除' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1811 桌面预算详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 点删除「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1');
    await page.getByRole('button', { name: '删除' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 详情「采购」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'PURCHASE', status:'DRAFT', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('采购').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 详情「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'PURCHASE', status:'DRAFT', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1812 桌面预算编辑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1/edit 空态「编辑预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1/edit');
    await expect(page.getByRole('heading', { name: '编辑预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1/edit 空态「修改预算信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1/edit');
    await expect(page.getByText('修改预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1/edit 空态「更新预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/1/edit');
    await expect(page.getByRole('button', { name: '更新预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1813 桌面预算详情变体空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 详情「维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'MAINTENANCE', status:'DRAFT', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 详情「运营」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'OPERATION', status:'DRAFT', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('运营').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 详情「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'PURCHASE', status:'APPROVED', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1814 桌面预算详情状态与删除', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/1 详情「已关闭」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'PURCHASE', status:'CLOSED', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('已关闭').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 失败态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/1'));
    await page.goto('/budgets/1');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/1 详情「2026年 · 采购预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/budgets/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, budgetYear:2026, budgetType:'PURCHASE', status:'DRAFT', totalAmount:100, usedAmount:0, committedAmount:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/budgets/1');
    await expect(page.getByText('2026年 · 采购预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1815 桌面保险编辑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1/edit 空态「编辑保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1/edit');
    await expect(page.getByText('编辑保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1/edit 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1/edit');
    await expect(page.getByRole('button', { name: '保存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1/edit 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1/edit');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1816 桌面保险详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「保险详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保险详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「理赔记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('heading', { name: '理赔记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「新增理赔」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('button', { name: '新增理赔' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1817 桌面保险理赔表列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「理赔编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('理赔编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「理赔日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('理赔日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「理赔金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('理赔金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1818 桌面保险理赔表列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「已赔付金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('已赔付金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「赔付日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('赔付日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「事故描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('事故描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1819 桌面保险状态变体空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 详情「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/insurance/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, policyNo:'P1', insuranceName:'E2E', insuranceType:'PROPERTY', status:'EXPIRED', premium:1, coverage:1, deductible:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/insurances/1');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 详情「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/insurance/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, policyNo:'P1', insuranceName:'E2E', insuranceType:'PROPERTY', status:'CANCELLED', premium:1, coverage:1, deductible:0 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/insurances/1');
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 理赔「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/insurance/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, policyNo:'P1', insuranceName:'E2E', insuranceType:'PROPERTY', status:'ACTIVE', premium:1, coverage:1, deductible:0 } }) });
      }
      if (path === '/insurance/1/claims') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { records: [{ id:1, claimNo:'C1', status:'PENDING', claimAmount:1, settledAmount:0 }], total:1 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/insurances/1');
    await expect(page.getByText('待处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1820 桌面保险理赔状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 理赔「已批准」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/insurance/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, policyNo:'P1', insuranceName:'E2E', insuranceType:'PROPERTY', status:'ACTIVE', premium:1, coverage:1, deductible:0 } }) });
      }
      if (path === '/insurance/1/claims') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { records: [{ id:1, claimNo:'C1', status:'APPROVED', claimAmount:1, settledAmount:1 }], total:1 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/insurances/1');
    await expect(page.getByText('已批准').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 理赔「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/insurance/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, policyNo:'P1', insuranceName:'E2E', insuranceType:'PROPERTY', status:'ACTIVE', premium:1, coverage:1, deductible:0 } }) });
      }
      if (path === '/insurance/1/claims') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { records: [{ id:1, claimNo:'C1', status:'REJECTED', claimAmount:1, settledAmount:0 }], total:1 } }) });
      }
      return mockApi(route);
    });
    await page.goto('/insurances/1');
    await expect(page.getByText('已拒绝').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1821 桌面保险详情操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('button', { name: '编辑' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1822 桌面保险详情字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保险类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1823 桌面保险详情金额空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('保额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('免赔额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1824 桌面保险详情日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 空态「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1825 桌面备件详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByRole('button', { name: '编辑' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('当前库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1826 桌面备件库存概况空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 空态「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('安全库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「库存状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('库存状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「库存正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('库存正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1827 桌面备件基本信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1828 桌面备件字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 空态「计量单位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('计量单位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('单价').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「领用记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByRole('heading', { name: '领用记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1829 桌面备件状态变体空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 详情「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 详情「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/spare-parts/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, partName:'E2E备件', partNo:'SP-001', currentStock:1, safetyStock:0, status:'ENABLED', unit:'个' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/spare-parts/1');
    await expect(page.getByText('启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 详情「缺货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/spare-parts/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, partName:'E2E备件', partNo:'SP-001', currentStock:1, safetyStock:5, status:'ENABLED', unit:'个' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/spare-parts/1');
    await expect(page.getByText('缺货').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1830 桌面备件编码与领用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 空态「编码:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('编码:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/1 领用「工单 #」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/spare-parts/1/usages') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: [{ id:1, workOrderId:9, quantity:1, usageDate:'2026-01-01T00:00:00' }] }) });
      }
      return mockApi(route);
    });
    await page.goto('/spare-parts/1');
    await expect(page.getByText('工单 #').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1831 桌面入库详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 空态「验收单详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '验收单详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「验收单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByText('验收单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1832 桌面入库详情字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 空态「验收日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByText('验收日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「总金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByText('总金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1833 桌面入库详情区块空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 空态「检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '检查项' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「入库资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '入库资产列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1834 桌面入库详情操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 草稿「提交验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '提交验收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 草稿「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 待质检「验收通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'PENDING_INSPECT' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '验收通过' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1835 桌面入库详情操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 待质检「保存质检结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'PENDING_INSPECT' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '保存质检结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 待质检「驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'PENDING_INSPECT' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '驳回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 失败态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/1'));
    await page.goto('/intake/1');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1836 桌面入库检查项表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 检查项「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观', expectedValue:'完好' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 检查项「预期值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观', expectedValue:'完好' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('预期值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 检查项「实际值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观', expectedValue:'完好' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('实际值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1837 桌面入库表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 检查项「结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 检查项表「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 资产「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1838 桌面入库资产表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 资产「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 资产「品牌」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('品牌').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 资产「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1839 桌面入库资产与待检空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 资产「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 资产「购置日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', intakeAssets: [{ id:1, assetNo:'A1', assetName:'设备' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('购置日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 检查项「待检」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('待检').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1840 桌面入库结果变体空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/1 检查项「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观', result:'PASS' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 检查项「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'DRAFT', checkItems: [{ id:1, itemName:'外观', result:'FAIL' }] } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 详情「驳回原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/intake-orders/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, orderNo:'IN-1', status:'REJECTED', rejectReason:'规格不符' } }) });
      }
      return mockApi(route);
    });
    await page.goto('/intake/1');
    await expect(page.getByText('驳回原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1841 桌面领用编辑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/1/edit 空态「编辑领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1/edit');
    await expect(page.getByRole('heading', { name: '编辑领用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1/edit 空态「保存修改」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1/edit');
    await expect(page.getByRole('button', { name: '保存修改' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1/edit 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1/edit');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1842 桌面借用编辑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/1/edit 空态「编辑借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByRole('heading', { name: '编辑借用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「保存修改」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByRole('button', { name: '保存修改' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1843 桌面借用编辑表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/1/edit 空态「借用信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByRole('heading', { name: '借用信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「借用用途」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByText('借用用途').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1844 桌面借用编辑字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/1/edit 空态「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByText('资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1/edit');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1845 桌面检验编辑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「编辑检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('编辑检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1846 桌面检验编辑操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1847 桌面检验编辑日期机构人空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「下次检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('下次检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1848 桌面检验编辑结果发现照片空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「检验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检查发现」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检查发现').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1849 桌面检验编辑证书费用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「证书到期日」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('证书到期日').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「检验费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('检验费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1850 桌面检验编辑附件更新取消空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「报告附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('报告附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「更新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByRole('button', { name: /更\s*新/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1851 桌面检验编辑扫码占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/edit 空态「扫码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByRole('button', { name: /扫\s*码/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「自动生成或手动输入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByPlaceholder('自动生成或手动输入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 空态「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/edit');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1852 桌面检验上传标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      if (path === '/inspections/1/photos') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: [] }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload 空态「检验照片上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('检验照片上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「上传新照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('上传新照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「选择照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('选择照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1853 桌面检验上传已传空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      if (path === '/inspections/1/photos') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: [] }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload 空态「已上传照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('已上传照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「暂无已上传的照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('暂无已上传的照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「返回详情页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('button', { name: '返回详情页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1854 桌面检验上传面包屑空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      if (path === '/inspections/1/photos') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: [] }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload 空态「检验记录列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('检验记录列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「检验记录详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('检验记录详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「照片上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('照片上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1855 桌面检验上传编号张数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      if (path === '/inspections/1/photos') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: [] }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload 空态「检验编号:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('检验编号:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「共 0 张」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('共 0 张').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload 空态「INSP-001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('INSP-001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1856 桌面检验详情标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「检验详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('button', { name: /编\s*辑/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1857 桌面检验详情类型日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1858 桌面检验详情结果机构空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「下次检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('下次检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1859 桌面检验详情人证空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「证书到期日」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('证书到期日').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1860 桌面检验详情费用模板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「检验费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「报告附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('报告附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1861 桌面检验详情发现结果值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/inspections/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, inspectionNo:'INSP-001', assetId:1, inspectionType:'ANNUAL', result:'PENDING' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/inspections/1 空态「检查发现」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('检查发现').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('待检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 空态「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByText('年度检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1862 桌面检验新建标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「新增检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('新增检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1863 桌面领用详情标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「领用单详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('领用单详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「提交审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('提交审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('button', { name: /编\s*辑/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1864 桌面领用详情基本信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1865 桌面领用详情类型状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「领用类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('领用类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1866 桌面领用详情日期状态流空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1867 桌面领用详情后续状态流空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「已签收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('已签收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「待归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('待归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1868 桌面领用详情资产值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/assignments/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'A-001', assetName:'笔记本', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/assignments/1 空态「已归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('已归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「A-001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('A-001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 空态「笔记本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByText('笔记本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1869 桌面借用详情标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/borrows/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'B-001', assetName:'投影仪', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/borrows/1 空态「借用详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('借用详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「提交审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('提交审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByRole('button', { name: /编\s*辑/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1870 桌面借用详情信息字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/borrows/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'B-001', assetName:'投影仪', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/borrows/1 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「详细信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('详细信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1871 桌面借用详情名称状态日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/borrows/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'B-001', assetName:'投影仪', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/borrows/1 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「借用日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('借用日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1872 桌面借用详情日期草稿编号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/borrows/1') {
        return route.fulfill({ status:200, contentType:'application/json', body: JSON.stringify({ code:200, message:'OK', data: { id:1, assetNo:'B-001', assetName:'投影仪', status:'DRAFT' } }) });
      }
      return mockApi(route);
    });
    await seedSession(page, adminUser);
  });

  test('/borrows/1 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「投影仪」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('投影仪').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 空态「B-001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByText('B-001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1873 桌面领用新建标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('新建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「创建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: '创建领用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1874 桌面领用新建字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「领用类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('领用类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1875 桌面领用新建使用人日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「使用人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('使用人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「使用部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('使用部门 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1876 桌面领用新建备注取消空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('长期领用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1877 桌面领用新建类型选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「短期借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('短期借用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「归还入库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('归还入库').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「调拨转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('调拨转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1878 桌面领用新建占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「使用人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用人 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「使用部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用部门 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 placeholder「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1879 桌面借用新建标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「创建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: '创建借用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1880 桌面借用新建字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「借用信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('借用信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1881 桌面借用新建用途备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「借用用途」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('借用用途').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1882 桌面检验模板管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「检验模板管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验模板管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: /搜\s*索/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: /重\s*置/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1883 桌面检验模板列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「检验周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1884 桌面检验模板状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1885 桌面检验模板筛选占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态 placeholder「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByPlaceholder('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 placeholder「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验类型', { exact: true }).nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1886 桌面检验记录管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「检验记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验记录管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「创建、编辑和追踪设备检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('创建、编辑和追踪设备检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「批量生成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: /批\s*量\s*生\s*成/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1887 桌面检验记录统计新增空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: /新\s*增\s*检\s*验/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1888 桌面检验记录待检搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('待检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「已过期/不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('已过期/不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: /搜\s*索/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1889 桌面检验记录筛选占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: /重\s*置/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「至」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('至').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 placeholder「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1890 桌面检验记录类型筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('年度检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('定期检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1891 桌面检验记录结果筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「专项检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('专项检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「全部结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('全部结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1892 桌面检验记录列头编号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「附条件通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('附条件通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1893 桌面检验记录类型日期列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「下次检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('下次检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1894 桌面检验记录机构结果列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1895 桌面检验记录操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('暂无检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「当前没有可显示的数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('当前没有可显示的数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1896 桌面盘点周期新建标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「新建盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('新建盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「周期名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('周期名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1897 桌面盘点周期新建类型保存空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「盘点类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('盘点类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态 placeholder「例如：2024年6月循环盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByPlaceholder('例如：2024年6月循环盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1898 桌面盘点周期列表标题统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「循环盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('循环盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「管理库存盘点计划与执行周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('管理库存盘点计划与执行周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「总周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('总周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1899 桌面盘点周期列表统计状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1900 桌面盘点周期列表筛选胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('全部状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已暂停').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1901 桌面盘点周期列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「周期名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('周期名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1902 桌面盘点周期列表时间操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「开始时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('开始时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「结束时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('结束时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1903 桌面盘点周期详情信息卡空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「周期信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('周期信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「统计信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('统计信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1904 桌面盘点周期详情任务统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「总任务数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('总任务数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「待盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('待盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「已盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('已盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1905 桌面盘点周期详情进度导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「已调整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('已调整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「完成进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('完成进度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「导出 PDF 报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('导出 PDF 报告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1906 桌面盘点周期详情任务操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「分配任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('分配任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「盘点任务列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('盘点任务列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「E2E周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('E2E周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1907 桌面盘点周期详情字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「开始时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('开始时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1908 桌面盘点周期详情结束状态操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 空态「结束时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('结束时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('已计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1909 桌面循环盘点规则标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 空态「循环盘点规则配置（ABC分类）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('循环盘点规则配置（ABC分类）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「触发A类盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('触发A类盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「A类（关键资产）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('A类（关键资产）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1910 桌面循环盘点规则列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 空态「分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「盘点频率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('盘点频率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「最小价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('最小价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1911 桌面循环盘点规则价值操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 空态「最大价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('最大价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「B类（重要资产）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('B类（重要资产）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1912 桌面循环盘点规则说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 空态「C类（一般资产）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('C类（一般资产）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「季度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('季度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「年度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('年度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1913 桌面循环盘点规则新增弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 点新增规则「ABC分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('ABC分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则「盘点频率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('盘点频率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则「最小价值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('最小价值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1914 桌面循环盘点规则弹窗价值分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 点新增规则「最大价值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('最大价值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则「适用资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('适用资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则 placeholder「不填表示无下限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByPlaceholder('不填表示无下限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1915 桌面循环盘点规则弹窗上限确定空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 点新增规则 placeholder「不填表示无上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByPlaceholder('不填表示无上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则 placeholder「JSON数组：[1,2,3] 或留空表示全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByPlaceholder('JSON数组：[1,2,3] 或留空表示全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则「确定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByRole('button', { name: /确\s*定/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1916 桌面循环盘点规则弹窗取消标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 点新增规则「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 点新增规则弹窗「新增规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await page.getByRole('button', { name: /新\s*增\s*规\s*则/ }).click();
    await expect(page.getByText('新增规则').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inventory/cycle-count 空态「月度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('月度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1917 桌面ABC分类管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「ABC 分类管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('ABC 分类管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「ABC 分类规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('ABC 分类规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1918 桌面ABC分类规则说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「A 类（高价值）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('A 类（高价值）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「B 类（中价值）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('B 类（中价值）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「C 类（低价值）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('C 类（低价值）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1919 桌面ABC分类列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1920 桌面ABC分类列头说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「ABC 分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('ABC 分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「需月度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('需月度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1921 桌面ABC分类盘点周期说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification 空态「需季度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('需季度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「需年度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('需年度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 点批量重新分类「确定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await page.getByRole('button', { name: /批\s*量\s*重\s*新\s*分\s*类/ }).click();
    await expect(page.getByRole('button', { name: /确\s*定/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1922 桌面安全检查模板标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 空态「安全检查表模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('安全检查表模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「配置检查表模板与检查项清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('配置检查表模板与检查项清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「全部模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('全部模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1923 桌面安全检查模板统计列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 空态「已启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('已启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「已禁用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('已禁用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「模板列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('模板列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1924 桌面安全检查模板列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 空态「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('检查项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1925 桌面安全检查模板操作筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 空态「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1926 桌面安全检查模板新增弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 空态「禁用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('禁用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByText('模板名称').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板 placeholder「如：消防安全检查表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByPlaceholder('如：消防安全检查表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1927 桌面安全检查模板弹窗分类确认空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 点新增模板「适用资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByText('适用资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByText('确认新增').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板 placeholder「JSON数组：[1,2,3] 或留空」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByPlaceholder('JSON数组：[1,2,3] 或留空').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1928 桌面安全检查模板取消与历史标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 点新增模板「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/history 空态「安全检查历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('安全检查历史').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「查看安全检查执行记录与结果明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('查看安全检查执行记录与结果明细').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1929 桌面安全检查历史统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history 空态「全部执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('全部执行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1930 桌面安全检查历史记录列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「执行记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('执行记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「执行ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('执行ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1931 桌面安全检查历史ID列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history 空态「模板ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('模板ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「执行人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('执行人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1932 桌面安全检查历史结果列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history 空态「执行日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('执行日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「总体结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('总体结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1933 桌面安全检查历史操作筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history 空态「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1934 桌面安全检查执行开始空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「开始安全检查」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute');
    await expect(page.getByText('开始安全检查').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「安全检查」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute');
    await expect(page.getByText('安全检查').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「开始执行检查」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute');
    await expect(page.getByText('开始执行检查').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1935 桌面安全检查执行检查项与保存空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「包含 0 个检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute');
    await expect(page.getByText('包含 0 个检查项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute/1 空态「安全检查执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute/1');
    await expect(page.getByText('安全检查执行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute/1 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute/1');
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1936 桌面安全检查完成与风险矩阵标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute/1 空态「完成检查」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute/1');
    await expect(page.getByRole('button', { name: /完\s*成\s*检\s*查/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「风险矩阵」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('风险矩阵').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「5x5 风险评估热力图与评估记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('5x5 风险评估热力图与评估记录管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1937 桌面风险矩阵区块标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「风险矩阵（5x5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('风险矩阵（5x5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「风险评估记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('风险评估记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「极低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('极低').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1938 桌面风险矩阵轴标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「极高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('极高').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「极小」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('极小').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「极大」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('极大').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1939 桌面风险矩阵等级筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「重大」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('重大').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「高危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('高危').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「中危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('中危').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1940 桌面风险矩阵低危筛选列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「低危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('低危').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1941 桌面风险矩阵评估列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「可能性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('可能性').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「影响程度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('影响程度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('风险等级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1942 桌面风险矩阵措施操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('评审日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1943 桌面风险评估新建标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「新增风险评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('新增风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「可能性（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('可能性（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1944 桌面风险评估新建表单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「影响程度（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('影响程度（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评审日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1945 桌面风险评估新建提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1946 桌面风险评估新建取消占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「描述风险缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('描述风险缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1947 桌面风险评估新建等级提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「自动计算的风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('自动计算的风险等级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「LOW（低危）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('LOW（低危）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「1 - 极低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('1 - 极低').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1948 桌面风险矩阵配置标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 空态「风险矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('风险矩阵配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「管理风险评估的概率维度、严重度维度和等级映射规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('管理风险评估的概率维度、严重度维度和等级映射规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「创建矩阵」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('创建矩阵').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1949 桌面风险矩阵配置列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 空态「矩阵名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('矩阵名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1950 桌面风险矩阵配置空表弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 空态「暂无矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByText('暂无矩阵配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「创建矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('创建矩阵配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1951 桌面风险矩阵配置弹窗字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「矩阵名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('矩阵名称').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵 placeholder「输入矩阵名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByPlaceholder('输入矩阵名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「概率维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('概率维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1952 桌面风险矩阵配置弹窗维度页签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「严重度维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('严重度维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「等级映射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('等级映射').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「添加维度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('添加维度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1953 桌面风险矩阵配置弹窗提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「概率维度配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByText('概率维度配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵弹窗「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByRole('button', { name: /^创\s*建$/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q1954 桌面风险评估编辑标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/1/edit 空态「编辑风险评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('编辑风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「可能性（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('可能性（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1955 桌面风险评估编辑字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/1/edit 空态「影响程度（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('影响程度（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「更新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByRole('button', { name: /更\s*新/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1956 桌面风险评估编辑提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/1/edit 空态「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('评审日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/1/edit 空态「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/1/edit');
    await expect(page.getByText('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1957 桌面SAM合规标题统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「SAM 合规管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('SAM 合规管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「总许可数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('总许可数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「合规」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('合规').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1958 桌面SAM合规用量统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「超用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('超用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「闲置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('闲置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1959 桌面SAM合规图表标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「合规率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('合规率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「许可类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('许可类型分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「风险告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('风险告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1960 桌面SAM扫描历史标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「席位使用率 TOP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('席位使用率 TOP').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「扫描历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('扫描历史').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「扫描ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('扫描ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1961 桌面SAM扫描历史列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「扫描时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('扫描时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「总许可」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('总许可').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1962 桌面报表构建器标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「自定义报表构建器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('自定义报表构建器').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「通过拖拽选择字段，快速创建自定义报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('通过拖拽选择字段，快速创建自定义报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「可用字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('可用字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1963 桌面报表构建器配置区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「报表名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('报表名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「报表类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('报表类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「图表类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('图表类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1964 桌面报表构建器操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「已选字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('已选字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「生成预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('生成预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「保存报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('保存报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1965 桌面报表构建器预览图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「数据预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('数据预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「表格」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('表格').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「柱状图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('柱状图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1966 桌面报表构建器图表占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「折线图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('折线图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「饼图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('饼图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态 placeholder「输入报表名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByPlaceholder('输入报表名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1967 桌面报表构建器资产字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「资产编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('资产编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1968 桌面报表构建器价值字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「购入日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('购入日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1969 桌面报表构建器位置运维字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「使用部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('使用部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「工单数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('工单数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1970 桌面报表构建器运维字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「保养次数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('保养次数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「故障次数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('故障次数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「MTBF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('MTBF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1971 桌面报表构建器财务字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「MTTR」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('MTTR').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「折旧金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('折旧金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「维保费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('维保费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1972 桌面报表构建器成本分组空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「能耗费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('能耗费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「总成本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('总成本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「财务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('财务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1973 桌面报表构建器分组类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder 空态「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「运维」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('运维').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1974 桌面TCO构成查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「资产 TCO 构成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('资产 TCO 构成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「请输入资产ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('请输入资产ID查询').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1975 桌面TCO趋势排行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「TCO 趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('TCO 趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「部门 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('部门 TCO 排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「请先查询资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('请先查询资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1976 桌面TCO分类排行查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「分类 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('分类 TCO 排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「请输入部门ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('请输入部门ID查询').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「请输入分类ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('请输入分类ID查询').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1977 桌面资产健康评分统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「平均健康分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('平均健康分').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('暂无数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「资产健康评分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('资产健康评分').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1978 桌面可靠性分析标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「可靠性分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('可靠性分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTBF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTTR」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTTR', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1979 桌面流程中心标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「业务流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「集中维护审批流程、发布状态、版本快照和业务入口。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('集中维护审批流程、发布状态、版本快照和业务入口。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「返回资产处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('返回资产处置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1980 桌面流程中心选中态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「条结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('条结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「当前选中流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('当前选中流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「创建并发布默认流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('创建并发布默认流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1981 桌面流程中心详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「流程信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('流程信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「版本历史与回滚」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('版本历史与回滚').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「该流程尚未形成可发布版本，业务入口会被阻断。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('该流程尚未形成可发布版本，业务入口会被阻断。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1982 桌面流程中心指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「当前版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('当前版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「发布快照」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('发布快照').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「审批/办理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('审批/办理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1983 桌面流程中心草稿指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「总节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('总节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「本地草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('本地草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「可发起状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('可发起状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1984 桌面流程中心信息字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「业务对象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务对象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「更新时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('更新时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「发布时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('发布时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1985 桌面流程中心人员时间空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「更新人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('更新人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「发布人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('发布人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1986 桌面流程中心契约字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「发起入口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('发起入口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「契约状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('契约状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「阻断原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('阻断原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1987 桌面流程中心未读占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「未记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('未记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「未读取」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('未读取').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「显示第」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('显示第').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1988 桌面流程中心预定义流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「资产转移流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('资产转移流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「资产清退流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('资产清退流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「资产报废转让流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('资产报废转让流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1989 桌面流程中心赔偿退役分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「资产赔偿流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('资产赔偿流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「资产退役流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('资产退役流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('首页', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1990 桌面流程中心分页按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('上一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('下一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「末页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('末页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1991 桌面流程中心转移说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「未知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('未知', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「业务：资产转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务：资产转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「用于资产转出、转入确认及双方部门资产管理员审批的流程定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('用于资产转出、转入确认及双方部门资产管理员审批的流程定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1992 桌面流程中心处置说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「用于闲置资产清退、部门审批、库房确认及 IT 审核的流程定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('用于闲置资产清退、部门审批、库房确认及 IT 审核的流程定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「用于资产报废转让多级审批、收款确认与核算归档的流程定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('用于资产报废转让多级审批、收款确认与核算归档的流程定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「用于资产损失赔偿、信息安全审批、财务审批与库房接收的流程定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('用于资产损失赔偿、信息安全审批、财务审批与库房接收的流程定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1993 桌面流程中心退役与业务标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「用于资产退役审批、部门审核及资产状态变更的流程定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('用于资产退役审批、部门审核及资产状态变更的流程定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「业务：资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务：资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「业务：资产报废转让」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务：资产报废转让').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1994 桌面流程中心业务编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「业务：资产赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务：资产赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「业务：资产退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('业务：资产退役').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「ASSET_TRANSFER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('ASSET_TRANSFER', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1995 桌面流程中心处置编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「ASSET_CLEARANCE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('ASSET_CLEARANCE', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「ASSET_SCRAP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('ASSET_SCRAP', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「ASSET_COMPENSATION」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('ASSET_COMPENSATION', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1996 桌面流程中心编码分页与模板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「RETIREMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('RETIREMENT', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「当前页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('当前页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 点新建流程空态「从模板创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await page.getByRole('button', { name: /新建流程/ }).first().click();
    await expect(page.getByText('从模板创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1997 桌面流程设计器工具栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「流程中心 / 设计器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('流程中心 / 设计器').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('保存草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「发布流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('发布流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1998 桌面流程设计器操作栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「计算处理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('计算处理人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「重新读取后端定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('重新读取后端定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「个节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('个节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q1999 桌面流程设计器快捷键空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「删除节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('删除节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「撤销」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('撤销').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「重做」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('重做').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2000 桌面流程设计器画布提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「滚轮缩放 · 拖拽布点 · 连线分流 · 拖拽吸附对齐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('滚轮缩放 · 拖拽布点 · 连线分流 · 拖拽吸附对齐').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「节点属性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('节点属性').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2001 桌面流程设计器节点面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「节点面板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('节点面板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「拖拽到画布创建节点，或点击按钮快速追加」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('拖拽到画布创建节点，或点击按钮快速追加').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「开始节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('开始节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2002 桌面流程设计器节点类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「审批节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('审批节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「办理节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('办理节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「抄送节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('抄送节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2003 桌面流程设计器分支结束空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「条件分支」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('条件分支').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「结束节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('结束节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「流程入口与触发条件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('流程入口与触发条件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2004 桌面流程设计器节点描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「指派审批人并定义会签规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('指派审批人并定义会签规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「指派办理人完成阻塞处理步骤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('指派办理人完成阻塞处理步骤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「向角色或用户发送非阻塞通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('向角色或用户发送非阻塞通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2005 桌面流程设计器节点帮助空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「按金额、类别或字段值分流」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('按金额、类别或字段值分流').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「流程收口与结果归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('流程收口与结果归档').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「适合表单提交、批量导入、定时任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('适合表单提交、批量导入、定时任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2006 桌面流程设计器节点助手空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「支持按岗位、部门负责人、固定审批人配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('支持按岗位、部门负责人、固定审批人配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「复用角色或指定用户配置办理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('复用角色或指定用户配置办理人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「不计入审批步骤，可挂载到后续处理节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('不计入审批步骤，可挂载到后续处理节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2007 桌面流程设计器分支帮助空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「支持通过/驳回、满足/不满足双向分支」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('支持通过/驳回、满足/不满足双向分支').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「适合结束审批、归档单据、推送通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('适合结束审批、归档单据、推送通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「未配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('未配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2008 桌面流程设计器画布节点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「提交资产申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('提交资产申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「部门负责人审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('部门负责人审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「金额阈值判断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('金额阈值判断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2009 桌面流程设计器画布收口空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「财务复核」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('财务复核').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「流程结束」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('流程结束').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「大额采购」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('大额采购').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2010 桌面流程设计器属性面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「常规采购」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('常规采购').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「节点名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('节点名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「节点说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('节点说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2011 桌面流程设计器编码连线空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「节点编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('节点编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「条连线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('条连线').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「环节子表单/区段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('环节子表单/区段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2012 桌面流程设计器子表单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「区段名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('区段名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「历史摘要字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('历史摘要字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「子表单 HTML」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('子表单 HTML').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2013 桌面流程设计器审批人配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「审批人类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('审批人类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「按角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('按角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「指定用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('指定用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2014 桌面流程设计器审批角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「审批角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('审批角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「审批模式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('审批模式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「用于发起页渲染和审批详情快照展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('用于发起页渲染和审批详情快照展示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2015 桌面流程设计器预览删除空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 空态「处理人预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('处理人预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「业务数据 JSON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByLabel('业务数据 JSON').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 空态「删除当前节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByText('删除当前节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2016 桌面流程设计器表单源码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer 点表单源码空态「自定义表单 HTML（保存草稿后生效）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await page.getByText('表单源码', { exact: true }).first().click();
    await expect(page.getByText('自定义表单 HTML（保存草稿后生效）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 点表单源码空态「表单源码会保存在流程定义中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await page.getByText('表单源码', { exact: true }).first().click();
    await expect(page.getByText('表单源码会保存在流程定义中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer 点表单源码空态「查看业务表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await page.getByText('表单源码', { exact: true }).first().click();
    await expect(page.getByText('查看业务表单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2017 桌面流程表单未配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「ASSET_TRANSFER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('ASSET_TRANSFER').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2018 桌面流程表单其他类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_CLEARANCE 空态「ASSET_CLEARANCE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('ASSET_CLEARANCE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP 空态「ASSET_SCRAP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('ASSET_SCRAP').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT 空态「RETIREMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('RETIREMENT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2019 桌面流程表单赔偿说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「ASSET_COMPENSATION」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('ASSET_COMPENSATION').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2020 桌面制造商筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers 空态「状态：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByText('状态：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2021 桌面制造商备注与供应商空表', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByText('新增制造商').first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「暂无供应商数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('暂无供应商数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('刷新', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2022 桌面菜单管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「菜单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「暂无菜单数据，请通过 DDL 初始化种子数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('暂无菜单数据，请通过 DDL 初始化种子数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2023 桌面菜单管理列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('排序', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('操作', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2024 桌面角色管理标题列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「角色管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2025 桌面部门岗位标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「部门管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('部门管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「自定义字段管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('自定义字段管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2026 桌面能耗管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/energy 空态「能耗管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByText('能耗管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/energy 空态「暂无能耗数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByText('暂无能耗数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/energy 空态「尚未采集到能耗数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByText('尚未采集到能耗数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2027 桌面GIS地图标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「GIS 资产地图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('GIS 资产地图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「资产定位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('资产定位管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「关联已有资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('关联已有资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2028 桌面GIS筛选与弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「资产分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('资产分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('全部状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理「纬度 (-90~90)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('纬度 (-90~90)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2029 桌面GIS筛选空间与弹窗经度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「选择空间单元」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('选择空间单元').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理「经度 (-180~180)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('经度 (-180~180)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点资产定位管理「位置描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await expect(page.getByText('位置描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2030 桌面GIS空间空树与新建定位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点选择空间单元「暂无位置数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '选择空间单元' }).click();
    await expect(page.getByText('暂无位置数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「纬度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('纬度', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2031 桌面GIS新建经度状态与空间搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点新建资产定位「经度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('经度', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点新建资产定位「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '资产定位管理' }).click();
    await page.getByRole('button', { name: '新建资产定位' }).click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点选择空间单元 placeholder「搜索位置名称 / 编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '选择空间单元' }).click();
    await expect(page.getByPlaceholder('搜索位置名称 / 编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2032 桌面GIS时间筛选与空间说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「近 12 月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('近 12 月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「自定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('button', { name: '自定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点选择空间单元「请先在「位置管理」中维护空间层级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '选择空间单元' }).click();
    await expect(page.getByText('请先在「位置管理」中维护空间层级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2033 桌面GIS自定义时间范围弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点自定义「自定义时间范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '自定义' }).click();
    await expect(page.getByText('自定义时间范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点自定义「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '自定义' }).click();
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点自定义「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '自定义' }).click();
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2034 桌面GIS自定义时间应用取消空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 点自定义「应用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '自定义' }).click();
    await expect(page.getByRole('button', { name: '应用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 点自定义「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await page.getByRole('button', { name: '自定义' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('全部', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2035 桌面平面图新建表单剩余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建「名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByText('名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 点新建「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByRole('button', { name: '创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans 点新建「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2036 桌面闲置资产标题与页签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「闲置资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置资产管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('待处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('历史记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2037 桌面闲置资产表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2038 桌面闲置资产表头部门状态操作', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2039 桌面折旧管理标题表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「折旧管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「当期折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('当期折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2040 桌面折旧表头与方法筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「全部方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('全部方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2041 桌面折旧查询重置原值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2042 桌面折旧净值与重估标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「资产减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('资产减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2043 桌面重估价值调整与记录空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「价值调整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('价值调整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「资产价值调整记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('资产价值调整记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「条记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('条记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2044 桌面重估表头类型新值差额', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「新值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('新值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「差额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('差额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2045 桌面故障代码标题与新建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「故障代码管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障代码管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障代码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 点新增根节点「新增故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByText('新增故障代码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2046 桌面故障代码新建表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点「故障编码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByText('故障编码 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 点新增根节点「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByText('故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 点新增根节点「排序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByText('排序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2047 桌面故障代码新建占位与提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点 placeholder「请输入故障编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByPlaceholder('请输入故障编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 点新增根节点 placeholder「请输入描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByPlaceholder('请输入描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 点新增根节点「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('button', { name: '创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2048 桌面故障代码取消与分类结构空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「分类结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('分类结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「创建一个新的根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('创建一个新的根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2049 桌面分类新建表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类「分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByText('分类编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类 placeholder「请输入分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByPlaceholder('请输入分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2050 桌面分类新建编码占位与提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类 placeholder「请输入分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByPlaceholder('请输入分类编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByRole('button', { name: '创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 点添加根分类「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2051 桌面位置新建说明与采购筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('全部状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2052 桌面检验年检标题与批量操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「管理设备检验记录与年检计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('管理设备检验记录与年检计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「批量删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('批量删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「批量导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('批量导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2053 桌面检验统计卡空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「总检验数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('总检验数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('通过', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2054 桌面检验过期与表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2055 桌面检验类型日期表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「下次检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('下次检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2056 桌面检验机构结果操作表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('结果', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2057 桌面风险评估统计卡空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「全部评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('全部评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「重大风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('重大风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「高危风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('高危风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2058 桌面风险评估中低危与热力图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「中/低危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('中/低危').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「项评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('项评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「影响\\概率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('影响\\概率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2059 桌面风险矩阵轴标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('columnheader', { name: /低\s*\(\s*2\s*\)/ })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「中等」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByText('中等').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('columnheader', { name: /高\s*\(\s*4\s*\)/ })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2060 桌面风险矩阵影响轴与单元格弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 空态「小」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('cell', { name: /小\s*\(\s*2\s*\)/ })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 空态「大」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('cell', { name: /大\s*\(\s*4\s*\)/ })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 点单元格空态「关闭」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await page.locator('td').filter({ hasText: '项评估' }).first().click();
    await expect(page.getByRole('button', { name: '关闭' })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2061 桌面风险矩阵单元格详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments 点单元格空态「风险评估详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await page.locator('td').filter({ hasText: '项评估' }).first().click();
    await expect(page.getByText('风险评估详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 点单元格空态「可能性：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await page.locator('td').filter({ hasText: '项评估' }).first().click();
    await expect(page.getByText(/可能性：/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments 点单元格空态「影响：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await page.locator('td').filter({ hasText: '项评估' }).first().click();
    await expect(page.getByText(/影响：/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2062 桌面风险矩阵配置默认维空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「较低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.locator('input[value="较低"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「较高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await expect(page.locator('input[value="较高"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「轻微」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '严重度维度' }).click();
    await expect(page.locator('input[value="轻微"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2063 桌面风险矩阵配置严重度维空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点创建矩阵「一般」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '严重度维度' }).click();
    await expect(page.locator('input[value="一般"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「非常严重」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '严重度维度' }).click();
    await expect(page.locator('input[value="非常严重"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点创建矩阵「灾难性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '严重度维度' }).click();
    await expect(page.locator('input[value="灾难性"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2064 桌面风险矩阵配置等级映射空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点等级映射「CRITICAL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="CRITICAL"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射「HIGH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="HIGH"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射 placeholder「最低分数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByPlaceholder('最低分数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2065 桌面风险矩阵配置映射等级值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点等级映射「MEDIUM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="MEDIUM"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射「LOW」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="LOW"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射 placeholder「风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByPlaceholder('风险等级 (LOW/MEDIUM/HIGH/CRITICAL)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2066 桌面风险矩阵配置映射规则说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点等级映射「按 minScore 降序排列」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByText('按 minScore 降序排列').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射分数「20」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="20"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射分数「10」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="10"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2067 桌面风险矩阵配置映射分数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-matrix 点等级映射分数「4」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="4"]')).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射分数「0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.locator('input[value="0"]').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/risk-matrix 点等级映射「风险等级映射规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await page.getByRole('button', { name: /创\s*建\s*矩\s*阵/ }).click();
    await page.getByRole('tab', { name: '等级映射' }).click();
    await expect(page.getByText('风险等级映射规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2068 桌面安全检查模板新增弹窗余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config 点新增模板标题「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.getByRole('heading', { name: '新增模板' })).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.locator('label', { hasText: '状态' })).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/safety-checklists/config 点新增模板弹窗「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).click();
    await expect(page.locator('[role="dialog"]').getByText('启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2069 桌面安全检查执行工作台预填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「固定资产工作台 / 点检执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('固定资产工作台 / 点检执行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「已带入点检上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('已带入点检上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「注塑机 M-201」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('注塑机 M-201').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2070 桌面安全检查执行工作台重点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「温度 / 振动 / 电流」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('温度 / 振动 / 电流').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「高温点位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('高温点位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「18 个高温点位待确认」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('18 个高温点位待确认').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2071 桌面安全检查执行工作台标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「资产：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('资产：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「重点：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('重点：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「来自固定资产工作台快捷入口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('来自固定资产工作台快捷入口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2072 桌面安全检查执行工作台风险说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/execute 空态「风险：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('风险：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「建议优先核对」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('建议优先核对').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute 空态「现场安全规则命中点位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute?source=quick-safety');
    await expect(page.getByText('现场安全规则命中点位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2073 桌面许可证列表与购买日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「席位使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('席位使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「软件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('软件名称', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 点新增许可证「购买日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('购买日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2074 桌面许可证列表类型厂商版本空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('厂商', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('版本', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2075 桌面许可证列表到期状态操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2076 桌面许可证标题与状态筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「软件许可证管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('软件许可证管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「状态：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('状态：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('全部', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2077 桌面许可证状态筛选pill空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 空态「有效」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('有效', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「已到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('已到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses 空态「暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByText('暂停', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2078 桌面许可证新增到期与币种空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.locator('[role="dialog"]').getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「¥」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('¥').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证标题「新增许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('heading', { name: '新增许可证' })).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2079 桌面SAM高风险与历史列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam 空态「暂无高风险项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('暂无高风险项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「合规率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('合规率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2080 桌面SAM扫描详情弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/sam/history') {
        return fulfill(apiRoute, {
          records: [{
            id: 1,
            scanDate: '2026-08-01T00:00:00',
            totalLicenses: 1,
            compliantCount: 1,
            overusedCount: 0,
            underusedCount: 0,
            expiredCount: 0,
            complianceRate: 100,
          }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/sam/1/details') {
        return fulfill(apiRoute, { details: [] });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
  });

  test('/sam 点查看详情「扫描详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('扫描详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「软件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('软件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「许可类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('许可类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2081 桌面SAM扫描详情席位列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/sam/history') {
        return fulfill(apiRoute, {
          records: [{
            id: 1,
            scanDate: '2026-08-01T00:00:00',
            totalLicenses: 1,
            compliantCount: 1,
            overusedCount: 0,
            underusedCount: 0,
            expiredCount: 0,
            complianceRate: 100,
          }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/sam/1/details') {
        return fulfill(apiRoute, { details: [] });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
  });

  test('/sam 点查看详情「总席位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('总席位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「已用席位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('已用席位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「使用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('使用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2082 桌面SAM扫描详情状态列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/sam/history') {
        return fulfill(apiRoute, {
          records: [{
            id: 1,
            scanDate: '2026-08-01T00:00:00',
            totalLicenses: 1,
            compliantCount: 1,
            overusedCount: 0,
            underusedCount: 0,
            expiredCount: 0,
            complianceRate: 100,
          }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/sam/1/details') {
        return fulfill(apiRoute, { details: [] });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
  });

  test('/sam 点查看详情「合规状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('合规状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('风险等级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 点查看详情「到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('到期', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2083 桌面SAM扫描详情建议与查看按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/sam/history') {
        return fulfill(apiRoute, {
          records: [{
            id: 1,
            scanDate: '2026-08-01T00:00:00',
            totalLicenses: 1,
            compliantCount: 1,
            overusedCount: 0,
            underusedCount: 0,
            expiredCount: 0,
            complianceRate: 100,
          }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      if (path === '/sam/1/details') {
        return fulfill(apiRoute, { details: [] });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
  });

  test('/sam 点查看详情「建议」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('建议').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/sam 空态「查看详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('button', { name: '查看详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam 点查看详情「暂无详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await page.getByRole('button', { name: '查看详情' }).first().click();
    await expect(page.getByText('暂无详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2084 桌面审计日志标题与筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('审计日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「系统日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('系统日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「全部日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('全部日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2085 桌面审计日志告警总计导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('告警', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「总计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2086 桌面审计日志表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「操作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「操作类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2087 桌面审计日志描述IP状态表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「IP地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('IP地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2088 桌面审计日志筛选与分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('显示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「项，共」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('项，共').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2089 桌面审计详情操作栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「导出日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('导出日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「操作详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('操作详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2090 桌面审计详情时间资产上下文空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「操作时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('操作时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('关联资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「操作上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('操作上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2091 桌面审计详情请求与描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「请求方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('请求方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「请求路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('请求路径').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「无详细描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('无详细描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2092 桌面审计详情关联资产表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「变更字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('变更字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2093 桌面审计详情变更值表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「变更前值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('变更前值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「变更后值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('变更后值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「变更时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('变更时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2094 桌面审计详情上下文ID空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「用户代理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('用户代理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「请求 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('请求 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「会话 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('会话 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2095 桌面审计详情轨迹节点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「租户 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('租户 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「操作轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('操作轨迹').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「当前节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('当前节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2096 桌面审计详情标题与IP空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 空态「审计日志详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('审计日志详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「IP 地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('IP 地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 空态「操作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByText('操作人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2097 桌面通知工作台预填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「工作台带入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('工作台带入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「工作台告警处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('工作台告警处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「核对告警来源」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('核对告警来源').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2098 桌面通知工作台建议空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「处置建议」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('处置建议').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「告警处置上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('告警处置上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「转预测维保工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('转预测维保工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2099 桌面通知其他工作台来源空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「安全告警处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=security-event');
    await expect(page.getByText('安全告警处置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「异常告警实时列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=data-alert');
    await expect(page.getByText('异常告警实时列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「工作台告警上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=unknown-source');
    await expect(page.getByText('工作台告警上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2100 桌面通知工作台建议后半空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「影响资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('影响资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「处置动作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('处置动作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「必要时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications?source=quick-alert');
    await expect(page.getByText('必要时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2101 桌面TCO查询栏标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「TCO 全生命周期成本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('TCO 全生命周期成本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('部门ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('分类ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2102 桌面可靠性排名与趋势空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「暂无排名数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('暂无排名数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态排序「按 MTBF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.locator('select').filter({ hasText: '按 MTBF' })).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2103 桌面资产健康空列表与筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「暂无不健康资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('暂无不健康资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「最低分:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('最低分:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2104 桌面资产健康列表标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「不健康资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('不健康资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「所有资产状态良好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('所有资产状态良好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「共 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('共 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2105 桌面资产健康页头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「资产健康评分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('资产健康评分').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「基于年龄」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('基于年龄').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「维修频率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('维修频率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2106 桌面资产健康副标题维度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health 空态「故障率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('故障率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「利用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('利用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health 空态「折旧进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByText('折旧进度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2107 桌面合同到期列与即将到期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 点即将到期「暂无即将到期合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('tab', { name: /即将到期/ }).click();
    await expect(page.getByText('暂无即将到期合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 点即将到期「当前没有在30天内到期的合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('tab', { name: /即将到期/ }).click();
    await expect(page.getByText('当前没有在30天内到期的合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2108 桌面合同时间轴与备件列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点时间轴「当前没有可用于时间轴视图的合同数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('tab', { name: '时间轴视图' }).click();
    await expect(page.getByText('当前没有可用于时间轴视图的合同数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2109 桌面备件库存列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('当前库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('安全库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('单价').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2110 桌面备件状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「暂无备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText(/暂无备件/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2111 桌面保险列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保险类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2112 桌面保险保费日期列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保费', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2113 桌面保险结束状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2114 桌面借用列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2115 桌面借用归还用途操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「预计归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('预计归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「用途」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('用途').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2116 桌面领用列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「领用类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('领用类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2117 桌面领用状态日期列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「预计归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('预计归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2118 桌面入库验收列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「验收单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('验收单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「验收日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('验收日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「总金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('总金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2119 桌面入库状态时间操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2120 桌面采购订单列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「采购单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「采购名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2121 桌面采购金额日期状态列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「采购日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2122 桌面采购操作与模型列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「模型名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('模型名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('型号', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2123 桌面资产模型分类制造商状态列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态「分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('分类', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2124 桌面资产模型字段集描述操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态「字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('字段集').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2125 桌面设备列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「设备名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('设备名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「设备ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('设备ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「上次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('上次维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2126 桌面设备维保列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「下次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('下次维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「使用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('使用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「维保状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维保状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2127 桌面设备操作与维保表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('设备', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「技术员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('技术员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2128 桌面设备维保记录表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2129 桌面维保列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「维保类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('维保类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「维保日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('维保日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2130 桌面维保执行人费用下次列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「执行人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('执行人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「下次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('下次维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2131 桌面维保操作与计划列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「计划名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('计划名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('关联资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2132 桌面维保计划周期负责人列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「计划周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('计划周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「下次执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('下次执行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2133 桌面维保计划优先级状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('优先级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2134 桌面工单新建表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「新建工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('新建工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「标题 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('标题 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「工单类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('工单类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2135 桌面工单新建资产描述费用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('关联资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「预计费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('预计费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2136 桌面工单新建截止负责人协作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「截止日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('截止日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「协作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('协作人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2137 桌面工单新建占位符空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 placeholder「请输入工单描述性标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请输入工单描述性标题').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「请详细说明工单内容及注意事项...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请详细说明工单内容及注意事项...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「添加人员...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('添加人员...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2138 桌面工单新建优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('优先级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「详细信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('详细信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「紧急」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('紧急').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2139 桌面工单新建优先级档位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('高', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('中', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('低', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2140 桌面工单新建提示与提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「处理提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('处理提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「提交工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('提交工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2141 桌面工单新建提示正文与类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「用于安排负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('用于安排负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「请结合工单类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('请结合工单类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态类型「维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.locator('select').filter({ hasText: '维修' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2142 桌面工单新建附件与详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「上传文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('上传文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「工单详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('工单详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2143 桌面工单详情信息栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1 空态「工单信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('工单信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「SLA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('SLA').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2144 桌面工单详情字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1 空态「工单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('工单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('优先级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2145 桌面工单详情部门描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1 空态「部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「申请人」栏', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('申请人').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2146 桌面工单审批记录与预算年度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1 空态「审批记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('审批记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 空态「暂无审批记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByText('暂无审批记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('年度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2147 桌面预算列表类型总额剩余列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('类型', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「剩余」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('剩余').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2148 桌面预算已使用承诺状态列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已承诺').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2149 桌面预算操作与退役列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「申请编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('申请编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2150 桌面退役资产名称分类原值列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('分类', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2151 桌面退役残值原因申请人列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('退役原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2152 桌面退役状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「新建退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('新建退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2153 桌面退役标题与空表文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「资产退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('资产退役管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「暂无退役申请记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('暂无退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「搜索编号或资产...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByPlaceholder('搜索编号或资产...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2154 桌面退役新建标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「资产退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「新建申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('新建申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2155 桌面退役新建面包屑与原因空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「折旧时间线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('折旧时间线').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2156 桌面退役新建残值评估空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「残值评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('残值评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「资产 ID *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产 ID *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('返回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2157 桌面退役新建备注与提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「备注与说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('备注与说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「退役操作不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役操作不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「提交退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('提交退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2158 桌面退役新建审批提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「提交此申请将启动多级审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('提交此申请将启动多级审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「搜索并选择资产后显示建议残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('搜索并选择资产后显示建议残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2159 桌面退役新建占位与时间线空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 placeholder「请先搜索并选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('请先搜索并选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 placeholder「可选的内部备注或附件说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('可选的内部备注或附件说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「2023 (当前)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('2023 (当前)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2160 桌面退役新建原因占位与年份空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 placeholder「请输入退役原因（如技术过时、不可修复的损坏...）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('请输入退役原因（如技术过时、不可修复的损坏...）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「2020」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('2020').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「2025」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('2025').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2161 桌面退役详情标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 空态「退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「申请详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('申请详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('返回列表').or(page.getByText('退役管理')).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2162 桌面退役详情分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 空态「退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('退役原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「申请编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('申请编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「审批状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('审批状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2163 桌面退役详情申请人时间空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「申请时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('申请时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 空态「更新时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByText('更新时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2164 桌面赔偿申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「资产赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「填写信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('填写信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2165 桌面赔偿申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「赔偿编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('赔偿编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2166 桌面赔偿申请日期类型资产空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「损坏类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('损坏类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2167 桌面赔偿申请责任字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「损坏日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('损坏日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「责任人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('责任人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「责任部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('责任部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2168 桌面赔偿申请损坏详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「损坏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('损坏详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「发现人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('发现人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「是否报险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('是否报险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2169 桌面赔偿申请说明与配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「资产损失说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('资产损失说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「赔偿配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('赔偿配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「赔偿方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('赔偿方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2170 桌面赔偿申请金额与提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「现金赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('现金赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「总赔偿金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('总赔偿金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('确认提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2171 桌面赔偿申请方式与流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「等价物赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('等价物赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「维修恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('维修恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2172 桌面赔偿申请提交栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('保存草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('提交申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2173 桌面处置列表列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「处置单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('处置单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2174 桌面处置日期状态操作列头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2175 桌面赔偿备注与合计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态 placeholder「补充说明事项...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByPlaceholder('补充说明事项...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「合计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('合计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2176 桌面赔偿损坏程度选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation 空态 placeholder「请详细描述资产损坏情况、发生经过及损失范围...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByPlaceholder('请详细描述资产损坏情况、发生经过及损失范围...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('返回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation 空态「赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation');
    await expect(page.getByText('赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2177 桌面清退申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「资产清退申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产清退申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2178 桌面清退申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「清退编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2179 桌面清退申请原因与资产空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「清退原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('添加资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2180 桌面清退配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「清退配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「处理方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('处理方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「入库保管」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('入库保管').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2181 桌面清退处理方式选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「变卖处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('变卖处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「捐赠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('捐赠').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2182 桌面清退残值紧急确认空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「预估残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('预估残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「紧急程度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('紧急程度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('确认提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2183 桌面清退提交提示与按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「请先选择至少一项资产后再提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('请先选择至少一项资产后再提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('保存草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「普通」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('普通').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2184 桌面清退紧急审批备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「紧急」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('紧急', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2185 桌面清退取消与风险正文空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「资产清退操作一旦提交审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产清退操作一旦提交审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态 placeholder「请输入清退相关的额外说明...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByPlaceholder('请输入清退相关的额外说明...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2186 桌面报废申请标题步骤空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「资产报废申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('资产报废申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「填写信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('填写信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2187 桌面报废配置确认基本信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「报废配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('确认提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2188 桌面报废编号申请人原因空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「报废编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「报废原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2189 桌面报废处置方式与资产空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「处置方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('处置方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('添加资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「暂无已选资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('暂无已选资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2190 桌面报废处置方式选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「变卖处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('变卖处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「报废拆解」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废拆解').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「捐赠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('捐赠').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2191 桌面报废残值审批备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「预估残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('预估残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2192 桌面报废提交提示与草稿空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「请先选择至少一项资产后再提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('请先选择至少一项资产后再提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「自动保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('自动保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2193 桌面报废审批说明与返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「标准流程需部门经理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('标准流程需部门经理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('返回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2194 桌面报废申请日期与风险提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「残值评估将影响」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('残值评估将影响').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2195 桌面报废面包屑与备注占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「新建申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('新建申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「选填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('选填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态 placeholder「请输入报废申请相关的补充说明...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByPlaceholder('请输入报废申请相关的补充说明...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2196 桌面转移申请标题步骤空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「资产转移申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产转移申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2197 桌面转移审批配置完成空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「审批配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('审批配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「基础信息填写」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('基础信息填写').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2198 桌面转移步骤副标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「选择调拨资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('选择调拨资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「流程配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('流程配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「提交状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('提交状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2199 桌面转移单据信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「单据信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('单据信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调拨编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2200 桌面转移调拨日期类型方向空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「调拨日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调拨类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调拨方向」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨方向').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2201 桌面转移转出方字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「转出方」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('转出方').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调出部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调出部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调出位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调出位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2202 桌面转移转入方字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「转入方」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('转入方').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调入部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调入部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调入位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调入位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2203 桌面转移资产选择空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('添加资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「已选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('已选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2204 桌面转移发布流程紧急程度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「发布流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('发布流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「资产转移流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产转移流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「紧急程度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('紧急程度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2205 桌面转移处理人预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「处理人预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('处理人预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「计算处理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('计算处理人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「高优先级申请将跳过非关键审批节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('高优先级申请将跳过非关键审批节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2206 桌面转移备注优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「选填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('选填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「普通」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('普通').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2207 桌面转移取消草稿提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('保存草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('提交申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2208 桌面转移返回自动保存必填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('返回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「自动保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('自动保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('必填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2209 桌面转移面包屑与部门提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「新建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('新建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「资产当前所属部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产当前所属部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「资产调拨目标部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产调拨目标部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2210 桌面检验新建字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2211 桌面检验新建日期类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验日期', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「下次检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('下次检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2212 桌面检验新建机构人结果空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2213 桌面检验新建发现照片证书空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「检查发现」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检查发现').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2214 桌面检验新建到期费用附件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「证书到期日」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('证书到期日').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「报告附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('报告附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2215 桌面检验新建扫码占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「扫码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('扫码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「自动生成或手动输入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('自动生成或手动输入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2216 桌面检验新建人发现证书占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 placeholder「请输入检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入检查发现内容」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检查发现内容').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2217 桌面检验新建剩余占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请选择检验模板（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('请选择检验模板（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「附件路径或URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('附件路径或URL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2218 桌面重估新建标题字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新增减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「查找」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('查找').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2219 桌面重估类型选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「减值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('减值', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('重估', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2220 桌面重估新值原因证据空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「新值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「原因说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('原因说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「证据材料」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('证据材料').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2221 桌面重估提交取消占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('提交申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态 placeholder「提供相关证明材料链接或描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByPlaceholder('提供相关证明材料链接或描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2222 桌面资产新建标题分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「位置归属」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('位置归属').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2223 桌面资产新建其余分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「财务信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('财务信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('附件', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2224 桌面资产新建编号分类状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「品牌」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('品牌').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2225 桌面资产新型号序列号供应商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「序列号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('序列号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2226 桌面资产新原值净值日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「当前净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('当前净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「购置日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('购置日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2227 桌面资产新部门位置RFID空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「使用部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('使用部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「存放位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('存放位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「RFID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('RFID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2228 桌面资产新重要描述备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「重要设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('重要设备').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2229 桌面资产新保存取消保修空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「保修期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('保修期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2230 桌面资产新折旧经纬度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「折旧率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('折旧率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「纬度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('纬度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「经度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('经度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2231 桌面资产导入导出标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/import-export 空态「资产批量导入导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('资产批量导入导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 空态「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('导入', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByText('导出', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2232 桌面智能报告无任务空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/smart-report 空态「未指定盘点任务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report');
    await expect(page.getByText('未指定盘点任务 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report 空态「返回盘点列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report');
    await expect(page.getByText('返回盘点列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report 空态「盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report');
    await expect(page.getByText('盘点任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2233 桌面固定资产工作台空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2234 桌面工作台在用闲置导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2235 桌面工作台待审批刷新空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2236 桌面工作台分类维保工单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2237 桌面工作台净值部门查看空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「部门资产统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('部门资产统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2238 桌面系统管理V3工作台空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「系统管理 V3 工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('系统管理 V3 工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「用户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('用户管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「当前分组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('当前分组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2239 桌面V3工作台组织权限空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「部门组织」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('部门组织').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「组织权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('组织权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2240 桌面V3工作台分组菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「流程平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('流程平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「基础资料」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('基础资料').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「角色权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('角色权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2241 桌面V3工作台权限岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「菜单权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('菜单权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「岗位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('岗位管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「数据权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('数据权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2242 桌面V3工作台交接租户集成空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「交接管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('交接管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「租户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('租户管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「集成配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('集成配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2243 桌面V3工作台消息参数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「消息与通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('消息与通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「系统参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('系统参数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「用户只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('用户只读列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2244 桌面V3工作台状态页脚空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「已接入真组件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('已接入真组件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「已注册模块」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('已注册模块').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「四十四项菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('四十四项菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2245 桌面V3工作台侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「部门树列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('部门树列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「角色-权限绑定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('角色-权限绑定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「权限编码库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('权限编码库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2246 桌面V3工作台岗位租户交接描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「交接任务摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('交接任务摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「数据范围 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('数据范围 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2247 桌面404空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/404 空态「404 — 页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「404」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2248 桌面无权限页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('无访问权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「您没有访问此页面的权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('您没有访问此页面的权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('返回首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2249 桌面V3工作台岗位描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「关键词搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('关键词搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「根部门摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('根部门摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「未闭环风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('未闭环风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2250 桌面V3工作台租户岗位描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「套餐状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('套餐状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「联系人摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('联系人摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「按域聚合」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('按域聚合').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2251 桌面V3工作台覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「已达成 44 项全量覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('已达成 44 项全量覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「流程平台 9/9」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('流程平台 9/9').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「组织权限组 8/8」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('组织权限组 8/8').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2252 桌面V3工作台分组覆盖计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「基础资料组 6/6」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('基础资料组 6/6').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「集成配置组 5/5」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('集成配置组 5/5').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「消息与通知组 8/8」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('消息与通知组 8/8').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2253 桌面V3工作台参数组与补强说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「系统参数组 8/8」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('系统参数组 8/8').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「仍需后续权限专项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('仍需后续权限专项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「真实写操作能力补强」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('真实写操作能力补强').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2254 桌面V3工作台菜单标识空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「system-user-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('system-user-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「六域顶部导航」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByRole('navigation', { name: 'Workbench V3 六域顶部导航' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「子项导航」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByLabel('Workbench V3 子项导航').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2255 桌面V3工作台副标题片段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「全部完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('全部完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「专属页面与专属 API」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('专属页面与专属 API').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「技术支持工单只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('技术支持工单只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2256 桌面V3工作台registry说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「V3 registry」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('V3 registry').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「module metadata」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('module metadata').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「system-tech-support」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('system-tech-support').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2257 桌面3D大屏标题标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「固定资产智慧运营大屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('固定资产智慧运营大屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「SUMMARY」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('SUMMARY').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「CATEGORY」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('CATEGORY').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2258 桌面3D大屏其余标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「FORECAST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('FORECAST').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「IOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('IOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「TOP 5」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('TOP 5').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2259 桌面3D大屏预警与状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「WARNING」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('WARNING').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「开场动画」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('开场动画').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「当前城市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('当前城市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2260 桌面3D大屏规模指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「资产总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「地图节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('地图节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2261 桌面3D大屏价值与英文标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「资产原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「资产净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「FIXED ASSET INTELLIGENCE PLATFORM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('FIXED ASSET INTELLIGENCE PLATFORM').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2262 桌面3D大屏城市与在线率空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「四川全域」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('四川全域').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「成都市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('成都市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「在线率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('在线率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2263 桌面3D大屏预警表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「异常事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('异常事件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「报警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('报警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2264 桌面3D大屏设备与次数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「次数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('次数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「办公终端」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('办公终端').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「智能仓储」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('智能仓储').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2265 桌面3D大屏RFID审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「RFID 标签」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('RFID 标签').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「审计链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('审计链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「维保到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('维保到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2266 桌面3D大屏预警事件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「借用超期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('借用超期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「标签离线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('标签离线').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「审批积压」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('审批积压').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2267 桌面3D大屏处理状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「盘点差异」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('盘点差异').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「处理中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('处理中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「已处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('已处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2268 桌面3D大屏设备子项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「在线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('在线').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「库位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('库位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「读写」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('读写').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2269 桌面3D大屏单位与留痕空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2270 桌面3D大屏分类带空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「枚」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('枚').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「IT 设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('IT 设备').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「办公家具」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('办公家具').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2271 桌面3D大屏其余分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「生产设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('生产设备').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「低值耗材」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('低值耗材').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('条', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2272 桌面角色缺失无权限空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「用户信息不完整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('用户信息不完整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「当前用户角色信息缺失」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('当前用户角色信息缺失').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「重新登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('重新登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2273 桌面角色缺失说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「请重新登录后重试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('请重新登录后重试').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「无法验证访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('无法验证访问权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「如问题持续请联系管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByText('如问题持续请联系管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2274 桌面SSO回调拆分空态', () => {
  test('/sso-callback 空态「SSO 登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('SSO 登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「Token 缺失」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('Token 缺失').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「SSO 登录失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2275 桌面3D大屏TOP城市空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「泸州市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('泸州市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「德阳市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('德阳市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「自贡市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('自贡市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2276 桌面3D大屏攀枝花与单位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「攀枝花市」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('攀枝花市').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 空态「%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2277 桌面工作台预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2278 桌面工作台预览资产卡片空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2279 桌面工作台预览待审批刷新空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2280 桌面工作台预览分类维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2281 桌面工作台预览净值部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「部门资产统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('部门资产统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2282 桌面工作台预览总价值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2283 桌面工作台home菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=home');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=home');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=home');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2284 桌面工作台资产菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「资产台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('资产台账管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「资产总览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('资产总览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2285 桌面工作台资产导出搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「导出全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('导出全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('导入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「共 0 条资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('共 0 条资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2286 桌面工作台流程待办空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「流程待办」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('流程待办').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「审批中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('审批中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('待我审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2287 桌面工作台设备菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「设备管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=device');
    await expect(page.getByText('设备管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=device');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「巡检管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=device');
    await expect(page.getByText('巡检管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2288 桌面工作台侧栏其余菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「备件管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('备件管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「数据监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('数据监控').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「报表分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('报表分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2289 桌面工作台侧栏告警策略空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「告警中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('告警中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「组织策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('组织策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「基础维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('基础维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2290 桌面工作台侧栏我的资产设计稿空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「我的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('我的资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产总览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('资产总览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2291 桌面工作台顶栏操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「全屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('全屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2292 桌面工作台顶栏设置状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「文字设置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('文字设置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「快捷操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('快捷操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「工作台菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('工作台菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2293 桌面工作台产品页地标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「运营首页真实产品页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('运营首页真实产品页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「运营首页产品页主体」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('运营首页产品页主体').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「工作台切换」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('工作台切换').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2294 桌面工作台预览地标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「B端工作台预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('B端工作台预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「流程待办」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('流程待办').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2295 桌面工作台欢迎语空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「近12个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('近12个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2296 桌面仪表盘完整标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2297 桌面个人资料空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「SUPER_ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('SUPER_ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「当前套餐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('当前套餐').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2298 桌面V3岗位描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「metadata-only catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('metadata-only catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「no-persistence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('no-persistence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「no-assignment」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('no-assignment').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2299 桌面V3岗位权限预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「no-permission-effect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('no-permission-effect').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「CUSTOM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('CUSTOM').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2300 桌面V3数据权限描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「可收紧 dataScope」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('可收紧 dataScope').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「角色数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('角色数据范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2301 桌面V3组织权限其余描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「只读覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('只读覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2302 桌面V3分组接入计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「8/8」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('8/8').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「9/9」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('9/9').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「6/6」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('6/6').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2303 桌面V3其余分组计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「5/5」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('5/5').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('已接入').nth(1)).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「5/5 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('5/5 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2304 桌面V3完整接入计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3 空态「8/8 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('8/8 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「9/9 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('9/9 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 空态「6/6 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('6/6 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2305 桌面工作台待办产品页地标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「流程待办真实产品页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByLabel('流程待办真实产品页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「流程待办产品页主体」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByLabel('流程待办产品页主体').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('我发起的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2306 桌面工作台待办筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「搜索编号、标题或发起人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByPlaceholder('搜索编号、标题或发起人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2307 桌面工作台待办空列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「当前没有待处理的审批事项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('当前没有待处理的审批事项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2308 桌面工作台待办类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('资产报废').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2309 桌面工作台待办工单优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「工单申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('工单申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「至」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=todo');
    await expect(page.getByText('至').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2310 桌面无权限返回上一页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('返回上一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「请联系管理员获取相应角色权限后再尝试访问」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('请联系管理员获取相应角色权限后再尝试访问').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2311 桌面工作台资产产品页地标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「资产总览真实产品页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByLabel('资产总览真实产品页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「资产总览产品页主体」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByLabel('资产总览产品页主体').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2312 桌面工作台资产统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「资产总净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('资产总净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「闲置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('闲置率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2313 桌面工作台资产维修导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「待处理维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('待处理维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('导出 PDF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「未找到符合条件的资产记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('未找到符合条件的资产记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2314 桌面工作台资产列表标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench/assets 空态「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('资产管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench/assets 空态「请调整筛选条件或新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByText('请调整筛选条件或新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2315 桌面工作台我的资产菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「我的资产真实产品页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByLabel('我的资产真实产品页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「我的资产产品页主体」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByLabel('我的资产产品页主体').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('资产台账管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2316 桌面工作台我的资产统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「导出全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('导出全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「共 0 条资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('共 0 条资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2317 桌面工作台我的资产净值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「资产总净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('资产总净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「闲置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('闲置率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('导入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2318 桌面工作台我的资产维修空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「待处理维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('待处理维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('导出 PDF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2319 桌面工作台我的资产空记录空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「未找到符合条件的资产记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('未找到符合条件的资产记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「请调整筛选条件或新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('请调整筛选条件或新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2320 桌面工作台我的资产搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 placeholder「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('资产管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「我的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench?menu=my-assets');
    await expect(page.getByText('我的资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2321 桌面工作台顶栏 title 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 title「切换全屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('切换全屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 title「文字与密度设置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('文字与密度设置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 title「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('通知中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2322 桌面工作台顶栏个人中心空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 title「个人中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('个人中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「快捷操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('快捷操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「工作台菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByLabel('工作台菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2323 桌面工作台预览顶栏title空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态 title「切换全屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByTitle('切换全屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态 title「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByTitle('通知中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态 title「个人中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByTitle('个人中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2324 桌面工作台预览密度与菜单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态 title「文字与密度设置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByTitle('文字与密度设置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「工作台菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('工作台菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「快捷操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('快捷操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2325 桌面工作台预览侧栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「我的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('我的资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「流程待办」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('流程待办').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2326 桌面工作台预览侧栏资产设备空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「资产总览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('资产总览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「设备管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('设备管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2327 桌面工作台预览侧栏巡检备件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「巡检管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('巡检管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「备件管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('备件管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「数据监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('数据监控').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2328 桌面工作台预览侧栏报表告警空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「报表分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('报表分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「告警中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('告警中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「组织策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('组织策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2329 桌面工作台预览侧栏维护设计稿空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「基础维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('基础维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「设计稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('设计稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「B端工作台预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('B端工作台预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2330 桌面工作台预览产品页地标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「运营首页真实产品页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('运营首页真实产品页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「运营首页产品页主体」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('运营首页产品页主体').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「工作台切换」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByLabel('工作台切换').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2331 桌面工作台预览欢迎语空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「近12个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('近12个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2332 桌面工作台预览系统管理员空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2333 桌面工作台预览待审批导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2334 桌面工作台预览工单维保空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workspace-preview 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workspace-preview 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workspace-preview');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2335 桌面SSO返回登录空态', () => {
  test('/sso-callback 空态「返回登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('返回登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「Token 缺失，SSO 登录失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「SSO 登录中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('SSO 登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2336 桌面供应商门户标题空态', () => {
  test('/vendor-portal 空态 heading「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('heading', { name: '供应商门户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 placeholder「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 button「登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('button', { name: '登录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2337 桌面供应商门户密码空态', () => {
  test('/vendor-portal 空态 placeholder「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2338 桌面403返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/403 空态 heading「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('返回上一页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByText('返回首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2339 桌面404标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/404 空态 heading「404 — 页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「404」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2340 桌面仪表盘标题角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态 heading「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '运营首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2341 桌面仪表盘闲置待审批空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2342 桌面仪表盘刷新净值工单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2343 桌面仪表盘维保分类查看空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2344 桌面仪表盘总价值欢迎空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2345 桌面资产台账标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态 heading「资产台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '资产台账管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「导出全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('导出全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2346 桌面资产台账导入筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态 heading「资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '资产台账', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('导出 PDF').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「资产总净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产总净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2347 桌面资产台账新增导入筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「新增资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('新增资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '导入' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「高级筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('高级筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2348 桌面资产台账列表搜索闲置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态 placeholder「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「闲置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('闲置率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2349 桌面资产台账折旧维修空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「待处理维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('待处理维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('资产管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2350 桌面资产台账空表状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「未找到符合条件的资产记录，请调整筛选条件或新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('未找到符合条件的资产记录，请调整筛选条件或新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「待退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('待退役').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「维修中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('维修中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2351 桌面资产台账退役报废空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「已退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('已退役').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「已报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('已报废').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「已清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('已清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2352 桌面资产台账闲置计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets 空态「闲置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('闲置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「条资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('条资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets 空态「本页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByText('本页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2353 桌面审批中心标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态 heading「审批中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '审批中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待我审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2354 桌面审批中心标签统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('我发起的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2355 桌面审批中心驳回搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态 placeholder「搜索编号、标题或发起人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByPlaceholder('搜索编号、标题或发起人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('审批', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2356 桌面审批中心空表类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「暂无审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('暂无审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「当前没有待处理的审批事项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('当前没有待处理的审批事项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2357 桌面审批中心类型胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产报废').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2358 桌面审批中心工单日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「工单申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('工单申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「至」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('至', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('combobox').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2359 桌面检验年检标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 heading「检验/年检管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('heading', { name: '检验/年检管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「管理设备检验记录与年检计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('管理设备检验记录与年检计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('新增检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2360 桌面检验年检统计搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「总检验数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('总检验数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 placeholder「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2361 桌面检验年检批量操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「批量删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('批量删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「批量导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('批量导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('重置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2362 桌面检验年检类型胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('年度检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('定期检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2363 桌面检验年检结果空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「专项检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('专项检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「全部结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('全部结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('暂无检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2364 桌面检验年检条件搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「附条件通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('附条件通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2365 桌面闲置资产标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 heading「闲置资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '闲置资产管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「闲置资产公告发布与认领流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置资产公告发布与认领流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('发布公告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2366 桌面闲置资产统计标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「闲置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('闲置总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「已发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已发布公告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('待处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2367 桌面闲置资产认领标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「待审批认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('待审批认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「已完成认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已完成认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「公告中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('公告中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2368 桌面闲置资产历史空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态「已认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('已认领').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('历史记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('暂无数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2369 桌面维保管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态 heading「维保管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '维保管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「设备维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('设备维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「新增维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('新增维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2370 桌面维保列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「维保列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('维保列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 heading「维保记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '维保记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 placeholder「按资产ID搜索...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByPlaceholder('按资产ID搜索...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2371 桌面维保状态统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「计划中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('计划中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「执行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('执行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('逾期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2372 桌面维保完成刷新空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.locator('select').filter({ hasText: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2373 桌面维保空表新增空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '新增', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: /全部/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「暂无维保记录，点击「新增」开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('暂无维保记录，点击「新增」开始创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2374 桌面折旧管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 heading「折旧管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '折旧管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「批量计算折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('批量计算折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2375 桌面折旧计划搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「折旧计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 heading「资产折旧计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '资产折旧计划管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 placeholder「搜索资产编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByPlaceholder('搜索资产编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2376 桌面折旧查询方法空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「全部方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('全部方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2377 桌面折旧方法胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「直线法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('直线法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「双倍余额递减法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('双倍余额递减法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「年数总和法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('年数总和法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2378 桌面折旧统计空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「工作量法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('工作量法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「本月折旧总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('本月折旧总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「暂无折旧计划数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('暂无折旧计划数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2379 桌面折旧待计算刷新空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「待计算资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('待计算资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2380 桌面盘点管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态 heading「盘点管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '盘点管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「实时同步」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('实时同步').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「新建任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('新建任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2381 桌面盘点任务统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('任务总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('盘点任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态 heading「资产盘点管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '资产盘点管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2382 桌面盘点进度预警空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「平均进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('平均进度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「已盘资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('已盘资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「盘亏预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('盘亏预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2383 桌面盘点工具栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「决策摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('决策摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「进度趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('进度趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2384 桌面盘点空表状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「暂无盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('暂无盘点任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2385 桌面盘点提交导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory 空态「已提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('已提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('button', { name: '导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory 空态「RFID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByText('RFID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2386 桌面资产借用标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态 heading「资产借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '资产借用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2387 桌面借用列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「借用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态 heading「借用记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '借用记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态 placeholder「搜索用途/备注...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByPlaceholder('搜索用途/备注...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2388 桌面借用搜索导出统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('button', { name: '导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「借用总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('借用总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2389 桌面借用状态空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「已借出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已借出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「已逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已逾期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「暂无借用记录，点击「新建借用单」开始申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('暂无借用记录，点击「新建借用单」开始申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2390 桌面借用审批归还空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「已归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows 空态「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2391 桌面领用归还标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态 heading「资产领用归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '资产领用归还' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('领用', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('新建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2392 桌面领用列表搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「领用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('领用列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态 heading「领用归还管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '领用归还管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态 placeholder「搜索编号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByPlaceholder('搜索编号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2393 桌面领用统计搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「已签收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('已签收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2394 桌面领用类型空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「待归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('待归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('长期领用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「暂无领用记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('暂无领用记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2395 桌面领用类型胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments 空态「短期借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('短期借用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「归还入库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('归还入库').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments 空态「调拨转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByText('调拨转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2396 桌面入库验收标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态 heading「入库验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('heading', { name: '入库验收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「管理资产入库验收全流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('管理资产入库验收全流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「新建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('新建验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2397 桌面入库验收搜索空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态 placeholder「搜索验收单号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByPlaceholder('搜索验收单号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「暂无验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('暂无验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2398 桌面入库验收状态胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「待质检」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('待质检').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「质检中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('质检中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「部分验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('部分验收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2399 桌面入库验收完成状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake 空态「已验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('已验收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2400 桌面保险管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态 heading「保险管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '保险管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「保单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2401 桌面保险台账列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「保单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态 heading「保险台账管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '保险台账管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「重置筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('重置筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2402 桌面保险统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「保单总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('保单总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「总保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('总保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2403 桌面保险类型空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「财产险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('财产险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「责任险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('责任险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「暂无保险数据，点击「新增保险」开始录入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('暂无保险数据，点击「新增保险」开始录入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2404 桌面保险搜索类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「车险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('车险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态 placeholder「保单号/保险名称/保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByPlaceholder('保单号/保险名称/保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2405 桌面保险日期类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「起始:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('起始:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances 空态「结束:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByText('结束:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2406 桌面备品备件标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态 heading「备品备件管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '备品备件管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「新增备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('新增备件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2407 桌面备件列表统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「备件列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态 heading「备件库存管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '备件库存管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「备件总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('备件总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2408 桌面备件告警搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「库存告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('库存告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「已启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('已启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态 placeholder「搜索备件编码/名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByPlaceholder('搜索备件编码/名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2409 桌面备件价值空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「库存总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('库存总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「暂无备件数据，点击「新增备件」开始录入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('暂无备件数据，点击「新增备件」开始录入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2410 桌面备件启停筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts 空态「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: /启用/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: /全部/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2411 桌面合同管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 heading「合同管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '合同管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「合同信息维护与到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同信息维护与到期预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('新增合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2412 桌面合同统计预警空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「全部合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('全部合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('即将到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「30天内到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('30天内到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2413 桌面合同搜索刷新空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 placeholder「搜索合同名称/编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByPlaceholder('搜索合同名称/编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2414 桌面合同类型胶囊空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「维保合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('维保合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「采购合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('采购合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「租赁合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('租赁合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2415 桌面合同服务到期视图空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「服务合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('服务合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「已到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('已到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「时间轴视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('时间轴视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2416 桌面采购订单标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态 heading「采购订单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('heading', { name: '采购订单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「采购订单创建、审批与收货全流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购订单创建、审批与收货全流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「新增采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('新增采购单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2417 桌面采购订单统计搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「总采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('总采购单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 placeholder「搜索采购单号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByPlaceholder('搜索采购单号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2418 桌面采购订单状态筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「部分收货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('部分收货').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2419 桌面故障代码标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态 heading「故障代码管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '故障代码管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障树').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「新增根节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('新增根节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2420 桌面故障代码体系空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障代码', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态 heading「三级故障编码体系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '三级故障编码体系' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「现象 → 原因 → 措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('现象 → 原因 → 措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2421 桌面故障代码空树统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「暂无故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('暂无故障代码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「节点总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('节点总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2422 桌面故障代码原因措施空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「故障原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「解决措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('解决措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「点击「新增根节点」创建第一级故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('点击「新增根节点」创建第一级故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2423 桌面维保计划标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 heading「维保计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('heading', { name: '维保计划管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「周期性维保计划配置与管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('周期性维保计划配置与管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「新建计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('新建计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2424 桌面维保计划搜索空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 placeholder「搜索计划名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByPlaceholder('搜索计划名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「暂无维保计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('暂无维保计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2425 桌面维保计划状态筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('暂停').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2426 桌面循环盘点周期标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态 heading「循环盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '循环盘点周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「管理库存盘点计划与执行周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('管理库存盘点计划与执行周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「新建周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('新建周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2427 桌面循环盘点统计空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「总周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('总周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「暂无盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('暂无盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2428 桌面循环盘点筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「已暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已暂停').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('button', { name: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2429 桌面检验模板标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「检验模板管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验模板管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('新增模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 placeholder「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByPlaceholder('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2430 桌面检验模板搜索重置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2431 桌面检验模板空表列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态「暂无检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('暂无检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「检验周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2432 桌面减值重估标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态 heading「资产减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '资产减值/重估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「价值调整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('价值调整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('新增减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2433 桌面减值重估统计空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('已拒绝').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「暂无减值/重估记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('暂无减值/重估记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2434 桌面减值重估副标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「减值/重估合计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('减值/重估合计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「需及时处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('需及时处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「本期已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('本期已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2435 桌面预算管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态 heading「预算管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '预算管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「资产预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('资产预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('新增预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2436 桌面预算统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「总预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('总预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已承诺').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2437 桌面预算列表空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「执行率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('执行率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「预算列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('预算列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「暂无预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('暂无预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2438 桌面预算类型告警空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「超支告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('超支告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「合同锁定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('合同锁定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「运营」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('运营', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2439 桌面资产退役标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态 heading「资产退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '资产退役管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「资产退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('资产退役', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「新建退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('新建退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2440 桌面资产退役统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「管理资产生命周期终止、处置流程和退役记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('管理资产生命周期终止、处置流程和退役记录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「退役总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('退役总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「待审核」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('待审核').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2441 桌面资产退役残值空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「残值回收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('残值回收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「主要退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('主要退役原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「暂无退役申请记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('暂无退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2442 桌面资产退役筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 空态「异常/已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('异常/已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「已撤回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('button', { name: '已撤回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「审批中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('button', { name: '审批中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2443 桌面重要设备标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 heading「重要设备管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '重要设备管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「关键生产设备维保追踪与状态监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('关键生产设备维保追踪与状态监控').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「新建维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('新建维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2444 桌面重要设备统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「总设备数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('总设备数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「维保中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维保中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「正常运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('正常运行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2445 桌面重要设备空表记录空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「暂无设备数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('暂无设备数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 heading「最近维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '最近维保记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「暂无维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('暂无维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2446 桌面重要设备筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「维保状态:」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维保状态:').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「维修中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('维修中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2447 桌面资产模型标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态 heading「资产模型管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByRole('heading', { name: '资产模型管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「管理资产品类的标准模板与规格定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('管理资产品类的标准模板与规格定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「新增模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('新增模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2448 桌面资产模型统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态「全部模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('全部模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('正常', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2449 桌面资产模型筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 空态 placeholder「搜索名称 / 型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByPlaceholder('搜索名称 / 型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「全部分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('全部分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models 空态「全部制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByText('全部制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2450 桌面检验记录标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 heading「检验记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('heading', { name: '检验记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「创建、编辑和追踪设备检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('创建、编辑和追踪设备检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「批量生成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('批量生成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2451 桌面检验记录统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('待检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「已过期/不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('已过期/不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2452 桌面检验记录搜索空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('新增检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 placeholder「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('暂无检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2453 桌面资产处置标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态 heading「资产处置管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '资产处置管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「全周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('全周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「本月处置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('本月处置总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2454 桌面资产处置回收标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「资产回收价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产回收价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「报废转让」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('报废转让').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2455 桌面资产处置调拨列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「资产清退列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产清退列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 placeholder「搜索资产清退单号 / 资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByPlaceholder('搜索资产清退单号 / 资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2456 桌面资产处置空表审批空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「暂无资产清退记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('暂无资产清退记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「新建资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('新建资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2457 桌面个人中心套餐空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「当前套餐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('当前套餐').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 空态「首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2458 桌面GIS地图标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态 heading「GIS 资产地图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: 'GIS 资产地图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「资产地理位置分布可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('资产地理位置分布可视化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「资产定位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('资产定位管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2459 桌面GIS数据来源空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis 空态「GIS 定位数据来源于资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('GIS 定位数据来源于资产台账').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「关联已有资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('关联已有资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态 heading「数据分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '数据分析' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2460 桌面数据分析范围空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「多维资产趋势、分类结构与运营指标分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('多维资产趋势、分类结构与运营指标分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「近 12 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('近 12 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2461 桌面数据分析图表标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据来自资产台账、维保记录与审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据来自资产台账、维保记录与审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2462 桌面数据分析排行处置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('部门资产排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('处置统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2463 桌面资产健康评分标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态 heading「资产健康评分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '资产健康评分' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「多维度资产健康度评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('多维度资产健康度评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「平均健康分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('平均健康分').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2464 桌面资产健康统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「健康资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('健康资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「警告资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('警告资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「危险资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('危险资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2465 桌面资产健康分布空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health 空态「评分分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('评分分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 空态「评分区间分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByText('评分区间分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「TCO 全生命周期成本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('TCO 全生命周期成本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2466 桌面TCO构成排行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「全生命周期成本总览与分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('全生命周期成本总览与分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「资产 TCO 构成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('资产 TCO 构成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「部门 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('部门 TCO 排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2467 桌面TCO趋势分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「TCO 趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('TCO 趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「分类 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('分类 TCO 排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态「请输入资产ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('请输入资产ID查询').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2468 桌面TCO查询栏空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByText('资产ID', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 空态 placeholder「输入部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByPlaceholder('输入部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2469 桌面可靠性分析标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「可靠性分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('可靠性分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTBF/MTTR/可用性/故障率分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR/可用性/故障率分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「资产可靠性排名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('资产可靠性排名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2470 桌面可靠性KPI趋势空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「MTBF/MTTR 趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR 趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「平均故障间隔」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均故障间隔').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「月均故障率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('月均故障率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2471 桌面可靠性可用性筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「平均修复时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均修复时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「设备可用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('设备可用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「按 MTBF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.locator('select').filter({ hasText: '按 MTBF' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2472 桌面用户管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态 heading「用户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '用户管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「新增用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('新增用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2473 桌面用户管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「用户总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「角色数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('角色数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「岗位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('岗位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2474 桌面用户列表搜索空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「用户列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态 placeholder「搜索用户名、姓名、邮箱或手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByPlaceholder('搜索用户名、姓名、邮箱或手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「暂无用户数据，可调整筛选或新建用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('暂无用户数据，可调整筛选或新建用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2475 桌面角色管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态 heading「角色管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '角色管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「RBAC」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('RBAC').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「新增角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('新增角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2476 桌面角色列表统计空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「角色列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「暂无角色数据，点击「新增角色」创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('暂无角色数据，点击「新增角色」创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2477 桌面部门管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态 heading「部门管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '部门管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「组织架构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('组织架构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「新增部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('新增部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2478 桌面部门管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「部门总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('部门总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「正常部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('正常部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「顶级部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('顶级部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2479 桌面部门搜索新增空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态 placeholder「搜索部门名称、编码、负责人...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByPlaceholder('搜索部门名称、编码、负责人...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「个部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('个部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('button', { name: '新增', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2480 桌面菜单管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态 heading「菜单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '菜单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「树形」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('树形').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「新增菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('新增菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2481 桌面岗位管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态 heading「岗位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「新增岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('新增岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2482 桌面岗位列表统计空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「岗位列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「暂无岗位数据，点击「新增岗位」创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('暂无岗位数据，点击「新增岗位」创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2483 桌面自定义字段标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态 heading「自定义字段管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByRole('heading', { name: '自定义字段管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「管理系统扩展字段定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('管理系统扩展字段定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态 placeholder「搜索字段名或显示名...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByPlaceholder('搜索字段名或显示名...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2484 桌面自定义字段集标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态「没有找到匹配的字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('没有找到匹配的字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态 heading「自定义字段集管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByRole('heading', { name: '自定义字段集管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态 placeholder「搜索字段集名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByPlaceholder('搜索字段集名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2485 桌面业务流程标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态 heading「业务流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '业务流程管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「集中维护审批流程、发布状态、版本快照和业务入口。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('集中维护审批流程、发布状态、版本快照和业务入口。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「新建流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('新建流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2486 桌面业务流程返回处置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「返回资产处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('返回资产处置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「从模板创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await page.getByText('新建流程').first().click();
    await expect(page.getByText('从模板创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「流程总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('流程总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2487 桌面业务流程统计筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「全部业务流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('全部业务流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「可用于发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('可用于发起').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「待完善发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('待完善发布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2488 桌面业务流程暂停搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「暂停发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('暂停发起').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态 placeholder「搜索流程名称、编码、说明或业务对象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByPlaceholder('搜索流程名称、编码、说明或业务对象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「草稿中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('草稿中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2489 桌面业务流程发布停用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows 空态「已发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('已发布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows 空态「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByText('已停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「赔偿编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2490 桌面赔偿配置方式空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「赔偿配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「现金赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('现金赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「等价物赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('等价物赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2491 桌面赔偿金额方式空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「总赔偿金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('总赔偿金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「赔偿方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「预估赔偿金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('预估赔偿金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2492 桌面赔偿申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「资产赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「标准赔偿流程 v1.0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('标准赔偿流程 v1.0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2493 桌面退役申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「资产退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「新建申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('新建申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2494 桌面退役申请搜索返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 placeholder「搜索资产编号或名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('搜索资产编号或名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「请输入资产编号或名称搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('请输入资产编号或名称搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2495 桌面新建借用单标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「借用信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('借用信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2496 桌面新建借用单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「请选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.locator('select').filter({ hasText: '请选择资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态 placeholder「借用用途」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByPlaceholder('借用用途').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「创建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('创建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2497 桌面新建领用单标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('新建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('基本信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「领用类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('领用类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2498 桌面新建领用单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('长期领用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「使用人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('使用人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「创建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('创建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2499 桌面新增预算标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('新增预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「创建新的预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('创建新的预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算年度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2500 桌面新增预算类型金额空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「预算类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「采购预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.locator('select').filter({ hasText: '采购预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2501 桌面新增预算部门返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('分类ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2502 桌面新建验收单标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「新建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('新建验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「填写验收信息和入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('填写验收信息和入库资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「验收单信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('验收单信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2503 桌面新建验收单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「验收日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('验收日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「选择供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('选择供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2504 桌面新增保险标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 placeholder「请输入保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2505 桌面新增保险名称类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 placeholder「请输入保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2506 桌面备件申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「备件申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「按维保工单、库存下限和供应商交期创建备件保障记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('按维保工单、库存下限和供应商交期创建备件保障记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: '返回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2507 桌面备件申请库存信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('当前库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('安全库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「申请信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('申请信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2508 桌面备件申请编码名称空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 placeholder「请输入备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2509 桌面备件申请规格缺口空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「建议补足缺口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('建议补足缺口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「预计到货时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('预计到货时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2510 桌面新建盘点周期标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态 heading「新建盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByRole('heading', { name: '新建盘点周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「周期名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('周期名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态 placeholder「例如：2024年6月循环盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByPlaceholder('例如：2024年6月循环盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2511 桌面新建盘点周期类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「盘点类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('盘点类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「全盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.locator('select').filter({ hasText: '全盘点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「新建工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('新建工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2512 桌面新建工单信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2513 桌面新建工单标题关联空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「标题 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('标题 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「请输入工单描述性标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请输入工单描述性标题').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「资产关联」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '资产关联' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2514 桌面新建工单类型关联空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「工单类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('工单类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('关联资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「其他」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.locator('select').filter({ hasText: '其他' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2515 桌面新建工单优先级描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「立即处理，影响关键业务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('立即处理，影响关键业务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「正常处理节奏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('正常处理节奏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「详细信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '详细信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2516 桌面新建工单优先级补充空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「优先处理，可能影响业务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('优先处理，可能影响业务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「有空时处理即可」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('有空时处理即可').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2517 桌面新建工单详细信息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('描述', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「请详细说明工单内容及注意事项...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请详细说明工单内容及注意事项...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「预计费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('预计费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2518 桌面新建工单人员安排空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「截止日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('截止日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「人员安排」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '人员安排' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2519 桌面新建工单协作附件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「协作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('协作人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「添加人员...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('添加人员...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '附件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2520 桌面新建工单提交提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「上传文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('上传文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「处理提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '处理提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「提交工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('提交工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2521 桌面新建工单搜索提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 placeholder「搜索资产编号或名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('搜索资产编号或名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「用于安排负责人、资产与截止时间的填写参考。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('用于安排负责人、资产与截止时间的填写参考。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2522 桌面新增风险评估标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「新增风险评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('新增风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2523 桌面新增风险评估等级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「可能性（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('可能性（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「影响程度（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('影响程度（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2524 桌面新增风险评估评审空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 placeholder「描述风险缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('描述风险缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评审日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2525 桌面新增风险评估提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 placeholder「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「自动计算的风险等级：LOW（低危）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('自动计算的风险等级：LOW（低危）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2526 桌面新增风险评估取消空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「1 - 极低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('1 - 极低').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「请结合工单类型、资产状态与现场影响填写说明，并在提交前确认负责人和截止日期。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('请结合工单类型、资产状态与现场影响填写说明，并在提交前确认负责人和截止日期。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2527 桌面盘点周期保存风险影响空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByRole('button', { name: '保存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「1 - 极小」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('1 - 极小').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2528 桌面转移申请说明工单优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态 placeholder「请输入调拨的具体说明或原因...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByPlaceholder('请输入调拨的具体说明或原因...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「紧急」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('紧急', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('高', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2529 桌面新建工单中低优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态「中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('中', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('低', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「*」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.locator('span.text-red-500').filter({ hasText: '*' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2530 桌面新建领用单占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态 placeholder「使用人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用人 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 placeholder「使用部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用部门 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2531 桌面借用验收占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态 placeholder「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByPlaceholder('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「选择供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('选择供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「输入总金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('输入总金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2532 桌面新建验收备注检查空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 placeholder「验收备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('验收备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「检查项名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('检查项名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「预期值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('预期值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2533 桌面新建验收资产占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 placeholder「自动生成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('自动生成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('必填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「品牌」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('品牌').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2534 桌面新建验收型号序列号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 placeholder「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「序列号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('序列号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2535 桌面新建验收分类金额空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('选择分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「0.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('0.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「资产备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('资产备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2536 桌面新建验收地点入库空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「选择地点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('选择地点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 placeholder「12」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByPlaceholder('12').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 heading「入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('heading', { name: '入库资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2537 桌面新建验收资产行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「资产 #1」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('资产 #1').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「添加」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: '添加' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「资产名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('资产名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2538 桌面新建验收编号分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('资产分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「填写验收信息和入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('填写验收信息和入库资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2539 桌面新建验收地点保修空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「存放地点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('存放地点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「保修期(月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('保修期(月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「创建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('创建验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2540 桌面新建保险占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 placeholder「请输入保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 placeholder「请输入保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 placeholder「请输入保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入保额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2541 桌面新建保险免赔备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 placeholder「请输入免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入免赔额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 placeholder「请输入备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByPlaceholder('请输入备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「购置日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('购置日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2542 桌面新建验收原值取消空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「验收日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('验收日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2543 桌面赔偿申请占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态 placeholder「输入责任人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('输入责任人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 placeholder「输入发现人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('输入发现人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 placeholder「搜索资产编号/名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('搜索资产编号/名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2544 桌面退役残值备件单位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 placeholder「0.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByPlaceholder('0.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 placeholder「件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「暂无可选部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('暂无可选部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2545 桌面新建验收资产标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「品牌」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('品牌', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('型号', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「序列号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('序列号', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2546 桌面新建资产名称占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：戴尔服务器 R740」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：戴尔服务器 R740').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：Dell」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：Dell').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：PowerEdge R740」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：PowerEdge R740').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2547 桌面新建资产序列号位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：SN-2024-001234」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：SN-2024-001234').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：××科技公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：××科技公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：研发中心 3F 机房」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：研发中心 3F 机房').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2548 桌面新建资产坐标标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：39.904200」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：39.904200').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：116.407400」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：116.407400').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：TAG-20240001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：TAG-20240001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2549 桌面新建资产财务占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：15000.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：15000.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：12000.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：12000.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：24」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：24').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2550 桌面新建资产折旧描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：0.1 = 10%/年」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：0.1 = 10%/年').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「资产用途、主要配置等...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('资产用途、主要配置等...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「其他需要说明的信息...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('其他需要说明的信息...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2551 桌面新建资产厂商坐标标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「品牌/厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('品牌/厂商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「纬度 (-90 ~ 90)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('纬度 (-90 ~ 90)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「RFID 标签」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('RFID 标签').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2552 桌面新建资产经度金额空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「经度 (-180 ~ 180)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('经度 (-180 ~ 180)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「原值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('原值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「当前净值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('当前净值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2553 桌面新建资产保修折旧空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「保修期（月）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('保修期（月）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「折旧率（0~1）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('折旧率（0~1）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「是否重要设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('是否重要设备').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2554 桌面新建资产分类状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「资产分类 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产分类 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2555 桌面新建资产分节说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「使用部门与物理位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('使用部门与物理位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「描述与备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('描述与备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「已输入 0 / 500 字」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('已输入 0 / 500 字').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2556 桌面新建资产附件关系空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「资产描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「已输入 0 / 200 字」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('已输入 0 / 200 字').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产图片与文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产图片与文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2557 桌面新建资产父子关系空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('父子关系').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产主附属关系管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产主附属关系管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「请先保存资产后再管理父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('请先保存资产后再管理父子关系').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2558 桌面新建资产名称创建空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「资产名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「新增资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('新增资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2559 桌面新建资产上传进度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「请先保存资产后再上传附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('请先保存资产后再上传附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「填写进度：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('填写进度：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产台账').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2560 桌面位置新增弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 点新增顶级位置「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「位置编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByText('位置编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 heading「新增位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('heading', { name: '新增位置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2561 桌面资产模型描述备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型 placeholder「模型描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByPlaceholder('模型描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 placeholder「内部备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByPlaceholder('内部备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 placeholder「{"cpu": "Intel i7", "memory": "32GB"}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByPlaceholder('{"cpu": "Intel i7", "memory": "32GB"}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2562 桌面资产模型字段集空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型「选择字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('选择字段集').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('型号', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「选择制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('选择制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q2563 桌面检验模板新增弹窗空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 placeholder「例如：年度安全检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByPlaceholder('例如：年度安全检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板 placeholder「例如：1,2,3」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByPlaceholder('例如：1,2,3').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「检验周期（月）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('检验周期（月）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2564 桌面检验模板检查项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板「检查项（每行一个）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('检查项（每行一个）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「适用资产类别ID（逗号分隔）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('适用资产类别ID（逗号分隔）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('定期检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2565 桌面检验模板状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('启用', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2566 桌面检验模板弹窗按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「确定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByRole('button', { name: /确\s*定/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 点新增模板「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: /新\s*增\s*模\s*板/ }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2567 桌面借用流程表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_BORROW 空态「ASSET_BORROW」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_BORROW');
    await expect(page.getByText('ASSET_BORROW').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_BORROW 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_BORROW');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_BORROW 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_BORROW');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2568 桌面领用流程表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_ASSIGNMENT 空态「ASSET_ASSIGNMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_ASSIGNMENT');
    await expect(page.getByText('ASSET_ASSIGNMENT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_ASSIGNMENT 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_ASSIGNMENT');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_ASSIGNMENT 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_ASSIGNMENT');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2569 桌面验收流程表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_INTAKE 空态「ASSET_INTAKE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_INTAKE');
    await expect(page.getByText('ASSET_INTAKE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_INTAKE 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_INTAKE');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_INTAKE 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_INTAKE');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2570 桌面流程表单源码提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_BORROW 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_BORROW');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_ASSIGNMENT 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_ASSIGNMENT');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_INTAKE 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_INTAKE');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2571 桌面处置流程表单源码提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_CLEARANCE 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2572 桌面退役赔偿流程表单源码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/RETIREMENT 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2573 桌面转移清退流程未配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_CLEARANCE 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2574 桌面处置流程直接发起空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_CLEARANCE 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2575 桌面退役赔偿流程发起空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/RETIREMENT 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT 空态「RETIREMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('RETIREMENT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2576 桌面资产退役流程表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_RETIREMENT 空态「ASSET_RETIREMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_RETIREMENT');
    await expect(page.getByText('ASSET_RETIREMENT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_RETIREMENT 空态「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_RETIREMENT');
    await expect(page.getByText('该流程尚未配置表单源码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_RETIREMENT 空态「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_RETIREMENT');
    await expect(page.getByText('直接发起申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2577 桌面退役赔偿流程编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_RETIREMENT 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_RETIREMENT');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「ASSET_COMPENSATION」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('ASSET_COMPENSATION').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_TRANSFER 空态「ASSET_TRANSFER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('ASSET_TRANSFER').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2578 桌面清退报废流程编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_CLEARANCE 空态「ASSET_CLEARANCE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('ASSET_CLEARANCE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP 空态「ASSET_SCRAP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('ASSET_SCRAP').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_COMPENSATION 空态「请在工作流设计器的「表单源码」标签页中添加表单 HTML。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('请在工作流设计器的「表单源码」标签页中添加表单 HTML。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2579 桌面大屏今日指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「今日新增数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日新增数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「今日借用数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日借用数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「今日调拨数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日调拨数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2580 桌面大屏昨日指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「昨日取消数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('昨日取消数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「昨日盘点数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('昨日盘点数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「昨日返库数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('昨日返库数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2581 桌面大屏飞机指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「定检飞机数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('定检飞机数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「故障飞机数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('故障飞机数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「备勤飞机数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('备勤飞机数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2582 桌面大屏异常因素分析空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「天气因素」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('天气因素').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「航空管制」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('航空管制').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「机械故障」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('机械故障').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2583 桌面大屏调配机场空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「飞机调配」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('飞机调配').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「机场原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('机场原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「旅客原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('旅客原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2584 桌面大屏航线异常空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「成都-北京」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('成都-北京').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「返航」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('返航').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「备降」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('备降').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2585 桌面大屏人员分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「飞行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('飞行', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「空乘」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('空乘').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「空保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('空保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2586 桌面大屏机组航线空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「机长」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('机长', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「副驾」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('副驾', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「西昌 - 成都」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('西昌 - 成都').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2587 桌面大屏重点航班空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「三亚 - 成都」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('三亚 - 成都').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('预警', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「1类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('1类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2588 桌面大屏航班号时刻空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「2类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「3U8888」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('3U8888').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「13:05 - 14:10」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('13:05 - 14:10').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2589 桌面大屏更多航线空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「重庆-北京」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('重庆-北京').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「哈尔滨-北京」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('哈尔滨-北京').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「杭州-北京」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('杭州-北京').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2590 桌面大屏重点取消合外空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「重点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('重点', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('取消', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「机长(合外)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('机长(合外)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2591 桌面大屏副驾航班号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「副驾(合外)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('副驾(合外)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「318」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('318', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「319」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('319', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2592 桌面大屏航班架次空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「341」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('341', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「332」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('332', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「10架」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('10架').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2593 桌面大屏飞机架次空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「6架」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('6架').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「88架」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('88架').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「56 件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('56 件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2594 桌面大屏对比指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「34.2%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('34.2%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「4.8万」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('4.8万').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「10」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('10', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2595 桌面大屏值班天气空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「王珂玉」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('王珂玉').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「吴菲菲 段琪」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('吴菲菲 段琪').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「湿度52%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('湿度52%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2596 桌面大屏风速天气空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「风速2米/秒」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('风速2米/秒').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「多云 6°」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('多云 6°').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「星期四」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('星期四').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2597 桌面大屏英文副标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「Asset Operation Analysis Platform」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('Asset Operation Analysis Platform').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「2」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「15」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('15', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2598 桌面大屏在册飞机空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「186架」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('186架').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「在册飞机总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('在册飞机总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「176架」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('176架').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2599 桌面大屏异常阈值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「执行航班飞机数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('执行航班飞机数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「异常阈值：100」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('异常阈值：100').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「2小时内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2小时内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2600 桌面大屏延误时段表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「2-4小时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2-4小时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「4小时以上」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('4小时以上').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「航班」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('航班', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2601 桌面大屏延误表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「起站」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('起站').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「延误状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('延误状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「延误原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('延误原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2602 桌面大屏国际国内空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「国际」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('国际', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「国内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('国内', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「8」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('8', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2603 桌面大屏承运人员空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「264人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('264人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「今日计划航班数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日计划航班数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「实时客座率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('实时客座率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2604 桌面大屏承运旅客空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「实时承运人数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('实时承运人数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「今日VIP旅客数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日VIP旅客数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「今日特殊旅客数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日特殊旅客数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2605 桌面大屏座位油量空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「今日提供座位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('今日提供座位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「耗油量（万吨）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('耗油量（万吨）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「节油量（百吨）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('节油量（百吨）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2606 桌面大屏燃油时刻空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「燃油价格（元/吨）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('燃油价格（元/吨）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「关舱门：13:05　起飞：15:30　落地：14:10」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('关舱门：13:05　起飞：15:30　落地：14:10').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「87.68%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('87.68%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2607 桌面大屏承运数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「2350」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2350').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「8800」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('8800').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「2100」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('2100').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2608 桌面大屏油量数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「10.77」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('10.77').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「5.78」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('5.78').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「12.24」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('12.24').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2609 桌面大屏人员占比空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「75.2%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('75.2%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「18」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('18', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「32」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('32', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2610 桌面大屏油量增减空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「0.22 ↑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('0.22 ↑').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「1.46 ↑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('1.46 ↑').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「0.00 -」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('0.00 -').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2611 桌面大屏值班标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「值班领导：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('值班领导：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「值班经理：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('值班经理：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「28」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('28', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2612 桌面大屏异常条数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「24」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('24', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「20」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('20', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「16」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('16', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2613 桌面大屏热力数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「12」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('12', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「75」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('75', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「13」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('13', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2614 桌面大屏关舱起飞落地空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「关舱门：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('关舱门：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「起飞：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('起飞：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「落地：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('落地：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2615 桌面三维大屏降级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态 heading「3D 地图已切换为安全降级模式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByRole('heading', { name: '3D 地图已切换为安全降级模式' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「当前环境无法创建 WebGL 渲染上下文，已阻止 3D 资源加载。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('当前环境无法创建 WebGL 渲染上下文，已阻止 3D 资源加载。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「资产指标面板仍可访问；如需完整 3D 大屏，请在支持 WebGL 的浏览器或启用硬件加速后重试。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('资产指标面板仍可访问；如需完整 3D 大屏，请在支持 WebGL 的浏览器或启用硬件加速后重试。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2616 桌面三维大屏降级指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「播放中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('播放中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「0.0万」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('0.0万').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「42%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('42%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2617 桌面三维大屏分类占比空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「24%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('24%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「21%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('21%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「13%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('13%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2618 桌面三维大屏状态标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「开场动画：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('开场动画：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「当前城市：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('当前城市：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「地图节点：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('地图节点：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2619 桌面三维大屏设备数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「112」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('112', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「36」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('36', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「131」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('131', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2620 桌面三维大屏链路数值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「39」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('39', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「98」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('98', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「420」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('420', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2621 桌面三维大屏读写留痕空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「96」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('96', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「100」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('100', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「21」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('21', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2622 桌面三维大屏日期零值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen-3d 空态「2026-08-23」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('2026-08-23').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('0', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });

  test('/bigscreen-3d 空态「四川全域」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('四川全域').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('WebGL') && !item.includes('ErrorBoundary'))).toEqual([]);
  });
});

test.describe('Q2623 桌面大屏KPI图标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「资」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('资', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('率', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('值', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2624 桌面大屏单位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/bigscreen 空态「%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('%', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「万」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('万').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen 空态「件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/bigscreen');
    await expect(page.getByText('件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2625 桌面登录页版权年份空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login 空态「2026」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('2026').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「©」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('©').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「·」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('·').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2626 桌面登录页链接标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login 空态 link「全息版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('link', { name: '全息版' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 link「流星版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('link', { name: '流星版' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 label「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByLabel('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2627 桌面登录页密码按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login 空态 label「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByLabel('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 button「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('button', { name: '登录系统' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 checkbox「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('checkbox', { name: '记住用户名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2628 桌面登录页SSO忘记密码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login 空态 button「MaxKey 单点登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('MaxKey 单点登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2629 桌面登录页认证标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login 空态「使用组织统一身份认证登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('使用组织统一身份认证登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 heading「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('heading', { name: '欢迎回来' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2630 桌面403按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/403 空态 heading「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态 button「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByRole('button', { name: '返回上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/403 空态 button「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/403');
    await expect(page.getByRole('button', { name: '返回首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2631 桌面404页面空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/404 空态「404 — 页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「404」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('404').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/404 空态「页面不存在」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/404');
    await expect(page.getByText('页面不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2632 桌面forbidden说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态「您没有访问此页面的权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('您没有访问此页面的权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「请联系管理员获取相应角色权限后再尝试访问」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('请联系管理员获取相应角色权限后再尝试访问').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态 button「返回首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByRole('button', { name: '返回首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2633 桌面forbidden标题返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden 空态 heading「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态 button「返回上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByRole('button', { name: '返回上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden 空态「无访问权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden');
    await expect(page.getByText('无访问权限', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2634 桌面SSO回调失败按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/sso-callback 空态 button「返回登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByRole('button', { name: '返回登录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「SSO 登录失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sso-callback 空态「Token 缺失，SSO 登录失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sso-callback');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2635 桌面仪表板核心指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「运营首页核心指标」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByLabel('运营首页核心指标').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 heading「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '运营首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2636 桌面仪表板资产卡片空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2637 桌面仪表板空数据文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「暂无分类数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('暂无分类数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「暂无工单数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('暂无工单数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2638 桌面仪表板操作按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态 button「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('button', { name: '导出数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 button「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('button', { name: '刷新视图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 heading「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '分类分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2639 桌面仪表板分区标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态 heading「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '资产价值趋势 (近12个月)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 heading「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '最近工单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 heading「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '维保预警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2640 桌面供应商门户登录空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/vendor-portal 空态 heading「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('heading', { name: '供应商门户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 placeholder「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 placeholder「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2641 桌面供应商门户登录按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/vendor-portal 空态 button「登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('button', { name: '登录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态「登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByText('登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2642 桌面登录4表单控件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login4 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 button「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('button', { name: '登录并进入工作台' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2643 桌面登录4辅助控件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login4 空态 button「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('button', { name: '忘记密码?' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 checkbox「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('checkbox', { name: '记住用户名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 link「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('link', { name: 'UNIVIEW 统一身份认证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2644 桌面登录4表单标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login4 空态「登录表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByLabel('登录表单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 label「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByLabel('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 button「显示密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('button', { name: '显示密码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2645 桌面登录4标题密码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login4 空态「固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 label「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByLabel('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 heading「UNIVIEW 固定资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('heading', { name: 'UNIVIEW 固定资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2646 桌面登录5表单控件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: '登录并进入工作台' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2647 桌面登录5辅助控件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态 button「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: '忘记密码?' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 checkbox「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('checkbox', { name: '记住用户名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 link「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('link', { name: 'UNIVIEW 统一身份认证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2648 桌面登录5表单标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态「登录表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByLabel('登录表单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 label「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByLabel('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「显示密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: '显示密码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2649 桌面登录5标题密码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态「使用组织账号进入固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('使用组织账号进入固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 label「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByLabel('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('登录系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2650 桌面登录5演示与预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态 link「查看工作台预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('link', { name: '查看工作台预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「开发环境快速体验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('开发环境快速体验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2651 桌面登录5演示账户按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态 button「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /系统管理员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /资产管理员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /运维人员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2652 桌面登录5演示账户描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态 button「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /部门负责人/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2653 桌面登录5演示账户文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
  });

  test('/login5 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2654 桌面闲置资产操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 button「发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '发布公告' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 button「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: /待处理/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「条结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('条结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2655 桌面闲置资产筛选页签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 button「公告中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: /公告中/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 button「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: /待审批/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 button「已认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: /已认领/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2656 桌面闲置资产表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 button「历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: /历史记录/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 columnheader「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '资产编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 columnheader「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '资产名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2657 桌面闲置资产其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 columnheader「部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 columnheader「闲置天数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '闲置天数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2658 桌面闲置资产操作列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态 heading「闲置资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '闲置资产管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle 空态「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByText('暂无数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2659 桌面设备管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 button「新建维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '新建维保记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 placeholder「搜索设备名称或编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByPlaceholder('搜索设备名称或编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 heading「重要设备管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '重要设备管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2660 桌面设备管理空数据文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态「暂无设备数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('暂无设备数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 heading「最近维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '最近维保记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态「暂无维保记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByText('暂无维保记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2661 桌面设备管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 columnheader「设备名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '设备名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「设备ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '设备ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「上次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '上次维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2662 桌面设备管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 columnheader「下次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '下次维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「使用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '使用率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「维保状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '维保状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2663 桌面设备维保记录表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 columnheader「设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '设备' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2664 桌面设备维保记录其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment 空态 columnheader「技术员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '技术员' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '费用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2665 桌面故障代码操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态 button「新增根节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('button', { name: '新增根节点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态 heading「故障代码管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '故障代码管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「点击「新增根节点」创建第一级故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('点击「新增根节点」创建第一级故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2666 桌面故障代码统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「节点总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('节点总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障现象').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2667 桌面故障代码体系文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 空态「解决措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('解决措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「故障树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障树').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes 空态「现象 → 原因 → 措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByText('现象 → 原因 → 措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2668 桌面合同管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 button「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '新增合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 columnheader「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '合同编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 columnheader「合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '合同名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2669 桌面合同管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 columnheader「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '金额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2670 桌面合同管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 placeholder「搜索合同名称/编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByPlaceholder('搜索合同名称/编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 heading「合同管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '合同管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 button「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2671 桌面合同管理页签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「全部合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('全部合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('即将到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「时间轴视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('时间轴视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2672 桌面合同管理到期文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同信息维护与到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同信息维护与到期预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「30天内到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('30天内到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 columnheader「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('columnheader', { name: '到期日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2673 桌面采购订单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态 button「新增采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '新增采购单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 heading「采购订单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('heading', { name: '采购订单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 placeholder「搜索采购单号、名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByPlaceholder('搜索采购单号、名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2674 桌面采购订单表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态 columnheader「采购单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '采购单号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 columnheader「采购名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '采购名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 columnheader「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '供应商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2675 桌面采购订单其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态 columnheader「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '金额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 columnheader「采购日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '采购日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2676 桌面采购订单操作列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态 button「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「采购订单创建、审批与收货全流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购订单创建、审批与收货全流程管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2677 桌面采购订单统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「总采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('总采购单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2678 桌面采购订单筛选文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('全部状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「部分收货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('部分收货').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByText('刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2679 桌面审批中心操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态 button「发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '发起申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态 heading「审批中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '审批中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「暂无审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('暂无审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2680 桌面审批中心筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态 placeholder「搜索编号、标题或发起人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByPlaceholder('搜索编号、标题或发起人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待我审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('我发起的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2681 桌面审批中心状态文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「当前没有待处理的审批事项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('当前没有待处理的审批事项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2682 桌面审批中心类型筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产报废').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2683 桌面审批中心其余筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「工单申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('工单申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2684 桌面维保管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态 button「新增维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '新增维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 heading「维保管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '维保管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 placeholder「按资产ID搜索...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByPlaceholder('按资产ID搜索...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2685 桌面维保管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态 columnheader「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '资产ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 columnheader「维保类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '维保类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 columnheader「维保日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '维保日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2686 桌面维保管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态 columnheader「执行人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '执行人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 columnheader「费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '费用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 columnheader「下次维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '下次维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2687 桌面维保管理列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 heading「维保记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '维保记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「暂无维保记录，点击「新增」开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('暂无维保记录，点击「新增」开始创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2688 桌面维保管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「计划中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('计划中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「执行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('执行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('逾期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2689 桌面维保管理其余文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态 button「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance 空态「设备维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByText('设备维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2690 桌面维保计划操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 button「新建计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '新建计划' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 heading「维保计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('heading', { name: '维保计划管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「暂无维保计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('暂无维保计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2691 桌面维保计划表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 placeholder「搜索计划名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByPlaceholder('搜索计划名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「计划名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '计划名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '关联资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2692 桌面维保计划其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 columnheader「计划周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '计划周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '负责人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「下次执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '下次执行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2693 桌面维保计划状态表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态 columnheader「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2694 桌面维保计划视图切换空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 空态「周期性维保计划配置与管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('周期性维保计划配置与管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans 空态 button「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2695 桌面个人中心套餐空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「当前套餐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('当前套餐').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2696 桌面个人中心账号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile 空态「SUPER_ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('SUPER_ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile 空态「ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByText('ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2697 桌面通知中心操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态 button「全部已读」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: '全部已读' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态 heading「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('heading', { name: '通知中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「暂无通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('暂无通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2698 桌面通知中心分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「审批通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('审批通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「预警通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('预警通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「系统通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('系统通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2699 桌面通知中心计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「共 0 条通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('共 0 条通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「所有通知都会显示在这里」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('所有通知都会显示在这里').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2700 桌面折旧管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 button「批量计算折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '批量计算折旧' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 heading「折旧管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '折旧管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「暂无折旧计划数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('暂无折旧计划数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2701 桌面折旧管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 placeholder「搜索资产编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByPlaceholder('搜索资产编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2702 桌面折旧管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「本月折旧总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('本月折旧总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「待计算资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('待计算资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2703 桌面折旧管理方法筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「全部方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('全部方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「直线法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('直线法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「双倍余额递减法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('双倍余额递减法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2704 桌面折旧管理其余方法空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「年数总和法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('年数总和法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「工作量法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('工作量法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 heading「资产折旧计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '资产折旧计划管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2705 桌面折旧管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 columnheader「折旧方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '折旧方法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 columnheader「当期折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '当期折旧' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 columnheader「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '累计折旧' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2706 桌面折旧管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 columnheader「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '原值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 columnheader「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '净值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2707 桌面折旧管理其余文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 columnheader「折旧率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('columnheader', { name: '折旧率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 button「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2708 桌面减值重估操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态 button「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('button', { name: '新增减值/重估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态 heading「资产减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '资产减值/重估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「暂无减值/重估记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('暂无减值/重估记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2709 桌面减值重估统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('已拒绝').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「价值调整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('价值调整').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2710 桌面减值重估其余统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「减值/重估合计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('减值/重估合计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「需及时处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('需及时处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「本期已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('本期已审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2711 桌面减值重估列表文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「本期驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('本期驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「减值/重估列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('减值/重估列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2712 桌面减值重估表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态 columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态 columnheader「新值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('columnheader', { name: '新值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态 columnheader「差额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('columnheader', { name: '差额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2713 桌面减值重估记录文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations 空态「条记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('条记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「资产价值调整记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('资产价值调整记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations 空态「资产减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByText('资产减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2714 桌面预算管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态 button「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('button', { name: '新增预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 heading「预算管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '预算管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「暂无预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('暂无预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2715 桌面预算管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「总预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('总预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已使用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('已承诺').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2716 桌面预算管理列表文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「执行率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('执行率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「预算列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('预算列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「资产预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('资产预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2717 桌面预算管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态 columnheader「年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '年度' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 columnheader「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '预算总额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2718 桌面预算管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态 columnheader「剩余」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '剩余' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 columnheader「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '已使用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 columnheader「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '已承诺' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2719 桌面预算管理状态操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「超支告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('超支告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2720 桌面预算管理其余文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets 空态「合同锁定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('合同锁定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「运营」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('运营').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets 空态「0 项预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByText('0 项预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2721 桌面数据分析标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态 heading「数据分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '数据分析' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「本月维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('本月维保').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2722 桌面数据分析指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「资产总值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产总值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2723 桌面数据分析图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「近 12 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('近 12 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「资产分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('资产分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2724 桌面数据分析排行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('部门资产排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('处置统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2725 桌面数据分析空数据文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「暂无分类数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无分类数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「暂无部门排行数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('暂无部门排行数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态「多维资产趋势、分类结构与运营指标分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('多维资产趋势、分类结构与运营指标分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2726 桌面数据分析来源标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics 空态「数据来自资产台账、维保记录与审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByText('数据来自资产台账、维保记录与审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态 heading「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '资产价值趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics 空态 heading「资产分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '资产分类分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2727 桌面可靠性分析标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态 heading「可靠性分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '可靠性分析' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「暂无排名数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('暂无排名数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('暂无趋势数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2728 桌面可靠性分析指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「平均故障间隔」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均故障间隔').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「平均修复时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('平均修复时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「设备可用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('设备可用率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2729 桌面可靠性分析其余指标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「月均故障率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('月均故障率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「总故障 0 次」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('总故障 0 次').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTBF/MTTR 趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR 趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2730 桌面可靠性分析排名空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability 空态「资产可靠性排名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('资产可靠性排名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「可用性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('可用性').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 空态「MTBF/MTTR/可用性/故障率分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('MTBF/MTTR/可用性/故障率分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2731 桌面审计日志操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 heading「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '审计日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 placeholder「搜索操作记录...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByPlaceholder('搜索操作记录...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 button「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('button', { name: '导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2732 桌面审计日志统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「总操作数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总操作数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「今日操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('今日操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「活跃用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('活跃用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2733 桌面审计日志趋势空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「风险事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('风险事件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近7天」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近7天').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作趋势（近7天）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2734 桌面审计日志分布空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作类型分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「系统日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('系统日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2735 桌面审计日志表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 columnheader「时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「操作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '操作人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「操作类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '操作类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2736 桌面审计日志其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 columnheader「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '描述' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「IP地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: 'IP地址' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2737 桌面审计日志筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「全部日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('全部日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「总计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2738 桌面审计日志分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('显示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「项，共」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('项，共').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2739 桌面审计日志副标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「按最近 7 天汇总操作趋势、风险事件和操作人分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('按最近 7 天汇总操作趋势、风险事件和操作人分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 heading「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '操作趋势（近7天）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 heading「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '操作类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2740 桌面审计日志最近操作标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 heading「最近操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '最近操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('审计日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2741 桌面资产处置操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态 button「新建资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '新建资产清退' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 heading「资产处置管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '资产处置管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「暂无资产清退记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('暂无资产清退记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2742 桌面资产处置统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态 placeholder「搜索资产清退单号 / 资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByPlaceholder('搜索资产清退单号 / 资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「本月处置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('本月处置总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产回收价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产回收价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2743 桌面资产处置表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态 columnheader「处置单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '处置单号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 columnheader「资产信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '资产信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 columnheader「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '申请人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2744 桌面资产处置其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态 columnheader「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '申请日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2745 桌面资产处置页签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「全周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('全周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2746 桌面资产处置类型入口空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「报废转让」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('报废转让').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2747 桌面资产处置列表文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals 空态「资产清退列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('资产清退列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「清退操作将资产移出当前库存，需确认资产状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('清退操作将资产移出当前库存，需确认资产状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals 空态「新建资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByText('新建资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2748 桌面检验管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 button「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '新增检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 heading「检验/年检管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('heading', { name: '检验/年检管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('暂无检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2749 桌面检验管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 placeholder「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「总检验数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('总检验数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('已过期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2750 桌面检验管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 columnheader「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '检验编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 columnheader「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '资产ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 columnheader「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '检验类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2751 桌面检验管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 columnheader「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '检验日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 columnheader「下次检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '下次检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 columnheader「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '检验机构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2752 桌面检验管理结果操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 columnheader「结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「管理设备检验记录与年检计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('管理设备检验记录与年检计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2753 桌面检验管理批量操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态 button「批量删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '批量删除' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 button「批量导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '批量导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2754 桌面检验管理类型筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('年度检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('定期检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2755 桌面检验管理结果筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「专项检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('专项检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「全部结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('全部结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2756 桌面检验管理其余筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态「附条件通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByText('附条件通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections 空态 button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2757 桌面检验记录操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 button「新增检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: '新增检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 heading「检验记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('heading', { name: '检验记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('暂无检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2758 桌面检验记录统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 placeholder「检验编号/检验机构/检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByPlaceholder('检验编号/检验机构/检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('总记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('待检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2759 桌面检验记录表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 columnheader「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '检验编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '资产ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '检验类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2760 桌面检验记录其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 columnheader「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '检验日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「下次检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '下次检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '检验机构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2761 桌面检验记录结果操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 columnheader「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '检验人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2762 桌面检验记录文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「创建、编辑和追踪设备检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('创建、编辑和追踪设备检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 button「批量生成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: '批量生成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「当前没有可显示的数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('当前没有可显示的数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2763 桌面检验记录统计筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「已过期/不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('已过期/不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态 button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2764 桌面检验记录类型筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('年度检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2765 桌面检验记录其余类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('定期检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「专项检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('专项检验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「全部结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('全部结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2766 桌面检验记录结果文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-records 空态「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('不通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「附条件通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('附条件通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records 空态「至」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByText('至').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2767 桌面检验模板操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态 button「新增模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: '新增模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「检验模板管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验模板管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态「暂无检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByText('暂无检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2768 桌面检验模板筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态 placeholder「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByPlaceholder('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: '重置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2769 桌面检验模板表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态 columnheader「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '模板名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 columnheader「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '检验类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 columnheader「检验周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '检验周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2770 桌面检验模板其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 columnheader「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '创建时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-templates 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2771 桌面盘点周期操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态 button「新建周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('button', { name: '新建周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态 heading「循环盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '循环盘点周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「暂无盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('暂无盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2772 桌面盘点周期统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「总周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('总周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2773 桌面盘点周期表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态 columnheader「周期名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '周期名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态 columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2774 桌面盘点周期其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态 columnheader「开始时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '开始时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态 columnheader「结束时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '结束时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态 columnheader「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('columnheader', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2775 桌面盘点周期筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「已暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已暂停').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('全部状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2776 桌面盘点周期文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles 空态「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('已取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「管理库存盘点计划与执行周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('管理库存盘点计划与执行周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles 空态「循环盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('循环盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2777 桌面新增预算表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态 button「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('新增预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「创建新的预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('创建新的预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2778 桌面新增预算字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「预算年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算年度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2779 桌面新增预算提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('分类ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态 button「创建预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '创建预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2780 桌面新增预算选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2781 桌面新增减值重估表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态 button「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新增减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「资产价值调整申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('资产价值调整申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2782 桌面新增减值重估字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态 button「查找」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('button', { name: '查找' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态 button「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('button', { name: '提交申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2783 桌面新增减值重估说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「新值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「原因说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('原因说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「证据材料」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('证据材料').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2784 桌面新增减值重估控件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('button', { name: '取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态 placeholder「输入资产ID搜索...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByPlaceholder('输入资产ID搜索...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态 placeholder「提供相关证明材料链接或描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByPlaceholder('提供相关证明材料链接或描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2785 桌面新增检验表单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「新增检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('新增检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2786 桌面新增检验表单标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 label「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 button「扫码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /扫\s*码/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2787 桌面新增检验表单其余标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2788 桌面新增检验表单机构人空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 label「下次检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('下次检验日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2789 桌面新增检验表单结果空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 label「检验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检查发现」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检查发现').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态「检验照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('检验照片').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2790 桌面新增检验表单证书空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 label「证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「证书到期日」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('证书到期日').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 label「检验费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('检验费用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2791 桌面新增检验表单附件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 label「报告附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByLabel('报告附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「自动生成或手动输入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('自动生成或手动输入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检验机构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2792 桌面新增检验表单占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 placeholder「请输入检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检验人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入检查发现内容」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入检查发现内容').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请输入证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('请输入证书编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2793 桌面新增检验表单其余占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「请选择检验模板（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByText('请选择检验模板（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 placeholder「附件路径或URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByPlaceholder('附件路径或URL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2794 桌面新建盘点周期操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态 button「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByRole('button', { name: '返回列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态 button「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态 heading「新建盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByRole('heading', { name: '新建盘点周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2795 桌面新建盘点周期字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「周期名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('周期名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态 placeholder「例如：2024年6月循环盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByPlaceholder('例如：2024年6月循环盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「盘点类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('盘点类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2796 桌面新建盘点周期默认选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/new 空态「新建盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('新建盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/new 空态「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('返回列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2797 桌面新建工单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 button「提交工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: /提\s*交\s*工\s*单/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「新建工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '新建工单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2798 桌面新建工单分区标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「详细信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '详细信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「人员安排」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '人员安排' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2799 桌面新建工单其余标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 heading「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「资产关联」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '资产关联' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 heading「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '附件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2800 桌面新建工单提示占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 heading「处理提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '处理提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「请输入工单描述性标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请输入工单描述性标题').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「搜索资产编号或名称...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('搜索资产编号或名称...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2801 桌面新建工单其余占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new 空态 placeholder「请详细说明工单内容及注意事项...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('请详细说明工单内容及注意事项...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 placeholder「添加人员...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByPlaceholder('添加人员...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态 button「上传文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: /上\s*传\s*文\s*件/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2802 桌面新建资产操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「新增资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('新增资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2803 桌面新建资产分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 heading「位置归属」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '位置归属' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 heading「财务信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '财务信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2804 桌面新建资产标题进度空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「填写进度：」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('填写进度：').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产台账').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2805 桌面新建资产名称占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：戴尔服务器 R740」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：戴尔服务器 R740').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：Dell」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：Dell').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：PowerEdge R740」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：PowerEdge R740').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2806 桌面新建资产序列号占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：SN-2024-001234」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：SN-2024-001234').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：××科技公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：××科技公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：研发中心 3F 机房」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：研发中心 3F 机房').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2807 桌面新建资产坐标占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：39.904200」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：39.904200').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：116.407400」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：116.407400').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：TAG-20240001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：TAG-20240001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2808 桌面新建资产财务占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：15000.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：15000.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：12000.00」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：12000.00').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：24」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：24').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2809 桌面新建资产描述占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：0.1 = 10%/年」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：0.1 = 10%/年').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「资产用途、主要配置等...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('资产用途、主要配置等...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「其他需要说明的信息...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('其他需要说明的信息...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2810 桌面新建资产字段标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「资产名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产分类 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产分类 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「是否重要设备」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('是否重要设备').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2811 桌面新建资产扩展标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「品牌/厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('品牌/厂商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「纬度 (-90 ~ 90)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('纬度 (-90 ~ 90)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「RFID 标签」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('RFID 标签').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2812 桌面新建资产财务标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「经度 (-180 ~ 180)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('经度 (-180 ~ 180)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「原值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('原值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「当前净值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('当前净值（元）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2813 桌面新建资产状态标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「保修期（月）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('保修期（月）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「折旧率（0~1）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('折旧率（0~1）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2814 桌面新建资产分组文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「使用部门与物理位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('使用部门与物理位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「描述与备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('描述与备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2815 桌面新建资产字数统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「已输入 0 / 500 字」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('已输入 0 / 500 字').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「已输入 0 / 200 字」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('已输入 0 / 200 字').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2816 桌面新建资产关系附件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「资产图片与文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产图片与文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('父子关系').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「资产主附属关系管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('资产主附属关系管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2817 桌面新建资产保存前提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态「请先保存资产后再管理父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('请先保存资产后再管理父子关系').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「请先保存资产后再上传附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('请先保存资产后再上传附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('附件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2818 桌面新建借用单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态 button「创建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: /创\s*建\s*借\s*用\s*单/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2819 桌面新建借用单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态 heading「借用信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('heading', { name: '借用信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态 placeholder「借用用途」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByPlaceholder('借用用途').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2820 桌面新建借用单其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new 空态「预计归还日期 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByText('预计归还日期 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态 placeholder「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByPlaceholder('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2821 桌面新建领用单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态 button「创建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: /创\s*建\s*领\s*用\s*单/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 heading「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('heading', { name: '新建领用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2822 桌面新建领用单字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「领用类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('领用类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「使用人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('使用人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2823 桌面新建领用单类型选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('长期领用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「短期借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('短期借用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2824 桌面新建领用单其余选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「归还入库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('归还入库').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「调拨转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('调拨转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「使用部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('使用部门 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2825 桌面新建领用单占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态 placeholder「使用人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用人 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 placeholder「使用部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('使用部门 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态 placeholder「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByPlaceholder('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2826 桌面新建验收单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 button「创建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: /创\s*建\s*验\s*收\s*单/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 heading「新建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('heading', { name: '新建验收单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 heading「验收单信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('heading', { name: '验收单信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2827 桌面新建验收单按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 button「添加」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: /添\s*加/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2828 桌面新建验收单检查项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态 heading「验收检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('heading', { name: '验收检查项' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「总金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('总金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「验收检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('验收检查项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2829 桌面新建退役申请操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 button「提交退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: /提\s*交\s*退\s*役\s*申\s*请/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 heading「资产退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '资产退役申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2830 桌面新建退役申请分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 heading「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '资产选择' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 heading「退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '退役原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2831 桌面新建退役申请其余分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 heading「残值评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '残值评估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 heading「备注与说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '备注与说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态 heading「折旧时间线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '折旧时间线' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2832 桌面新建退役申请提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态「新建申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('新建申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「退役操作不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('退役操作不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2833 桌面新建预算操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态 button「创建预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: /创\s*建\s*预\s*算/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态 button「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: /返\s*回\s*列\s*表/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2834 桌面新建预算字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「预算年度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算年度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「预算总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2835 桌面新建预算其余字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets/new 空态「部门ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('部门ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「分类ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('分类ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「创建新的预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('创建新的预算记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2836 桌面新建保险操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 button「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2837 桌面新建备件操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态 button「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: /提\s*交\s*申\s*请/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 heading「备件申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('heading', { name: '备件申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2838 桌面新建备件字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 heading「申请信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('heading', { name: '申请信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2839 桌面新建备件库存字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('当前库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('安全库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2840 桌面新建备件申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「计量单位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('计量单位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「建议补足缺口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('建议补足缺口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「预计到货时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('预计到货时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2841 桌面新建备件其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('单价').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「按维保工单、库存下限和供应商交期创建备件保障记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('按维保工单、库存下限和供应商交期创建备件保障记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 placeholder「请输入备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入备件编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2842 桌面新建风险评估操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「新增风险评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('新增风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2843 桌面新建风险评估字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「可能性（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('可能性（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「影响程度（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('影响程度（1-5）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2844 桌面新建风险评估其余字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评审日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2845 桌面新建风险评估等级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「自动计算的风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('自动计算的风险等级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「LOW（低危）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('LOW（低危）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「输入资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('输入资产ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2846 桌面新建风险评估占位与赔偿标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 placeholder「描述风险缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('描述风险缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('评估人ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「资产赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2847 桌面新建赔偿申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「赔偿编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「赔偿配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「总赔偿金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('总赔偿金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2848 桌面新建赔偿申请方式空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「赔偿方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「预估赔偿金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('预估赔偿金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2849 桌面新建赔偿申请选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态「现金赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('现金赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「等价物赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('等价物赔偿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「标准赔偿流程 v1.0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('标准赔偿流程 v1.0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2850 桌面新建赔偿申请占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态 placeholder「输入责任人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('输入责任人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 placeholder「输入发现人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('输入发现人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 placeholder「搜索资产编号/名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByPlaceholder('搜索资产编号/名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2851 桌面新建报废申请操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态 button「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('button', { name: /添\s*加\s*资\s*产/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2852 桌面新建报废申请分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态 heading「报废配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '报废配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2853 桌面新建报废申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「报废编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「报废原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2854 桌面新建报废申请资产空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「处置方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('处置方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「暂无已选资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('暂无已选资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「预估残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('预估残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2855 桌面新建报废申请提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「请先选择至少一项资产后再提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('请先选择至少一项资产后再提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2856 桌面新建报废申请其余文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「自动保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('自动保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「新建申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('新建申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2857 桌面新建报废申请风险备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「选填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('选填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态 placeholder「请输入报废申请相关的补充说明...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByPlaceholder('请输入报废申请相关的补充说明...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2858 桌面新建报废申请处置选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「变卖处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('变卖处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「报废拆解」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('报废拆解').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「捐赠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('捐赠').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2859 桌面新建清退申请操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态 button「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('button', { name: /添\s*加\s*资\s*产/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态 button「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('button', { name: /保\s*存\s*草\s*稿/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2860 桌面新建清退申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「清退编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2861 桌面新建清退申请原因空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('申请日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「清退原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2862 桌面新建清退申请配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「清退配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('清退配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「处理方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('处理方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「暂未选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('暂未选择资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2863 桌面新建清退申请紧急空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「入库保管」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('入库保管').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「预估残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('预估残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「紧急程度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('紧急程度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2864 桌面新建清退申请提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「资产清退申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产清退申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「不可逆」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('不可逆').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2865 桌面新建清退申请提交空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('确认提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「请先选择至少一项资产后再提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('请先选择至少一项资产后再提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2866 桌面新建清退申请备注空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态 placeholder「请输入清退相关的额外说明...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByPlaceholder('请输入清退相关的额外说明...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「资产清退操作一旦提交审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产清退操作一旦提交审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2867 桌面新建清退申请选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new 空态「变卖处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('变卖处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「捐赠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('捐赠').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 空态「普通」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('普通').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2868 桌面新建转移申请操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态 button「提交申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /提\s*交\s*申\s*请/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 button「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /保\s*存\s*草\s*稿/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2869 桌面新建转移申请其余按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 button「添加资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /添\s*加\s*资\s*产/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 button「计算处理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('button', { name: /计\s*算\s*处\s*理\s*人/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2870 桌面新建转移申请分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态 heading「基本信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '基本信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 heading「单据信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '单据信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 heading「资产选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '资产选择' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2871 桌面新建转移申请方向空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「转出方」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('转出方').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「转入方」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('转入方').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 heading「审批配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '审批配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2872 桌面新建转移申请字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「调拨编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('申请人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调拨日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2873 桌面新建转移申请类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「调拨类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调拨方向」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调拨方向').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调出部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调出部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2874 桌面新建转移申请位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「调出位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调出位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调入部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调入部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「调入位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('调入位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2875 桌面新建转移申请流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「已选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('已选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「发布流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('发布流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「资产转移流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产转移流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2876 桌面新建转移申请审批空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「紧急程度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('紧急程度').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「处理人预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('处理人预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「高优先级申请将跳过非关键审批节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('高优先级申请将跳过非关键审批节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2877 桌面新建转移申请部门提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「资产当前所属部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产当前所属部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「资产调拨目标部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产调拨目标部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「自动保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('自动保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2878 桌面新建转移申请标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「资产转移申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产转移申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「选填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('选填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('必填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2879 桌面新建转移申请步骤空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「基础信息填写」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('基础信息填写').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「选择调拨资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('选择调拨资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「流程配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('流程配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2880 桌面新建转移申请完成步骤空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「提交状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('提交状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 placeholder「请输入调拨的具体说明或原因...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByPlaceholder('请输入调拨的具体说明或原因...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2881 桌面新建转移申请其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「新建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('新建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「发起部门或位置之间的正式资产转移申请。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('发起部门或位置之间的正式资产转移申请。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2882 桌面折旧管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 button「批量计算折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: /批\s*量\s*计\s*算\s*折\s*旧/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: /查\s*询/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: /重\s*置/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2883 桌面折旧管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态 heading「折旧管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '折旧管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 heading「资产折旧计划管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '资产折旧计划管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「暂无折旧计划数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('暂无折旧计划数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2884 桌面折旧管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「折旧计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧计划').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「本月折旧总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('本月折旧总额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「待计算资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('待计算资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2885 桌面折旧管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「折旧方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态 placeholder「搜索资产编号...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByPlaceholder('搜索资产编号...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2886 桌面折旧管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「当期折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('当期折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('累计折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2887 桌面折旧管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2888 桌面折旧管理统计文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「直线0 / 双倍0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('直线0 / 双倍0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2889 桌面合同管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态 button「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: /新\s*增\s*合\s*同/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 heading「合同管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '合同管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态 placeholder「搜索合同名称/编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByPlaceholder('搜索合同名称/编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2890 桌面合同管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同信息维护与到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同信息维护与到期预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「全部合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('全部合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('即将到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2891 桌面合同管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「30天内到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('30天内到期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「时间轴视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('时间轴视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2892 桌面合同管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('合同名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2893 桌面合同管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('金额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2894 桌面折旧管理方法选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「全部方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('全部方法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「直线法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('直线法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「双倍余额递减法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('双倍余额递减法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2895 桌面折旧其余方法与清退紧急空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation 空态「年数总和法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('年数总和法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation 空态「工作量法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByText('工作量法').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2896 桌面供应商管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 空态 button「新增供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('button', { name: /新\s*增\s*供\s*应\s*商/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态 heading「供应商管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '供应商管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态 placeholder「搜索供应商名称、编码、联系人...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByPlaceholder('搜索供应商名称、编码、联系人...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2897 桌面供应商管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 空态「合作供应商信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('合作供应商信息维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「全部供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('全部供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「暂无供应商数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('暂无供应商数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2898 桌面供应商管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 空态「合作中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('合作中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('已停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors 空态「刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByText('刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2899 桌面位置管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态 button「新增顶级位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('button', { name: /新\s*增\s*顶\s*级\s*位\s*置/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态 heading「位置管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('heading', { name: '位置管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「暂无位置数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('暂无位置数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2900 桌面位置管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态「资产存放位置的层级管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('资产存放位置的层级管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「总位置数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('总位置数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态「顶级位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByText('顶级位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2901 桌面位置展开与分类标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 空态 button「全部折叠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('button', { name: /全\s*部\s*折\s*叠/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations 空态 button「全部展开」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('button', { name: /全\s*部\s*展\s*开/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态 heading「资产分类管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '资产分类管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2902 桌面分类管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态 button「添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('button', { name: /添\s*加\s*根\s*分\s*类/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「管理资产分类层级结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('管理资产分类层级结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「暂无分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('暂无分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2903 桌面分类管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「总分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('总分类数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「分类结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('分类结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2904 桌面分类管理选中空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「当前选中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('当前选中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「子分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('子分类数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('选择分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2905 桌面分类管理提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 空态「请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories 空态「点击上方按钮添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByText('点击上方按钮添加根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「提交此申请将启动多级审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('提交此申请将启动多级审批流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2906 桌面部门管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态 button「新增部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('button', { name: /新\s*增\s*部\s*门/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「暂无部门数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('暂无部门数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「请选择一个部门查看详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('请选择一个部门查看详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2907 桌面部门管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态 heading「部门管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '部门管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「组织架构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('组织架构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「部门总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('部门总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2908 桌面部门管理其余统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「正常部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('正常部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「顶级部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('顶级部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「个部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('个部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2909 桌面部门管理提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「支持搜索过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('支持搜索过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「支持展开/收起子级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('支持展开/收起子级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态 placeholder「搜索部门名称、编码、负责人...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByPlaceholder('搜索部门名称、编码、负责人...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2910 桌面部门创建提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 空态「点击下方按钮创建第一个部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('点击下方按钮创建第一个部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「点击左侧组织架构树中的节点即可查看」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('点击左侧组织架构树中的节点即可查看').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「搜索并选择资产后显示建议残值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('搜索并选择资产后显示建议残值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2911 桌面用户管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态 button「新增用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('button', { name: /新\s*增\s*用\s*户/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态 heading「用户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '用户管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「账号与权限管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('账号与权限管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2912 桌面用户管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「用户总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「角色数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('角色数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「岗位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('岗位数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2913 桌面用户管理列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「总页数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('总页数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「暂无用户数据，可调整筛选或新建用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('暂无用户数据，可调整筛选或新建用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2914 桌面用户管理筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态 placeholder「搜索用户名、姓名、邮箱或手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByPlaceholder('搜索用户名、姓名、邮箱或手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态 button「重置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('button', { name: /重\s*置/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「邮箱/手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('邮箱/手机号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2915 桌面用户管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2916 桌面用户状态与角色操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态 button「新增角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('button', { name: /新\s*增\s*角\s*色/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2917 桌面角色管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态 heading「角色管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '角色管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色与权限管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色与权限管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「暂无角色数据，点击「新增角色」创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('暂无角色数据，点击「新增角色」创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2918 桌面角色管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「角色列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「RBAC」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('RBAC').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2919 桌面角色管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「当前页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('当前页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('角色编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2920 桌面角色管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 空态「数据权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('数据权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 空态「ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByText('ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2921 桌面岗位管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态 button「新增岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('button', { name: /新\s*增\s*岗\s*位/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态 heading「岗位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「暂无岗位数据，点击「新增岗位」创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('暂无岗位数据，点击「新增岗位」创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2922 桌面岗位管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「岗位信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位信息维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位总量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2923 桌面岗位管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「岗位编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('创建时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2924 桌面岗位管理其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态 placeholder「搜索岗位编码、名称或备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByPlaceholder('搜索岗位编码、名称或备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2925 桌面岗位状态与菜单操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态 button「新增菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('button', { name: /新\s*增\s*菜\s*单/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2926 桌面菜单管理标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态 heading「菜单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '菜单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「系统菜单目录与按钮权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('系统菜单目录与按钮权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「暂无菜单数据，请通过 DDL 初始化种子数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('暂无菜单数据，请通过 DDL 初始化种子数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2927 桌面菜单管理统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「树形」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('树形').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「总计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('总计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2928 桌面菜单管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「菜单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「菜单名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「权限标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('权限标识').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2929 桌面菜单管理类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「路由」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('路由').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「按钮」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('按钮').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2930 桌面菜单管理其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2931 桌面菜单操作与退役资产ID空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 空态「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByText('操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 空态「资产 ID *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产 ID *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 空态「预计归还日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('预计归还日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2932 桌面领用资产与转移普通空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/new 空态「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/new');
    await expect(page.getByText('资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态「普通」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('普通').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts 空态「新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByText('新增').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2933 桌面运营首页操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 button「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByRole('button', { name: /导\s*出\s*数\s*据/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 button「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByRole('button', { name: /刷\s*新\s*视\s*图/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 heading「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByRole('heading', { name: '运营首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2934 桌面运营首页统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 button「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByRole('button', { name: /查\s*看\s*全\s*部/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2935 桌面运营首页资产统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2936 桌面运营首页图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「资产价值趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('资产价值趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2937 桌面运营首页工单净值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「部门资产统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('部门资产统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2938 桌面运营首页待办空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「流程待办」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('流程待办').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2939 桌面运营首页导航空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「设备管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('设备管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('工单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「巡检管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('巡检管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2940 桌面运营首页其余导航空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「备件管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('备件管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「数据监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('数据监控').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「报表分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('报表分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2941 桌面运营首页策略导航空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「告警中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('告警中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「组织策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('组织策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「基础维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('基础维护').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2942 桌面运营首页用户区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「我的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('我的资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产总览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('资产总览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2943 桌面运营首页工具区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态「近12个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByText('近12个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2944 桌面运营首页工具标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 title「切换全屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('切换全屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 title「文字与密度设置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('文字与密度设置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbench 空态 title「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('通知中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2945 桌面运营首页个人中心与用户筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbench 空态 title「个人中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbench');
    await expect(page.getByTitle('个人中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('正常').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2946 桌面审批与用户全部空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「至」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('至').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保单号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2947 桌面新建保险字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保险公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2948 桌面新建保险金额空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保费').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('保额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('免赔额').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2949 桌面新建保险日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2950 桌面新建保险其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「财产险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('财产险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('生效中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2951 桌面新建重估类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations/new 空态「减值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('减值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态 heading「新增减值/重估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByRole('heading', { name: '新增减值/重估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2952 桌面新建报废步骤空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「填写信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('填写信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('确认提交').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「资产报废申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('资产报废申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2953 桌面新建报废提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「标准流程需部门经理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('标准流程需部门经理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「残值评估将影响」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('残值评估将影响').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 空态「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('备注').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2954 桌面报废描述与审批类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 空态「发起资产报废处置流程，提交后将进入审批环节。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('发起资产报废处置流程，提交后将进入审批环节。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('全部类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「暂无可选部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('暂无可选部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2955 桌面备件待确认与重估类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「待确认」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('待确认').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 空态「资产价值调整申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('资产价值调整申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2956 桌面新建备件占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态 placeholder「请输入备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入备件名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 placeholder「请输入规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByPlaceholder('请输入规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2957 桌面报表中心操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态 button「导出 CSV」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: /导\s*出\s*CSV/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态 button「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: /导\s*出\s*PDF/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态 heading「报表中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '报表中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2958 桌面报表中心分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「资产报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「财务报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('财务报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「运维报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('运维报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2959 桌面报表中心工单汇总空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「工单报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('工单报表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产汇总表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产汇总表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产分类统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产分类统计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2960 桌面报表中心分布排行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「资产状态分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产状态分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('部门资产排行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「资产增长趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('资产增长趋势').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2961 桌面报表中心描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「全量资产数量、总价值、净值等核心指标概览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('全量资产数量、总价值、净值等核心指标概览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「按资产分类统计数量和总价值的分布情况」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('按资产分类统计数量和总价值的分布情况').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「各状态（在用/闲置/退役/待审批）资产数量占比」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('各状态（在用/闲置/退役/待审批）资产数量占比').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2962 桌面报表中心其余描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports 空态「各部门资产数量排名与价值对比」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('各部门资产数量排名与价值对比').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「月度资产价值与净值变化趋势分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('月度资产价值与净值变化趋势分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「预定义报表查看与数据可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('预定义报表查看与数据可视化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2963 桌面定时报表操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports/scheduled 空态 button「新建定时报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('button', { name: /新\s*建\s*定\s*时\s*报\s*表/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「管理定时报表调度和邮件推送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('管理定时报表调度和邮件推送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports 空态「近 12 个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByText('近 12 个月').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2964 桌面定时报表标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports/scheduled 空态 heading「定时报表配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('heading', { name: '定时报表配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「暂无定时报表配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('暂无定时报表配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled 空态「点击"新建定时报表"开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('点击"新建定时报表"开始创建').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2965 桌面登录页文案空态', () => {
  test('/login 空态「运行稳定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('运行稳定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「宇视科技」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('宇视科技').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「智能运维入口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('智能运维入口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2966 桌面登录页操作空态', () => {
  test('/login 空态 heading「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('heading', { name: '欢迎回来' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 button「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('button', { name: /登\s*录\s*系\s*统/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 checkbox「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByRole('checkbox', { name: '记住用户名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2967 桌面登录页演示账户空态', () => {
  test('/login 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2968 桌面登录页角色文案空态', () => {
  test('/login 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「全生命周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全生命周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2969 桌面登录页能力文案空态', () => {
  test('/login 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2970 桌面登录页标语文案空态', () => {
  test('/login 空态「资产全生命周期管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('资产全生命周期管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「智能审批 · 实时盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('智能审批 · 实时盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「从采购到报废，一个平台全覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('从采购到报废，一个平台全覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2971 桌面登录页组织文案空态', () => {
  test('/login 空态「流程闭环，数据驱动决策」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('流程闭环，数据驱动决策').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「使用组织账号登录，进入固定资产工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('使用组织账号登录，进入固定资产工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「全息版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('全息版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2972 桌面登录页品牌文案空态', () => {
  test('/login 空态「流星版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('流星版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「浙江宇视科技有限公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('浙江宇视科技有限公司').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW · 宇视科技」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW · 宇视科技').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2973 桌面登录页输入文案空态', () => {
  test('/login 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2974 桌面登录页SSO文案空态', () => {
  test('/login 空态「MaxKey 单点登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('MaxKey 单点登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「使用组织统一身份认证登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('使用组织统一身份认证登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW 固定资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW 固定资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2975 桌面登录页字段文案空态', () => {
  test('/login 空态「固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2976 桌面登录页说明文案空态', () => {
  test('/login 空态「固定资产平台的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('固定资产平台的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「帮助团队高效进入固定资产工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('帮助团队高效进入固定资产工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「UNIVIEW 固定资产连接资产台账、流程审批、盘点巡检和运营分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('UNIVIEW 固定资产连接资产台账、流程审批、盘点巡检和运营分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2977 桌面登录页版权文案空态', () => {
  test('/login 空态「2026」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('2026').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「©」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('©').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「·」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('·').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2978 桌面登录4标语文案空态', () => {
  test('/login4 空态「设备健康预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('设备健康预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「连接 MES、设备状态、维保工单与资产全生命周期。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('连接 MES、设备状态、维保工单与资产全生命周期。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「安全单点登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('安全单点登录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2979 桌面登录4协同文案空态', () => {
  test('/login4 空态「运维闭环协同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('运维闭环协同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「开发环境快速体验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('开发环境快速体验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 固定资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 固定资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2980 桌面登录4入口文案空态', () => {
  test('/login4 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('登录系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「使用组织账号进入固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('使用组织账号进入固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2981 桌面登录4字段文案空态', () => {
  test('/login4 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2982 桌面登录4操作文案空态', () => {
  test('/login4 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('登录并进入工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('UNIVIEW 统一身份认证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2983 桌面登录4版本文案空态', () => {
  test('/login4 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「设计稿版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('设计稿版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「当前版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('当前版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2984 桌面登录4皮肤切换空态', () => {
  test('/login4 空态「标准版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('标准版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全息版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全息版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「流星版」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('流星版').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2985 桌面登录4角色文案空态', () => {
  test('/login4 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2986 桌面登录4权限文案空态', () => {
  test('/login4 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「全生命周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('全生命周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2987 桌面登录4能力文案空态', () => {
  test('/login4 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2988 桌面登录4表单辅助空态', () => {
  test('/login4 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态 button「显示密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByRole('button', { name: /显\s*示\s*密\s*码/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login4 空态「固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login4');
    await expect(page.getByText('固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2989 桌面登录5入口文案空态', () => {
  test('/login5 空态「开发环境快速体验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('开发环境快速体验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「查看工作台预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('查看工作台预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「登录系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('登录系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2990 桌面登录5演示账户空态', () => {
  test('/login5 空态「演示账户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('演示账户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资产管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2991 桌面登录5角色文案空态', () => {
  test('/login5 空态「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('部门负责人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('运维人员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「全域权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('全域权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2992 桌面登录5能力文案空态', () => {
  test('/login5 空态「资源审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('资源审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「巡检维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('巡检维修').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2993 桌面登录5操作文案空态', () => {
  test('/login5 空态「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('忘记密码?').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('登录并进入工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('UNIVIEW 统一身份认证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2994 桌面登录5字段文案空态', () => {
  test('/login5 空态「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 placeholder「请输入账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入账号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2995 桌面登录5说明文案空态', () => {
  test('/login5 空态 placeholder「请输入密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByPlaceholder('请输入密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态「使用组织账号进入固定资产平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByText('使用组织账号进入固定资产平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「显示密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /显\s*示\s*密\s*码/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2996 桌面登录5角色按钮空态', () => {
  test('/login5 空态 button「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /系\s*统\s*管\s*理\s*员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「资产管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /资\s*产\s*管\s*理\s*员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「运维人员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /运\s*维\s*人\s*员/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2997 桌面登录5其余按钮空态', () => {
  test('/login5 空态 button「部门负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /部\s*门\s*负\s*责\s*人/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「登录并进入工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /登\s*录\s*并\s*进\s*入\s*工\s*作\s*台/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 button「忘记密码?」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('button', { name: /忘\s*记\s*密\s*码/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2998 桌面登录5链接空态', () => {
  test('/login5 空态 checkbox「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('checkbox', { name: '记住用户名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 link「UNIVIEW 统一身份认证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('link', { name: 'UNIVIEW 统一身份认证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 link「查看工作台预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByRole('link', { name: '查看工作台预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q2999 桌面登录5标签空态', () => {
  test('/login5 空态 label「用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByLabel('用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login5 空态 label「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login5');
    await expect(page.getByLabel('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 heading「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('heading', { name: '供应商门户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3000 桌面供应商门户操作空态', () => {
  test('/vendor-portal 空态 placeholder「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('供应商编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 placeholder「密码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByPlaceholder('密码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 空态 button「登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByRole('button', { name: /登\s*录/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3001 桌面供应商门户文案空态', () => {
  test('/vendor-portal 空态「供应商门户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendor-portal');
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「记住用户名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('记住用户名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/login 空态「欢迎回来」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/login');
    await expect(page.getByText('欢迎回来').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3002 桌面仪表盘操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态 button「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('button', { name: /导\s*出\s*数\s*据/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 button「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('button', { name: /刷\s*新\s*视\s*图/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态 heading「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByRole('heading', { name: '运营首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3003 桌面仪表盘资产统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「总资产数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总资产数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「在用资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('在用资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「闲置资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('闲置资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3004 桌面仪表盘图表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('总价值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「分类分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('分类分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「维保预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('维保预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3005 桌面仪表盘工单净值空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「最近工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('最近工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('净值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「部门资产统计 (Top 5 部门)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('部门资产统计 (Top 5 部门)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3006 桌面仪表盘欢迎空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「运营首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「欢迎回来，系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('欢迎回来，系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3007 桌面仪表盘趋势空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「查看全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('查看全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「资产价值趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('资产价值趋势 (近12个月)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 空态「导出数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('导出数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3008 桌面仪表盘刷新与审批待办空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/dashboard 空态「刷新视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/dashboard');
    await expect(page.getByText('刷新视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待我审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「验收日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('验收日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3009 桌面验收描述与审批标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「填写验收信息和入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('填写验收信息和入库资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('我发起的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已通过').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3010 桌面审批其余标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('已驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('待审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3011 桌面审批类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产清退').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('资产报废').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3012 桌面审批空态文案', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态「工单申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('工单申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「暂无审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('暂无审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态「当前没有待处理的审批事项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByText('当前没有待处理的审批事项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3013 桌面审批操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals 空态 heading「审批中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '审批中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态 button「发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: /发\s*起\s*申\s*请/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals 空态 placeholder「搜索编号、标题或发起人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByPlaceholder('搜索编号、标题或发起人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3014 桌面通知中心操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态 heading「通知中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('heading', { name: '通知中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态 button「全部已读」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: /全\s*部\s*已\s*读/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「暂无通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('暂无通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3015 桌面通知中心类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「审批通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('审批通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「预警通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('预警通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「系统通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('系统通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3016 桌面通知中心统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications 空态「共 0 条通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('共 0 条通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('全部').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications 空态「所有通知都会显示在这里」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByText('所有通知都会显示在这里').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3017 桌面审计日志操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 heading「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '审计日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 placeholder「搜索操作记录...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByPlaceholder('搜索操作记录...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 button「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('button', { name: /导\s*出/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3018 桌面审计日志统计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「总操作数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总操作数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「今日操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('今日操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「活跃用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('活跃用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3019 桌面审计日志趋势空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「风险事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('风险事件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近7天」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近7天').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作趋势（近7天）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3020 桌面审计日志分布空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('操作类型分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「最近操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('最近操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「系统日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('系统日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3021 桌面审计日志表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 columnheader「时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「操作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '操作人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「操作类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '操作类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3022 桌面审计日志其余表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 columnheader「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '描述' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「IP地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: 'IP地址' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3023 桌面审计日志筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「全部日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('全部日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('告警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「总计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('总计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3024 桌面审计日志分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('显示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「项，共」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('项，共').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3025 桌面审计日志描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态「按最近 7 天汇总操作趋势、风险事件和操作人分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('按最近 7 天汇总操作趋势、风险事件和操作人分布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 heading「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '操作趋势（近7天）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态 heading「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '操作类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3026 桌面审计日志标题导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit 空态 heading「最近操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '最近操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('审计日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 空态「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByText('导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3027 桌面通知告警上下文空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new 空态「资产 #1」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('资产 #1').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态 heading「入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('heading', { name: '入库资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 空态「品牌」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByText('品牌').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3028 桌面新建备件编码角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态 textbox「备件编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('textbox', { name: '备件编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 textbox「备件名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('textbox', { name: '备件名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 textbox「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('textbox', { name: '规格型号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3029 桌面新建备件库存角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态 spinbutton「当前库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('spinbutton', { name: '当前库存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 spinbutton「安全库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('spinbutton', { name: '安全库存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 spinbutton「单价」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('spinbutton', { name: '单价' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3030 桌面新建备件单位说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态 textbox「计量单位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('textbox', { name: '计量单位' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 textbox「申请说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('textbox', { name: '申请说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3031 桌面新建备件关联字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「关联工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('关联工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('关联资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「供应来源」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('供应来源').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3032 桌面新建备件待选说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「待选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('待选择').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「申请说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('申请说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「当前库存 (件)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('当前库存 (件)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3033 桌面备件工作台预填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「固定资产工作台 / 备件申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('固定资产工作台 / 备件申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「温控模块传感器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('温控模块传感器').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态 heading「温控模块传感器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByRole('heading', { name: '温控模块传感器' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3034 桌面备件工作台关联预填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「注塑机 M-201」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('注塑机 M-201').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「WO-20250612001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('WO-20250612001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「UNIVIEW 备件仓」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('UNIVIEW 备件仓').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3035 桌面备件工作台建议空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「来自固定资产工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('来自固定资产工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「低储备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('低储备件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「预测维保工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('预测维保工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3036 桌面备件未知来源与保险编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「工作台带入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=unknown-source');
    await expect(page.getByText('工作台带入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 textbox「保单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('textbox', { name: '保单号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 textbox「保险名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('textbox', { name: '保险名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3037 桌面新建保险其余角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 textbox「保险公司」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('textbox', { name: '保险公司' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 textbox「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('textbox', { name: '备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 spinbutton「保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('spinbutton', { name: '保费' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3038 桌面新建保险额度角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 spinbutton「保额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('spinbutton', { name: '保额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 spinbutton「免赔额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('spinbutton', { name: '免赔额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 button「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3039 桌面新建保险选择角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 combobox「保险类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('combobox', { name: '保险类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 combobox「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('combobox', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3040 桌面新建资产品牌角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 textbox「品牌/厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '品牌/厂商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 textbox「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '规格型号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 textbox「序列号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '序列号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3041 桌面新建资产位置角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 textbox「供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '供应商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 textbox「存放位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '存放位置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 textbox「RFID 标签」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: 'RFID 标签' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3042 桌面新建资产坐标角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 spinbutton「纬度 (-90 ~ 90)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '纬度 (-90 ~ 90)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 spinbutton「经度 (-180 ~ 180)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '经度 (-180 ~ 180)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态「GIS 坐标（用于地图定位，选填）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByText('GIS 坐标（用于地图定位，选填）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3043 桌面新建资产财务角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 spinbutton「原值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '原值（元）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 spinbutton「当前净值（元）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '当前净值（元）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 spinbutton「保修期（月）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '保修期（月）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3044 桌面新建赔偿角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态 textbox「赔偿编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('textbox', { name: '赔偿编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 textbox「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('textbox', { name: '申请人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 textbox「发现人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('textbox', { name: '发现人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3045 桌面新建调拨角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 空态 textbox「调拨编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('textbox', { name: '调拨编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 textbox「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('textbox', { name: '申请人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 空态 textbox「调拨日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('textbox', { name: '调拨日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3046 桌面新建保险日期返回空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/new 空态 button「返回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByRole('button', { name: /返\s*回/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 空态「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/new');
    await expect(page.getByText('结束日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3047 桌面备件库存单位与调拨建议空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/new 空态「安全库存 (件)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('安全库存 (件)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「优先本仓调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('优先本仓调拨').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 空态「2026-06-18」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/new?source=spare-request');
    await expect(page.getByText('2026-06-18').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3048 桌面新建资产日期折旧空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 textbox「购置日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('textbox', { name: '购置日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 spinbutton「折旧率（0~1）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('spinbutton', { name: '折旧率（0~1）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 select「请选择部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.locator('select').filter({ hasText: '请选择部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3049 桌面新建赔偿日期角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 空态 textbox「申请日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('textbox', { name: '申请日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态 textbox「损坏日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('textbox', { name: '损坏日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 空态「损坏类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('损坏类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3050 桌面新增采购单角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单 textbox「采购单号 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('textbox', { name: '采购单号 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 textbox「采购名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('textbox', { name: '采购名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 textbox「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('textbox', { name: '备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3051 桌面新增采购单日期角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单 textbox「采购日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('textbox', { name: '采购日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 textbox「预计到货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('textbox', { name: '预计到货' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3052 桌面新增采购单明细角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单 button「添加行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('button', { name: /添\s*加\s*行/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 placeholder「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByPlaceholder('资产名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3053 桌面新建维保计划角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划 textbox「计划名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('textbox', { name: '计划名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 textbox「默认执行人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('textbox', { name: '默认执行人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 textbox「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('textbox', { name: '备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3054 桌面新建维保计划日期角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划 textbox「开始日期 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('textbox', { name: '开始日期 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 textbox「结束日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('textbox', { name: '结束日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 spinbutton「每月第几天」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('spinbutton', { name: '每月第几天' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3055 桌面新建维保计划费用操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划 spinbutton「预估费用(元)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('spinbutton', { name: '预估费用(元)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 button「确认新建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3056 桌面新增资产模型角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型 textbox「模型名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByRole('textbox', { name: '模型名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 textbox「型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByRole('textbox', { name: '型号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 heading「新增资产模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByRole('heading', { name: '新增资产模型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3057 桌面新增角色角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色 textbox「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('textbox', { name: '角色名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色 textbox「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('textbox', { name: '角色编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色 textbox「描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('textbox', { name: '描述' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3058 桌面新增菜单角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单 textbox「菜单名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('textbox', { name: '菜单名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 textbox「路由路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('textbox', { name: '路由路径' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 textbox「组件路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('textbox', { name: '组件路径' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3059 桌面新增菜单排序保存空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单 spinbutton「排序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('spinbutton', { name: '排序号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 button「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单「菜单类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).click();
    await expect(page.getByText('菜单类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3060 桌面退役预算未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 空态 spinbutton「资产 ID *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('spinbutton', { name: '资产 ID *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态 heading「新增预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('heading', { name: '新增预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 空态 button「返回列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByRole('button', { name: /返\s*回\s*列\s*表/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3061 桌面新建检验编号角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 textbox「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '检验编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「证书编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '证书编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「检查发现」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '检查发现' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3062 桌面新建检验机构角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 textbox「检验机构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '检验机构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「检验人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '检验人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「报告附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '报告附件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3063 桌面新建检验费用类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 spinbutton「检验费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('spinbutton', { name: '检验费用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 combobox「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('combobox', { name: '检验类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 combobox「检验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('combobox', { name: '检验结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3064 桌面新增合同角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同 textbox「合同名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByRole('textbox', { name: '合同名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 textbox「合同编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByRole('textbox', { name: '合同编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 spinbutton「金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByRole('spinbutton', { name: '金额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3065 桌面新增供应商角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商 textbox「供应商名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '供应商名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 textbox「供应商编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '供应商编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 textbox「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '联系人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3066 桌面新增供应商联系角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商 textbox「联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '联系电话' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 textbox「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '邮箱' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 textbox「地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('textbox', { name: '地址' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3067 桌面新增制造商角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 textbox「名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 textbox「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 textbox「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '联系人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3068 桌面新增制造商联系角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 textbox「电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '电话' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 textbox「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '邮箱' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 textbox「官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).click();
    await expect(page.getByRole('textbox', { name: '官网' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3069 桌面新建资产占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new 空态 placeholder「例：Dell」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：Dell').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：PowerEdge R740」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：PowerEdge R740').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new 空态 placeholder「例：SN-2024-001234」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByPlaceholder('例：SN-2024-001234').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3070 桌面新增角色操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色 button「保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('button', { name: /保\s*存/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色「数据权限范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByText('数据权限范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3071 桌面新增资产模型其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('选择分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「选择制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).click();
    await expect(page.getByText('选择制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3072 桌面新增采购合同标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 点新增采购单 heading「新增采购订单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByRole('heading', { name: '新增采购订单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/purchase-orders 点新增采购单 placeholder「规格型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await page.getByRole('button', { name: '新增采购单' }).click();
    await expect(page.getByPlaceholder('规格型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 heading「新增合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).click();
    await expect(page.getByRole('heading', { name: '新增合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3073 桌面新建检验模板操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 combobox「检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('combobox', { name: '检验模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3074 桌面新建检验日期角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/new 空态 textbox「检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '检验日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「下次检验日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '下次检验日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 空态 textbox「证书到期日」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/new');
    await expect(page.getByRole('textbox', { name: '证书到期日' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3075 桌面新增合同占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同 placeholder「请输入合同名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('请输入合同名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 placeholder「如 C001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('如 C001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 placeholder「请输入备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByPlaceholder('请输入备注信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3076 桌面新增合同操作类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「合同类型 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('合同类型 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3077 桌面新增制造商国家角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 textbox「国家」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByRole('textbox', { name: '国家' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 textbox「地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByRole('textbox', { name: '地址' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3078 桌面新增制造商确认占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 heading「新增制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByRole('heading', { name: '新增制造商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入国家」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入国家').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3079 桌面新增许可证名称角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证 textbox「软件名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('textbox', { name: '软件名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 textbox「厂商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('textbox', { name: '厂商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 textbox「软件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('textbox', { name: '软件类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3080 桌面新增许可证版本席位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证 textbox「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('textbox', { name: '版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 textbox「采购单号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('textbox', { name: '采购单号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 spinbutton「总席位数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '总席位数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3081 桌面新增许可证价格操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证 spinbutton「购买价格」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '购买价格' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 heading「新增许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('heading', { name: '新增许可证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3082 桌面新增用户名称角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户 textbox「用户名 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('textbox', { name: '用户名 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户 textbox「真实姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('textbox', { name: '真实姓名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户 textbox「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('textbox', { name: '邮箱' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3083 桌面新增用户联系操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户 textbox「手机号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('textbox', { name: '手机号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户 textbox「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('textbox', { name: '备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3084 桌面新建平面图占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建 placeholder「平面图名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByPlaceholder('平面图名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建 placeholder「楼栋」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByPlaceholder('楼栋').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建 placeholder「楼层」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByPlaceholder('楼层').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3085 桌面新建平面图标题操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建 placeholder「图片URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByPlaceholder('图片URL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建 heading「新建平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByRole('heading', { name: '新建平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/floorplans 点新建 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3086 桌面新增菜单父级可见空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 select「顶级目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.locator('select').filter({ hasText: '顶级目录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 select「显示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.locator('select').filter({ hasText: '显示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3087 桌面新增菜单权限标识空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单切按钮 textbox「权限标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await page.locator('select').filter({ hasText: '目录' }).first().selectOption('F');
    await expect(page.getByRole('textbox', { name: '权限标识' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 select「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.locator('select').filter({ hasText: '正常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单切按钮 placeholder「如：system:user:add」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await page.locator('select').filter({ hasText: '目录' }).first().selectOption('F');
    await expect(page.getByPlaceholder('如：system:user:add').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3088 桌面新增岗位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位 textbox「岗位编码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('textbox', { name: '岗位编码 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 textbox「岗位名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('textbox', { name: '岗位名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 textbox「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('textbox', { name: '备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3089 桌面新增部门角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门 textbox「部门名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('textbox', { name: '部门名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 textbox「部门编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('textbox', { name: '部门编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 textbox「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('textbox', { name: '负责人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3090 桌面新增位置角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 点新增顶级位置 textbox「位置名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('textbox', { name: '位置名称 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 textbox「位置编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('textbox', { name: '位置编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 heading「新增位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('heading', { name: '新增位置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3091 桌面新增供应商操作标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 heading「新增供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByRole('heading', { name: '新增供应商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3092 桌面维保计划条件角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划切每年 spinbutton「第几个月」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).first().click();
    await page.getByText('每月', { exact: true }).first().click();
    await page.getByRole('option', { name: '每年' }).click();
    await expect(page.getByRole('spinbutton', { name: '第几个月' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划切每年 spinbutton「第几天」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).first().click();
    await page.getByText('每月', { exact: true }).first().click();
    await page.getByRole('option', { name: '每年' }).click();
    await expect(page.getByRole('spinbutton', { name: '第几天' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/maintenance/plans 点新建计划切手动 spinbutton「间隔(天)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).first().click();
    await page.getByText('每月', { exact: true }).first().click();
    await page.getByRole('option', { name: '手动' }).click();
    await expect(page.getByRole('spinbutton', { name: '间隔(天)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3093 桌面新增岗位排序操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位 spinbutton「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 placeholder「如：CEO、CTO」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByPlaceholder('如：CEO、CTO').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3094 桌面新增部门联系排序空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门 textbox「联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('textbox', { name: '联系电话' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 textbox「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('textbox', { name: '邮箱' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 spinbutton「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3095 桌面新增位置操作占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/locations 点新增顶级位置 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 placeholder「如 A栋3层」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByPlaceholder('如 A栋3层').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3096 桌面新增故障码角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点 textbox「故障编码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('textbox', { name: '故障编码 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点 textbox「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('textbox', { name: '故障现象' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点 spinbutton「排序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '排序号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3097 桌面添加根分类角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类 textbox「分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByRole('textbox', { name: '分类名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类 textbox「分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByRole('textbox', { name: '分类编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3098 桌面新增检验模板角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 textbox「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('textbox', { name: '模板名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 combobox「检验类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('combobox', { name: '检验类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 spinbutton「检验周期（月）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('spinbutton', { name: '检验周期（月）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3099 桌面新增检验模板检查项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 textbox「适用资产类别ID（逗号分隔）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('textbox', { name: '适用资产类别ID（逗号分隔）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 textbox「检查项（每行一个）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('textbox', { name: '检查项（每行一个）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 combobox「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('combobox', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3100 桌面新增资产模型其余角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型「自定义字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('自定义字段集').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 button「创建模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByRole('button', { name: /创\s*建\s*模\s*型/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3101 桌面新增用户标题密码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「分配角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('分配角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「初始密码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('初始密码 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3102 桌面新增许可证其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「授权类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('授权类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证 placeholder「请输入版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByPlaceholder('请输入版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3103 桌面新增合同货币日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts 点新增合同「货币」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('货币').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「开始日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('开始日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3104 桌面新增制造商占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入制造商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入制造商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3105 桌面新增供应商占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商 placeholder「请输入供应商名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入供应商名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 placeholder「如 V001」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('如 V001').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 placeholder「请输入地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3106 桌面维保每周岗位部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance/plans 点新建计划切每周「星期几」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await page.getByRole('button', { name: '新建计划' }).first().click();
    await page.getByText('每月', { exact: true }).first().click();
    await page.getByRole('option', { name: '每周' }).click();
    await expect(page.getByText('星期几').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 placeholder「数字越小越靠前」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByPlaceholder('数字越小越靠前').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「部门类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3107 桌面新增角色占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/roles 点新增角色 placeholder「请输入角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).first().click();
    await expect(page.getByPlaceholder('请输入角色名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色 placeholder「如：ADMIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).first().click();
    await expect(page.getByPlaceholder('如：ADMIN').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/roles 点新增角色 placeholder「角色描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '新增角色' }).first().click();
    await expect(page.getByPlaceholder('角色描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3108 桌面新增菜单占位标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/menus 点新增菜单 placeholder「菜单显示名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.getByPlaceholder('菜单显示名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 placeholder「如：system/user」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.getByPlaceholder('如：system/user').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/menus 点新增菜单 placeholder「如：system/user/index」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await page.getByRole('button', { name: '新增菜单' }).first().click();
    await expect(page.getByPlaceholder('如：system/user/index').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3109 桌面新增部门操作选择空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门 button「确认新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*认\s*新\s*增/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 select「顶级部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.locator('select').filter({ hasText: '顶级部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3110 桌面平面图用户许可证其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans 点新建 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await page.getByRole('button', { name: '新建' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「所属部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('所属部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「购买日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('购买日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3111 桌面新建风险评估选择空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 spinbutton「资产ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('spinbutton', { name: '资产ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 combobox「可能性（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('combobox', { name: '可能性（1-5）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 combobox「影响程度（1-5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('combobox', { name: '影响程度（1-5）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3112 桌面新建风险评估措施角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 textbox「缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('textbox', { name: '缓解措施' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 textbox「评审日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('textbox', { name: '评审日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 spinbutton「评估人ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('spinbutton', { name: '评估人ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3113 桌面新建风险评估操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「自动计算的风险等级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('自动计算的风险等级').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3114 桌面新增检验模板占位确定空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 placeholder「例如：年度安全检验模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByPlaceholder('例如：年度安全检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 placeholder「例如：1,2,3」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByPlaceholder('例如：1,2,3').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 button「确定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('button', { name: /确\s*定/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3115 桌面新增故障码占位操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点 placeholder「请输入故障编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByPlaceholder('请输入故障编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点 placeholder「请输入描述」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByPlaceholder('请输入描述').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点 button「创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('button', { name: /创\s*建/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3116 桌面添加根分类其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「创建一个新的根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByText('创建一个新的根分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类 placeholder「请输入分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByPlaceholder('请输入分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3117 桌面新增岗位其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts 点新增岗位 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 placeholder「如：董事长、首席执行官」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByPlaceholder('如：董事长、首席执行官').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3118 桌面新增部门领导状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门「部门领导」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('部门领导').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「秘书」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('秘书').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3119 桌面新增用户岗位制造商空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户「分配岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('分配岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('状态', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入联系人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3120 桌面新增制造商其余占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入邮箱').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入官网').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3121 桌面新增供应商其余占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商 placeholder「请输入联系人姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入联系人姓名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 placeholder「请输入联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入联系电话').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/vendors 点新增供应商 placeholder「请输入邮箱地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByPlaceholder('请输入邮箱地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3122 桌面模板取消与到期日期空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「到期日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('到期日期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3123 桌面新建风险评估标题占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments/new 空态「新增风险评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('新增风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态 placeholder「描述风险缓解措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByPlaceholder('描述风险缓解措施').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 空态「LOW（低危）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('LOW（低危）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3124 桌面新增故障码取消标题空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes 点新增根节点 button「取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('button', { name: /取\s*消/ }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点 heading「新增故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByRole('heading', { name: '新增故障代码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/fault-codes 点新增根节点「故障编码 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await page.getByRole('button', { name: '新增根节点' }).first().click();
    await expect(page.getByText('故障编码 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3125 桌面添加根分类编码占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories 点添加根分类 placeholder「请输入分类编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByPlaceholder('请输入分类编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/categories 点添加根分类「分类名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await page.getByRole('button', { name: '添加根分类' }).first().click();
    await expect(page.getByText('分类名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置 placeholder「如 RD-A-3F」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByPlaceholder('如 RD-A-3F').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3126 桌面新增部门备注搜索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts 点新增部门「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门「搜索用户...」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByText('搜索用户...').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 select「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.locator('select').filter({ hasText: '正常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3127 桌面新增模型占位角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-models 点新增模型 placeholder「如：Dell Latitude 5540」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByPlaceholder('如：Dell Latitude 5540').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型 placeholder「产品型号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByPlaceholder('产品型号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「选择字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('选择字段集').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3128 桌面新增用户未分配空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/users 点新增用户 select「未分配」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.locator('select').filter({ hasText: '未分配' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「暂无角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('暂无角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/users 点新增用户「暂无岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await page.getByRole('button', { name: '新增用户' }).first().click();
    await expect(page.getByText('暂无岗位').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3129 桌面弹窗创建说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors 点新增供应商「填写以下信息以创建新供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await page.getByRole('button', { name: '新增供应商' }).first().click();
    await expect(page.getByText('填写以下信息以创建新供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/manufacturers 点新增制造商「填写以下信息以创建新制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByText('填写以下信息以创建新制造商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/contracts 点新增合同「填写以下信息以创建新合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('填写以下信息以创建新合同').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3130 桌面许可证岗位部门说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses 点新增许可证「填写以下信息以创建新许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('填写以下信息以创建新许可证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/posts 点新增岗位 placeholder「岗位描述（可选）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await page.getByRole('button', { name: '新增岗位' }).first().click();
    await expect(page.getByPlaceholder('岗位描述（可选）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/system/depts 点新增部门 placeholder「部门描述、职责说明等」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await page.getByRole('button', { name: '新增部门' }).first().click();
    await expect(page.getByPlaceholder('部门描述、职责说明等').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3131 桌面制造商备注与模型说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商 placeholder「请输入备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByPlaceholder('请输入备注信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/asset-models 点新增模型「定义一个新的资产模型模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await page.getByRole('button', { name: '新增模型' }).first().click();
    await expect(page.getByText('定义一个新的资产模型模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/locations 点新增顶级位置「位置名称 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await page.getByRole('button', { name: '新增顶级位置' }).first().click();
    await expect(page.getByText('位置名称 *').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3132 桌面自定义字段搜索角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields 空态 textbox「搜索自定义字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByRole('textbox', { name: '搜索自定义字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fields 空态「共 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('共 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态 textbox「搜索字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByRole('textbox', { name: '搜索字段集' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3133 桌面自定义字段集标题占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fieldsets 空态 heading「自定义字段集管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByRole('heading', { name: '自定义字段集管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/custom-fieldsets 空态 placeholder「搜索字段集名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByPlaceholder('搜索字段集名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts 点新增合同「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await page.getByRole('button', { name: '新增合同' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3134 桌面弹窗备注与模板名称空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers 点新增制造商「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await page.getByRole('button', { name: '新增制造商' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/licenses 点新增许可证「备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await page.getByRole('button', { name: '新增许可证' }).first().click();
    await expect(page.getByText('备注', { exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板「模板名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByText('模板名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3135 桌面检验模板检查项占位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspection-templates 点新增模板 placeholder「外观检查」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByPlaceholder(/外观检查/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 placeholder「性能测试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByPlaceholder(/性能测试/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

  test('/inspection-templates 点新增模板 placeholder「精度校验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-templates');
    await page.getByRole('button', { name: '新增模板' }).first().click();
    await expect(page.getByPlaceholder(/精度校验/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });
});

test.describe('Q3136 桌面采购其余状态筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders 空态「已收货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '已收货' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '已取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders 空态「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '草稿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3137 桌面编号规则 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules heading「编号规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('heading', { name: '编号规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules heading「编号规则 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('heading', { name: '编号规则 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules heading「详情读取与 no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('heading', { name: '详情读取与 no-persistence preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3138 桌面编号规则 textbox 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules textbox「模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('textbox', { name: '模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules textbox「样例时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('textbox', { name: '样例时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules textbox「样例序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('textbox', { name: '样例序号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3139 桌面编号规则操作与空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules button「读取规则详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('button', { name: '读取规则详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules button「运行无持久化编号预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('button', { name: '运行无持久化编号预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 空态「暂无编号规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('暂无编号规则；空态不代表序列分配、并发唯一或创建链路闭环。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3140 桌面编号规则编码与计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「规则编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('规则编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules select「资产编号规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.locator('select').filter({ hasText: '资产编号规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「显示 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('显示 0 条，只读展示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3141 桌面编号规则 landmark 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules region「编号规则只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('region', { name: '编号规则只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「编号规则详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByLabel('编号规则详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「变量说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/变量说明：/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3142 桌面编号规则边界文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「真实调用 /numbering-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/真实调用 \/numbering-rules/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不会持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/不会持久化、不刷新缓存、不预留或占用序号/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「当前不保证并发唯一」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/当前不保证并发唯一/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3143 桌面编号规则覆盖边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「未接入资产/工单/流程创建链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/未接入资产\/工单\/流程创建链路/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不代表基础资料组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/不代表基础资料组完成/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不代表 44/44」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/不代表 44\/44 或 Workbench V3 全量完成/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3144 桌面编号规则只读徽标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「read-only / tenantScoped=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('read-only / tenantScoped=true / sequenceAllocated=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「元数据未加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/元数据未加载/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不分配或预留序列号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText(/不分配或预留序列号/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3145 桌面自定义字段 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「自定义字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '自定义字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「字段定义 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '字段定义 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「详情与 no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '详情与 no-persistence preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3146 桌面自定义字段检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields placeholder「按字段名或字段标签搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByPlaceholder('按字段名或字段标签搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields textbox「自定义字段关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('textbox', { name: '自定义字段关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields button「查询只读目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('button', { name: '查询只读目录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3147 桌面自定义字段空表预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 空态「暂无自定义字段定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('暂无自定义字段定义。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「请选择一个字段定义查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('请选择一个字段定义查看详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields button「运行无持久化校验预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('button', { name: '运行无持久化校验预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3148 桌面 SLA 配置 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「SLA 配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: 'SLA 配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「策略列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '策略列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「策略详情与提醒阈值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '策略详情与提醒阈值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3149 桌面 SLA 配置空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 空态「暂无 SLA 策略。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('暂无 SLA 策略。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 空态「暂无超时记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('暂无超时记录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3150 桌面 SLA 配置分区 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「操作留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '操作留痕' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「模拟结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '模拟结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '超时记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3151 桌面 SLA 配置操作按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「保存策略草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '保存策略草稿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「运行模拟」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '运行模拟' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「导出脱敏快照」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '导出脱敏快照' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3152 桌面 SLA 配置未选择空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「尚未运行模拟。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('尚未运行模拟。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「未选择策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('未选择策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「暂无审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('暂无审计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3153 桌面字段集 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '字段集' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「字段集 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '字段集 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 空态「暂无字段集。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('暂无字段集。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3154 桌面字段集检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets placeholder「按字段集名称或描述搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByPlaceholder('按字段集名称或描述搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets textbox「字段集关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('textbox', { name: '字段集关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets button「查询只读字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('button', { name: '查询只读字段集' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3155 桌面字段集诊断预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets textbox「分类 ID 只读诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('textbox', { name: '分类 ID 只读诊断' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets button「查询分类字段集」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('button', { name: '查询分类字段集' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「请选择一个字段集查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('请选择一个字段集查看详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3156 桌面字段集预览操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets textbox「预览字段 ID（逗号或换行分隔）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('textbox', { name: '预览字段 ID（逗号或换行分隔）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets button「运行无持久化字段集预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('button', { name: '运行无持久化字段集预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「无当前租户只读匹配」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText(/无当前租户只读匹配/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3157 桌面 SLA 配置启停导出空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「启用策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '启用策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config button「停用策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('button', { name: '停用策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「脱敏导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '脱敏导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3158 桌面邮件模板 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「邮件模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '邮件模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「模板 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '模板 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「详情与 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '详情与 safe preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3159 桌面邮件模板空表检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates placeholder「按编码、名称或正文搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByPlaceholder('按编码、名称或正文搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 空态「暂无邮件模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('暂无邮件模板。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「请选择一个模板查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('请选择一个模板查看详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3160 桌面邮件模板编码查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates textbox「邮件模板关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('textbox', { name: '邮件模板关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates placeholder「templateCode」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByPlaceholder('templateCode').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates button「按编码加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('button', { name: '按编码加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3161 桌面邮件模板预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates textbox「预览变量（key=value，每行一组）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('textbox', { name: '预览变量（key=value，每行一组）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates button「生成无持久化预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('button', { name: '生成无持久化预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates select「全部分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.locator('select').filter({ hasText: '全部分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3162 桌面通知偏好 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences heading「通知偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('heading', { name: '通知偏好' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences heading「偏好 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('heading', { name: '偏好 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 空态「暂无通知偏好。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('暂无通知偏好。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3163 桌面通知偏好分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences heading「分类详情与 no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('heading', { name: '分类详情与 no-persistence preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「通知偏好分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('通知偏好分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences button「读取分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('button', { name: '读取分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3164 桌面通知偏好时间空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences textbox「样例时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('textbox', { name: '样例时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences textbox「免打扰开始」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('textbox', { name: '免打扰开始' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences textbox「免打扰结束」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('textbox', { name: '免打扰结束' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3165 桌面通知偏好预览渠道空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences button「运行无持久化偏好预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('button', { name: '运行无持久化偏好预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences select「请选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.locator('select').filter({ hasText: '请选择分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences select「全部渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.locator('select').filter({ hasText: '全部渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3166 桌面供应商管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management heading「供应商管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('heading', { name: '供应商管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management heading「供应商列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('heading', { name: '供应商列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 空态「暂无供应商数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('暂无供应商数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3167 桌面供应商管理检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management placeholder「按名称、编码、联系人或地址搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByPlaceholder('按名称、编码、联系人或地址搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management textbox「供应商关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('textbox', { name: '供应商关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3168 桌面供应商管理筛选计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('显示 0 / 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3169 桌面位置管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「位置管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '位置管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「位置列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '位置列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 空态「暂无位置数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('暂无位置数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3170 桌面位置管理根位置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 空态「暂无根位置。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('暂无根位置。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「根位置摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '根位置摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management placeholder「按名称、编码、描述或父级搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByPlaceholder('按名称、编码、描述或父级搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3171 桌面位置管理检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management textbox「位置关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('textbox', { name: '位置关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3172 桌面基础参数空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「基础参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: '基础参数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「SYSTEM 基础参数目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: 'SYSTEM 基础参数目录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 空态「暂无 SYSTEM 基础参数，请通过 V3 新增。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('暂无 SYSTEM 基础参数，请通过 V3 新增。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3173 桌面基础参数表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「保存基础参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: '保存基础参数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params textbox「参数键」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('textbox', { name: '参数键' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params textbox「参数名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('textbox', { name: '参数名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3174 桌面基础参数操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params textbox「参数值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('textbox', { name: '参数值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params button「影响预演」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('button', { name: '影响预演' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params button「刷新缓存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('button', { name: '刷新缓存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3175 桌面基础参数默认键空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「公司名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('公司名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「系统名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('系统名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「默认时区」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('默认时区').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3176 桌面 SLA 配置计数文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「策略总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('策略总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「导出仅返回脱敏摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('导出仅返回脱敏摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「暂无 SLA 策略，请先由后端种子」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText(/暂无 SLA 策略，请先由后端种子/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3177 桌面自定义字段边界文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields textbox「预览样例（fieldName=value，每行一组）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('textbox', { name: '预览样例（fieldName=value，每行一组）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「真实调用 /system/custom-fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText(/真实调用 \/system\/custom-fields/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不代表字段集完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText(/不代表字段集完成/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3178 桌面基础参数其余默认键空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「默认币种」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('默认币种').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「维保预警天数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('维保预警天数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「备份频率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('备份频率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3179 桌面基础参数审计字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params textbox「审计原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('textbox', { name: '审计原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params spinbutton「操作人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('spinbutton', { name: '操作人 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「基础参数默认键」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: '基础参数默认键' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3180 桌面用户管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management heading「用户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('heading', { name: '用户管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management heading「用户列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('heading', { name: '用户列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 空态「暂无用户数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('暂无用户数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3181 桌面用户管理检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management placeholder「按账号、姓名、电话搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByPlaceholder('按账号、姓名、电话搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management textbox「用户关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('textbox', { name: '用户关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3182 桌面邮件日志空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「邮件日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '邮件日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「日志 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '日志 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 空态「暂无可展示的邮件日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText(/暂无可展示的邮件日志/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3183 桌面邮件日志检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs placeholder「模板编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByPlaceholder('模板编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs placeholder「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByPlaceholder('业务类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs placeholder「业务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByPlaceholder('业务 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3184 桌面邮件日志详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「脱敏详情与业务关联」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '脱敏详情与业务关联' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「暂无业务关联日志。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('暂无业务关联日志。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「请选择一条日志查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('请选择一条日志查看脱敏详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3185 桌面通知模板空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates heading「通知模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('heading', { name: '通知模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 空态「暂无通知模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('暂无通知模板。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates textbox「通知模板关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('textbox', { name: '通知模板关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3186 桌面通知模板预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates placeholder「templateCode」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByPlaceholder('templateCode').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates button「按编码加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('button', { name: '按编码加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates button「生成无持久化预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('button', { name: '生成无持久化预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3187 桌面部门组织空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「部门组织」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '部门组织' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「部门列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '部门列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 空态「暂无部门组织数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('暂无部门组织数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3188 桌面部门组织根部门空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 空态「暂无根部门。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('暂无根部门。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「根部门摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '根部门摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org placeholder「按名称或编码搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByPlaceholder('按名称或编码搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3189 桌面部门组织检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org textbox「部门关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('textbox', { name: '部门关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3190 桌面通知渠道空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels heading「通知渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('heading', { name: '通知渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels heading「渠道 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('heading', { name: '渠道 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 空态「暂无通知渠道配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText(/暂无通知渠道配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3191 桌面通知渠道详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels heading「渠道详情与 no-send preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('heading', { name: '渠道详情与 no-send preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「通知渠道配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('通知渠道配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels button「读取详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('button', { name: '读取详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3192 桌面通知渠道预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels textbox「配置名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('textbox', { name: '配置名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels textbox「样例相对路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('textbox', { name: '样例相对路径' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels button「运行无持久化渠道预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('button', { name: '运行无持久化渠道预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3193 桌面通知渠道其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels select「请选择配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.locator('select').filter({ hasText: '请选择配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「地址已配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('地址已配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「预览渠道类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('预览渠道类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3194 桌面字段集边界文案空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「详情、字段与 no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '详情、字段与 no-persistence preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「真实调用 /system/custom-fieldsets」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText(/真实调用 \/system\/custom-fieldsets/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「read-only / tenant-scoped」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('read-only / tenant-scoped / no-persistence preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3195 桌面 Webhook 配置空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config heading「Webhook 配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('heading', { name: 'Webhook 配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config heading「新增 Webhook 配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('heading', { name: '新增 Webhook 配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 空态「暂无 Webhook 配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText(/暂无 Webhook 配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3196 桌面 Webhook 配置表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config placeholder「资产 Webhook」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByPlaceholder('资产 Webhook').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config placeholder「ASSET_SYNC」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByPlaceholder('ASSET_SYNC').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config textbox「目标 URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('textbox', { name: '目标 URL' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3197 桌面岗位管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「岗位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: '岗位管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「岗位 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: '岗位 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 空态「暂无可展示的岗位元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText(/暂无可展示的岗位元数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3198 桌面岗位管理检索预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management placeholder「按编码或名称筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByPlaceholder('按编码或名称筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management textbox「postCode」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('textbox', { name: 'postCode' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management textbox「postName」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('textbox', { name: 'postName' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3199 桌面 Webhook 其余表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config textbox「配置名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('textbox', { name: '配置名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config textbox「事件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('textbox', { name: '事件类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config placeholder「https://hooks.example.com/asset/sync」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByPlaceholder('https://hooks.example.com/asset/sync').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3200 桌面 Webhook 签名启用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config select「签名策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.locator('select').filter({ hasText: 'NONE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config placeholder「仅提交，不回显」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByPlaceholder('仅提交，不回显').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config checkbox「启用配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('checkbox', { name: '启用配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3201 桌面 Webhook 操作边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config button「新增配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('button', { name: '新增配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「只加载 /system/webhook-configs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText(/只加载 \/system\/webhook-configs/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText(/config-only/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3202 桌面岗位 preview heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「metadata 详情与 dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: 'metadata 详情与 dry-run preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「请选择一条岗位元数据查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('请选择一条岗位元数据查看详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management button「运行 dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('button', { name: '运行 dry-run preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3203 桌面岗位 preview 字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management textbox「sortOrder」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('textbox', { name: 'sortOrder' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management textbox「remark」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('textbox', { name: 'remark' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3204 桌面岗位检索操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management textbox「岗位关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('textbox', { name: '岗位关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management button「只读刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('button', { name: '只读刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3205 桌面安全策略 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy heading「安全策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('heading', { name: '安全策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy heading「SECURITY 配置态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('heading', { name: 'SECURITY 配置态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy heading「审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('heading', { name: '审计摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3206 桌面安全策略表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy textbox「配置键」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('textbox', { name: '配置键' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy textbox「配置值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('textbox', { name: '配置值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy textbox「审计原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('textbox', { name: '审计原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3207 桌面安全策略操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy button「保存安全策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('button', { name: '保存安全策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy button「影响预演」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('button', { name: '影响预演' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「口令长度下限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('口令长度下限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3208 桌面邮件网关空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「邮件网关配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '邮件网关配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「网关 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '网关 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 空态「暂无可展示的邮件网关元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/暂无可展示的邮件网关元数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3209 桌面邮件网关检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway placeholder「按编码、名称或脱敏元数据筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByPlaceholder('按编码、名称或脱敏元数据筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway textbox「邮件网关关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('textbox', { name: '邮件网关关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway button「只读刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('button', { name: '只读刷新' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3210 桌面邮件网关预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「脱敏详情与 dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '脱敏详情与 dry-run preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway textbox「hostMasked」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('textbox', { name: 'hostMasked' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「请选择一条邮件网关元数据查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('请选择一条邮件网关元数据查看脱敏详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3211 桌面流程通知开关空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch heading「流程通知开关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('heading', { name: '流程通知开关' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch heading「通知开关 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('heading', { name: '通知开关 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 空态「暂无流程通知开关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/暂无流程通知开关/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3212 桌面流程通知开关查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch heading「业务类型查询与 no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('heading', { name: '业务类型查询与 no-persistence preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch select「请选择业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.locator('select').filter({ hasText: '请选择业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch button「读取业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('button', { name: '读取业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3213 桌面流程通知开关预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch combobox「事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('combobox', { name: '事件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch button「运行无持久化通知开关预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('button', { name: '运行无持久化通知开关预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「真实调用 /notification-switches」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/真实调用 \/notification-switches/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3214 桌面接口管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces heading「接口管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByRole('heading', { name: '接口管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 空态「暂无接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText(/暂无接口/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「只加载接口管理数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText(/只加载接口管理数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3215 桌面字段映射空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping heading「字段映射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByRole('heading', { name: '字段映射' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 空态「暂无字段映射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText(/暂无字段映射/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping button「预览转换」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByRole('button', { name: '预览转换' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3216 桌面同步规则空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules heading「同步规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByRole('heading', { name: '同步规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 空态「暂无同步规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/暂无同步规则/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules button「单条日志重试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByRole('button', { name: '单条日志重试' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3217 桌面缓存管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management heading「缓存管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByRole('heading', { name: '缓存管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 空态「暂无可管理缓存命名空间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText(/暂无可管理缓存命名空间/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management button「刷新全部白名单命名空间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByRole('button', { name: '刷新全部白名单命名空间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3218 桌面文件存储空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「文件存储」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '文件存储' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「附件元数据目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '附件元数据目录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 空态「暂无附件元数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('暂无附件元数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3219 桌面文件存储检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage placeholder「按文件名、业务类型或文件类型搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByPlaceholder('按文件名、业务类型或文件类型搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage textbox「附件关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('textbox', { name: '附件关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage select「全部业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.locator('select').filter({ hasText: '全部业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3220 桌面安全策略边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「no-login-chain」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/no-login-chain/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「真实调用 /system-config/security」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/真实调用 \/system-config\/security/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy select「配置类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('combobox', { name: '配置类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3221 桌面岗位边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「真实调用 /system/posts」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText(/真实调用 \/system\/posts/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management region「岗位只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('region', { name: '岗位只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「显示 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText(/显示 0/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3222 桌面邮件网关余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway textbox「senderMasked」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('textbox', { name: 'senderMasked' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway button「运行 dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('button', { name: '运行 dry-run preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway select「全部 TLS 模式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.locator('select').filter({ hasText: '全部 TLS 模式' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3223 桌面审计日志空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '审计日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「只读审计事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '只读审计事件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 空态「暂无审计日志。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('暂无审计日志。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3224 桌面审计日志检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log placeholder「按操作人、说明或资源类型搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByPlaceholder('按操作人、说明或资源类型搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log textbox「审计日志关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('textbox', { name: '审计日志关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「全部操作类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: '全部操作类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3225 桌面审计日志详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「追溯详情（脱敏）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '追溯详情（脱敏）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「请选择一条审计日志查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('请选择一条审计日志查看脱敏详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log button「导出脱敏快照（禁用，不生成文件）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('button', { name: '导出脱敏快照（禁用，不生成文件）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3226 桌面导入导出空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「导入导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '导入导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「任务历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '任务历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 空态「暂无导入导出任务数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('暂无导入导出任务数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3227 桌面导入导出检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export placeholder「按业务对象或操作人搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByPlaceholder('按业务对象或操作人搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export textbox「任务关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('textbox', { name: '任务关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3228 桌面流程定义空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「流程定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '流程定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「流程模板列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '流程模板列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 空态「暂无流程定义模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('暂无流程定义模板。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3229 桌面流程定义检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition placeholder「按业务类型或名称搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByPlaceholder('按业务类型或名称搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition textbox「业务类型关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('textbox', { name: '业务类型关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3230 桌面外部系统空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems heading「外部系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('heading', { name: '外部系统' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems heading「新增外部系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('heading', { name: '新增外部系统' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 空态「暂无外部系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText(/暂无外部系统/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3231 桌面外部系统表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems placeholder「ERP_CORE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByPlaceholder('ERP_CORE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems placeholder「ERP Core」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByPlaceholder('ERP Core').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems placeholder「https://erp.example.com/api」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByPlaceholder('https://erp.example.com/api').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3232 桌面流程邮件空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail heading「流程邮件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('heading', { name: '流程邮件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail heading「流程节点邮件配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('heading', { name: '流程节点邮件配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 空态「暂无流程邮件配置数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('暂无流程邮件配置数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3233 桌面流程邮件检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail placeholder「按业务类型、节点或模板搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByPlaceholder('按业务类型、节点或模板搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail textbox「邮件配置关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('textbox', { name: '邮件配置关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail placeholder「业务类型精确筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByPlaceholder('业务类型精确筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3234 桌面交接管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「交接管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '交接管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「交接任务列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '交接任务列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 空态「暂无交接任务数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('暂无交接任务数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3235 桌面文档中心空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「文档中心」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '文档中心' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「文档列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '文档列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 空态「暂无文档数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('暂无文档数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3236 桌面技术支持空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「技术支持」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '技术支持' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「工单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '工单列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 空态「暂无技术支持工单数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('暂无技术支持工单数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3237 桌面租户管理空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「租户管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '租户管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「租户列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '租户列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 空态「暂无租户数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('暂无租户数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3238 桌面审批规则空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「审批规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '审批规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「规则列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '规则列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 空态「暂无审批规则。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('暂无审批规则。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3239 桌面角色权限空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「角色权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '角色权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「角色-权限绑定目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '角色-权限绑定目录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 空态「暂无角色权限绑定目录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('暂无角色权限绑定目录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3240 桌面数据权限空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「数据权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '数据权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「角色数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '角色数据范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 空态「暂无角色数据范围。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('暂无角色数据范围。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3241 桌面导入导出余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('任务总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3242 桌面流程定义余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「详情摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '详情摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「暂无可展示的流程摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('暂无可展示的流程摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3243 桌面菜单权限空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「菜单权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '菜单权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 空态「暂无菜单权限编码目录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('暂无菜单权限编码目录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions placeholder「按权限域、权限编码或角色编码搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByPlaceholder('按权限域、权限编码或角色编码搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3244 桌面待办字段空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「待办字段配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '待办字段配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「字段列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '字段列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 空态「暂无待办字段配置。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('暂无待办字段配置。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3245 桌面表单存储空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「表单存储」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '表单存储' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「实例列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '实例列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 空态「暂无表单实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('暂无表单实例。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3246 桌面运行监控空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「运行监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '运行监控' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「审批实例列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '审批实例列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 空态「暂无审批实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('暂无审批实例。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3247 桌面运行监控检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor placeholder「按流程类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByPlaceholder('按流程类型筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor textbox「流程类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('textbox', { name: '流程类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3248 桌面审计日志分区空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '操作类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「操作人排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '操作人排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3249 桌面审计日志边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「真实调用 /audit-logs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText(/真实调用 \/audit-logs/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「GET-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText(/GET-only/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3250 桌面外部系统余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems textbox「系统编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('textbox', { name: '系统编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems placeholder「仅提交，不回显」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByPlaceholder('仅提交，不回显').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems textbox「审计原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('textbox', { name: '审计原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3251 桌面审批规则余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「真实调用 /approval-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText(/真实调用 \/approval-rules/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「规则详情与安全摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '规则详情与安全摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3252 桌面角色权限检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions placeholder「按角色名称、编码或权限编码搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByPlaceholder('按角色名称、编码或权限编码搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions textbox「角色权限关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('textbox', { name: '角色权限关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「暂无权限库存。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('暂无权限库存。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3253 桌面交接管理检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover placeholder「按标题、交接人或接收人搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByPlaceholder('按标题、交接人或接收人搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover textbox「交接任务关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('textbox', { name: '交接任务关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3254 桌面文档中心检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center placeholder「按标题、摘要或作者搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByPlaceholder('按标题、摘要或作者搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center textbox「文档关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('textbox', { name: '文档关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center placeholder「分类精确筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByPlaceholder('分类精确筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3255 桌面技术支持检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support placeholder「按标题、摘要或提单人搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByPlaceholder('按标题、摘要或提单人搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support textbox「工单关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('textbox', { name: '工单关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support combobox「工单状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('combobox', { name: '工单状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3256 桌面租户管理检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management placeholder「按租户标识、名称或联系人搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByPlaceholder('按租户标识、名称或联系人搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management textbox「租户关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('textbox', { name: '租户关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3257 桌面数据权限检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions placeholder「按角色名称或编码搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByPlaceholder('按角色名称或编码搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions textbox「角色关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('textbox', { name: '角色关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions select「全部范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.locator('select').filter({ hasText: '全部范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3258 桌面待办字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「真实调用 /todo-fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText(/真实调用 \/todo-fields/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「角色覆盖与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '角色覆盖与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3259 桌面表单存储余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「实例详情与脱敏摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '实例详情与脱敏摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「暂无字段摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('暂无字段摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「暂无附件引用。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('暂无附件引用。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3260 桌面运行监控 SLA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「SLA 节点耗时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: 'SLA 节点耗时' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「暂无节点耗时异常。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('暂无节点耗时异常。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「暂无超时记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('暂无超时记录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3261 桌面流程控制台空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「流程控制台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '流程控制台' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「流程模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '流程模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 空态「暂无符合条件的流程模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('暂无符合条件的流程模板。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3262 桌面流程控制台运行空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「运行实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '运行实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 空态「暂无符合条件的运行实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('暂无符合条件的运行实例。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「SLA 超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: 'SLA 超时记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3263 桌面资产分类空表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '资产分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「分类列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '分类列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 空态「暂无资产分类数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('暂无资产分类数据。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3264 桌面资产分类检索树空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category placeholder「按名称或编码搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByPlaceholder('按名称或编码搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category textbox「分类关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('textbox', { name: '分类关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「暂无分类树节点。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('暂无分类树节点。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3265 桌面流程设计器空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「流程设计器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '流程设计器' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「大画布流程建模工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '大画布流程建模工作台' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「暂无发布/回滚审计版本。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('暂无发布/回滚审计版本。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3266 桌面表单配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「表单配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '表单配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「表单字段布局工作台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '表单字段布局工作台' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「暂无表单定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/暂无表单定义/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3267 桌面表单配置检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config placeholder「搜索表单模板或 key」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByPlaceholder('搜索表单模板或 key').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「表单对象搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '表单对象搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3268 桌面流程设计器余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer combobox「业务流程类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('combobox', { name: '业务流程类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「当前定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '当前定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3269 桌面流程控制台检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center placeholder「按模板、流程类型或实例编号搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByPlaceholder('按模板、流程类型或实例编号搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center textbox「流程关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('textbox', { name: '流程关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center select「全部模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.locator('select').filter({ hasText: '全部模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3270 桌面文件存储余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage select「全部文件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.locator('select').filter({ hasText: '全部文件类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q3271 桌面流程控制台余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「运行健康摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '运行健康摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 空态「暂无 SLA 超时记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('暂无 SLA 超时记录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3272 桌面流程控制台 KPI 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「待处理数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('待处理数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「流程模板总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('流程模板总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「已配置模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('已配置模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3273 桌面流程控制台筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center combobox「模板状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('combobox', { name: '模板状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center combobox「实例状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('combobox', { name: '实例状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3274 桌面流程控制台健康空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「暂无运行实例，继续以只读方式观察」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/暂无运行实例，继续以只读方式观察/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「暂无异常耗时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('暂无异常耗时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「暂无异常轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('暂无异常轨迹').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3275 桌面流程控制台 SLA 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「导出仅返回脱敏摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('导出仅返回脱敏摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 只读联动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/SLA 只读联动/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「运行实例总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('运行实例总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3276 桌面资产分类余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「分类树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '分类树' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3277 桌面流程设计器校验空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「校验与审计证据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '校验与审计证据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「尚未执行图结构校验。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('尚未执行图结构校验。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「版本历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '版本历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3278 桌面流程设计器操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「载入设计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '载入设计' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '保存草稿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「图结构校验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '图结构校验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3279 桌面流程设计器发布空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「发布流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '发布流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「恢复最近版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '恢复最近版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer textbox「草稿名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('textbox', { name: '草稿名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3280 桌面流程设计器草稿空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer textbox「草稿说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('textbox', { name: '草稿说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「拖拽环节节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('拖拽环节节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「连线分支」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('连线分支').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3281 桌面表单配置定义空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「当前定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '当前定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「版本历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '版本历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「暂无发布/停用/回滚审计版本。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('暂无发布/停用/回滚审计版本。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3282 桌面表单配置校验空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「schema 校验与遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: 'schema 校验与遮罩预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「尚未执行 schema 校验。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('尚未执行 schema 校验。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「引用分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '引用分析' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3283 桌面表单配置操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「载入表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '载入表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「新建表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '新建表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '保存草稿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3284 桌面表单配置发布空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「schema 校验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: 'schema 校验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「发布表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '发布表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「停用表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '停用表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3285 桌面表单配置预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「字段表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '字段表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「桌面预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '桌面预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「钉钉 H5 预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '钉钉 H5 预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3286 桌面表单配置属性空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「字段属性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '字段属性' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「草稿名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '草稿名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「草稿说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '草稿说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3287 桌面表单配置字段键空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「字段名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '字段名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「字段 Key」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '字段 Key' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config combobox「字段类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('combobox', { name: '字段类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3288 桌面表单配置默认字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「资产编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('资产编号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「联系方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('联系方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「系统生成或手工录入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('系统生成或手工录入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3289 桌面表单配置字段操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「新增分组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '新增分组' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「新增字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '新增字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「从草稿移除字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '从草稿移除字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3290 桌面表单配置分组输入空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「分组名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '分组名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config placeholder「value:label，每行一个」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByPlaceholder('value:label，每行一个').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config combobox「当前表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('combobox', { name: '当前表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3291 桌面流程控制台表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「实例编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '实例编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3292 桌面流程控制台说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/只读风险提示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「健康提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('健康提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3293 桌面流程设计器属性空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「右侧属性配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('右侧属性配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「状态 / 版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('状态 / 版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点 / 连线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点 / 连线').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3294 桌面表单配置其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config button「恢复最近版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('button', { name: '恢复最近版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「基础信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '基础信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「占位提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '占位提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3295 桌面表单配置遮罩输入空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「默认值 / 遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '默认值 / 遮罩预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「帮助说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '帮助说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「选项配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '选项配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3296 桌面表单配置勾选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config textbox「分组说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('textbox', { name: '分组说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config checkbox「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('checkbox', { name: '必填' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config checkbox「敏感」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('checkbox', { name: '敏感' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3297 桌面表单配置引用空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「引用数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/引用数量/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「真实调用 /references」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/真实调用 \/references/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「权限 fail-closed」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/权限 fail-closed/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3298 桌面资产分类说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「只读展示分类列表与分类树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText(/只读展示分类列表与分类树/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「共 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('共 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3299 桌面流程设计器说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「真实调用 /workflows」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/真实调用 \/workflows/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「发布与版本恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/发布与版本恢复/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「最后更新时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('最后更新时间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3300 桌面流程控制台检索余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center select「全部实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.locator('select').filter({ hasText: '全部实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「流程类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '流程类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「模板配置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('模板配置率').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3301 桌面流程设计器节点面板空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点面板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点面板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「开始节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('开始节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「审批节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('审批节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3302 桌面流程设计器节点类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「办理节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('办理节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「抄送节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('抄送节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「条件分支」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('条件分支').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3303 桌面流程设计器节点余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「结束节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('结束节点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「拖拽到画布创建节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/拖拽到画布创建节点/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点属性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点属性').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3304 桌面流程设计器节点配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「审批人类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('审批人类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer radio「按角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('radio', { name: '按角色' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3305 桌面流程设计器节点操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer radio「指定用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('radio', { name: '指定用户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer button「删除当前节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('button', { name: '删除当前节点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「环节子表单/区段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('环节子表单/区段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3306 桌面文件存储 KPI 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「附件总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('附件总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「总容量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('总容量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('业务类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3307 桌面文件存储筛选余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage combobox「业务类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('combobox', { name: '业务类型筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage combobox「文件类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('combobox', { name: '文件类型筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「文件类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('文件类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3308 桌面文件存储表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「文件名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '文件名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「业务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '业务 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「创建时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '创建时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3309 桌面文件存储分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「第 1 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText(/第 1 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3310 桌面文件存储只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读元数据目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText(/只读元数据目录/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不支持上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText(/不支持上传/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「仅展示附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText(/仅展示附件元数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3311 桌面缓存管理余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「只管理应用内白名单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText(/只管理应用内白名单/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「CacheManager 命名空间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText(/CacheManager 命名空间/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「空缓存不会被报告为普通成功」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText(/空缓存不会被报告为普通成功/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3312 桌面同步规则余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「只读队列摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/只读队列摘要/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「dryRun 默认 true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/dryRun 默认 true/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「队列消费已禁用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/队列消费已禁用/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3313 桌面安全策略提示余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「会话最长时长」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('会话最长时长').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「MFA 配置开关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('MFA 配置开关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy spinbutton「操作人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('spinbutton', { name: '操作人 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3314 桌面安全策略审计余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy heading「保存安全策略配置态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('heading', { name: '保存安全策略配置态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy textbox「审计证据摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('textbox', { name: '审计证据摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不改变会话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/不改变会话/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3315 桌面流程控制台模板表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3316 桌面流程控制台运行表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「业务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '业务 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「当前步骤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '当前步骤' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「更新时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '更新时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3317 桌面流程控制台健康余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「流程类型覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('流程类型覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「数据来源」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('数据来源').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「脱敏导出提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('脱敏导出提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3318 桌面流程设计器节点字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「节点编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('节点编码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「审批角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('审批角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3319 桌面流程设计器节点表单空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「区段名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('区段名称').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「历史摘要字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('历史摘要字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「子表单 HTML」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('子表单 HTML').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3320 桌面流程设计器节点辅助空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「适合表单提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/适合表单提交/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「流程入口与触发条件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('流程入口与触发条件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer placeholder「reason,amount」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByPlaceholder('reason,amount').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3321 桌面表单配置其余勾选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config checkbox「遮罩」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('checkbox', { name: '遮罩' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「桌面/H5 双端布局」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('桌面/H5 双端布局').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「字段表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('字段表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3322 桌面表单配置布局空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「双列布局模拟」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/双列布局模拟/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「单列移动端布局」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/单列移动端布局/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「敏感字段默认遮罩」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/敏感字段默认遮罩/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3323 桌面表单配置状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「章节 / 字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('章节 / 字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「UNCONFIGURED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('UNCONFIGURED').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('必填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3324 桌面资产分类表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category columnheader「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('columnheader', { name: '编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category columnheader「父级 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('columnheader', { name: '父级 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category columnheader「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('columnheader', { name: '排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3325 桌面流程控制台 SLA 列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center columnheader「节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('columnheader', { name: '节点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 节点耗时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 节点耗时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 异常轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 异常轨迹').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3326 桌面流程设计器审批模式空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「审批模式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('审批模式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer select「依次审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.locator('select').filter({ hasText: '依次审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「用于发起页渲染」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/用于发起页渲染/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3327 桌面文件存储列余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「大小」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '大小' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「上传人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '上传人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3328 桌面安全策略配置提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「登录失败限制」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('登录失败限制').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「审计记录开关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('审计记录开关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「configuration-state」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('configuration-state').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3329 桌面安全策略复核空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「当前复核对象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/当前复核对象/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「operatorId」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/operatorId/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「masked before」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/masked before/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3330 桌面同步规则队列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/待处理/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「运行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/运行中/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「批处理队列消费入口不开放」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText(/批处理队列消费入口不开放/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3331 桌面流程控制台页码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「脱敏展示 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText(/脱敏展示 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3332 桌面流程设计器节点描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「指派审批人并定义会签规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('指派审批人并定义会签规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「向角色或用户发送非阻塞通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('向角色或用户发送非阻塞通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「按金额、类别或字段值分流」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('按金额、类别或字段值分流').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3333 桌面表单配置工作台说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「表单对象、字段列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/表单对象、字段列表/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「保存草稿前不影响已发布版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/保存草稿前不影响已发布版本/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「真实调用 /form-definitions」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/真实调用 \/form-definitions/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3334 桌面文件存储页脚空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「显示 0 / 0 条元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText(/显示 0 \/ 0 条元数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「0 B」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('0 B').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage columnheader「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('columnheader', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3335 桌面安全策略当前配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「当前配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/当前配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「未返回或已脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/未返回或已脱敏/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「runtimeEffect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/runtimeEffect/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3336 桌面 Webhook HMAC 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config select「HMAC_SHA256」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.locator('select').filter({ hasText: 'HMAC_SHA256' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config textbox「签名密钥」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('textbox', { name: '签名密钥' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config textbox「共享 Secret」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('textbox', { name: '共享 Secret' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3337 桌面字段映射表达式空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「trim(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('trim(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「upper(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('upper(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「lower(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('lower(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3338 桌面流程通知开关渠道空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch combobox「渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('combobox', { name: '渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch select「启用样例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.locator('select').filter({ hasText: '启用样例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「样例启停状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('样例启停状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3339 桌面流程通知开关样例空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch select「停用样例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.locator('select').filter({ hasText: '停用样例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch select「站内信」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.locator('select').filter({ hasText: '站内信' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「当前业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/当前业务类型/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3340 桌面流程设计器节点帮助空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「复用角色或指定用户配置办理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('复用角色或指定用户配置办理人').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「流程收口与结果归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('流程收口与结果归档').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「支持按岗位」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/支持按岗位/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3341 桌面表单配置遮罩预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「******」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('******').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「选填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('选填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config select「资产流程表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.locator('select').filter({ hasText: '资产流程表单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3342 桌面流程通知开关边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「不执行启停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/不执行启停/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「不发送通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/不发送通知/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「不触发流程运行时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/不触发流程运行时/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3343 桌面字段映射预览说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「预览不写库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText(/预览不写库/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「不外呼」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText(/不外呼/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「转换表达式仅允许」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText(/转换表达式仅允许/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3344 桌面 Webhook 签名策略空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config select「NONE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.locator('select').filter({ hasText: 'NONE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「请通过 V3 创建配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText(/请通过 V3 创建配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「签名策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('签名策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3345 桌面流程设计器节点帮助余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「不计入审批步骤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/不计入审批步骤/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「支持通过/驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/支持通过\/驳回/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「适合结束审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/适合结束审批/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3346 桌面表单配置草稿说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「javascript URL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/javascript URL/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「剥离 HTML」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/剥离 HTML/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「危险 style」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText(/危险 style/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3347 桌面流程设计器节点帮助再余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「固定审批人配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/固定审批人配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「可挂载到后续处理节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/可挂载到后续处理节点/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「满足/不满足双向分支」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText(/满足\/不满足双向分支/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3348 桌面流程通知开关业务类型空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch combobox「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('combobox', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch combobox「样例启停状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('combobox', { name: '样例启停状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「只读，不执行启停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText(/只读，不执行启停/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3349 桌面接口管理余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「配置校验仅验证元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText(/配置校验仅验证元数据/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText(/未触发真实外部调用/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「请通过 V3 接口创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText(/请通过 V3 接口创建/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3350 桌面邮件网关只读边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「no-send」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/no-send/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「no-network」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/no-network/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「no-secret」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/no-secret/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3351 桌面邮件网关 preview 字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「previewAccepted」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/previewAccepted/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/runtimeEffect=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「cacheRefreshed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/cacheRefreshed=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3352 桌面邮件网关连接边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「credentialExposed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/credentialExposed=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「smtpConnect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/smtpConnect=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「javaMailSenderUsed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/javaMailSenderUsed=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3353 桌面邮件网关勾选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway checkbox「authConfigured」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('checkbox', { name: 'authConfigured' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway checkbox「enabled」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('checkbox', { name: 'enabled' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「mailSenderProviderUsed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/mailSenderProviderUsed=false/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3354 桌面邮件网关其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「这不代表邮件子系统完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/这不代表邮件子系统完成/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「readonlyBoundary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/readonlyBoundary/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「acceptedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/acceptedFields/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3355 桌面外部系统表单余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems textbox「系统名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('textbox', { name: '系统名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems combobox「系统类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('combobox', { name: '系统类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems combobox「认证方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('combobox', { name: '认证方式' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3356 桌面外部系统认证余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems select「BEARER_TOKEN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.locator('select').filter({ hasText: 'BEARER_TOKEN' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems select「OAUTH_CLIENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.locator('select').filter({ hasText: 'OAUTH_CLIENT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems textbox「一次性认证材料」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('textbox', { name: '一次性认证材料' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3357 桌面外部系统操作余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems spinbutton「操作人 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('spinbutton', { name: '操作人 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems textbox「基础地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('textbox', { name: '基础地址' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems checkbox「启用外部系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('checkbox', { name: '启用外部系统' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3358 桌面外部系统说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「请通过 V3 创建目录项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText(/请通过 V3 创建目录项/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText(/未触发真实外部调用/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText(/config-only/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3359 桌面交接管理 KPI 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「交接任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('交接任务总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「待处理/进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('待处理/进行中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3360 桌面交接管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover combobox「交接状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('combobox', { name: '交接状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3361 桌面交接管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover select「待交接」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.locator('select').filter({ hasText: '待交接' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover select「交接中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.locator('select').filter({ hasText: '交接中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover select「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.locator('select').filter({ hasText: '已取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3362 桌面交接管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '标题' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「交接人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '交接人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「接收人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '接收人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3363 桌面交接管理表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '工单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3364 桌面交接管理只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText(/只读边界/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('只读提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读展示交接任务摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText(/只读展示交接任务摘要/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3365 桌面文档中心 KPI 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「文档总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('文档总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「已发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('已发布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3366 桌面文档中心筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center combobox「文档状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('combobox', { name: '文档状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center select「草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.locator('select').filter({ hasText: '草稿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center select「已归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.locator('select').filter({ hasText: '已归档' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3367 桌面文档中心表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「作者」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '作者' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「附件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '附件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3368 桌面文档中心只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText(/只读边界/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读展示文档标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText(/只读展示文档标题/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3369 桌面技术支持 KPI 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「工单总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('工单总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「待处理/处理中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('待处理/处理中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「安全边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText(/安全边界/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3370 桌面技术支持优先级空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「全部优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '全部优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「紧急」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '紧急' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support combobox「工单优先级筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('combobox', { name: '工单优先级筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3371 桌面技术支持表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「提单人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '提单人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「诊断包」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '诊断包' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3372 桌面技术支持只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「处理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '处理人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「禁止导出敏感配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText(/禁止导出敏感配置/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「只读展示技术支持工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText(/只读展示技术支持工单/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3373 桌面交接管理分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3374 桌面交接管理其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover select「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.locator('select').filter({ hasText: '已完成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「真实资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText(/真实资产/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3375 桌面文档中心分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center textbox「文档分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('textbox', { name: '文档分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3376 桌面文档中心其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center select「已发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.locator('select').filter({ hasText: '已发布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「更新时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '更新时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3377 桌面技术支持分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3378 桌面技术支持筛选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「已解决」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '已解决' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「已关闭」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '已关闭' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「普通」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '普通' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3379 桌面技术支持其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「只读提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('只读提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3380 桌面租户管理KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「租户总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('租户总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「正常租户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('正常租户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3381 桌面租户管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3382 桌面租户管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management select「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.locator('select').filter({ hasText: '正常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management select「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.locator('select').filter({ hasText: '已停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3383 桌面租户管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「租户标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '租户标识' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「套餐」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '套餐' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3384 桌面租户管理表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「用户上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '用户上限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「资产上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '资产上限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '联系人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3385 桌面租户管理只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3386 桌面租户管理分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3387 桌面数据权限KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「角色总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('角色总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「全部数据（ALL）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('全部数据（ALL）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「已收紧范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('已收紧范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3388 桌面数据权限操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「自定义（CUSTOM）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('自定义（CUSTOM）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「边界提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('边界提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3389 桌面数据权限范围筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions combobox「数据范围筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('combobox', { name: '数据范围筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions select「全部数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.locator('select').filter({ hasText: '全部数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions select「已收紧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.locator('select').filter({ hasText: '已收紧' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3390 桌面数据权限表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions columnheader「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('columnheader', { name: '角色编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions columnheader「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('columnheader', { name: '角色名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions columnheader「数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('columnheader', { name: '数据范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3391 桌面数据权限表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions columnheader「风险说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('columnheader', { name: '风险说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions columnheader「自定义部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('columnheader', { name: '自定义部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3392 桌面数据权限分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3393 桌面角色权限KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「角色数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('角色数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「权限库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('权限库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「角色-权限绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('角色-权限绑定数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3394 桌面角色权限操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「未绑定权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('未绑定权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3395 桌面角色权限状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3396 桌面角色权限只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「权限库存与绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '权限库存与绑定数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「本页不支持分配/编辑/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('本页不支持分配/编辑/删除，不代表菜单权限或数据权限闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「显示 0 / 0 个角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText(/显示 0 \/ 0 个角色/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3397 桌面角色权限表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions columnheader「角色编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('columnheader', { name: '角色编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions columnheader「角色名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('columnheader', { name: '角色名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3398 桌面角色权限表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions columnheader「已绑定权限数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('columnheader', { name: '已绑定权限数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions columnheader「绑定权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('columnheader', { name: '绑定权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3399 桌面菜单权限KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「权限域数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('权限域数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「权限编码库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('权限编码库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「已绑定权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('已绑定权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3400 桌面菜单权限操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「未绑定权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('未绑定权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3401 桌面菜单权限筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions textbox「菜单权限关键词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('textbox', { name: '菜单权限关键词' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3402 桌面菜单权限只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「仅作为菜单权限只读目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('本页不支持菜单分配、编辑或删除，仅作为菜单权限只读目录。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「当前后端未提供 sys_menu」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('当前后端未提供 sys_menu/菜单分配接口，本页仅展示现有权限编码库存与绑定覆盖。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3403 桌面待办字段预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「预览可见字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('预览可见字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「敏感字段脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('敏感字段脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「暂无敏感字段预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('暂无敏感字段预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3404 桌面待办字段角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields combobox「待办字段角色选择」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('combobox', { name: '待办字段角色选择' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields select「APPROVER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.locator('select').filter({ hasText: 'APPROVER' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields select「AUDITOR」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.locator('select').filter({ hasText: 'AUDITOR' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3405 桌面待办字段操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「配置操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '配置操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「保存字段配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '保存字段配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「刷新预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '刷新预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3406 桌面表单存储操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「真实调用 /form-storage」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText(/真实调用 \/form-storage/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「操作留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '操作留痕' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3407 桌面表单存储摘要空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「未选择实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('未选择实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「字段摘要为空」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('字段摘要为空').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件引用为空」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件引用为空').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3408 桌面表单存储按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「创建示例实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '创建示例实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「导出脱敏结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '导出脱敏结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「尚未生成导出快照。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('尚未生成导出快照。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3409 桌面运行监控KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「待处理数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('待处理数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「当前页实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('当前页实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「总实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('总实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3410 桌面运行监控SLA KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「SLA 超时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('SLA 超时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「SLA 预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('SLA 预警').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「高风险轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('高风险轨迹').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3411 桌面运行监控筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor select「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.locator('select').filter({ hasText: '待处理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3412 桌面运行监控表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「流程编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '流程编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「SLA 异常轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: 'SLA 异常轨迹' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「暂无异常轨迹。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('暂无异常轨迹。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3413 桌面流程邮件KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「配置总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('配置总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「已启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('已启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3414 桌面流程邮件操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail combobox「启用状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('combobox', { name: '启用状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3415 桌面流程邮件表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「触发事件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '触发事件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3416 桌面导入导出KPI余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「失败任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('失败任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export combobox「任务类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('combobox', { name: '任务类型筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3417 桌面导入导出筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export combobox「任务状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('combobox', { name: '任务状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「导入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '导入' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「运行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '运行中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3418 桌面导入导出表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「业务对象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '业务对象' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「成功/失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '成功/失败' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3419 桌面审批规则操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「操作留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '操作留痕' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「模拟结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '模拟结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「冲突检测」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '冲突检测' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3420 桌面审批规则按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「创建示例规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '创建示例规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「模拟命中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '模拟命中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「尚未运行模拟。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('尚未运行模拟。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3421 桌面审批规则其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「未选择规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('未选择规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「暂无命中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('暂无命中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「暂无冲突检测结果。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('暂无冲突检测结果。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3422 桌面待办字段按钮余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「保存稳定排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '保存稳定排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「保存角色覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '保存角色覆盖' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields button「恢复默认配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('button', { name: '恢复默认配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3423 桌面待办字段说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「字段配置强制租户隔离」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('字段配置强制租户隔离').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「当前角色」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('当前角色').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields select「USER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.locator('select').filter({ hasText: 'USER' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3424 桌面表单存储标题余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「字段 maskedValue」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '字段 maskedValue' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「附件脱敏引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '附件脱敏引用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「归档留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('归档留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3425 桌面表单存储按钮余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「更新字段摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '更新字段摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「登记附件引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '登记附件引用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「导出脱敏快照」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '导出脱敏快照' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3426 桌面表单存储留痕空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「尚未归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('尚未归档').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「尚未删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('尚未删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「服务层强制租户隔离」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('服务层强制租户隔离').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3427 桌面运行监控表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「流程类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '流程类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「业务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '业务 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「当前步骤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '当前步骤' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3428 桌面运行监控列与状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「申请人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '申请人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「发起时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '发起时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor select「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.locator('select').filter({ hasText: '已完成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3429 桌面运行监控SLA余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor select「已退回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.locator('select').filter({ hasText: '已退回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「SLA 只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('SLA 只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「超时记录与脱敏导出提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '超时记录与脱敏导出提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3430 桌面流程邮件表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '节点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「收件人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '收件人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3431 桌面流程邮件分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3432 桌面流程邮件只读余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail select「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.locator('select').filter({ hasText: '已停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail textbox「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('textbox', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3433 桌面导入导出表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: 'ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「格式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '格式' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3434 桌面导入导出列余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「操作人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '操作人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「错误摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '错误摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3435 桌面导入导出分页空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export button「上一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('button', { name: '上一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export button「下一页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('button', { name: '下一页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3436 桌面导入导出筛选项余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「成功」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '成功' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '已取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3437 桌面审批规则按钮余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「更新优先级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '更新优先级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「启用规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '启用规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「停用规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '停用规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3438 桌面审批规则摘要空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules button「冲突检测」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('button', { name: '冲突检测' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「暂无条件摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('暂无条件摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「尚未启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('尚未启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3439 桌面审批规则边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「表达式只能使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('表达式只能使用自有白名单 parser/evaluator').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「候选处理人摘要已脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('候选处理人摘要已脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3440 桌面审计日志KPI空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「命中日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('命中日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「统计总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('统计总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「操作类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('操作类型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3441 桌面审计日志筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log combobox「操作类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('combobox', { name: '操作类型筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log combobox「资源类型筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('combobox', { name: '资源类型筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「全部资源类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: '全部资源类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3442 桌面审计日志只读空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「只读审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('只读审计日志').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不代表全模块审计覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不代表全模块审计覆盖、不可篡改合规归档或 SIEM/外部审计平台集成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不创建 Blob」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不创建 Blob、不触发下载、不创建后台导出任务。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3443 桌面审计日志列表余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log region「审计日志列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('region', { name: '审计日志列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「before/after/raw 均为脱敏摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('before/after/raw 均为脱敏摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3444 桌面文档中心余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('只读提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「附件安全」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('附件安全').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3445 桌面文档中心表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '标题' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3446 桌面技术支持余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「高」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '高' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support select「低」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.locator('select').filter({ hasText: '低' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('当前过滤结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3447 桌面数据权限只读余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「数据权限为只读 catalog。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('数据权限为只读 catalog。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「可调整角色数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('可调整角色数据范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions select「自定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.locator('select').filter({ hasText: '自定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3448 桌面租户管理说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读展示 /tenants」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText(/只读展示 \/tenants/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「新建、编辑、停用、启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('新建、编辑、停用、启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「V3 只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('V3 只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3449 桌面运行监控导出提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「导出仅返回脱敏摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('导出仅返回脱敏摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「第 n / n 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText(/第 \d+ \/ \d+ 页/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3450 桌面审计日志趋势余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「粒度：daily」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('粒度：daily').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log region「审计趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('region', { name: '审计趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「CREATE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'CREATE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3451 桌面流程定义操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition button「搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('button', { name: '搜索' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3452 桌面流程定义状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition select「已配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.locator('select').filter({ hasText: '已配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition select「未配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.locator('select').filter({ hasText: '未配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition select「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.locator('select').filter({ hasText: '已停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3453 桌面流程定义表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '版本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3454 桌面流程定义表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「可发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '可发起' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「节点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '节点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3455 桌面流程定义说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition columnheader「摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('columnheader', { name: '摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「只读展示 /workflows」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText(/只读展示 \/workflows/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「节点摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('节点摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3456 桌面表单存储删除按钮空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「归档实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '归档实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '删除留痕' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage button「删除附件引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('button', { name: '删除附件引用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3457 桌面审批规则停用余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「尚未停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('尚未停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('已停用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「条件摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('条件摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3458 桌面审计日志选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「UPDATE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'UPDATE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「ASSET」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'ASSET' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「GET-only / tenant-scoped / masked」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('GET-only / tenant-scoped / masked').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3459 桌面文档中心分类空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center textbox「文档分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('textbox', { name: '文档分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3460 桌面导入导出其余筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '失败' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export select「等待中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.locator('select').filter({ hasText: '等待中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3461 桌面流程邮件其余空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「零业务调用风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('零业务调用风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail select「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.locator('select').filter({ hasText: '全部' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3462 桌面技术支持只读余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「只读提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('只读提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「标题」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '标题' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3463 桌面角色权限库存余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「库存 0 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('库存 0 项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「已绑定 0 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('已绑定 0 项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「绑定关系 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('绑定关系 0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3464 桌面菜单权限状态余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「按权限域聚合」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('按权限域聚合').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「菜单权限覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('菜单权限覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3465 桌面审计日志资源选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「WORKFLOW」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'WORKFLOW' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「SYSTEM_CONFIG」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'SYSTEM_CONFIG' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「DELETE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'DELETE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3466 桌面交接管理状态列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「状态记录与未闭环风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('状态记录与未闭环风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「发起、推进、取消交接」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('发起、推进、取消交接').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3467 桌面技术支持状态列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「诊断包必须脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('诊断包必须脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「处理状态与诊断包脱敏标记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('处理状态与诊断包脱敏标记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3468 桌面导入导出状态列空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「脱敏错误摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('脱敏错误摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「执行导入、导出、重试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('执行导入、导出、重试').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3469 桌面表单存储标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「字段摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('字段摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「可更新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('可更新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3470 桌面审计登录选项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log select「LOGIN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.locator('select').filter({ hasText: 'LOGIN' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不采集审计写入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不采集审计写入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「点数：0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('点数：0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3471 桌面文档发布状态空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「发布状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('发布状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「编辑、发布、归档、删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('编辑、发布、归档、删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「删除留痕需后续专项处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('删除留痕需后续专项处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3472 桌面流程邮件发送边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「真实邮件发送未接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('真实邮件发送未接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「测试发送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('测试发送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「只读展示流程节点邮件配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('只读展示流程节点邮件配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3473 桌面流程定义版本说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「版本状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('版本状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「流程模板、版本状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('流程模板、版本状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「/workflows/{businessType}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('/workflows/{businessType}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3474 桌面待办字段可见计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「0/0 可见」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('0/0 可见').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「inherited/default/overridden」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('inherited/default/overridden').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「preview 只读且默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('preview 只读且默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3475 桌面用户管理操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「只读展示 /user-management/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText(/只读展示 \/user-management\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3476 桌面用户管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3477 桌面用户管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「账号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '账号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「姓名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '姓名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '电话' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3478 桌面用户管理表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「邮箱」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '邮箱' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「部门 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '部门 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3479 桌面用户管理计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「后端总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('后端总数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3480 桌面部门组织状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org select「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.locator('select').filter({ hasText: '正常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3481 桌面部门组织状态筛余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText(/只读展示 \/depts\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3482 桌面部门组织表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「父级 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '父级 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3483 桌面部门组织表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「负责人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '负责人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3484 桌面部门组织计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「搜索调用列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('搜索调用列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/tree」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/tree').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3485 桌面位置管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3486 桌面位置管理状态筛余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「只读展示 /locations/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText(/只读展示 \/locations\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「/locations/root」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('/locations/root').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3487 桌面位置管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management columnheader「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('columnheader', { name: '编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management columnheader「父级 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('columnheader', { name: '父级 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3488 桌面位置管理表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management columnheader「排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('columnheader', { name: '排序' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「显示 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText(/显示 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3489 桌面供应商管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management select「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.locator('select').filter({ hasText: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3490 桌面供应商管理表头空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management columnheader「名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('columnheader', { name: '名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management columnheader「编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('columnheader', { name: '编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management columnheader「联系人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('columnheader', { name: '联系人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3491 桌面供应商管理表头余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management columnheader「联系电话」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('columnheader', { name: '联系电话' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management columnheader「状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('columnheader', { name: '状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「只读展示 /vendors/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText(/只读展示 \/vendors\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3492 桌面岗位管理状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management combobox「岗位状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('combobox', { name: '岗位状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「metadata-only post catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('metadata-only post catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「read-only / no-assignment / no-permission-effect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('read-only / no-assignment / no-permission-effect').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3493 桌面岗位管理边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「不分配用户、不改变权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('不分配用户、不改变权限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「仅代表 system-post-management 已接入真组件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('仅代表 system-post-management 已接入真组件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3494 桌面岗位管理说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「preview 仅返回 previewAccepted」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('preview 仅返回 previewAccepted').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management complementary「岗位 metadata 详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('complementary', { name: '岗位 metadata 详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「不代表组织权限组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('不代表组织权限组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3495 桌面岗位管理计数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「显示 0 / 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText(/显示 0 \/ 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management select「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.locator('select').filter({ hasText: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「岗位权限 runtime」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('岗位权限 runtime').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3496 桌面基础参数操作余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params button「重新加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('button', { name: '重新加载' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params button「保存基础参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('button', { name: '保存基础参数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params combobox「参数类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('combobox', { name: '参数类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3497 桌面基础参数类型筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params select「STRING」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.locator('select').filter({ hasText: 'STRING' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params select「NUMBER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.locator('select').filter({ hasText: 'NUMBER' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params select「BOOLEAN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.locator('select').filter({ hasText: 'BOOLEAN' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3498 桌面基础参数类型筛余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params select「SELECT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.locator('select').filter({ hasText: 'SELECT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params select「JSON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.locator('select').filter({ hasText: 'JSON' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「真实调用 /system-config/system」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText(/真实调用 \/system-config\/system/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3499 桌面基础参数边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「仅覆盖 SYSTEM 分组基础参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('仅覆盖 SYSTEM 分组基础参数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「仍非 44 项全量覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('仍非 44 项全量覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「不代表 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('不代表 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3500 桌面基础参数预演说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「预演不会持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('预演不会持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「必须显示 DEGRADED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('必须显示 DEGRADED').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「本页展示基础参数目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('本页展示基础参数目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3501 桌面基础参数默认键编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「companyName」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('companyName').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「timezone」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('timezone').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「defaultCurrency」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('defaultCurrency').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3502 桌面基础参数默认键余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「maintenanceWarningDays」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('maintenanceWarningDays').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「backupFrequency」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('backupFrequency').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「/system/configs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('/system/configs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3503 桌面邮件模板筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates combobox「邮件模板分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('combobox', { name: '邮件模板分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates combobox「邮件模板内容类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('combobox', { name: '邮件模板内容类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates select「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.locator('select').filter({ hasText: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3504 桌面邮件模板操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates region「邮件模板列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('region', { name: '邮件模板列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates complementary「邮件模板详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('complementary', { name: '邮件模板详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3505 桌面邮件模板计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates textbox「按编码查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('textbox', { name: '按编码查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「显示 0 / 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText(/显示 0 \/ 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「catalog / tenant-scoped / safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('catalog / tenant-scoped / safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3506 桌面邮件模板边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「仅邮件模板 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('仅邮件模板 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不发送邮件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不发送邮件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「真实调用 /mail-templates/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText(/真实调用 \/mail-templates\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3507 桌面邮件模板预览说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「preview 只返回 HTML escaped」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('preview 只返回 HTML escaped').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不代表邮件子系统完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不代表邮件子系统完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不代表消息与通知全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不代表消息与通知全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3508 桌面邮件模板元数据空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不配置 SMTP/邮件网关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不配置 SMTP/邮件网关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「敏感变量名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('敏感变量名').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3509 桌面邮件日志状态筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs combobox「邮件日志状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('combobox', { name: '邮件日志状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs select「全部状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.locator('select').filter({ hasText: '全部状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3510 桌面邮件日志检索空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs textbox「邮件日志模板编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('textbox', { name: '邮件日志模板编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs textbox「邮件日志业务类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('textbox', { name: '邮件日志业务类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs textbox「邮件日志业务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('textbox', { name: '邮件日志业务 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3511 桌面邮件日志列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs region「邮件日志列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('region', { name: '邮件日志列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs complementary「邮件日志脱敏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('complementary', { name: '邮件日志脱敏详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「显示 0 / 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText(/显示 0 \/ 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3512 桌面邮件日志边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「read-only / redacted / tenantScoped」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('read-only / redacted / tenantScoped').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「真实调用 /mail-logs/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText(/真实调用 \/mail-logs\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「redacted=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('redacted=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3513 桌面邮件日志脱敏说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「收件人、抄送、密送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('收件人、抄送、密送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「不保证日志采集链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('不保证日志采集链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「这不代表日志采集链路已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('这不代表日志采集链路已完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3514 桌面邮件日志业务查询空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「biz lookup 结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('biz lookup 结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「仅调用 /mail-logs/biz」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText(/仅调用 \/mail-logs\/biz/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「no retry/export/send」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('no retry/export/send').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3515 桌面邮件日志覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「仍非 44/44」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('仍非 44/44').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「消息与通知组未全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('消息与通知组未全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「不进行重试、重发、导出或发送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('不进行重试、重发、导出或发送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3516 桌面通知模板筛选空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates combobox「通知模板分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('combobox', { name: '通知模板分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates combobox「通知模板渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('combobox', { name: '通知模板渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates select「全部分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.locator('select').filter({ hasText: '全部分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3517 桌面通知模板操作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates select「全部渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.locator('select').filter({ hasText: '全部渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates button「查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('button', { name: '查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates region「通知模板列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('region', { name: '通知模板列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3518 桌面通知模板详情空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates complementary「通知模板详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('complementary', { name: '通知模板详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates textbox「按编码查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('textbox', { name: '按编码查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「显示 0 / 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText(/显示 0 \/ 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3519 桌面通知模板边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「仅通知模板 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('仅通知模板 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不发送通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不发送通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「真实调用 /notification-templates/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText(/真实调用 \/notification-templates\/list/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3520 桌面通知模板非目标空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不配置渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不配置渠道').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不修改偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不修改偏好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不控制流程通知开关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不控制流程通知开关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3521 桌面通知模板覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不代表消息与通知全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不代表消息与通知全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不保存偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不保存偏好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3522 桌面通知模板 heading 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates heading「模板 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('heading', { name: '模板 catalog' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates heading「详情与 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('heading', { name: '详情与 safe preview' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates placeholder「按编码、名称或正文搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByPlaceholder('按编码、名称或正文搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3523 桌面通知模板预览余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates textbox「预览变量（key=value，每行一组）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('textbox', { name: '预览变量（key=value，每行一组）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不接入邮件网关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不接入邮件网关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「catalog / tenant-scoped / safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('catalog / tenant-scoped / safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3524 桌面通知偏好列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences region「通知偏好只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('region', { name: '通知偏好只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences complementary「通知偏好详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('complementary', { name: '通知偏好详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「显示 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText(/显示 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3525 桌面通知偏好边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「read-only / tenant-scoped / runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('read-only / tenant-scoped / runtimeEffect=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读通知偏好目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读通知偏好目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「不保存偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('不保存偏好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3526 桌面通知偏好预览说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「真实调用 /notification-preferences」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText(/真实调用 \/notification-preferences/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「preview 仅返回 wouldReceive」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('preview 仅返回 wouldReceive').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「仅代表 system-notification-preferences」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('仅代表 system-notification-preferences').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3527 桌面通知偏好覆盖余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「消息与通知组未全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('消息与通知组未全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「仍非 44/44」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('仍非 44/44').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences combobox「预览渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('combobox', { name: '预览渠道' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3528 桌面通知偏好元数据空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「保留分类词」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('保留分类词').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「不改变真实通知发送决策」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('不改变真实通知发送决策').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3529 桌面通知偏好详情余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「当前分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('当前分类').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读，无运行时改动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读，无运行时改动').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('runtimeEffect=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3530 桌面通知渠道列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels region「通知渠道只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('region', { name: '通知渠道只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels complementary「通知渠道详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('complementary', { name: '通知渠道详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「显示 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText(/显示 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3531 桌面通知渠道边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「read-only / tenantScoped=true / noSend=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('read-only / tenantScoped=true / noSend=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「只读通知渠道目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('只读通知渠道目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不保存渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不保存渠道').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3532 桌面通知渠道接口空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「真实调用 /system/channel-configs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText(/真实调用 \/system\/channel-configs/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不发送测试消息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不发送测试消息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不调用外部 webhook」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不调用外部 webhook').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3533 桌面通知渠道覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「仅代表 system-notification-channels」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('仅代表 system-notification-channels').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「空态不代表真实发送链闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('空态不代表真实发送链闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「preview 仅返回 configured」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('preview 仅返回 configured').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3534 桌面通知渠道元数据空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不创建渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不创建渠道').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不会显示或保存原始地址」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不会显示或保存原始地址').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3535 桌面通知渠道表单余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「签名已配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('签名已配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「启用状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('启用状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「当前渠道」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('当前渠道').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3536 桌面自定义字段列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields region「自定义字段定义列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('region', { name: '自定义字段定义列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields complementary「字段详情与校验预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('complementary', { name: '字段详情与校验预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「分页 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText(/分页 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3537 桌面自定义字段边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「definition catalog / tenant-scoped / no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('definition catalog / tenant-scoped / no-persistence preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「只读自定义字段定义目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('只读自定义字段定义目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「preview 仅按当前租户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('preview 仅按当前租户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3538 桌面自定义字段覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不代表资产字段值读写链路完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不代表资产字段值读写链路完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不代表基础资料组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不代表基础资料组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「全集 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText(/全集 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3539 桌面自定义字段元数据空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不提供字段定义 CRUD runtime」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不提供字段定义 CRUD runtime').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「加密字段只展示安全标记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('加密字段只展示安全标记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3540 桌面字段集列表空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets region「字段集只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('region', { name: '字段集只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets complementary「字段集详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('complementary', { name: '字段集详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「分页 0 / 0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText(/分页 0 \/ 0 条/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3541 桌面字段集计数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「全集 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText(/全集 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「read-only / tenant-scoped / no-persistence preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('read-only / tenant-scoped / no-persistence preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「只读字段集目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('只读字段集目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3542 桌面字段集边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不提供字段集新增」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不提供字段集新增').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「preview 仅校验当前租户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('preview 仅校验当前租户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「仅代表 system-custom-field-sets」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('仅代表 system-custom-field-sets').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3543 桌面字段集覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「基础资料组未全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('基础资料组未全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「未覆盖全部 44 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('未覆盖全部 44 项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3544 桌面字段集诊断空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「分类诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('分类诊断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不影响资产模型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不影响资产模型').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不保存字段分配或分类绑定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不保存字段分配或分类绑定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3545 桌面编号规则详情余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules complementary「编号规则详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('complementary', { name: '编号规则详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules combobox「规则编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('combobox', { name: '规则编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「sequenceAllocated=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('sequenceAllocated=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3546 桌面编号规则预览边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「预览不会持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('预览不会持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不刷新缓存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('不刷新缓存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不预留或占用序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('不预留或占用序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3547 桌面编号规则覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「仅代表 system-numbering-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('仅代表 system-numbering-rules').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「preview 仅返回 previewValue」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('preview 仅返回 previewValue').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「空态不代表序列分配」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('空态不代表序列分配').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3548 桌面编号规则当前规则空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「当前规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('当前规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「不执行序号分配或缓存刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('不执行序号分配或缓存刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「read-only / tenantScoped=true / sequenceAllocated=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('read-only / tenantScoped=true / sequenceAllocated=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3549 桌面 SLA KPI 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「启用策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('启用策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「运行风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('运行风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「真实调用 /sla-config」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText(/真实调用 \/sla-config/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3550 桌面 SLA 安全边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「SLA 运行态安全边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('SLA 运行态安全边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「不发送真实通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('不发送真实通知').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「通知目标已脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('通知目标已脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3551 桌面 SLA 详情余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「响应/解决」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('响应/解决').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「提醒/升级阈值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('提醒/升级阈值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「变量值仅展示遮罩摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('变量值仅展示遮罩摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3552 桌面 SLA 导出余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「不返回联系方式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('不返回联系方式').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「命中策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('命中策略').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「0 条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('0 条').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3553 桌面邮件网关筛选余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway combobox「TLS 模式」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('combobox', { name: 'TLS 模式' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway region「邮件网关只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('region', { name: '邮件网关只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway complementary「邮件网关脱敏详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('complementary', { name: '邮件网关脱敏详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3554 桌面邮件网关计数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「显示 0 / 0 条，只读展示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/显示 0 \/ 0 条，只读展示/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「metadata-only mail gateway catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('metadata-only mail gateway catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「真实调用 /system/mail-gateways」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText(/真实调用 \/system\/mail-gateways/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3555 桌面邮件网关边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「仅代表 system-mail-gateway 已接入真组件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('仅代表 system-mail-gateway 已接入真组件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「元数据边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('元数据边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway textbox「port」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('textbox', { name: 'port' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3556 桌面邮件网关表单余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway combobox「tlsMode」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('combobox', { name: 'tlsMode' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway textbox「priority」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('textbox', { name: 'priority' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway select「STARTTLS 元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.locator('select').filter({ hasText: 'STARTTLS 元数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3557 桌面安全策略 landmark 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy region「安全策略配置态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('region', { name: '安全策略配置态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy region「安全策略审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('region', { name: '安全策略审计摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy select「STRING」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.locator('select').filter({ hasText: 'STRING' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3558 桌面安全策略类型筛空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy select「NUMBER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.locator('select').filter({ hasText: 'NUMBER' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy select「BOOLEAN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.locator('select').filter({ hasText: 'BOOLEAN' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「no-direct-effect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText(/no-direct-effect/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3559 桌面安全策略覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不直接生效」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('不直接生效').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「仍非 44 项全量覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('仍非 44 项全量覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「也不是 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('也不是 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3560 桌面安全策略默认键编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「credential.minLength」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('credential.minLength').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「session.maxMinutes」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('session.maxMinutes').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「signIn.maxAttempts」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('signIn.maxAttempts').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3561 桌面安全策略开关键编码空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「mfa.enabled」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('mfa.enabled').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「audit.enabled」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('audit.enabled').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「provided/not-provided」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('provided/not-provided').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3562 桌面安全策略审计要求空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「reason 或 auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('reason 或 auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「保存必须携带」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('保存必须携带').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「服务端只记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('服务端只记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3563 桌面安全策略链路边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不接登录链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('不接登录链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「MFA 或移动端」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('MFA 或移动端').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「/system-config/security/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('/system-config/security/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3564 桌面安全策略预览边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「本页只展示配置态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('本页只展示配置态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「移动端运行状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('移动端运行状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「runtimeEffect 必须为 false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('runtimeEffect 必须为 false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});
test.describe('Q3565 桌面文件存储边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不访问文件系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不访问文件系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不代表文件生命周期闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不代表文件生命周期闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3566 桌面文件存储覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不包含导入/导出或执行动作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不包含导入/导出或执行动作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读展示 /system/file-storage/attachments/catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('只读展示 /system/file-storage/attachments/catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「统计摘要与筛选结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('统计摘要与筛选结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3567 桌面文件存储计数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「共 0 页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('共 0 页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3568 桌面基础参数默认键余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「systemName」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('systemName').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「影响预演与缓存刷新结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('影响预演与缓存刷新结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「缓存刷新无真实缓存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('缓存刷新无真实缓存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3569 桌面邮件网关覆盖余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「不代表消息与通知组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('不代表消息与通知组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「rejectedInputs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('rejectedInputs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「noPersistence=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('noPersistence=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3570 桌面邮件网关预览字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「邮件子系统或 Workbench V3」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('邮件子系统或 Workbench V3').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「configured、acceptedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('configured、acceptedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3571 桌面邮件网关边界旗标余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「noSend=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('noSend=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「noNetwork=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('noNetwork=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「tenantScoped」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('tenantScoped').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3572 桌面资产分类查询余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「搜索仅调用 /categories/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('搜索仅调用 /categories/list').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「查询能力」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('查询能力').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「/categories/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('/categories/list').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});
test.describe('Q3573 桌面审批规则表达式边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「动态类加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('动态类加载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3574 桌面审批规则审计字段空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「创建、更新、启用、停用、模拟与冲突检测」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('创建、更新、启用、停用、模拟与冲突检测').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「启用审计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('启用审计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「停用审计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('停用审计').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3575 桌面审批规则解释器边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「命中规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('命中规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「OGNL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('OGNL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3576 桌面流程平台侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「规则白名单、模拟命中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('规则白名单、模拟命中').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「字段可见性、稳定排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('字段可见性、稳定排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「schema 安全过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('schema 安全过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3577 桌面流程平台侧栏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「草稿保存、图结构校验、发布与版本恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('草稿保存、图结构校验、发布与版本恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「归档删除留痕与导出脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('归档删除留痕与导出脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「只读健康摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('只读健康摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3578 桌面流程平台侧栏覆盖空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「默认恢复与预览脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('默认恢复与预览脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「提醒阈值、超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('提醒阈值、超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「流程模板目录、业务类型搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('流程模板目录、业务类型搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3579 桌面系统参数侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「脱敏预览与 no-direct-effect 审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('脱敏预览与 no-direct-effect 审计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「缓存刷新降级结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('缓存刷新降级结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「趋势分布与导出限制提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('趋势分布与导出限制提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3580 桌面系统参数侧栏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「导入导出行数上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('导入导出行数上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「可观测空态与白名单刷新」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('可观测空态与白名单刷新').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「分类版本状态与附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('分类版本状态与附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3581 桌面组织权限侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「权限库存与风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('权限库存与风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「按域聚合的菜单权限只读覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('按域聚合的菜单权限只读覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「套餐状态与联系人摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('套餐状态与联系人摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3582 桌面集成配置侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「转换白名单与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('转换白名单与预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「敏感字段脱敏与租户隔离」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('敏感字段脱敏与租户隔离').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「认证掩码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('认证掩码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3583 桌面消息通知侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「变量白名单与无持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('变量白名单与无持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「邮件日志只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('邮件日志只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「免打扰诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('免打扰诊断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3584 桌面集成配置侧栏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「源字段、目标字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('源字段、目标字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「dry-run、单条日志重试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('dry-run、单条日志重试').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「接口目录、方法、路径摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('接口目录、方法、路径摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});
test.describe('Q3585 桌面流程平台侧栏再扫空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「实例字段值、附件引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('实例字段值、附件引用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「策略计算、提醒阈值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('策略计算、提醒阈值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「审批实例列表、待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('审批实例列表、待处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3586 桌面流程平台侧栏节点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「流程模板、运行实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('流程模板、运行实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「业务类型搜索与节点摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('业务类型搜索与节点摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「发布停用与版本恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('发布停用与版本恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3587 桌面组织权限侧栏再扫空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「根部门摘要与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('根部门摘要与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「岗位 metadata-only catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('岗位 metadata-only catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「CUSTOM 部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('CUSTOM 部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3588 桌面组织权限侧栏 catalog 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「角色数据范围 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('角色数据范围 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「交接任务摘要只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('交接任务摘要只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「租户主数据只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('租户主数据只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3589 桌面基础资料侧栏描述空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「分类树、关键词查询与只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('分类树、关键词查询与只读列表').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「供应商只读列表、关键词搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('供应商只读列表、关键词搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「根位置摘要与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('根位置摘要与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3590 桌面基础资料侧栏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「无序号预留预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('无序号预留预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「字段定义只读目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('字段定义只读目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「字段明细、分类诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('字段明细、分类诊断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3591 桌面消息通知侧栏再扫空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「零业务调用风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('零业务调用风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「流程节点邮件配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('流程节点邮件配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「通知渠道只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('通知渠道只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3592 桌面消息通知侧栏 catalog 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「偏好只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('偏好只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「通知开关只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('通知开关只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「metadata-only catalog、脱敏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('metadata-only catalog、脱敏详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3593 桌面系统参数侧栏再扫空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「附件元数据、业务类型筛选与只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('附件元数据、业务类型筛选与只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「任务历史只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('任务历史只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「技术支持工单只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('技术支持工单只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3594 桌面系统参数侧栏审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「GET-only 审计日志、脱敏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('GET-only 审计日志、脱敏详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「文档目录只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('文档目录只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「脱敏错误摘要与导入导出行数上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('脱敏错误摘要与导入导出行数上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3595 桌面集成配置侧栏再扫空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「目录状态、认证掩码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('目录状态、认证掩码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「config-only 校验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('config-only 校验').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「只读队列摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('只读队列摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3596 桌面系统参数侧栏配置空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「应用内命名空间、可观测空态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('应用内命名空间、可观测空态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「SECURITY 配置态、脱敏预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('SECURITY 配置态、脱敏预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「SYSTEM 参数目录、影响预演」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('SYSTEM 参数目录、影响预演').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});
test.describe('Q3597 桌面消息通知侧栏预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「no-send/no-network preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('no-send/no-network preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「no-send/no-runtime-effect 预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('no-send/no-runtime-effect 预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「业务查询与 meta 边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('业务查询与 meta 边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3598 桌面消息通知侧栏偏好空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「分类详情、免打扰诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('分类详情、免打扰诊断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「无持久化 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('无持久化 safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「模板 catalog、变量白名单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('模板 catalog、变量白名单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3599 桌面组织权限侧栏岗位空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「no-persistence/no-assignment」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('no-persistence/no-assignment').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「no-permission-effect preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('no-permission-effect preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「用户只读列表、关键词搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('用户只读列表、关键词搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3600 桌面基础资料侧栏预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「字段定义只读目录与无持久化校验预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('字段定义只读目录与无持久化校验预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「字段集只读目录、字段明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('字段集只读目录、字段明细').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「编号规则只读 catalog、详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('编号规则只读 catalog、详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3601 桌面系统参数页头覆盖空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-tech-support 已接入技术支持工单只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-tech-support 已接入技术支持工单只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「权限专项与真实写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('权限专项与真实写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「专属页面与专属 API」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('专属页面与专属 API').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3602 桌面工作台页脚模块空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-flow-designer」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-flow-designer').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-form-config」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-form-config').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-todo-fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-todo-fields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3603 桌面工作台页脚余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-field-mapping」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-field-mapping').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-sync-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-sync-rules').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-webhook-config」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-webhook-config').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3604 桌面工作台页脚组织空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-role-permissions」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-role-permissions').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-menu-permissions」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-menu-permissions').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-data-permissions」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-data-permissions').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3605 桌面工作台页脚流程空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-settings-command-center」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-settings-command-center').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-flow-definition」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-flow-definition').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-runtime-monitor」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-runtime-monitor').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3606 桌面工作台页脚消息空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-notification-templates」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-notification-templates').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-notification-channels」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-notification-channels').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-notification-preferences」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-notification-preferences').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3607 桌面工作台页脚资料空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-custom-fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-custom-fields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-custom-field-sets」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-custom-field-sets').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-numbering-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-numbering-rules').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3608 桌面工作台页脚参数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-security-policy」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-security-policy').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-file-storage」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-file-storage').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-cache-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-cache-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});
test.describe('Q3609 桌面工作台页脚集成空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-external-systems」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-external-systems').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-interfaces」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-interfaces').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「system-base-params」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('system-base-params').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3610 桌面工作台页脚审计空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-audit-log」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-audit-log').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-import-export」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-import-export').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「system-doc-center」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('system-doc-center').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3611 桌面工作台页脚邮件空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-mail-templates」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-mail-templates').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-mail-logs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-mail-logs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-workflow-mail」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-workflow-mail').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3612 桌面工作台页脚通知开关空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-workflow-notification-switch」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-workflow-notification-switch').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-mail-gateway」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-mail-gateway').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「system-tech-support」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('system-tech-support').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3613 桌面工作台页脚基础资料空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-vendor-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-vendor-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-location-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-location-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「system-asset-category」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('system-asset-category').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3614 桌面工作台页脚组织余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-dept-org」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-dept-org').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-post-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-post-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-tenant-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-tenant-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3615 桌面工作台页脚交接空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-handover」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-handover').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-user-management」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-user-management').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「system-form-storage」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('system-form-storage').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3616 桌面工作台页脚流程余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-approval-rules」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-approval-rules').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-sla-config」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-sla-config').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「system-form-storage」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('system-form-storage').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3617 桌面工作台六域计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「9/9 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('9/9 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「6/6 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('6/6 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「5/5 已接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('5/5 已接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3618 桌面子项导航与同步说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules complementary「Workbench V3 子项导航」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByRole('complementary', { name: 'Workbench V3 子项导航' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「只保留 dry-run」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('只保留 dry-run').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「单条日志重试、只读队列摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('单条日志重试、只读队列摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3619 桌面表单存储闭环提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「可通过创建示例实例验证 /form-storage 最小闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('可通过创建示例实例验证 /form-storage 最小闭环。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「不读取 uploads」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('不读取 uploads').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「不提供二进制上传或下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('不提供二进制上传或下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3620 桌面待办字段闭环提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「可保存示例配置验证 /todo-fields 闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('可保存示例配置验证 /todo-fields 闭环。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「0 个」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('0 个').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「列表、保存、排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('列表、保存、排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3621 桌面审批规则闭环提示空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「可创建示例规则验证 /approval-rules 闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('可创建示例规则验证 /approval-rules 闭环。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「短数组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('短数组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「脚本引擎」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('脚本引擎').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3622 桌面流程通知开关 landmark 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch region「流程通知开关只读列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('region', { name: '流程通知开关只读列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch complementary「流程通知开关详情与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('complementary', { name: '流程通知开关详情与预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3623 桌面审计日志 landmark 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log complementary「审计日志详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('complementary', { name: '审计日志详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log region「操作类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('region', { name: '操作类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log region「操作人排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('region', { name: '操作人排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3624 桌面通知偏好 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「quietWindowMatched」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('quietWindowMatched').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「missingPreferences」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('missingPreferences').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3625 桌面编号规则 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「missingVariables」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('missingVariables').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「noSequenceReserved」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('noSequenceReserved').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3626 桌面岗位管理 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「duplicateRisk」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('duplicateRisk').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「referenceImpact」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('referenceImpact').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3627 桌面通知模板 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「preview 只返回 HTML escaped」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('preview 只返回 HTML escaped').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「nonPersistent=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('nonPersistent=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3628 桌面通知渠道 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「webhookUrlMasked」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('webhookUrlMasked').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「signatureConfigured」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('signatureConfigured').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3629 桌面邮件模板 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「rejectedVariables」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('rejectedVariables').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「usedVariables」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('usedVariables').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3630 桌面邮件日志 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「供应商标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('供应商标识').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「请求标识」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('请求标识').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3631 桌面自定义字段 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「usedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('usedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不回显样例原文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不回显样例原文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3632 桌面字段集 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「wouldBindCategory」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('wouldBindCategory').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「missingFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('missingFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3633 桌面角色权限 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「只读展示 /system/role-permissions/catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「返回的角色-权限绑定目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('返回的角色-权限绑定目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3634 桌面菜单权限 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「只读展示 /system/role-permissions/catalog 的权限编码库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog 的权限编码库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「按权限域聚合为菜单权限覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('按权限域聚合为菜单权限覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3635 桌面审计日志 note 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log note landmark', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('note').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不提供 POST/PUT/PATCH/DELETE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不提供 POST/PUT/PATCH/DELETE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「GET 族端点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('GET 族端点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3636 桌面编号规则预览旗标余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「cacheRefreshed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('cacheRefreshed=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「persistent=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('persistent=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「rejectedVariables」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('rejectedVariables').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3637 桌面通知渠道预览字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「webhookUrlConfigured」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('webhookUrlConfigured').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「sampleEndpointAccepted」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('sampleEndpointAccepted').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「rejectedInputs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('rejectedInputs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3638 桌面岗位管理预览旗标余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「noAssignment」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('noAssignment').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「noPermissionEffect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('noPermissionEffect').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「acceptedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('acceptedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3639 桌面 SLA 运行态边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「不修改审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('不修改审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「工作流定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('工作流定义').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「业务单据或任务状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('业务单据或任务状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3640 桌面邮件日志脱敏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「头信息与载荷」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('头信息与载荷').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「发件人、主题、正文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('发件人、主题、正文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「诊断摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('诊断摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3641 桌面自定义字段预览字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「required/type/options/regex」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('required/type/options/regex').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「valid、missing、rejected」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('valid、missing、rejected').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「errors、usedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('errors、usedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3642 桌面字段集预览字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「rejectedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('rejectedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「分类诊断上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('分类诊断上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「字段 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('字段 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3643 桌面同步规则队列失败计数空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「失败 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('失败 0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「待处理 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('待处理 0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「运行中 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('运行中 0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3644 桌面 SLA 脱敏边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「通知目标、联系方式、变量值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('通知目标、联系方式、变量值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「附件引用与导出内容」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('附件引用与导出内容').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「masked/summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('masked/summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3645 桌面通知渠道未用 combobox/checkbox 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels combobox「通知渠道配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('combobox', { name: '通知渠道配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels combobox「预览渠道类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('combobox', { name: '预览渠道类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels checkbox「签名已配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('checkbox', { name: '签名已配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3646 桌面通知渠道未用 checkbox 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels checkbox「地址已配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('checkbox', { name: '地址已配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels checkbox「启用状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('checkbox', { name: '启用状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「noPersistence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('noPersistence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3647 桌面通知偏好未用 combobox 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences combobox「通知偏好分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('combobox', { name: '通知偏好分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「站内信=」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('站内信=').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「邮件=」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('邮件=').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3648 桌面 Webhook 未用签名策略 combobox 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config combobox「签名策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('combobox', { name: '签名策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「只加载 /system/webhook-configs 配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('只加载 /system/webhook-configs 配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「配置校验仅做 config-only 验证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('配置校验仅做 config-only 验证').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3649 桌面 SLA 导出字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「表单实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('表单实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「变量原文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('变量原文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「storage key」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('storage key').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3650 桌面 SLA 导出遮罩余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「附件路径」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('附件路径').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「附件路径或 storage key」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('附件路径或 storage key').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「只展示 masked/summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('只展示 masked/summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3651 桌面运行监控只读边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「本页不提供通过、驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('本页不提供通过、驳回').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「业务状态修改动作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('业务状态修改动作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「节点耗时、异常轨迹、超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('节点耗时、异常轨迹、超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3652 桌面运行监控未用状态筛选 combobox 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor combobox「状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('combobox', { name: '状态筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「导出提示均来自 SLA 摘要接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('导出提示均来自 SLA 摘要接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「通过、驳回、重试、终止」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('通过、驳回、重试、终止').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3653 桌面表单存储脱敏边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「字段值、附件引用、错误和导出均默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('字段值、附件引用、错误和导出均默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件只登记引用元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件只登记引用元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「列表、详情、创建、更新、归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('列表、详情、创建、更新、归档').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3654 桌面待办字段审计边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「角色覆盖、默认恢复与预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('角色覆盖、默认恢复与预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「confirmed、operatorId」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('confirmed、operatorId').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「reason/auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('reason/auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3655 桌面待办字段确认留痕余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「保存、排序、角色覆盖和默认恢复都需要 confirmed」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('保存、排序、角色覆盖和默认恢复都需要 confirmed').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「默认恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('默认恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「角色覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('角色覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3656 桌面审批规则表达式余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「字段、比较、短数组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('字段、比较、短数组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「列表、详情、创建、更新、启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('列表、详情、创建、更新、启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「模拟与冲突检测接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('模拟与冲突检测接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3657 桌面同步规则队列计数余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「只保留 dry-run、单条日志重试、只读队列摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('只保留 dry-run、单条日志重试、只读队列摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「dryRun 默认 true；只保留」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('dryRun 默认 true；只保留').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「失败 0；队列消费已禁用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('失败 0；队列消费已禁用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3658 桌面表单存储接口说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「删除留痕、附件引用与导出脱敏接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('删除留痕、附件引用与导出脱敏接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「归档、删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('归档、删除留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「错误和导出均默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('错误和导出均默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3659 桌面交接只读边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「真实资产/工单/审批对象转移未闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('真实资产/工单/审批对象转移未闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「本页仅展示交接任务摘要与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('本页仅展示交接任务摘要与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3660 桌面交接专项处理余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「发起、推进、取消交接等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('发起、推进、取消交接等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「需后续专项处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('需后续专项处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读边界：本页仅展示交接任务摘要与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('只读边界：本页仅展示交接任务摘要与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3661 桌面文档中心只读边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「附件安全与删除留痕需后续专项处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('附件安全与删除留痕需后续专项处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「本页仅展示文档目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('本页仅展示文档目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「发布状态与附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('发布状态与附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3662 桌面文档中心写操作边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「附件上传等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('附件上传等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「分类、版本、发布状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('分类、版本、发布状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3663 桌面技术支持安全边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「禁止导出敏感配置原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('禁止导出敏感配置原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「创建、分配、处理、关闭工单与导出诊断包」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('创建、分配、处理、关闭工单与导出诊断包').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「本页仅展示工单只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('本页仅展示工单只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3664 桌面技术支持诊断包边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「安全边界：诊断包必须脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('安全边界：诊断包必须脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「导出诊断包等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('导出诊断包等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「关闭工单与导出诊断包」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('关闭工单与导出诊断包').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3665 桌面文档中心目录边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读边界：本页仅展示文档目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('只读边界：本页仅展示文档目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「编辑、发布、归档、删除、附件上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('编辑、发布、归档、删除、附件上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「标题、分类、版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('标题、分类、版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3666 桌面交接对象转移余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「真实资产/工单/审批对象转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('真实资产/工单/审批对象转移').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「取消交接等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('取消交接等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3667 桌面租户管理只读边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「本页仅展示租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('本页仅展示租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「套餐与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('套餐与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「/tenants/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('/tenants/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3668 桌面租户管理写操作边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「租户主数据、套餐与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('租户主数据、套餐与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读边界：本页仅展示租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读边界：本页仅展示租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3669 桌面数据权限范围说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「展示并收紧角色 dataScope」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('展示并收紧角色 dataScope').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「CUSTOM 部门规则仍未接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('CUSTOM 部门规则仍未接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「并为 CUSTOM 配置部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('并为 CUSTOM 配置部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3670 桌面数据权限写入边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「写入需 system:role-permission:edit」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('写入需 system:role-permission:edit').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「查询运行时仍未按部门清单过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('查询运行时仍未按部门清单过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「收紧角色 dataScope」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('收紧角色 dataScope').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3671 桌面租户接口路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读展示 /tenants 与 /tenants/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读展示 /tenants 与 /tenants/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「返回的租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('返回的租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「停用、启用等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('停用、启用等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3672 桌面数据权限 CUSTOM 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「CUSTOM 配置部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('CUSTOM 配置部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「部门规则仍未接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('部门规则仍未接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「未按部门清单过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('未按部门清单过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3673 桌面租户主数据边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「/tenants 与 /tenants/meta 返回的租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('/tenants 与 /tenants/meta 返回的租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「仅展示租户主数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('仅展示租户主数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3674 桌面数据权限编辑权限余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「system:role-permission:edit」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('system:role-permission:edit').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「查询运行时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('查询运行时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「角色 dataScope」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('角色 dataScope').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3675 桌面导入导出只读说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「只读展示导入导出任务历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('只读展示导入导出任务历史').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「错误报告摘要与只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('错误报告摘要与只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「本页仅展示任务历史与脱敏错误摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('本页仅展示任务历史与脱敏错误摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3676 桌面导入导出写操作边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「导入上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('导入上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「导出上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('导出上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3677 桌面用户管理检索说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「搜索调用后端 keyword」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('搜索调用后端 keyword').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「返回的用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('返回的用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「只读展示 /user-management/list 返回的用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('只读展示 /user-management/list 返回的用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3678 桌面导入导出取消边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「取消等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('取消等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「任务历史、错误报告摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('任务历史、错误报告摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「只读边界：本页仅展示任务历史与脱敏错误摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('只读边界：本页仅展示任务历史与脱敏错误摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3679 桌面导入导出行数上限余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「行，导出上限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('行，导出上限').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「导出上限」行后缀', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText(/导出上限 .* 行/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「重试、取消等写操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('重试、取消等写操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3680 桌面用户管理 keyword 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「后端 keyword」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('后端 keyword').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「搜索调用后端」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('搜索调用后端').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「list 返回的用户，搜索调用后端 keyword」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('list 返回的用户，搜索调用后端 keyword').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3681 桌面导入导出任务历史余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「任务历史与脱敏错误摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('任务历史与脱敏错误摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「错误报告摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('错误报告摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3682 桌面用户管理状态筛选说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「keyword，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('keyword，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「/user-management/list 返回的用户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('/user-management/list 返回的用户').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「按账号、姓名、电话搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByPlaceholder('按账号、姓名、电话搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3683 桌面位置管理检索说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「返回的位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('返回的位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「/locations/list 与 /locations/root 返回的位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('/locations/list 与 /locations/root 返回的位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3684 桌面位置管理接口路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「只读展示 /locations/list 与 /locations/root」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('只读展示 /locations/list 与 /locations/root').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「root 返回的位置，搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('root 返回的位置，搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3685 桌面供应商管理检索说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「返回的供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('返回的供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「/vendors/list 返回的供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('/vendors/list 返回的供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3686 桌面供应商管理接口路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「只读展示 /vendors/list 返回的供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('只读展示 /vendors/list 返回的供应商').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「list 返回的供应商，搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('list 返回的供应商，搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3687 桌面位置管理前端筛选余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「与 /locations/root」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('与 /locations/root').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「位置，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('位置，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3688 桌面供应商管理前端筛选余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「供应商，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('供应商，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3689 桌面位置管理 list/root 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「/locations/list 与 /locations/root」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('/locations/list 与 /locations/root').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「返回的位置，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('返回的位置，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「只读展示 /locations/list 与 /locations/root 返回的位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('只读展示 /locations/list 与 /locations/root 返回的位置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3690 桌面供应商 list 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「只读展示 /vendors/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('只读展示 /vendors/list').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「返回的供应商，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('返回的供应商，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「/vendors/list 返回的供应商，搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('/vendors/list 返回的供应商，搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3691 桌面部门组织返回的组织部门余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「与 /depts/tree」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('与 /depts/tree').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/list 与 /depts/tree」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/list 与 /depts/tree').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3692 桌面部门组织接口路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list 与 /depts/tree」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('只读展示 /depts/list 与 /depts/tree').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/tree 返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/tree 返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「组织部门，搜索调用列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('组织部门，搜索调用列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3693 桌面部门组织检索说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「返回的组织部门，搜索调用列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('返回的组织部门，搜索调用列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「搜索调用列表接口，状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('搜索调用列表接口，状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list 与 /depts/tree 返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('只读展示 /depts/list 与 /depts/tree 返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3694 桌面部门组织 tree 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「与 /depts/tree 返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('与 /depts/tree 返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/list 与 /depts/tree 返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/list 与 /depts/tree 返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3695 桌面部门组织筛选说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「搜索调用列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('搜索调用列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「组织部门，搜索调用列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('组织部门，搜索调用列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「tree 返回的组织部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('tree 返回的组织部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3696 桌面部门组织只读说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「返回的组织部门，搜索调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('返回的组织部门，搜索调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3697 桌面部门组织列表接口余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「调用列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('调用列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「组织部门，搜索调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('组织部门，搜索调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3698 桌面部门组织 list/tree 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「与 /depts/tree 返回的组织部门，搜索调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('与 /depts/tree 返回的组织部门，搜索调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「列表接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('列表接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3699 桌面外部系统真实调用路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「真实调用 /system/external-systems」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('真实调用 /system/external-systems').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「只展示目录、状态、认证掩码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('只展示目录、状态、认证掩码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「认证掩码和 config-only 校验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('认证掩码和 config-only 校验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3700 桌面外部系统目录掩码余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「目录、状态、认证掩码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('目录、状态、认证掩码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「config-only 校验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('config-only 校验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「/system/external-systems」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('/system/external-systems').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3701 桌面外部系统只展示目录余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「只展示目录、状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('只展示目录、状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「认证掩码和 config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('认证掩码和 config-only').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「真实调用 /system/external-systems；只展示目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('真实调用 /system/external-systems；只展示目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3702 桌面外部系统校验结果余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「状态、认证掩码和 config-only 校验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('状态、认证掩码和 config-only 校验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「目录、状态、认证掩码和 config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('目录、状态、认证掩码和 config-only').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「只展示目录、状态、认证掩码和 config-only 校验结果，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('只展示目录、状态、认证掩码和 config-only 校验结果，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3703 桌面外部系统目录状态余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「只展示目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('只展示目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「状态、认证掩码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('状态、认证掩码').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「校验结果，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('校验结果，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3704 桌面外部系统 config-only 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「只展示目录、状态、认证掩码和 config-only 校验结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('只展示目录、状态、认证掩码和 config-only 校验结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「config-only 校验结果，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('config-only 校验结果，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「状态、认证掩码和 config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('状态、认证掩码和 config-only').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3705 桌面外部系统路径与掩码余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「真实调用 /system」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('真实调用 /system').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「认证掩码和 config-only 校验结果，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('认证掩码和 config-only 校验结果，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems 「/system/external-systems；只展示目录、状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByText('/system/external-systems；只展示目录、状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3706 桌面流程设计器处理人预览未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-designer label「处理人预览面板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByLabel('处理人预览面板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer button「计算处理人」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByRole('button', { name: '计算处理人' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-designer textbox「业务数据 JSON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-designer');
    await expect(page.getByRole('textbox', { name: '业务数据 JSON' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3707 桌面文件存储附件元数据余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「返回的附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('返回的附件元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「附件元数据、统计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('附件元数据、统计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不支持上传/下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不支持上传/下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3708 桌面文件存储只读目录余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为 /system/file-storage/attachments/catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为 /system/file-storage/attachments/catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「上传/下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('上传/下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「/system/file-storage/attachments/catalog 返回的附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('/system/file-storage/attachments/catalog 返回的附件元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3709 桌面文件存储统计摘要余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「返回的附件元数据、统计摘要与筛选结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('返回的附件元数据、统计摘要与筛选结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「附件元数据、统计摘要与筛选结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('附件元数据、统计摘要与筛选结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为 /system/file-storage」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为 /system/file-storage').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3710 桌面文件存储上传下载边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读展示 /system/file-storage/attachments/catalog 返回的附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('只读展示 /system/file-storage/attachments/catalog 返回的附件元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不支持上传/下载/预览/删除，不访问文件系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不支持上传/下载/预览/删除，不访问文件系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读元数据目录，不支持上传/下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('只读元数据目录，不支持上传/下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3711 桌面文件存储 catalog 返回余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「catalog 返回的附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('catalog 返回的附件元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「返回的附件元数据、统计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('返回的附件元数据、统计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为 /system/file-storage/attachments/catalog 只读元数据目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为 /system/file-storage/attachments/catalog 只读元数据目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3712 桌面文件存储预览删除边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「上传/下载/预览/删除，不访问文件系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('上传/下载/预览/删除，不访问文件系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「/system/file-storage/attachments/catalog 只读元数据目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('/system/file-storage/attachments/catalog 只读元数据目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不支持上传/下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不支持上传/下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3713 桌面文件存储生命周期边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「预览/删除，不访问文件系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('预览/删除，不访问文件系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3714 桌面文件存储只读说明整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「本页不包含导入/导出或执行动作，仅展示附件元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('本页不包含导入/导出或执行动作，仅展示附件元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读展示 /system/file-storage/attachments/catalog 返回的附件元数据、统计摘要与筛选结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('只读展示 /system/file-storage/attachments/catalog 返回的附件元数据、统计摘要与筛选结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3715 桌面审批规则短数组与禁止 JS 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「短数组和 AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('短数组和 AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL、JS」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL、JS').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL、JS、SQL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL、JS、SQL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3716 桌面审批规则反射与 OGNL 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「SQL、OGNL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('SQL、OGNL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「反射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('反射').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「脚本引擎、反射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('脚本引擎、反射').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3717 桌面审批规则 parser 白名单余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「parser/evaluator：字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('parser/evaluator：字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL、JS、SQL、OGNL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL、JS、SQL、OGNL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3718 桌面审批规则 JS/OGNL 组合余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「短数组和 AND/OR/NOT；禁止 SpEL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('短数组和 AND/OR/NOT；禁止 SpEL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「JS、SQL、OGNL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('JS、SQL、OGNL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「OGNL、脚本引擎、反射」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('OGNL、脚本引擎、反射').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3719 桌面审批规则字段比较组合余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「自有白名单 parser/evaluator：字段、比较、短数组」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('自有白名单 parser/evaluator：字段、比较、短数组').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「字段、比较、短数组和 AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('字段、比较、短数组和 AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL、JS、SQL、OGNL、脚本引擎」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL、JS、SQL、OGNL、脚本引擎').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3720 桌面审批规则反射类加载余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「反射、动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('反射、动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「AND/OR/NOT；禁止 SpEL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('AND/OR/NOT；禁止 SpEL').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「SQL、OGNL、脚本引擎」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('SQL、OGNL、脚本引擎').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3721 桌面审批规则脚本引擎组合余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「脚本引擎、反射、动态类加载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('脚本引擎、反射、动态类加载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「比较、短数组和 AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('比较、短数组和 AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「表达式只能使用自有白名单 parser/evaluator：字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('表达式只能使用自有白名单 parser/evaluator：字段').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3722 桌面审批规则接口路径整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「JS、SQL、OGNL、脚本引擎」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('JS、SQL、OGNL、脚本引擎').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「真实调用 /approval-rules 列表、详情、创建、更新、启用、停用、模拟与冲突检测接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('真实调用 /approval-rules 列表、详情、创建、更新、启用、停用、模拟与冲突检测接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「列表、详情、创建、更新、启用、停用、模拟与冲突检测接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('列表、详情、创建、更新、启用、停用、模拟与冲突检测接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3723 桌面表单存储 uploads 边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「不读取 uploads，不提供二进制」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('不读取 uploads，不提供二进制').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件引用与导出脱敏接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件引用与导出脱敏接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「更新、归档、删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('更新、归档、删除留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3724 桌面表单存储默认脱敏余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「默认脱敏；附件只登记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('默认脱敏；附件只登记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「字段值、附件引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('字段值、附件引用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3725 桌面表单存储引用元数据余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「导出均默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('导出均默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「引用元数据，不读取 uploads」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('引用元数据，不读取 uploads').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件只登记引用元数据，不读取 uploads」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件只登记引用元数据，不读取 uploads').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3726 桌面表单存储归档删除余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「错误和导出均默认脱敏；附件只登记引用元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('错误和导出均默认脱敏；附件只登记引用元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「创建、更新、归档、删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('创建、更新、归档、删除留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「/form-storage 列表、详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('/form-storage 列表、详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3727 桌面表单存储二进制上传余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「均默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('均默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「不提供二进制上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('不提供二进制上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件引用与导出脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件引用与导出脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3728 桌面表单存储归档接口余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「归档、删除留痕、附件引用与导出脱敏接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('归档、删除留痕、附件引用与导出脱敏接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「不读取 uploads，不提供二进制上传或下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('不读取 uploads，不提供二进制上传或下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「导出脱敏接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('导出脱敏接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3729 桌面表单存储整句说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕、附件引用与导出脱敏接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕、附件引用与导出脱敏接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「服务层强制租户隔离；字段值、附件引用、错误和导出均默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('服务层强制租户隔离；字段值、附件引用、错误和导出均默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「附件只登记引用元数据，不读取 uploads，不提供二进制上传或下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('附件只登记引用元数据，不读取 uploads，不提供二进制上传或下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3730 桌面表单存储列表归档余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「删除留痕、附件引用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('删除留痕、附件引用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「列表、详情、创建、更新、归档、删除留痕」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('列表、详情、创建、更新、归档、删除留痕').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage 「字段值、附件引用、错误和导出均默认脱敏；附件只登记引用元数据，不读取 uploads，不提供二进制上传或下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByText('字段值、附件引用、错误和导出均默认脱敏；附件只登记引用元数据，不读取 uploads，不提供二进制上传或下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3731 桌面待办字段预览接口余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「保存、排序、角色覆盖、默认恢复与预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('保存、排序、角色覆盖、默认恢复与预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「列表、保存、排序、角色覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('列表、保存、排序、角色覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「真实调用 /todo-fields 列表、保存、排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('真实调用 /todo-fields 列表、保存、排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3732 桌面待办字段审计字段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「都需要 confirmed、operatorId、reason/auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('都需要 confirmed、operatorId、reason/auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「角色覆盖和默认恢复都需要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('角色覆盖和默认恢复都需要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「保存、排序、角色覆盖和默认恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('保存、排序、角色覆盖和默认恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3733 桌面待办字段租户隔离余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「强制租户隔离；保存、排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('强制租户隔离；保存、排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「真实调用 /todo-fields 列表、保存、排序、角色覆盖、默认恢复与预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('真实调用 /todo-fields 列表、保存、排序、角色覆盖、默认恢复与预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「preview 只读」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('preview 只读').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3734 桌面待办字段默认恢复余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「默认恢复与预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('默认恢复与预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「confirmed、operatorId、reason/auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('confirmed、operatorId、reason/auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「默认恢复都需要 confirmed、operatorId」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('默认恢复都需要 confirmed、operatorId').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3735 桌面待办字段保存排序余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「字段配置强制租户隔离；保存、排序、角色覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('字段配置强制租户隔离；保存、排序、角色覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「保存、排序、角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('保存、排序、角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「列表、保存、排序、角色覆盖、默认恢复」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('列表、保存、排序、角色覆盖、默认恢复').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3736 桌面待办字段路径排序余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「/todo-fields 列表、保存、排序」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('/todo-fields 列表、保存、排序').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「排序、角色覆盖、默认恢复与预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('排序、角色覆盖、默认恢复与预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「只读且默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('只读且默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3737 桌面待办字段角色覆盖审计余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「保存、排序、角色覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('保存、排序、角色覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「默认恢复与预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('默认恢复与预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3738 桌面待办字段整句说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「真实调用 /todo-fields 列表、保存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('真实调用 /todo-fields 列表、保存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「预览接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('预览接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields 「字段配置强制租户隔离；保存、排序、角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence；preview 只读且默认脱敏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByText('字段配置强制租户隔离；保存、排序、角色覆盖和默认恢复都需要 confirmed、operatorId、reason/auditEvidence；preview 只读且默认脱敏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3739 桌面字段映射预览边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「预览不写库、不外呼」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('预览不写库、不外呼').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「trim(value)、upper(value)、lower(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('trim(value)、upper(value)、lower(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「仅允许 trim(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('仅允许 trim(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3740 桌面字段映射转换表达式余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「转换表达式仅允许 trim(value)、upper(value)、lower(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('转换表达式仅允许 trim(value)、upper(value)、lower(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「trim(value)、upper(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('trim(value)、upper(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「upper(value)、lower(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('upper(value)、lower(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3741 桌面字段映射整句说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「转换表达式仅允许 trim(value)、upper(value)、lower(value)，预览不写库、不外呼」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('转换表达式仅允许 trim(value)、upper(value)、lower(value)，预览不写库、不外呼').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「仅允许 trim(value)、upper(value)、lower(value)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('仅允许 trim(value)、upper(value)、lower(value)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping 「lower(value)，预览不写库、不外呼」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByText('lower(value)，预览不写库、不外呼').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3742 桌面接口管理元数据校验余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「只加载接口管理数据，配置校验仅验证元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('只加载接口管理数据，配置校验仅验证元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「配置校验仅验证元数据，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('配置校验仅验证元数据，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「只加载接口管理数据，配置校验仅验证元数据，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('只加载接口管理数据，配置校验仅验证元数据，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3743 桌面接口管理只加载余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「校验仅验证元数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('校验仅验证元数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「只加载接口管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('只加载接口管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces 「配置校验仅验证元数据，未触发」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByText('配置校验仅验证元数据，未触发').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3744 桌面菜单权限编码库存余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「catalog 的权限编码库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('catalog 的权限编码库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「权限域聚合为菜单权限覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('权限域聚合为菜单权限覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「菜单分配、编辑或删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('菜单分配、编辑或删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3745 桌面菜单权限只读目录余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「本页不支持菜单分配」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('本页不支持菜单分配').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「/system/role-permissions/catalog 的权限编码库存，按权限域聚合」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('/system/role-permissions/catalog 的权限编码库存，按权限域聚合').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「只读展示 /system/role-permissions/catalog 的权限编码库存，按权限域聚合为菜单权限覆盖视图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog 的权限编码库存，按权限域聚合为菜单权限覆盖视图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3746 桌面菜单权限覆盖视图余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「权限编码库存，按权限域聚合」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('权限编码库存，按权限域聚合').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「不支持菜单分配、编辑或删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('不支持菜单分配、编辑或删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions 「仅作为菜单权限只读目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByText('仅作为菜单权限只读目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3747 桌面角色权限 catalog 返回余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「/system/role-permissions/catalog 返回的角色-权限绑定目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('/system/role-permissions/catalog 返回的角色-权限绑定目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「绑定目录、权限库存与绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('绑定目录、权限库存与绑定数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「只读展示 /system/role-permissions/catalog 返回的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog 返回的').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3748 桌面角色权限绑定目录余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「角色-权限绑定目录、权限库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('角色-权限绑定目录、权限库存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「返回的角色-权限绑定目录、权限库存与绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('返回的角色-权限绑定目录、权限库存与绑定数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录、权限库存与绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录、权限库存与绑定数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3749 桌面角色权限库存绑定余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「catalog 返回的角色-权限绑定目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('catalog 返回的角色-权限绑定目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「catalog 返回的角色-权限绑定目录、权限库存与绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('catalog 返回的角色-权限绑定目录、权限库存与绑定数量').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions 「只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByText('只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3750 桌面数据权限 dataScope 整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「展示并收紧角色 dataScope；CUSTOM 部门规则仍未接入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('展示并收紧角色 dataScope；CUSTOM 部门规则仍未接入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「可调整角色数据范围，并为 CUSTOM 配置部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('可调整角色数据范围，并为 CUSTOM 配置部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「写入需 system:role-permission:edit。查询运行时仍未按部门清单过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('写入需 system:role-permission:edit。查询运行时仍未按部门清单过滤').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3751 桌面数据权限 CUSTOM 组合余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「角色 dataScope；CUSTOM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('角色 dataScope；CUSTOM').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「可调整角色数据范围，并为 CUSTOM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('可调整角色数据范围，并为 CUSTOM').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「写入需 system:role-permission:edit。查询运行时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('写入需 system:role-permission:edit。查询运行时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3752 桌面数据权限部门清单余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「为 CUSTOM 配置部门清单。写入需」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('为 CUSTOM 配置部门清单。写入需').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「角色数据范围，并为 CUSTOM 配置部门清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('角色数据范围，并为 CUSTOM 配置部门清单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions 「展示并收紧角色 dataScope；CUSTOM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByText('展示并收紧角色 dataScope；CUSTOM').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3753 桌面运行监控审批 SLA 路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「只读展示 /approvals/list」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('只读展示 /approvals/list').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「/approvals/pending/count」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('/approvals/pending/count').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「/sla-config/runtime-summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('/sla-config/runtime-summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3754 桌面审计日志导出与端点余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不生成真实导出文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不生成真实导出文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/action-type-distribution」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/action-type-distribution').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「只读审计日志：真实调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('只读审计日志：真实调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3755 桌面运行监控 timeout-records 与审批实例摘要空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「/sla-config/timeout-records」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('/sla-config/timeout-records').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「返回的审批实例、SLA 风险摘要和超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('返回的审批实例、SLA 风险摘要和超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「不提供通过、驳回、重试、终止或业务状态修改动作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('不提供通过、驳回、重试、终止或业务状态修改动作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3756 桌面运行监控审批与 SLA 摘要路径组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「只读展示 /approvals/list、/approvals/pending/count」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('只读展示 /approvals/list、/approvals/pending/count').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「/sla-config/runtime-summary 与 /sla-config/timeout-records」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('/sla-config/runtime-summary 与 /sla-config/timeout-records').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「SLA 风险摘要和超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('SLA 风险摘要和超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3757 桌面运行监控 subtitle 长组合与只读动作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「返回的审批实例、SLA 风险摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('返回的审批实例、SLA 风险摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「审批实例、SLA 风险摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('审批实例、SLA 风险摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3758 桌面审计日志 stats/trends/ranking 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/stats」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/stats').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/trends」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/trends').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/operator-ranking」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/operator-ranking').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3759 桌面审计日志 meta 与只读写入边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不采集审计写入，不生成真实导出文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不采集审计写入，不生成真实导出文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不提供 POST/PUT/PATCH/DELETE，不采集审计写入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不提供 POST/PUT/PATCH/DELETE，不采集审计写入').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3760 桌面审计日志 GET 族与 tenant-scoped 子串空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「真实调用 /audit-logs GET 族端点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('真实调用 /audit-logs GET 族端点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/stats、/audit-logs/trends」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/stats、/audit-logs/trends').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「tenant-scoped / masked」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('tenant-scoped / masked').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3761 桌面岗位管理 posts 端点族余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「/system/posts/all」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('/system/posts/all').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「/system/posts/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('/system/posts/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「/system/posts/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('/system/posts/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3762 桌面岗位管理 metadata-only 与只读标志空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「readOnly=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('readOnly=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「metadata-only post catalog + dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('metadata-only post catalog + dry-run preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「tenantScoped=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('tenantScoped=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3763 桌面运行监控 subtitle 整句与只读动作空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「本页不提供通过、驳回、重试、终止或业务状态修改动作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('本页不提供通过、驳回、重试、终止或业务状态修改动作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records 返回的审批实例、SLA 风险摘要和超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records 返回的审批实例、SLA 风险摘要和超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor 「与 /sla-config/timeout-records 返回的审批实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByText('与 /sla-config/timeout-records 返回的审批实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3764 桌面审计日志 {id}/meta 与采集闭环空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「与 /audit-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('与 /audit-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「本页不是全局审计采集闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('本页不是全局审计采集闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3765 桌面审计日志导出能力限制说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「导出能力仅展示脱敏快照」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('导出能力仅展示脱敏快照').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不触发下载、不创建后台导出任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不触发下载、不创建后台导出任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「导出能力仅展示脱敏快照与限制说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('导出能力仅展示脱敏快照与限制说明').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3766 桌面岗位管理 posts/{id} 与预览端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「/system/posts/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('/system/posts/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「与 /system/posts/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('与 /system/posts/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「noPersistence=true、noAssignment=true、noPermissionEffect=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('noPersistence=true、noAssignment=true、noPermissionEffect=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3767 桌面岗位管理 preview 字段组合余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「duplicateRisk、referenceImpact」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('duplicateRisk、referenceImpact').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「runtimeEffect=false、cacheRefreshed=false 与 readonlyBoundary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('runtimeEffect=false、cacheRefreshed=false 与 readonlyBoundary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「acceptedFields、rejectedInputs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('acceptedFields、rejectedInputs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3768 桌面缓存管理白名单命名空间说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「只管理应用内白名单 CacheManager 命名空间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('只管理应用内白名单 CacheManager 命名空间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「应用内白名单 CacheManager 命名空间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('应用内白名单 CacheManager 命名空间').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「白名单 CacheManager 命名空间，空缓存不会被报告为普通成功」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('白名单 CacheManager 命名空间，空缓存不会被报告为普通成功').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3769 桌面流程控制台只读聚合路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读聚合 /workflows」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读聚合 /workflows').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「展示流程模板、运行实例、SLA 风险摘要与超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('展示流程模板、运行实例、SLA 风险摘要与超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 风险摘要与超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 风险摘要与超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3770 桌面流程控制台只读风险提示余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「不代表流程控制闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('不代表流程控制闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「本页仅复用既有只读查询结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('本页仅复用既有只读查询结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「不支持发起/审批/重试/终止/发布/编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('不支持发起/审批/重试/终止/发布/编辑').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3771 桌面流程控制台只读聚合长路径空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读风险提示：不支持发起/审批/重试/终止/发布/编辑」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读风险提示：不支持发起/审批/重试/终止/发布/编辑').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读聚合 /workflows、/approvals/list、/approvals/pending/count」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读聚合 /workflows、/approvals/list、/approvals/pending/count').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3772 桌面租户管理 subtitle 整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读展示 /tenants 与 /tenants/meta 返回的租户主数据、套餐与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读展示 /tenants 与 /tenants/meta 返回的租户主数据、套餐与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「返回的租户主数据、套餐与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('返回的租户主数据、套餐与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「/tenants/meta 返回的租户主数据、套餐与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('/tenants/meta 返回的租户主数据、套餐与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3773 桌面技术支持工单说明长组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「只读展示技术支持工单、优先级、处理状态与诊断包脱敏标记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('只读展示技术支持工单、优先级、处理状态与诊断包脱敏标记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「优先级、处理状态与诊断包脱敏标记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('优先级、处理状态与诊断包脱敏标记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「处理状态与诊断包脱敏标记」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('处理状态与诊断包脱敏标记').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3774 桌面导入导出任务历史整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「只读展示导入导出任务历史、错误报告摘要与只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('只读展示导入导出任务历史、错误报告摘要与只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「导入导出任务历史、错误报告摘要与只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('导入导出任务历史、错误报告摘要与只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「任务历史、错误报告摘要与只读边界」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('任务历史、错误报告摘要与只读边界').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3775 桌面交接管理摘要与风险提示整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读展示交接任务摘要、状态记录与未闭环风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('只读展示交接任务摘要、状态记录与未闭环风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「交接任务摘要、状态记录与未闭环风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('交接任务摘要、状态记录与未闭环风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「摘要、状态记录与未闭环风险提示」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('摘要、状态记录与未闭环风险提示').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3776 桌面文档中心标题分类版本整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读展示文档标题、分类、版本、发布状态与附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('只读展示文档标题、分类、版本、发布状态与附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「文档标题、分类、版本、发布状态与附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('文档标题、分类、版本、发布状态与附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「分类、版本、发布状态与附件计数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('分类、版本、发布状态与附件计数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3777 桌面流程邮件节点配置说明余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「只读展示流程节点邮件配置、触发事件、模板与收件人范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('只读展示流程节点邮件配置、触发事件、模板与收件人范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「触发事件、模板与收件人范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('触发事件、模板与收件人范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「真实邮件发送未接入流程平台，存在零业务调用风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('真实邮件发送未接入流程平台，存在零业务调用风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3778 桌面流程控制台风险提示闭环组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读风险提示：不支持发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读风险提示：不支持发起').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「展示流程模板、运行实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('展示流程模板、运行实例').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3779 桌面用户管理检索说明整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「只读展示 /user-management/list 返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('只读展示 /user-management/list 返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-user-management 「用户，搜索调用后端 keyword，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByText('用户，搜索调用后端 keyword，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3780 桌面基础参数接口与覆盖说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「真实调用 /system-config/system 与 /system/configs」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('真实调用 /system-config/system 与 /system/configs').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「也不代表 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('也不代表 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3781 桌面基础参数预演与 DEGRADED 说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「缓存刷新无真实缓存时必须显示 DEGRADED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('缓存刷新无真实缓存时必须显示 DEGRADED').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「本页展示基础参数目录、保存、影响预演与缓存刷新结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('本页展示基础参数目录、保存、影响预演与缓存刷新结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3782 桌面资产分类 list/tree 检索整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「只读展示分类列表与分类树，搜索仅调用 /categories/list 查询能力」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('只读展示分类列表与分类树，搜索仅调用 /categories/list 查询能力').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「分类列表与分类树，搜索仅调用 /categories/list 查询能力」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('分类列表与分类树，搜索仅调用 /categories/list 查询能力').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category 「/categories/list 查询能力」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByText('/categories/list 查询能力').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3783 桌面安全策略 no-login-chain 组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「no-login-chain / no-direct-effect」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('no-login-chain / no-direct-effect').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「真实调用 /system-config/security 与 /system-config/security/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('真实调用 /system-config/security 与 /system-config/security/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不改变会话、MFA 或移动端运行状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('不改变会话、MFA 或移动端运行状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3784 桌面安全策略配置态预览说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「本页只展示配置态、预览态和审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('本页只展示配置态、预览态和审计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「配置态、预览态和审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('配置态、预览态和审计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「本模块不接登录链路、不直接生效」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('本模块不接登录链路、不直接生效').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3785 桌面流程控制台 subtitle 整段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records，展示流程模板、运行实例、SLA 风险摘要与超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records，展示流程模板、运行实例、SLA 风险摘要与超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「与 /sla-config/timeout-records，展示流程模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('与 /sla-config/timeout-records，展示流程模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「运行实例、SLA 风险摘要与超时记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('运行实例、SLA 风险摘要与超时记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3786 桌面交接写操作边界长组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「本页仅展示交接任务摘要与状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('本页仅展示交接任务摘要与状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「真实资产/工单/审批对象转移未闭环，需后续专项处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('真实资产/工单/审批对象转移未闭环，需后续专项处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3787 桌面 SLA 配置端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「真实调用 /sla-config 策略列表、详情、更新、启停、simulate、runtime-summary、timeout-records 与 export 接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('真实调用 /sla-config 策略列表、详情、更新、启停、simulate、runtime-summary、timeout-records 与 export 接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「策略列表、详情、更新、启停、simulate」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('策略列表、详情、更新、启停、simulate').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「timeout-records 与 export 接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('timeout-records 与 export 接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3788 桌面 SLA 运行态安全边界长组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「模拟、摘要、超时记录和脱敏导出不修改审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('模拟、摘要、超时记录和脱敏导出不修改审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「不修改审批、工作流定义、表单实例、业务单据或任务状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('不修改审批、工作流定义、表单实例、业务单据或任务状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3789 桌面审批规则表达式白名单整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「禁止 SpEL、JS、SQL、OGNL、脚本引擎、反射、动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('禁止 SpEL、JS、SQL、OGNL、脚本引擎、反射、动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「表达式只能使用自有白名单 parser/evaluator：字段、比较、短数组和 AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('表达式只能使用自有白名单 parser/evaluator：字段、比较、短数组和 AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「脚本引擎、反射、动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('脚本引擎、反射、动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3790 桌面文档中心写操作边界长组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「本页仅展示文档目录；编辑、发布、归档、删除、附件上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('本页仅展示文档目录；编辑、发布、归档、删除、附件上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读边界：本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('只读边界：本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3791 桌面技术支持诊断包脱敏长组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「诊断包必须脱敏，禁止导出敏感配置原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('诊断包必须脱敏，禁止导出敏感配置原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「安全边界：诊断包必须脱敏，禁止导出敏感配置原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('安全边界：诊断包必须脱敏，禁止导出敏感配置原值').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3792 桌面 SLA simulate/runtime-summary 路径余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「启停、simulate、runtime-summary、timeout-records」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('启停、simulate、runtime-summary、timeout-records').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「runtime-summary、timeout-records 与 export 接口」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('runtime-summary、timeout-records 与 export 接口').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「详情、更新、启停、simulate」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('详情、更新、启停、simulate').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3793 桌面审批规则白名单整段余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「表达式只能使用自有白名单 parser/evaluator：字段、比较、短数组和 AND/OR/NOT；禁止 SpEL、JS、SQL、OGNL、脚本引擎、反射、动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('表达式只能使用自有白名单 parser/evaluator：字段、比较、短数组和 AND/OR/NOT；禁止 SpEL、JS、SQL、OGNL、脚本引擎、反射、动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「OGNL、脚本引擎、反射、动态类加载和任意方法调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('OGNL、脚本引擎、反射、动态类加载和任意方法调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules 「比较、短数组和 AND/OR/NOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByText('比较、短数组和 AND/OR/NOT').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3794 桌面流程邮件写操作与零调用风险空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「本页仅展示流程节点邮件配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('本页仅展示流程节点邮件配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「存在零业务调用风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('存在零业务调用风险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3795 桌面通知偏好 category/meta/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「/notification-preferences/{category}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('/notification-preferences/{category}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「/notification-preferences/meta 与 /notification-preferences/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('/notification-preferences/meta 与 /notification-preferences/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「真实调用 /notification-preferences、/notification-preferences/{category}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('真实调用 /notification-preferences、/notification-preferences/{category}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3796 桌面通知偏好 metadata-only 边界加号空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读通知偏好目录 + 无持久化偏好决策预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读通知偏好目录 + 无持久化偏好决策预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「无持久化偏好决策预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('无持久化偏好决策预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「不保存偏好、不发送通知、不改变真实通知发送决策」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('不保存偏好、不发送通知、不改变真实通知发送决策').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3797 桌面通知渠道 {id}/meta/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「/system/channel-configs/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('/system/channel-configs/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「/system/channel-configs/meta 与 /system/channel-configs/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('/system/channel-configs/meta 与 /system/channel-configs/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「真实调用 /system/channel-configs、/system/channel-configs/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('真实调用 /system/channel-configs、/system/channel-configs/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3798 桌面通知渠道无发送无外联边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「只读通知渠道目录 + 无持久化、无发送、无外联脱敏预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('只读通知渠道目录 + 无持久化、无发送、无外联脱敏预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「无持久化、无发送、无外联脱敏预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('无持久化、无发送、无外联脱敏预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不保存渠道、不发送测试消息、不调用外部 webhook」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不保存渠道、不发送测试消息、不调用外部 webhook').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3799 桌面编号规则 ruleKey/meta/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「/numbering-rules/{ruleKey}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('/numbering-rules/{ruleKey}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「/numbering-rules/meta 与 /numbering-rules/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('/numbering-rules/meta 与 /numbering-rules/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「真实调用 /numbering-rules、/numbering-rules/{ruleKey}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('真实调用 /numbering-rules、/numbering-rules/{ruleKey}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3800 桌面编号规则 catalog + dry-run 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「read-only numbering rule catalog + no-persistence deterministic preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('read-only numbering rule catalog + no-persistence deterministic preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「no-persistence deterministic preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('no-persistence deterministic preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「预览不会持久化、不刷新缓存、不预留或占用序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('预览不会持久化、不刷新缓存、不预留或占用序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3801 桌面通知偏好 preview 端点组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「与 /notification-preferences/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('与 /notification-preferences/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「/notification-preferences/{category}、/notification-preferences/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('/notification-preferences/{category}、/notification-preferences/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读通知偏好目录 + 无持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读通知偏好目录 + 无持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3802 桌面通知渠道 preview 端点组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「与 /system/channel-configs/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('与 /system/channel-configs/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「/system/channel-configs/{id}、/system/channel-configs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('/system/channel-configs/{id}、/system/channel-configs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「只读通知渠道目录 + 无持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('只读通知渠道目录 + 无持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3803 桌面邮件模板 {id}/code/meta 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「/mail-templates/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('/mail-templates/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「/mail-templates/code/{code}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('/mail-templates/code/{code}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「/mail-templates/meta 与 /mail-templates/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('/mail-templates/meta 与 /mail-templates/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3804 桌面邮件模板 catalog + safe preview 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「仅邮件模板 catalog + 无持久化 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('仅邮件模板 catalog + 无持久化 safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「无持久化 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('无持久化 safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3805 桌面通知模板 {id}/code/meta 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「/notification-templates/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('/notification-templates/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「/notification-templates/code/{code}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('/notification-templates/code/{code}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「/notification-templates/meta 与 /notification-templates/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('/notification-templates/meta 与 /notification-templates/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3806 桌面通知模板 catalog + safe preview 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「仅通知模板 catalog + 无持久化 safe preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('仅通知模板 catalog + 无持久化 safe preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不发送通知、不配置渠道、不修改偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不发送通知、不配置渠道、不修改偏好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不控制流程通知开关、不接入邮件网关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不控制流程通知开关、不接入邮件网关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3807 桌面流程通知开关 biz-type 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「/notification-switches/biz-type/{bizType}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('/notification-switches/biz-type/{bizType}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「/notification-switches/meta 与 /notification-switches/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('/notification-switches/meta 与 /notification-switches/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3808 桌面邮件网关 {id}/meta/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「/system/mail-gateways/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('/system/mail-gateways/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「/system/mail-gateways/meta 与 /system/mail-gateways/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('/system/mail-gateways/meta 与 /system/mail-gateways/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「真实调用 /system/mail-gateways、/system/mail-gateways/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('真实调用 /system/mail-gateways、/system/mail-gateways/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3809 桌面邮件网关 metadata-only 加号边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「metadata-only mail gateway catalog + dry-run preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('metadata-only mail gateway catalog + dry-run preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「noSend=true、noNetwork=true、credentialExposed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('noSend=true、noNetwork=true、credentialExposed=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3810 桌面流程通知开关 no-send 边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「read-only notification switch catalog + no-persistence preview + no-send preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('read-only notification switch catalog + no-persistence preview + no-send preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「no-persistence preview + no-send preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('no-persistence preview + no-send preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「不写库、不发送通知、不改变流程运行时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('不写库、不发送通知、不改变流程运行时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3811 桌面邮件日志 {id}/biz/meta 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「/mail-logs/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('/mail-logs/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「/mail-logs/biz 与 /mail-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('/mail-logs/biz 与 /mail-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「真实调用 /mail-logs/list、/mail-logs/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('真实调用 /mail-logs/list、/mail-logs/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3812 桌面邮件日志不触发发送管线空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「SMTP/mail gateway、workflow mail 或 notification pipeline」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('SMTP/mail gateway、workflow mail 或 notification pipeline').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「邮件日志仅 read-only catalog/detail/biz/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('邮件日志仅 read-only catalog/detail/biz/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3813 桌面自定义字段 all/{id}/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/all」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/all').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/meta 与 /system/custom-fields/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/meta 与 /system/custom-fields/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3814 桌面自定义字段端点族组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/all、/system/custom-fields/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/all、/system/custom-fields/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「与 /system/custom-fields/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('与 /system/custom-fields/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3815 桌面字段集 all/{id}/fields 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/all」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/all').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/{id}/fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/{id}/fields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3816 桌面字段集 by-category/meta/preview 端点空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/by-category/{categoryId}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/by-category/{categoryId}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3817 桌面邮件日志 redacted/tenantScoped 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「redacted=true，tenantScoped=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('redacted=true，tenantScoped=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「不保证日志采集链路，不代表邮件子系统完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('不保证日志采集链路，不代表邮件子系统完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「no retry/export/send，不保证日志采集链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('no retry/export/send，不保证日志采集链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3818 桌面自定义字段端点与 preview 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/{id}、/system/custom-fields/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/{id}、/system/custom-fields/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/meta 与 /system/custom-fields/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/meta 与 /system/custom-fields/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3819 桌面字段集端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「与 /system/custom-fieldsets/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('与 /system/custom-fieldsets/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3820 桌面字段集 preview 诊断上下文空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「preview 仅校验当前租户字段 ID 与字段集/分类诊断上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('preview 仅校验当前租户字段 ID 与字段集/分类诊断上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「返回 valid、missingFields、rejectedFields、usedFields、wouldBindCategory」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('返回 valid、missingFields、rejectedFields、usedFields、wouldBindCategory').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「tenantScoped、noPersistence、runtimeEffect=false、readonlyBoundary 与 errors」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('tenantScoped、noPersistence、runtimeEffect=false、readonlyBoundary 与 errors').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3821 桌面自定义字段 catalog + 校验预览边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「只读自定义字段定义目录 + 无持久化校验预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('只读自定义字段定义目录 + 无持久化校验预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「无持久化校验预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('无持久化校验预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不完成字段集、分类绑定、资产字段值链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不完成字段集、分类绑定、资产字段值链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3822 桌面自定义字段加密标记与 preview 校验空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「加密字段只展示安全标记，不回显样例原文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('加密字段只展示安全标记，不回显样例原文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「preview 仅按当前租户字段定义校验 required/type/options/regex」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('preview 仅按当前租户字段定义校验 required/type/options/regex').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「返回 valid、missing、rejected、errors、usedFields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('返回 valid、missing、rejected、errors、usedFields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3823 桌面字段集 missingFields 诊断组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「missingFields、rejectedFields、usedFields、wouldBindCategory」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('missingFields、rejectedFields、usedFields、wouldBindCategory').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「字段集/分类诊断上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('字段集/分类诊断上下文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「readonlyBoundary 与 errors」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('readonlyBoundary 与 errors').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3824 桌面自定义字段运行时 schema 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「运行时表单 schema、加密值存储或 secret handling」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('运行时表单 schema、加密值存储或 secret handling').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不回显样例原文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不回显样例原文').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「tenantScoped、noPersistence 与 runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('tenantScoped、noPersistence 与 runtimeEffect=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3825 桌面字段集 by-category 长路径组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「by-category/{categoryId}、/system/custom-fieldsets/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('by-category/{categoryId}、/system/custom-fieldsets/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3826 桌面自定义字段 secret handling 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「加密值存储或 secret handling」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('加密值存储或 secret handling').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「资产字段值链路、运行时表单 schema」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('资产字段值链路、运行时表单 schema').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「required/type/options/regex」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('required/type/options/regex').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3827 桌面表单配置 form-definitions 端点族空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「真实调用 /form-definitions、/schema/validate、/publish、/disable、/versions/{version}/rollback、/preview 与 /references。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('真实调用 /form-definitions、/schema/validate、/publish、/disable、/versions/{version}/rollback、/preview 与 /references。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「/form-definitions、/schema/validate、/publish、/disable」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('/form-definitions、/schema/validate、/publish、/disable').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「/versions/{version}/rollback、/preview 与 /references」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('/versions/{version}/rollback、/preview 与 /references').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3828 桌面表单配置 schema 白名单剥离边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「schema 只保留 sections/fields 白名单结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('schema 只保留 sections/fields 白名单结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「剥离 HTML/source/script/on*、javascript URL 和危险 style」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('剥离 HTML/source/script/on*、javascript URL 和危险 style').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「敏感字段默认值仅展示遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('敏感字段默认值仅展示遮罩预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3829 桌面流程设计器 designer/draft 端点族空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「真实调用 /workflows、/workflows/{businessType}/designer/draft、/designer/validate、/publish 与 /versions/{version}/rollback。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('真实调用 /workflows、/workflows/{businessType}/designer/draft、/designer/validate、/publish 与 /versions/{version}/rollback。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「/workflows/{businessType}/designer/draft」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('/workflows/{businessType}/designer/draft').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「/designer/validate、/publish 与 /versions/{version}/rollback」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('/designer/validate、/publish 与 /versions/{version}/rollback').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3830 桌面流程设计器发布 fail-closed 边界空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「发布与版本恢复均要求后端权限 fail-closed、confirmed=true、operatorId」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('发布与版本恢复均要求后端权限 fail-closed、confirmed=true、operatorId').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「影响范围与回滚预案」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('影响范围与回滚预案').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「前端按钮仅作体验辅助」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('前端按钮仅作体验辅助').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3831 桌面 Webhook config-only 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「只加载 /system/webhook-configs 配置，配置校验仅做 config-only 验证，未触发真实外部调用。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('只加载 /system/webhook-configs 配置，配置校验仅做 config-only 验证，未触发真实外部调用。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「配置校验仅做 config-only 验证，未触发真实外部调用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('配置校验仅做 config-only 验证，未触发真实外部调用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config 「webhook-configs 配置，配置校验仅做 config-only」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByText('webhook-configs 配置，配置校验仅做 config-only').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3832 桌面同步规则队列摘要组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「待处理 0，运行中 0，失败 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('待处理 0，运行中 0，失败 0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「只读队列摘要：待处理 0，运行中 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('只读队列摘要：待处理 0，运行中 0').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules 「dryRun 默认 true；只保留 dry-run、单条日志重试、只读队列摘要，批处理队列消费入口不开放。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByText('dryRun 默认 true；只保留 dry-run、单条日志重试、只读队列摘要，批处理队列消费入口不开放。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3833 桌面流程定义 /workflows 只读目录空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「只读展示 /workflows 与 /workflows/{businessType} 返回的流程模板、版本状态和节点摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('只读展示 /workflows 与 /workflows/{businessType} 返回的流程模板、版本状态和节点摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「返回的流程模板、版本状态和节点摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('返回的流程模板、版本状态和节点摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition 「/workflows 与 /workflows/{businessType} 返回的流程模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByText('/workflows 与 /workflows/{businessType} 返回的流程模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3834 桌面文件存储 catalog 生命周期闭环整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage 「/system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByText('/system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3835 桌面表单配置同屏联动整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「表单对象、字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('表单对象、字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「桌面预览、钉钉 H5 预览和字段属性同屏联动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('桌面预览、钉钉 H5 预览和字段属性同屏联动').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「钉钉 H5 预览和字段属性同屏联动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('钉钉 H5 预览和字段属性同屏联动').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3836 桌面表单配置字段同步与 PC 预览空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「修改字段后会同步桌面预览与钉钉 H5 预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('修改字段后会同步桌面预览与钉钉 H5 预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「同步桌面预览与钉钉 H5 预览，保存草稿前不影响已发布版本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('同步桌面预览与钉钉 H5 预览，保存草稿前不影响已发布版本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「双列布局模拟 PC 工作台表单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('双列布局模拟 PC 工作台表单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3837 桌面流程设计器画布节点库说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「左侧节点库、可视化流程画布、右侧节点属性同屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('左侧节点库、可视化流程画布、右侧节点属性同屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「可视化流程画布、右侧节点属性同屏」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('可视化流程画布、右侧节点属性同屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「左侧节点库、可视化流程画布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('左侧节点库、可视化流程画布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3838 桌面表单配置 H5 遮罩预览说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「单列移动端布局，敏感字段默认遮罩。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('单列移动端布局，敏感字段默认遮罩。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「修改字段后会同步桌面预览与钉钉 H5 预览，保存草稿前不影响已发布版本。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('修改字段后会同步桌面预览与钉钉 H5 预览，保存草稿前不影响已发布版本。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3839 桌面流程设计器拖拽保存草稿说明空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「拖拽左侧节点到画布，移动节点后保存草稿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('拖拽左侧节点到画布，移动节点后保存草稿').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「左侧节点库、可视化流程画布、右侧节点属性同屏；拖拽左侧节点到画布，移动节点后保存草稿。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('左侧节点库、可视化流程画布、右侧节点属性同屏；拖拽左侧节点到画布，移动节点后保存草稿。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「右侧节点属性同屏；拖拽左侧节点到画布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('右侧节点属性同屏；拖拽左侧节点到画布').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3840 桌面流程控制台只读风险整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「只读风险提示：不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环；本页仅复用既有只读查询结果。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('只读风险提示：不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环；本页仅复用既有只读查询结果。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「不代表流程控制闭环；本页仅复用既有只读查询结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('不代表流程控制闭环；本页仅复用既有只读查询结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环；本页仅复用既有只读查询结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('不支持发起/审批/重试/终止/发布/编辑，不代表流程控制闭环；本页仅复用既有只读查询结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3841 桌面 SLA 运行态安全边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「SLA 运行态安全边界：模拟、摘要、超时记录和脱敏导出不修改审批、工作流定义、表单实例、业务单据或任务状态；不发送真实通知，通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('SLA 运行态安全边界：模拟、摘要、超时记录和脱敏导出不修改审批、工作流定义、表单实例、业务单据或任务状态；不发送真实通知，通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「不发送真实通知，通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('不发送真实通知，通知目标、联系方式、变量值、附件引用与导出内容只展示 masked/summary').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config 「模拟、摘要、超时记录和脱敏导出不修改审批、工作流定义、表单实例、业务单据或任务状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByText('模拟、摘要、超时记录和脱敏导出不修改审批、工作流定义、表单实例、业务单据或任务状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3842 桌面交接写边界与未闭环整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「只读边界：本页仅展示交接任务摘要与状态；发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内。真实资产/工单/审批对象转移未闭环，需后续专项处理。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('只读边界：本页仅展示交接任务摘要与状态；发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内。真实资产/工单/审批对象转移未闭环，需后续专项处理。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「写操作不在 V3 只读 catalog 范围内。真实资产/工单/审批对象转移未闭环」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内。真实资产/工单/审批对象转移未闭环').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover 「本页仅展示交接任务摘要与状态；发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByText('本页仅展示交接任务摘要与状态；发起、推进、取消交接等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3843 桌面安全策略 no-login-chain 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「no-login-chain / no-direct-effect：本模块不接登录链路、不直接生效、不改变会话、MFA 或移动端运行状态；runtimeEffect 必须为 false。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('no-login-chain / no-direct-effect：本模块不接登录链路、不直接生效、不改变会话、MFA 或移动端运行状态；runtimeEffect 必须为 false。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不接登录链路、不直接生效、不改变会话、MFA 或移动端运行状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('不接登录链路、不直接生效、不改变会话、MFA 或移动端运行状态').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「不改变会话、MFA 或移动端运行状态；runtimeEffect 必须为 false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('不改变会话、MFA 或移动端运行状态；runtimeEffect 必须为 false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3844 桌面安全策略配置态/审计摘要整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「真实调用 /system-config/security 与 /system-config/security/preview；本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖，也不是 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('真实调用 /system-config/security 与 /system-config/security/preview；本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖，也不是 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy 「/system-config/security/preview；本页只展示配置态、预览态和审计摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByText('/system-config/security/preview；本页只展示配置态、预览态和审计摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3845 桌面流程邮件写边界与零调用风险整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「只读边界：本页仅展示流程节点邮件配置；新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内。真实邮件发送未接入流程平台，存在零业务调用风险。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('只读边界：本页仅展示流程节点邮件配置；新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内。真实邮件发送未接入流程平台，存在零业务调用风险。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「本页仅展示流程节点邮件配置；新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('本页仅展示流程节点邮件配置；新增、编辑、删除、测试发送等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail 「写操作不在 V3 只读 catalog 范围内。真实邮件发送未接入流程平台」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内。真实邮件发送未接入流程平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3846 桌面文档中心写边界与附件留痕整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「只读边界：本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内。附件安全与删除留痕需后续专项处理。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('只读边界：本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内。附件安全与删除留痕需后续专项处理。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「写操作不在 V3 只读 catalog 范围内。附件安全与删除留痕需后续专项处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('写操作不在 V3 只读 catalog 范围内。附件安全与删除留痕需后续专项处理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center 「本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内。附件安全」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByText('本页仅展示文档目录；编辑、发布、归档、删除、附件上传等写操作不在 V3 只读 catalog 范围内。附件安全').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3847 桌面控制台 SLA 联动 9/9 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 只读联动已纳入流程平台 9/9；不支持发起/审批/重试/终止/发布/编辑；仍非 44 项全量覆盖，不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 只读联动已纳入流程平台 9/9；不支持发起/审批/重试/终止/发布/编辑；仍非 44 项全量覆盖，不代表 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「SLA 只读联动已纳入流程平台 9/9」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('SLA 只读联动已纳入流程平台 9/9').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center 「仍非 44 项全量覆盖，不代表 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByText('仍非 44 项全量覆盖，不代表 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3848 桌面部门组织 list/tree 检索整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org 「/depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByText('/depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3849 桌面位置 list/root 检索整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选在前端结果内完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选在前端结果内完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management 「/locations/root 返回的位置，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByText('/locations/root 返回的位置，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3850 桌面供应商 list 检索整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「只读展示 /vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('只读展示 /vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「只读展示 /vendors/list 返回的供应商，搜索与状态筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('只读展示 /vendors/list 返回的供应商，搜索与状态筛选').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management 「/vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByText('/vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3851 桌面技术支持诊断包安全边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「安全边界：诊断包必须脱敏，禁止导出敏感配置原值。本页仅展示工单只读 catalog；创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('安全边界：诊断包必须脱敏，禁止导出敏感配置原值。本页仅展示工单只读 catalog；创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「本页仅展示工单只读 catalog；创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('本页仅展示工单只读 catalog；创建、分配、处理、关闭工单与导出诊断包等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support 「禁止导出敏感配置原值。本页仅展示工单只读 catalog」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByText('禁止导出敏感配置原值。本页仅展示工单只读 catalog').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3852 桌面租户写边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读边界：本页仅展示租户主数据；新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读边界：本页仅展示租户主数据；新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「本页仅展示租户主数据；新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('本页仅展示租户主数据；新建、编辑、停用、启用等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management 「只读边界：本页仅展示租户主数据；新建、编辑、停用、启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByText('只读边界：本页仅展示租户主数据；新建、编辑、停用、启用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3853 桌面导入导出写边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「只读边界：本页仅展示任务历史与脱敏错误摘要；执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('只读边界：本页仅展示任务历史与脱敏错误摘要；执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「本页仅展示任务历史与脱敏错误摘要；执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('本页仅展示任务历史与脱敏错误摘要；执行导入、导出、重试、取消等写操作不在 V3 只读 catalog 范围内').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export 「任务历史与脱敏错误摘要；执行导入、导出、重试、取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByText('任务历史与脱敏错误摘要；执行导入、导出、重试、取消').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3854 桌面编号规则端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3855 桌面编号规则 preview 不持久化整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「read-only numbering rule catalog + no-persistence deterministic preview；预览不会持久化、不刷新缓存、不预留或占用序号。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('read-only numbering rule catalog + no-persistence deterministic preview；预览不会持久化、不刷新缓存、不预留或占用序号。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「no-persistence deterministic preview；预览不会持久化、不刷新缓存、不预留或占用序号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('no-persistence deterministic preview；预览不会持久化、不刷新缓存、不预留或占用序号').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「deterministic preview；预览不会持久化、不刷新缓存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('deterministic preview；预览不会持久化、不刷新缓存').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3856 桌面缓存白名单整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告为普通成功。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告为普通成功。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management 「CacheManager 命名空间，空缓存不会被报告为普通成功」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByText('CacheManager 命名空间，空缓存不会被报告为普通成功').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3857 桌面基础参数 SYSTEM 覆盖整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「真实调用 /system-config/system 与 /system/configs；仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('真实调用 /system-config/system 与 /system/configs；仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「/system/configs；仅覆盖 SYSTEM 分组基础参数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('/system/configs；仅覆盖 SYSTEM 分组基础参数').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3858 桌面基础参数 DEGRADED 预演整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「本页展示基础参数目录、保存、影响预演与缓存刷新结果；预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('本页展示基础参数目录、保存、影响预演与缓存刷新结果；预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「影响预演与缓存刷新结果；预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('影响预演与缓存刷新结果；预演不会持久化，缓存刷新无真实缓存时必须显示 DEGRADED').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params 「保存、影响预演与缓存刷新结果；预演不会持久化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByText('保存、影响预演与缓存刷新结果；预演不会持久化').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3859 桌面岗位 posts 端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「真实调用 /system/posts、/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('真实调用 /system/posts、/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「真实调用 /system/posts、/system/posts/all、/system/posts/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('真实调用 /system/posts、/system/posts/all、/system/posts/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3860 桌面岗位 metadata-only 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「metadata-only post catalog + dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noAssignment=true、noPermissionEffect=true。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('metadata-only post catalog + dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noAssignment=true、noPermissionEffect=true。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「仅代表 system-post-management 已接入真组件；不代表组织权限组、岗位权限 runtime、用户岗位分配或 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('仅代表 system-post-management 已接入真组件；不代表组织权限组、岗位权限 runtime、用户岗位分配或 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management 「不代表组织权限组、岗位权限 runtime、用户岗位分配或 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByText('不代表组织权限组、岗位权限 runtime、用户岗位分配或 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3861 桌面邮件模板端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3862 桌面邮件模板 safe preview 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「仅邮件模板 catalog + 无持久化 safe preview；不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出、不接入流程邮件。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('仅邮件模板 catalog + 无持久化 safe preview；不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出、不接入流程邮件。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「无持久化 safe preview；不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出、不接入流程邮件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('无持久化 safe preview；不发送邮件、不配置 SMTP/邮件网关、不处理邮件日志/重试/导出、不接入流程邮件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不处理邮件日志/重试/导出、不接入流程邮件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不处理邮件日志/重试/导出、不接入流程邮件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3863 桌面通知偏好端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3864 桌面通知偏好无持久化边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读通知偏好目录 + 无持久化偏好决策预览；不保存偏好、不发送通知、不改变真实通知发送决策。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读通知偏好目录 + 无持久化偏好决策预览；不保存偏好、不发送通知、不改变真实通知发送决策。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「无持久化偏好决策预览；不保存偏好、不发送通知、不改变真实通知发送决策」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('无持久化偏好决策预览；不保存偏好、不发送通知、不改变真实通知发送决策').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「只读通知偏好目录 + 无持久化偏好决策预览；不保存偏好」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('只读通知偏好目录 + 无持久化偏好决策预览；不保存偏好').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3865 桌面通知渠道端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「真实调用 /system/channel-configs、/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('真实调用 /system/channel-configs、/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「只读通知渠道目录 + 无持久化、无发送、无外联脱敏预览；不保存渠道、不发送测试消息、不调用外部 webhook。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('只读通知渠道目录 + 无持久化、无发送、无外联脱敏预览；不保存渠道、不发送测试消息、不调用外部 webhook。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3866 桌面流程通知开关端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「read-only notification switch catalog + no-persistence preview + no-send preview；workflowRuntimeEffect=false；不写库、不发送通知、不改变流程运行时。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('read-only notification switch catalog + no-persistence preview + no-send preview；workflowRuntimeEffect=false；不写库、不发送通知、不改变流程运行时。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3867 桌面邮件网关端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3868 桌面邮件网关 metadata-only 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「metadata-only mail gateway catalog + dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('metadata-only mail gateway catalog + dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('dry-run preview；readOnly=true、tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('noPersistence=true、noSend=true、noNetwork=true、credentialExposed=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3869 桌面邮件日志端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3870 桌面邮件日志 redacted 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「邮件日志仅 read-only catalog/detail/biz/meta；redacted=true，tenantScoped=true，no retry/export/send，不保证日志采集链路，不代表邮件子系统完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('邮件日志仅 read-only catalog/detail/biz/meta；redacted=true，tenantScoped=true，no retry/export/send，不保证日志采集链路，不代表邮件子系统完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「redacted=true，tenantScoped=true，no retry/export/send，不保证日志采集链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('redacted=true，tenantScoped=true，no retry/export/send，不保证日志采集链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「catalog/detail/biz/meta；redacted=true，tenantScoped=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('catalog/detail/biz/meta；redacted=true，tenantScoped=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3871 桌面通知模板端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3872 桌面通知模板 safe preview 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「仅通知模板 catalog + 无持久化 safe preview；不发送通知、不配置渠道、不修改偏好、不控制流程通知开关、不接入邮件网关。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('仅通知模板 catalog + 无持久化 safe preview；不发送通知、不配置渠道、不修改偏好、不控制流程通知开关、不接入邮件网关。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「无持久化 safe preview；不发送通知、不配置渠道、不修改偏好、不控制流程通知开关、不接入邮件网关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('无持久化 safe preview；不发送通知、不配置渠道、不修改偏好、不控制流程通知开关、不接入邮件网关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不修改偏好、不控制流程通知开关、不接入邮件网关」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不修改偏好、不控制流程通知开关、不接入邮件网关').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3873 桌面邮件日志不触发管线整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「SMTP/mail gateway、workflow mail 或 notification pipeline」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('SMTP/mail gateway、workflow mail 或 notification pipeline').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3874 桌面邮件网关 credentialExposed 组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「metadata-only mail gateway catalog + dry-run preview；readOnly=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('metadata-only mail gateway catalog + dry-run preview；readOnly=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('tenantScoped=true、noPersistence=true、noSend=true、noNetwork=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「/system/mail-gateways/meta 与 /system/mail-gateways/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('/system/mail-gateways/meta 与 /system/mail-gateways/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3875 桌面邮件日志端点+管线整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta；页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta；页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「/mail-logs/meta；页面不触发重试、导出、发送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('/mail-logs/meta；页面不触发重试、导出、发送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「与 /mail-logs/meta；页面不触发重试」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('与 /mail-logs/meta；页面不触发重试').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3876 桌面自定义字段 CRUD/schema 边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「只读自定义字段定义目录 + 无持久化校验预览；不提供字段定义 CRUD runtime，不完成字段集、分类绑定、资产字段值链路、运行时表单 schema、加密值存储或 secret handling。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('只读自定义字段定义目录 + 无持久化校验预览；不提供字段定义 CRUD runtime，不完成字段集、分类绑定、资产字段值链路、运行时表单 schema、加密值存储或 secret handling。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不提供字段定义 CRUD runtime，不完成字段集、分类绑定、资产字段值链路」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不提供字段定义 CRUD runtime，不完成字段集、分类绑定、资产字段值链路').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「分类绑定、资产字段值链路、运行时表单 schema、加密值存储或 secret handling」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('分类绑定、资产字段值链路、运行时表单 schema、加密值存储或 secret handling').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3877 桌面自定义字段端点族整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3878 桌面字段集分配/绑定预览边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「只读字段集目录 + 无持久化字段分配/分类绑定预览；不提供字段集新增、编辑、删除，不保存字段分配或分类绑定，不影响资产模型、资产字段值或运行时表单 schema。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('只读字段集目录 + 无持久化字段分配/分类绑定预览；不提供字段集新增、编辑、删除，不保存字段分配或分类绑定，不影响资产模型、资产字段值或运行时表单 schema。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不提供字段集新增、编辑、删除，不保存字段分配或分类绑定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不提供字段集新增、编辑、删除，不保存字段分配或分类绑定').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不保存字段分配或分类绑定，不影响资产模型、资产字段值或运行时表单 schema」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不保存字段分配或分类绑定，不影响资产模型、资产字段值或运行时表单 schema').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3879 桌面字段集端点族整句余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「无持久化字段分配/分类绑定预览；不提供字段集新增、编辑、删除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('无持久化字段分配/分类绑定预览；不提供字段集新增、编辑、删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「不影响资产模型、资产字段值或运行时表单 schema」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('不影响资产模型、资产字段值或运行时表单 schema').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3880 桌面审计 GET 族端点整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「真实调用 /audit-logs、/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('真实调用 /audit-logs、/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3881 桌面审计只读写入/导出边界整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「只读审计日志：真实调用 /audit-logs GET 族端点；不提供 POST/PUT/PATCH/DELETE，不采集审计写入，不生成真实导出文件。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('只读审计日志：真实调用 /audit-logs GET 族端点；不提供 POST/PUT/PATCH/DELETE，不采集审计写入，不生成真实导出文件。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不提供 POST/PUT/PATCH/DELETE，不采集审计写入，不生成真实导出文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不提供 POST/PUT/PATCH/DELETE，不采集审计写入，不生成真实导出文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「真实调用 /audit-logs GET 族端点；不提供 POST/PUT/PATCH/DELETE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('真实调用 /audit-logs GET 族端点；不提供 POST/PUT/PATCH/DELETE').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3882 桌面审计 trends/ranking 端点组合空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不采集审计写入，不生成真实导出文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不采集审计写入，不生成真实导出文件').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3883 桌面邮件日志业务查询结果未用 label 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs label「邮件日志业务查询结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByLabel('邮件日志业务查询结果').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「仅调用 /mail-logs/biz 获取同业务下的脱敏日志，不进行重试、重发、导出或发送。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('仅调用 /mail-logs/biz 获取同业务下的脱敏日志，不进行重试、重发、导出或发送。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「获取同业务下的脱敏日志，不进行重试、重发、导出或发送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('获取同业务下的脱敏日志，不进行重试、重发、导出或发送').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3884 桌面通知偏好分类详情未用 label 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences label「通知偏好分类详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByLabel('通知偏好分类详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「仅代表 system-notification-preferences 单模块候选；消息与通知组未全组完成，仍非 44/44，也不是 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('仅代表 system-notification-preferences 单模块候选；消息与通知组未全组完成，仍非 44/44，也不是 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences 「preview 仅返回 wouldReceive、inAppEnabled、emailEnabled、quietWindowMatched、missingPreferences、rejectedInputs、tenantScoped、noPersistence 与 runtimeEffect=false。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByText('preview 仅返回 wouldReceive、inAppEnabled、emailEnabled、quietWindowMatched、missingPreferences、rejectedInputs、tenantScoped、noPersistence 与 runtimeEffect=false。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3885 桌面字段集分类诊断未用 label 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets label「分类字段集诊断」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByLabel('分类字段集诊断').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「仅代表 system-custom-field-sets 的第 30 个真实模块候选；基础资料组未全组完成，未覆盖全部 44 项，也不是 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('仅代表 system-custom-field-sets 的第 30 个真实模块候选；基础资料组未全组完成，未覆盖全部 44 项，也不是 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets 「第 30 个真实模块候选；基础资料组未全组完成，未覆盖全部 44 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByText('第 30 个真实模块候选；基础资料组未全组完成，未覆盖全部 44 项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3886 桌面通知开关业务类型详情未用 label 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch label「流程通知开关业务类型详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByLabel('流程通知开关业务类型详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「仅代表 system-workflow-notification-switch 单模块候选；not flow-platform acceptance，not message-notification group completion，not Workbench V3 full completion，消息与通知组未全组完成，仍非 44/44。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('仅代表 system-workflow-notification-switch 单模块候选；not flow-platform acceptance，not message-notification group completion，not Workbench V3 full completion，消息与通知组未全组完成，仍非 44/44。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「只读，不执行启停、不发送通知、不触发流程运行时。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('只读，不执行启停、不发送通知、不触发流程运行时。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3887 桌面通知渠道脱敏详情未用 label 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels label「通知渠道脱敏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByLabel('通知渠道脱敏详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「不会显示或保存原始地址、签名材料、请求头或消息体。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('不会显示或保存原始地址、签名材料、请求头或消息体。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels 「仅代表 system-notification-channels 单模块候选；消息与通知组未全组完成，仍非 44/44，也不是 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByText('仅代表 system-notification-channels 单模块候选；消息与通知组未全组完成，仍非 44/44，也不是 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3888 桌面编号规则 coverage 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「仅代表 system-numbering-rules 单模块候选；当前不保证并发唯一，未接入资产/工单/流程创建链路，不代表基础资料组完成，不代表 44/44 或 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('仅代表 system-numbering-rules 单模块候选；当前不保证并发唯一，未接入资产/工单/流程创建链路，不代表基础资料组完成，不代表 44/44 或 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「当前不保证并发唯一，未接入资产/工单/流程创建链路，不代表基础资料组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('当前不保证并发唯一，未接入资产/工单/流程创建链路，不代表基础资料组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules 「未接入资产/工单/流程创建链路，不代表基础资料组完成，不代表 44/44 或 Workbench V3 全量完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByText('未接入资产/工单/流程创建链路，不代表基础资料组完成，不代表 44/44 或 Workbench V3 全量完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3889 桌面邮件网关 coverage 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「仅代表 system-mail-gateway 已接入真组件；不代表消息与通知组、邮件子系统或 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('仅代表 system-mail-gateway 已接入真组件；不代表消息与通知组、邮件子系统或 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「不代表消息与通知组、邮件子系统或 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('不代表消息与通知组、邮件子系统或 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway 「已接入真组件；不代表消息与通知组、邮件子系统」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByText('已接入真组件；不代表消息与通知组、邮件子系统').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3890 桌面通知开关/偏好 coverage 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「not flow-platform acceptance，not message-notification group completion，not Workbench V3 full completion」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('not flow-platform acceptance，not message-notification group completion，not Workbench V3 full completion').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「not message-notification group completion，not Workbench V3 full completion，消息与通知组未全组完成，仍非 44/44」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('not message-notification group completion，not Workbench V3 full completion，消息与通知组未全组完成，仍非 44/44').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch 「不执行启停、不发送通知、不触发流程运行时」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByText('不执行启停、不发送通知、不触发流程运行时').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3891 桌面邮件日志 redaction 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「收件人、抄送、密送、发件人、主题、正文、错误、供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('收件人、抄送、密送、发件人、主题、正文、错误、供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「发件人、主题、正文、错误、供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('发件人、主题、正文、错误、供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「仍非 44/44，不代表 Workbench V3 全量完成；消息与通知组未全组完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('仍非 44/44，不代表 Workbench V3 全量完成；消息与通知组未全组完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3892 桌面自定义字段 preview 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「preview 仅按当前租户字段定义校验 required/type/options/regex，返回 valid、missing、rejected、errors、usedFields、tenantScoped、noPersistence 与 runtimeEffect=false；加密字段只展示安全标记，不回显样例原文。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('preview 仅按当前租户字段定义校验 required/type/options/regex，返回 valid、missing、rejected、errors、usedFields、tenantScoped、noPersistence 与 runtimeEffect=false；加密字段只展示安全标记，不回显样例原文。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「返回 valid、missing、rejected、errors、usedFields、tenantScoped、noPersistence 与 runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('返回 valid、missing、rejected、errors、usedFields、tenantScoped、noPersistence 与 runtimeEffect=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不代表字段集完成，不代表资产字段值读写链路完成，不代表基础资料组完成，不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不代表字段集完成，不代表资产字段值读写链路完成，不代表基础资料组完成，不代表 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3893 桌面邮件模板 previewPolicy 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「preview 只返回 HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true。敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('preview 只返回 HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true。敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不代表邮件子系统完成，不代表消息与通知全组完成，不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不代表邮件子系统完成，不代表消息与通知全组完成，不代表 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3894 桌面通知模板 previewPolicy 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「preview 只返回 HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true。敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('preview 只返回 HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true。敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「不代表消息与通知全组完成，不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('不代表消息与通知全组完成，不代表 Workbench V3 全量完成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3895 桌面邮件日志 redaction 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('供应商标识、请求标识、头信息与载荷只展示掩码或诊断摘要').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「不代表 Workbench V3 全量完成；消息与通知组未全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('不代表 Workbench V3 全量完成；消息与通知组未全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs 「主题、正文、错误、供应商标识、请求标识、头信息与载荷」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByText('主题、正文、错误、供应商标识、请求标识、头信息与载荷').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3896 桌面自定义字段完成边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「不代表资产字段值读写链路完成，不代表基础资料组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('不代表资产字段值读写链路完成，不代表基础资料组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「usedFields、tenantScoped、noPersistence 与 runtimeEffect=false」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('usedFields、tenantScoped、noPersistence 与 runtimeEffect=false').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields 「required/type/options/regex，返回 valid、missing、rejected、errors」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByText('required/type/options/regex，返回 valid、missing、rejected、errors').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3897 桌面邮件模板敏感变量拒绝整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('敏感变量名（password/token/secret/apiKey/clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken）会被拒绝。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates 「不代表邮件子系统完成，不代表消息与通知全组完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByText('不代表邮件子系统完成，不代表消息与通知全组完成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3898 桌面通知模板敏感变量拒绝余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('HTML escaped 结果、missingVariables、rejectedVariables、usedVariables 与 nonPersistent=true').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('clientSecret/privateKey/authorization/cookie/credential/accessKey/refreshToken').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates 「password/token/secret/apiKey/clientSecret/privateKey」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByText('password/token/secret/apiKey/clientSecret/privateKey').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3899 桌面审计采集闭环整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「本页不是全局审计采集闭环，不代表全模块审计覆盖、不可篡改合规归档或 SIEM/外部审计平台集成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('本页不是全局审计采集闭环，不代表全模块审计覆盖、不可篡改合规归档或 SIEM/外部审计平台集成。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不是全局审计采集闭环，不代表全模块审计覆盖、不可篡改合规归档」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不是全局审计采集闭环，不代表全模块审计覆盖、不可篡改合规归档').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不可篡改合规归档或 SIEM/外部审计平台集成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不可篡改合规归档或 SIEM/外部审计平台集成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3900 桌面审计导出限制整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「导出能力仅展示脱敏快照与限制说明；不创建 Blob、不触发下载、不创建后台导出任务。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('导出能力仅展示脱敏快照与限制说明；不创建 Blob、不触发下载、不创建后台导出任务。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「仅展示脱敏快照与限制说明；不创建 Blob、不触发下载、不创建后台导出任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('仅展示脱敏快照与限制说明；不创建 Blob、不触发下载、不创建后台导出任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「脱敏快照与限制说明；不创建 Blob、不触发下载」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('脱敏快照与限制说明；不创建 Blob、不触发下载').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3901 桌面表单配置 fail-closed 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「权限 fail-closed；schema 只保留 sections/fields 白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('权限 fail-closed；schema 只保留 sections/fields 白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「权限 fail-closed；schema 只保留 sections/fields 白名单结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('权限 fail-closed；schema 只保留 sections/fields 白名单结构').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3902 桌面流程设计器 fail-closed 整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「发布与版本恢复均要求后端权限 fail-closed、confirmed=true、operatorId、审计原因、影响范围与回滚预案；前端按钮仅作体验辅助。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('发布与版本恢复均要求后端权限 fail-closed、confirmed=true、operatorId、审计原因、影响范围与回滚预案；前端按钮仅作体验辅助。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「confirmed=true、operatorId、审计原因、影响范围与回滚预案；前端按钮仅作体验辅助」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('confirmed=true、operatorId、审计原因、影响范围与回滚预案；前端按钮仅作体验辅助').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「审计原因、影响范围与回滚预案；前端按钮仅作体验辅助」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('审计原因、影响范围与回滚预案；前端按钮仅作体验辅助').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3903 桌面表单配置 fail-closed 余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「剥离 HTML/source/script/on*、javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('剥离 HTML/source/script/on*、javascript URL 和危险 style；敏感字段默认值仅展示遮罩预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「sections/fields 白名单结构，剥离 HTML/source/script/on*」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('sections/fields 白名单结构，剥离 HTML/source/script/on*').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3904 桌面流程设计器回滚预案余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「影响范围与回滚预案；前端按钮仅作体验辅助」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('影响范围与回滚预案；前端按钮仅作体验辅助').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「后端权限 fail-closed、confirmed=true、operatorId、审计原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('后端权限 fail-closed、confirmed=true、operatorId、审计原因').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer 「前端按钮仅作体验辅助」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByText('前端按钮仅作体验辅助').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3905 桌面审计 SIEM 边界余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「全模块审计覆盖、不可篡改合规归档或 SIEM/外部审计平台集成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('全模块审计覆盖、不可篡改合规归档或 SIEM/外部审计平台集成').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不创建后台导出任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不创建后台导出任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log 「不触发下载、不创建后台导出任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByText('不触发下载、不创建后台导出任务').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3906 桌面表单配置遮罩预览余项空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「危险 style；敏感字段默认值仅展示遮罩预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('危险 style；敏感字段默认值仅展示遮罩预览').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「HTML/source/script/on*、javascript URL 和危险 style」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('HTML/source/script/on*、javascript URL 和危险 style').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config 「schema 只保留 sections/fields 白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByText('schema 只保留 sections/fields 白名单结构，剥离 HTML/source/script/on*、javascript URL 和危险 style').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3907 桌面工单新建未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new textbox「标题 *」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('textbox', { name: '标题 *' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new button「提交工单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('button', { name: '提交工单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new link「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('link', { name: '工单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3908 桌面退役新建未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new button「提交退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: '提交退役申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new button「退役管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByRole('button', { name: '退役管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 「审批通过后资产将从在册清单中移除」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('审批通过后资产将从在册清单中移除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3909 桌面验收/风险未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake/new button「创建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/new');
    await expect(page.getByRole('button', { name: '创建验收单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments button「新增评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '新增评估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「风险矩阵」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '风险矩阵' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3910 桌面风险/赔偿未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments heading「风险矩阵（5x5）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '风险矩阵（5x5）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new heading「资产赔偿申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('heading', { name: '资产赔偿申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new heading「损坏详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('heading', { name: '损坏详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3911 桌面报废申请未用 link/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new link「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('link', { name: '资产管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new link「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('link', { name: '资产报废' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new heading「资产报废申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '资产报废申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3912 桌面转移申请未用 link/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new link「资产转移申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('link', { name: '资产转移申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new heading「资产转移申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '资产转移申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new link「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('link', { name: '资产管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3913 桌面清退申请未用 link/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/clearance/new link「仪表板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('link', { name: '仪表板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new heading「资产清退申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('heading', { name: '资产清退申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new heading「清退配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('heading', { name: '清退配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3914 桌面赔偿/清退未用 heading/link 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new heading「赔偿配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('heading', { name: '赔偿配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new heading「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('heading', { name: '确认提交' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new link「资产处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByRole('link', { name: '资产处置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3915 桌面借用 heading/退役余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows/new heading「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/new');
    await expect(page.getByRole('heading', { name: '新建借用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 「请确认退役原因和残值信息无误」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('请确认退役原因和残值信息无误').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 「部门负责人 → 财务总监」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('部门负责人 → 财务总监').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3916 桌面退役/报废未用余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/new 「提交后将启动多级审批流程（部门负责人 → 财务总监）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/new');
    await expect(page.getByText('提交后将启动多级审批流程（部门负责人 → 财务总监）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 「资产报废操作一旦提交审批，所选资产将进入处置流程。提交前请仔细确认报废原因、残值评估及处置方式。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('资产报废操作一旦提交审批，所选资产将进入处置流程。提交前请仔细确认报废原因、残值评估及处置方式。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 「残值评估将影响审批路径，高价值资产需走专项审批。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('残值评估将影响审批路径，高价值资产需走专项审批。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3917 桌面报废/清退/转移未用余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 「变卖处理后资产将从台账中永久移除，此操作不可撤销」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('变卖处理后资产将从台账中永久移除，此操作不可撤销').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 「提交前请确认资产范围和清退原因无误，避免误操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('提交前请确认资产范围和清退原因无误，避免误操作').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 「必填 · 资产当前所属部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('必填 · 资产当前所属部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3918 桌面转移/清退/报废未用余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 「必填 · 资产调拨目标部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('必填 · 资产调拨目标部门').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new heading「确认提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '确认提交' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 「所选资产将进入处置流程。提交前请确认资产范围和清退原因无误，避免误操作。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('所选资产将进入处置流程。提交前请确认资产范围和清退原因无误，避免误操作。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3919 桌面工单/资产/退役未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/new link「首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('link', { name: '首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new link「资产台账」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('link', { name: '资产台账' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement button「新建退役申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('button', { name: '新建退役申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3920 桌面新建资产未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new heading「描述与备注」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '描述与备注' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new heading「父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '父子关系' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new heading「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '新建资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3921 桌面报废/转移未用空资产整句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 「暂无已选资产，点击上方「添加资产」按钮选择需要报废的资产。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('暂无已选资产，点击上方「添加资产」按钮选择需要报废的资产。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 「暂无已选资产，点击上方「添加资产」按钮选择需要调拨的资产。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('暂无已选资产，点击上方「添加资产」按钮选择需要调拨的资产。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new 「标准流程需部门经理 → 资产管理员 → 财务三级审批。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('标准流程需部门经理 → 资产管理员 → 财务三级审批。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3922 桌面转移/预算/重估未用余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 「高优先级申请将跳过非关键审批节点，加速流转。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('高优先级申请将跳过非关键审批节点，加速流转。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets/new 「预算总额 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/new');
    await expect(page.getByText('预算总额 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 「新值 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新值 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3923 桌面报废/赔偿未用金额标签空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new 「预估残值 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('预估残值 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 「原值 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('原值 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 「预估赔偿金额 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('预估赔偿金额 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3924 桌面赔偿/转移未用流程余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/compensation/new 「总赔偿金额 (¥)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/new');
    await expect(page.getByText('总赔偿金额 (¥)').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 「请先发布资产转移流程后再提交审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('请先发布资产转移流程后再提交审批').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 「搜索申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('搜索申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3925 桌面转移未用计数/必填空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new 「已选 0 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('已选 0 项').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 「* 必填」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('* 必填').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement placeholder「2024/05/01 - 2024/05/31」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByPlaceholder('2024/05/01 - 2024/05/31').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3926 桌面退役列表未用空态余句', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 「日期范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('日期范围').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 「暂无待审核」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('暂无待审核').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 「0 项申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('0 项申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3927 桌面退役列表未用指标余句空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement 「共 0 条记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('共 0 条记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 「回收率 0%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('回收率 0%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement 「占全部退役的 0%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByText('占全部退役的 0%').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3928 桌面借用/领用/验收未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows button「新建借用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('button', { name: '新建借用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「新建领用单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '新建领用单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake button「新建验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '新建验收单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3929 桌面保险/备件未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances button「新增保险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '新增保险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts button「新增备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: '新增备件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances button「重置筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '重置筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3930 桌面 GIS 未用 heading/整句/空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis heading「资产分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: '资产分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「GIS 定位数据来源于资产台账，通过"关联已有资产"为资产标注坐标，或在资产表单中直接填写经纬度。点击地图标记可跳转资产详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('GIS 定位数据来源于资产台账，通过"关联已有资产"为资产标注坐标，或在资产表单中直接填写经纬度。点击地图标记可跳转资产详情。').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis 空态「暂无资产定位数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByText('暂无资产定位数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3931 桌面重估/风险/平面图未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations heading「资产价值调整记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '资产价值调整记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「风险评估记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '风险评估记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans heading「2D/3D 平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '2D/3D 平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3932 桌面平面图/许可证/制造商未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans heading「平面图列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '平面图列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses heading「软件许可证管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '软件许可证管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers heading「制造商管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '制造商管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3933 桌面故障/ABC/平面图未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes heading「暂无故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '暂无故障代码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification heading「ABC 分类管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: 'ABC 分类管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans heading「请选择平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '请选择平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3934 桌面转移/工单未用 heading/余句 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new heading「调拨方向」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '调拨方向' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new heading「故障代码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByRole('heading', { name: '故障代码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 空态「关联故障代码（三级选择：现象→原因→措施）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/new');
    await expect(page.getByText('关联故障代码（三级选择：现象→原因→措施）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3935 桌面 ABC 分类未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification heading「A 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: 'A 类资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification heading「B 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: 'B 类资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification heading「C 类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: 'C 类资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3936 桌面 ABC/循环盘点未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification button「导出报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('button', { name: '导出报告' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count button「新增规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByRole('button', { name: '新增规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count button「触发A类盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByRole('button', { name: '触发A类盘点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3937 桌面位置/SAM 未用 button/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/locations button「全部展开」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('button', { name: '全部展开' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations button「全部折叠」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('button', { name: '全部折叠' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「SAM 合规管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: 'SAM 合规管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3938 桌面 ABC/SAM 未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/abc-classification heading「未分类资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: '未分类资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification heading「资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByRole('heading', { name: '资产列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam button「触发合规扫描」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('button', { name: '触发合规扫描' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3939 桌面 SAM 未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「合规率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '合规率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「许可类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '许可类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「席位使用率 TOP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '席位使用率 TOP' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3940 桌面 SAM/ABC/循环盘点未用余句 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「扫描历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '扫描历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「A 类（高价值）需月度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('A 类（高价值）需月度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/cycle-count 空态「A类（关键资产）：月度盘点 | B类（重要资产）：季度盘点 | C类（一般资产）：年度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('A类（关键资产）：月度盘点 | B类（重要资产）：季度盘点 | C类（一般资产）：年度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3941 桌面安全检查/报表构建器未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config heading「安全检查表模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '安全检查表模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config heading「模板列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '模板列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「自定义报表构建器」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '自定义报表构建器' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3942 桌面报表构建器/导入导出未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder heading「可用字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '可用字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「数据预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '数据预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export heading「资产批量导入导出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByRole('heading', { name: '资产批量导入导出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3943 桌面 ABC/循环盘点未用余句 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/cycle-count 空态「暂无循环盘点规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('暂无循环盘点规则').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「B 类（中价值）需季度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('B 类（中价值）需季度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/abc-classification 空态「C 类（低价值）需年度盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('C 类（低价值）需年度盘点').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3944 桌面设备/分类/TCO 未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment heading「设备列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '设备列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「分类结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '分类结构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「TCO 全生命周期成本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: 'TCO 全生命周期成本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3945 桌面 TCO 未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「资产 TCO 构成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '资产 TCO 构成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「TCO 趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: 'TCO 趋势 (近12个月)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「部门 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '部门 TCO 排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3946 桌面 TCO/健康评分未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「分类 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '分类 TCO 排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「评分分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '评分分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「评分区间分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '评分区间分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3947 桌面安全历史/风险矩阵未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history heading「安全检查历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '安全检查历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history heading「执行记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '执行记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix heading「风险矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByRole('heading', { name: '风险矩阵配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3948 桌面报表/健康评分未用 button/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports button「导出 CSV」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: '导出 CSV' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports button「导出 PDF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: '导出 PDF' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「不健康资产 Top 20」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '不健康资产 Top 20' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3949 桌面定时报表/可靠性未用 button/heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports/scheduled button「新建定时报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('button', { name: '新建定时报表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「MTBF/MTTR 趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: 'MTBF/MTTR 趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「资产可靠性排名」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '资产可靠性排名' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3950 桌面 SAM/健康评分未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「风险告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '风险告警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「暂无高风险项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '暂无高风险项' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health heading「不健康资产列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('heading', { name: '不健康资产列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3951 桌面 GIS/能耗/预算未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/gis heading「暂无资产定位数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: '暂无资产定位数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/energy heading「暂无能耗数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByRole('heading', { name: '暂无能耗数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「2026 年度预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '2026 年度预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3952 桌面平面图/SAM/导入导出未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans heading「暂无平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '暂无平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '暂无数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export button「下载导入模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByRole('button', { name: '下载导入模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3953 桌面分类/平面图未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories heading「暂无分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '暂无分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans button「前往 GIS 地图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('button', { name: '前往 GIS 地图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「选择分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '选择分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3954 桌面资产列表/报表中心未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets heading「暂无资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '暂无资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets button「新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '新建资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports heading「资产汇总表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '资产汇总表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3955 桌面报表中心未用卡片 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports heading「资产分类统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '资产分类统计' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports heading「资产状态分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '资产状态分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports heading「部门资产排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '部门资产排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3956 桌面报表中心未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports heading「资产增长趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '资产增长趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports button「查看」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: '查看' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports button「资产报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('button', { name: '资产报表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3957 桌面合同未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts button「维保合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '维保合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts button「采购合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '采购合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts button「租赁合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '租赁合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3958 桌面合同未用类型/状态筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts button「服务合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '服务合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts button「生效中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '生效中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts button「已到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('button', { name: '已到期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3959 桌面闲置资产未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle button「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '待处理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle button「公告中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '公告中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle button「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '待审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3960 桌面闲置/采购未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle button「已认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '已认领' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle button「历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('button', { name: '历史记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders button「已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '已审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3961 桌面采购/领用未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/purchase-orders button「部分收货」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('button', { name: '部分收货' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '已驳回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「已签收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '已签收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3962 桌面领用未用状态/类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments button「待归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '待归还' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「已归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '已归还' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「长期领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '长期领用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3963 桌面领用未用类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments button「短期借用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '短期借用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「归还入库」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '归还入库' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments button「调拨转移」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('button', { name: '调拨转移' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3964 桌面借用/维保未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows button「已借出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('button', { name: '已借出' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows button「已逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('button', { name: '已逾期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance button「计划中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '计划中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3965 桌面维保未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance button「执行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '执行中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance button「逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '逾期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance button「已完成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('button', { name: '已完成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3966 桌面退役/重估未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement button「异常/已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('button', { name: '异常/已驳回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations button「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('button', { name: '已通过' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations button「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('button', { name: '已拒绝' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3967 桌面折旧未用方法筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation button「全部方法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '全部方法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation button「直线法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '直线法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation button「双倍余额递减法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '双倍余额递减法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3968 桌面折旧/处置未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation button「年数总和法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '年数总和法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation button「工作量法」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('button', { name: '工作量法' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals heading「资产清退列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '资产清退列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3969 桌面处置未用类型/筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals button「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '资产清退' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals button「资产赔偿」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '资产赔偿' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals button「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '已拒绝' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3970 桌面保险未用状态/类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances button「已过期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '已过期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances button「财产险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '财产险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances button「责任险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '责任险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3971 桌面保险/设备未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances button「车险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '车险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment button「正常运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '正常运行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment button「维修中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '维修中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3972 桌面设备未用状态筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment button「闲置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '闲置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment button「正常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '正常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment button「即将到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('button', { name: '即将到期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3973 桌面合同/处置/审批未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts heading「暂无数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '暂无数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals button「工单管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '工单管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals button「资产报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '资产报废' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3974 桌面审批/盘点周期未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals button「工单申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '工单申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles button「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('button', { name: '已计划' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles button「已暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('button', { name: '已暂停' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3975 桌面检验未用类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections button「年度检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '年度检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections button「定期检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '定期检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections button「专项检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '专项检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3976 桌面检验未用结果筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections button「全部结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '全部结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections button「不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '不通过' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections button「附条件通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('button', { name: '附条件通过' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3977 桌面备件/盘点周期未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts button「启用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: '启用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts button「停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('button', { name: '停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles button「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('button', { name: '进行中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3978 桌面预算未用类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets button「采购」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('button', { name: '采购' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets button「维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('button', { name: '维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets button「运营」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('button', { name: '运营' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3979 桌面预算/验收未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets button「预算列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('button', { name: '预算列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake button「待质检」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '待质检' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake button「质检中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '质检中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3980 桌面验收未用状态筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake button「部分验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '部分验收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake button「已验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('button', { name: '已验收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances button「已取消」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('button', { name: '已取消' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3981 桌面审批未用类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals button「资产调拨」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '资产调拨' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals button「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '资产清退' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals button「全部类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '全部类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3982 桌面资产未用状态筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets button「待退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '待退役' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets button「已退役」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '已退役' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets button「已报废」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '已报废' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3983 桌面资产/维保计划未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets button「已清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '已清退' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets button「在用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '在用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans button「暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('button', { name: '暂停' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3984 桌面通知未用类型筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications button「系统通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: '系统通知' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications button「审批通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: '审批通知' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications button「预警通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: '预警通知' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3985 桌面盘点未用摘要/筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory button「决策摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('button', { name: '决策摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory button「进度趋势」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('button', { name: '进度趋势' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory button「已提交」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('button', { name: '已提交' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3986 桌面资产/许可证未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets button「导出全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '导出全部' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets button「高级筛选」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('button', { name: '高级筛选' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses button「有效」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('button', { name: '有效' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3987 桌面检验记录/审计/处置未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/inspection-records button「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('button', { name: '待检验' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit heading「操作趋势（近7天）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '操作趋势（近7天）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals button「报废转让」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('button', { name: '报废转让' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3988 桌面 TCO 未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「资产 TCO 构成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '资产 TCO 构成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「TCO 趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: 'TCO 趋势 (近12个月)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「部门 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '部门 TCO 排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3989 桌面 TCO/GIS 未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「分类 TCO 排行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '分类 TCO 排行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis button「资产定位管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('button', { name: '资产定位管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses button「暂停」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('button', { name: '暂停' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3990 桌面履历/盘点报告未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1/timeline heading「资产履历时间线」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1/timeline');
    await expect(page.getByRole('heading', { name: '资产履历时间线' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1/timeline heading「暂无履历记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1/timeline');
    await expect(page.getByRole('heading', { name: '暂无履历记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report button「返回盘点列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report');
    await expect(page.getByRole('button', { name: '返回盘点列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3991 桌面个人/盘点周期未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile heading「系统管理员」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByRole('heading', { name: '系统管理员' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 heading「周期信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: '周期信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 heading「统计信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: '统计信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3992 桌面盘点周期未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 heading「操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: '操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 heading「盘点任务列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: '盘点任务列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 button「导出 PDF 报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('button', { name: '导出 PDF 报告' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3993 桌面盘点/健康/维保未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 button「分配任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('button', { name: '分配任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health button「批量计算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('button', { name: '批量计算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans heading「暂无维保计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('heading', { name: '暂无维保计划' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3994 桌面流程中心未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows heading「流程信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '流程信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「流程总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '流程总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「草稿中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '草稿中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3995 桌面流程中心未用 KPI/新建 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows button「已发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '已发布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「已停用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '已停用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「新建流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '新建流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3996 桌面流程中心未用回滚/入口 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows heading「版本历史与回滚」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '版本历史与回滚' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「返回资产处置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '返回资产处置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「创建并发布默认流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '创建并发布默认流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3997 桌面流程/审批未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows heading「资产退役流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '资产退役流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals button「待我审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '待我审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals button「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('button', { name: '我发起的' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3998 桌面风险未用等级筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments button「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '全部' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments button「重大」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '重大' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments button「高危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '高危' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q3999 桌面风险/通知未用筛选 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/risk-assessments button「中危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '中危' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments button「低危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('button', { name: '低危' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications button「全部」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('button', { name: '全部' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4000 桌面流程定义未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows heading「资产转移流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '资产转移流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows heading「资产清退流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '资产清退流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows heading「资产报废转让流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '资产报废转让流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4001 桌面流程/周期/盘点报告未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows heading「资产赔偿流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '资产赔偿流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles/1 heading「E2E周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: 'E2E周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report heading「未指定盘点任务 ID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report');
    await expect(page.getByRole('heading', { name: '未指定盘点任务 ID' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4002 桌面流程中心未用分页 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows button「首页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '首页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「末页」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '末页' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「全部业务流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '全部业务流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4003 桌面流程中心未用 KPI 副文案 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflows button「可用于发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '可用于发起' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「待完善发布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '待完善发布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows button「暂停发起」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('button', { name: '暂停发起' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4004 桌面工单详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1 heading「工单详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByRole('heading', { name: '工单详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 heading「工单信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByRole('heading', { name: '工单信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 heading「审批记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByRole('heading', { name: '审批记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4005 桌面工单验收/详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workorders/1/acceptance heading「工单验收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1/acceptance');
    await expect(page.getByRole('heading', { name: '工单验收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1/acceptance heading「验收操作」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1/acceptance');
    await expect(page.getByRole('heading', { name: '验收操作' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/1 heading「暂无审批记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/1');
    await expect(page.getByRole('heading', { name: '暂无审批记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4006 桌面安全/借用/领用未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config button「禁用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('button', { name: '禁用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1 heading「借用详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/1');
    await expect(page.getByRole('heading', { name: '借用详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 heading「领用单详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('heading', { name: '领用单详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4007 桌面权限/个人未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/forbidden?reason=roles_missing heading「用户信息不完整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByRole('heading', { name: '用户信息不完整' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/forbidden?reason=roles_missing button「重新登录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/forbidden?reason=roles_missing');
    await expect(page.getByRole('button', { name: '重新登录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/profile heading「账号资料」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByRole('heading', { name: '账号资料' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4008 桌面个人/流程/退役未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/profile heading「角色与权限明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/profile');
    await expect(page.getByRole('heading', { name: '角色与权限明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows heading「当前选中流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '当前选中流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 heading「退役申请 #1」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '退役申请 #1' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4009 桌面退役详情未用分区 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 heading「关联资产摘要」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '关联资产摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 heading「退役原因 & 申请信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '退役原因 & 申请信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 heading「历史记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '历史记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4010 桌面退役详情未用审批 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 heading「审批状态」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '审批状态' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/2 heading「退役申请 #2」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/2');
    await expect(page.getByRole('heading', { name: '退役申请 #2' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/3 heading「退役申请 #3」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/3');
    await expect(page.getByRole('heading', { name: '退役申请 #3' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4011 桌面退役详情未用空态 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement/1 heading「暂无关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '暂无关联资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/1 heading「暂无审批历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/1');
    await expect(page.getByRole('heading', { name: '暂无审批历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/2 heading「暂无关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/2');
    await expect(page.getByRole('heading', { name: '暂无关联资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4012 桌面盘点报告未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/smart-report/1 heading「盘点报告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '盘点报告' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report/1 heading「盘点进度概览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '盘点进度概览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report/1 heading「差异分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '差异分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4013 桌面盘点报告/任务未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/smart-report/1 heading「资产状态概览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '资产状态概览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report/1 heading「差异资产明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '差异资产明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 heading「盘点进度实时监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '盘点进度实时监控' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4014 桌面盘点任务未用分区 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/tasks/1 heading「盘点范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '盘点范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 heading「盘点结果对比」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '盘点结果对比' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 heading「资产明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '资产明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4015 桌面盘点任务未用任务名 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/tasks/1 heading「办公室盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '办公室盘点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/INV-001 heading「E2E盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/INV-001');
    await expect(page.getByRole('heading', { name: 'E2E盘点任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/RFID-1 heading「RFID扫描任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/RFID-1');
    await expect(page.getByRole('heading', { name: 'RFID扫描任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4016 桌面 RFID 扫描未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/scan/1 heading「最近扫描记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('heading', { name: '最近扫描记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 heading「差异列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('heading', { name: '差异列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 heading「任务概览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('heading', { name: '任务概览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4017 桌面处置详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/1 heading「资产处置详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/1');
    await expect(page.getByRole('heading', { name: '资产处置详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/1 heading「处置详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/1');
    await expect(page.getByRole('heading', { name: '处置详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/1 heading「风险说明」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/1');
    await expect(page.getByRole('heading', { name: '风险说明' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4018 桌面处置/盘点未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/1 heading「审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/1');
    await expect(page.getByRole('heading', { name: '审批流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 button「待盘」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('button', { name: '待盘' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 button「已盘」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('button', { name: '已盘' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4019 桌面盘点/流程表单未用角色空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/tasks/1 button「异常」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('button', { name: '异常' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT heading「RETIREMENT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByRole('heading', { name: 'RETIREMENT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT button「直接发起申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByRole('button', { name: '直接发起申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4020 桌面流程表单未用业务编码 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_TRANSFER heading「ASSET_TRANSFER」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByRole('heading', { name: 'ASSET_TRANSFER' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_CLEARANCE heading「ASSET_CLEARANCE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByRole('heading', { name: 'ASSET_CLEARANCE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_SCRAP heading「ASSET_SCRAP」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByRole('heading', { name: 'ASSET_SCRAP' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4021 桌面流程/领用未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ASSET_COMPENSATION heading「ASSET_COMPENSATION」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByRole('heading', { name: 'ASSET_COMPENSATION' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RETIREMENT heading「该流程尚未配置表单源码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByRole('heading', { name: '该流程尚未配置表单源码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 heading「审批信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('heading', { name: '审批信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4022 桌面领用详情未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assignments/1 heading「备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('heading', { name: '备注信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 heading「暂无审批信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('heading', { name: '暂无审批信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1 heading「暂无备注信息」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/1');
    await expect(page.getByRole('heading', { name: '暂无备注信息' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4023 桌面 RFID 扫描未用 button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/scan/1 button「开始扫描」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('button', { name: '开始扫描' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 button「提交盘点」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('button', { name: '提交盘点' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 button「批量确认」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('button', { name: '批量确认' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4024 桌面 RFID/流程未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/scan/1 heading「暂无扫描记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('heading', { name: '暂无扫描记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 heading「暂无差异」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('heading', { name: '暂无差异' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/scan/1 link「返回盘点列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/scan/1');
    await expect(page.getByRole('link', { name: '返回盘点列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4025 桌面审计详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 heading「操作详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('heading', { name: '操作详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 heading「操作上下文」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('heading', { name: '操作上下文' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 heading「操作轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('heading', { name: '操作轨迹' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4026 桌面审计详情未用 heading/button 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit/1 heading「关联资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('heading', { name: '关联资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 heading「审计日志详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('heading', { name: '审计日志详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 button「导出日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('button', { name: '导出日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4027 桌面资产详情未用分区 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1 heading「变更记录 Timeline」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '变更记录 Timeline' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「主附属关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '主附属关系' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「资产履历」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '资产履历' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4028 桌面资产/审计未用 heading/link 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1 heading「图片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '图片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '文件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit/1 link「审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/1');
    await expect(page.getByRole('link', { name: '审计日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4029 桌面资产详情未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/1 heading「暂无折旧数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '暂无折旧数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「暂无关联子资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '暂无关联子资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「暂无变更记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '暂无变更记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4030 桌面资产/流程未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/UNKNOWN heading「UNKNOWN」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/UNKNOWN');
    await expect(page.getByRole('heading', { name: 'UNKNOWN' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FOOBAR heading「FOOBAR」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FOOBAR');
    await expect(page.getByRole('heading', { name: 'FOOBAR' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/CUSTOM heading「CUSTOM」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/CUSTOM');
    await expect(page.getByRole('heading', { name: 'CUSTOM' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4031 桌面检验详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/1 heading「检验详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('heading', { name: '检验详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 heading「检验照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('heading', { name: '检验照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 heading「检验历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('heading', { name: '检验历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4032 桌面检验详情未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/1 heading「暂无检验照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('heading', { name: '暂无检验照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1 heading「暂无检验历史」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1');
    await expect(page.getByRole('heading', { name: '暂无检验历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ALPHA heading「ALPHA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ALPHA');
    await expect(page.getByRole('heading', { name: 'ALPHA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4033 桌面保险详情未用 heading 空态', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances/1 heading「保险详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('heading', { name: '保险详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/1 heading「暂无理赔记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/1');
    await expect(page.getByRole('heading', { name: '暂无理赔记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/DEMO heading「DEMO」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/DEMO');
    await expect(page.getByRole('heading', { name: 'DEMO' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4034 桌面健康评分/测试结果未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health heading「维度评分明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('heading', { name: '维度评分明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health heading「暂无维度评分明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('heading', { name: '暂无维度评分明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/test-results heading「加载测试结果失败」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/test-results');
    await expect(page.getByRole('heading', { name: '加载测试结果失败' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4035 桌面备件详情未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts/1 heading「暂无领用记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts/1');
    await expect(page.getByRole('heading', { name: '暂无领用记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/BETA heading「BETA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/BETA');
    await expect(page.getByRole('heading', { name: 'BETA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/GAMMA heading「GAMMA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/GAMMA');
    await expect(page.getByRole('heading', { name: 'GAMMA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4036 桌面 TCO 未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「请先查询资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '请先查询资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「请输入部门ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '请输入部门ID查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco heading「请输入分类ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '请输入分类ID查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4037 桌面 TCO/验收未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/tco heading「请输入资产ID查询」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/tco');
    await expect(page.getByRole('heading', { name: '请输入资产ID查询' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 heading「暂无入库资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '暂无入库资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/1 heading「暂无检查项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/1');
    await expect(page.getByRole('heading', { name: '暂无检查项' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4038 桌面健康/资产详情未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/asset-health heading「暂无不健康资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('heading', { name: '暂无不健康资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「暂无成本数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '暂无成本数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「暂无趋势数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '暂无趋势数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4039 桌面可靠性 KPI 未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability heading「MTBF」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: 'MTBF', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「MTTR」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: 'MTTR', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「可用性」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '可用性', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4040 桌面可靠性 KPI 副文案未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability heading「月均故障率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '月均故障率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「平均故障间隔」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '平均故障间隔' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「平均修复时间」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '平均修复时间' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4041 桌面可靠性空态/可用率未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/reliability heading「设备可用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '设备可用率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「总故障 0 次」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '总故障 0 次' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability heading「暂无排名数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/reliability');
    await expect(page.getByRole('heading', { name: '暂无排名数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4042 桌面盘点/通知未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/tasks/1 heading「暂无盘盈记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '暂无盘盈记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/1 heading「暂无盘亏记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByRole('heading', { name: '暂无盘亏记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/notifications heading「暂无通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('heading', { name: '暂无通知' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4043 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/DELTA heading「DELTA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/DELTA');
    await expect(page.getByRole('heading', { name: 'DELTA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EPSILON heading「EPSILON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EPSILON');
    await expect(page.getByRole('heading', { name: 'EPSILON' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ZETA heading「ZETA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ZETA');
    await expect(page.getByRole('heading', { name: 'ZETA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4044 桌面资产新建/检验上传未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets/new heading「请先保存资产后再管理父子关系」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '请先保存资产后再管理父子关系' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/new heading「资产主附属关系管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '资产主附属关系管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload heading「暂无已上传的照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '暂无已上传的照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4045 桌面通知/资产详情未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications heading「所有通知都会显示在这里」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('heading', { name: '所有通知都会显示在这里' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「成本构成」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '成本构成' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「成本明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '成本明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4046 桌面位置空态与流程表单未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/locations heading「暂无位置数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('heading', { name: '暂无位置数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/OMEGA heading「OMEGA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/OMEGA');
    await expect(page.getByRole('heading', { name: 'OMEGA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/KAPPA heading「KAPPA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/KAPPA');
    await expect(page.getByRole('heading', { name: 'KAPPA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4047 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIGMA heading「SIGMA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIGMA');
    await expect(page.getByRole('heading', { name: 'SIGMA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THETA heading「THETA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THETA');
    await expect(page.getByRole('heading', { name: 'THETA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/LAMBDA heading「LAMBDA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/LAMBDA');
    await expect(page.getByRole('heading', { name: 'LAMBDA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4048 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/IOTA heading「IOTA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/IOTA');
    await expect(page.getByRole('heading', { name: 'IOTA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/RHO heading「RHO」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/RHO');
    await expect(page.getByRole('heading', { name: 'RHO' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TAU heading「TAU」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TAU');
    await expect(page.getByRole('heading', { name: 'TAU' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4049 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/PHI heading「PHI」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/PHI');
    await expect(page.getByRole('heading', { name: 'PHI' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/CHI heading「CHI」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/CHI');
    await expect(page.getByRole('heading', { name: 'CHI' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/PSI heading「PSI」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/PSI');
    await expect(page.getByRole('heading', { name: 'PSI' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4050 桌面定时报表/风险矩阵未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/reports/scheduled heading「暂无定时报表配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('heading', { name: '暂无定时报表配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled heading「点击"新建定时报表"开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('heading', { name: '点击"新建定时报表"开始创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix heading「暂无矩阵配置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByRole('heading', { name: '暂无矩阵配置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4051 桌面盘点周期/供应商/报废未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles/1 heading「暂无盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByRole('heading', { name: '暂无盘点任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors heading「暂无供应商数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '暂无供应商数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/scrap/new heading「暂无已选资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '暂无已选资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4052 桌面审计/分析未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit heading「暂无审计日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '暂无审计日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「暂无分类数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '暂无分类数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「暂无部门排行数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '暂无部门排行数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4053 桌面分析统计与流程表单未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics heading「数据来自资产台账、维保记录与审批流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '数据来自资产台账、维保记录与审批流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NU heading「NU」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NU');
    await expect(page.getByRole('heading', { name: 'NU' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/XI heading「XI」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/XI');
    await expect(page.getByRole('heading', { name: 'XI' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4054 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ETA heading「ETA」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ETA');
    await expect(page.getByRole('heading', { name: 'ETA' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/MU heading「MU」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/MU');
    await expect(page.getByRole('heading', { name: 'MU' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/UPSILON heading「UPSILON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/UPSILON');
    await expect(page.getByRole('heading', { name: 'UPSILON' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4055 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/PI heading「PI」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/PI');
    await expect(page.getByRole('heading', { name: 'PI' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/OMICRON heading「OMICRON」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/OMICRON');
    await expect(page.getByRole('heading', { name: 'OMICRON' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SANDBOX heading「SANDBOX」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SANDBOX');
    await expect(page.getByRole('heading', { name: 'SANDBOX' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4056 桌面检验上传/资产趋势未用角色', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload heading「检验照片上传」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '检验照片上传' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload button「检验记录列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('button', { name: '检验记录列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/1 heading「趋势 (近12个月)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '趋势 (近12个月)', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4057 桌面检验编号与流程表单未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload heading「检验编号」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '检验编号' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/PREVIEW heading「PREVIEW」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/PREVIEW');
    await expect(page.getByRole('heading', { name: 'PREVIEW' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/STAGING heading「STAGING」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/STAGING');
    await expect(page.getByRole('heading', { name: 'STAGING' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4058 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/PILOT heading「PILOT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/PILOT');
    await expect(page.getByRole('heading', { name: 'PILOT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HELLO heading「HELLO」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HELLO');
    await expect(page.getByRole('heading', { name: 'HELLO' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/WORLD heading「WORLD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/WORLD');
    await expect(page.getByRole('heading', { name: 'WORLD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4059 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SAMPLE heading「SAMPLE」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SAMPLE');
    await expect(page.getByRole('heading', { name: 'SAMPLE' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TRIAL heading「TRIAL」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TRIAL');
    await expect(page.getByRole('heading', { name: 'TRIAL' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/DRAFT heading「DRAFT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/DRAFT');
    await expect(page.getByRole('heading', { name: 'DRAFT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4060 桌面检验上传分区未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections/1/upload heading「上传新照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '上传新照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload heading「已上传照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '已上传照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload heading「共 0 张」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('heading', { name: '共 0 张' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4061 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/MOCK heading「MOCK」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/MOCK');
    await expect(page.getByRole('heading', { name: 'MOCK' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/UNUSED heading「UNUSED」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/UNUSED');
    await expect(page.getByRole('heading', { name: 'UNUSED' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NEXT heading「NEXT」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NEXT');
    await expect(page.getByRole('heading', { name: 'NEXT' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4062 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/FIRST heading「FIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIRST');
    await expect(page.getByRole('heading', { name: 'FIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SECOND heading「SECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SECOND');
    await expect(page.getByRole('heading', { name: 'SECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRD heading「THIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRD');
    await expect(page.getByRole('heading', { name: 'THIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4063 桌面报废/调拨未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/scrap/new heading「选择需要报废的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByRole('heading', { name: '选择需要报废的资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new heading「选择需要调拨的资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '选择需要调拨的资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FOURTH heading「FOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FOURTH');
    await expect(page.getByRole('heading', { name: 'FOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4064 桌面盘点报告/故障代码未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory/smart-report/1 heading「暂无差异资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '暂无差异资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「点击「新增根节点」创建第一级故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '点击「新增根节点」创建第一级故障现象' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「现象 → 原因 → 措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '现象 → 原因 → 措施' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4065 桌面平面图/检验上传未用角色', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/floorplans heading「请从左侧选择一个平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '请从左侧选择一个平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/upload button「选择照片」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/1/upload');
    await expect(page.getByRole('button', { name: '选择照片' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory/smart-report/1 heading「前0条」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/smart-report/1');
    await expect(page.getByRole('heading', { name: '前0条' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4066 桌面调拨/清退未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals/transfer/new heading「已选 0 项」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByRole('heading', { name: '已选 0 项' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new heading「暂未选择资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByRole('heading', { name: '暂未选择资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTH heading「FIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTH');
    await expect(page.getByRole('heading', { name: 'FIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4067 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXTH heading「SIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTH');
    await expect(page.getByRole('heading', { name: 'SIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTH heading「SEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTH heading「EIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTH');
    await expect(page.getByRole('heading', { name: 'EIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4068 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINTH heading「NINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINTH');
    await expect(page.getByRole('heading', { name: 'NINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTH heading「TENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTH');
    await expect(page.getByRole('heading', { name: 'TENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ELEVENTH heading「ELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ELEVENTH');
    await expect(page.getByRole('heading', { name: 'ELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4069 桌面报表构建器/分析 KPI 未用角色', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder heading「点击"生成预览"查看数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '点击"生成预览"查看数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「生成预览」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '生成预览' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「资产总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '资产总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4070 桌面分析 KPI 未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics heading「资产总值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '资产总值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「本月维保」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '本月维保' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「待审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '待审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4071 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWELFTH heading「TWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWELFTH');
    await expect(page.getByRole('heading', { name: 'TWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTEENTH heading「THIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTEENTH');
    await expect(page.getByRole('heading', { name: 'THIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FOURTEENTH heading「FOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FOURTEENTH');
    await expect(page.getByRole('heading', { name: 'FOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4072 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/FIFTEENTH heading「FIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTEENTH');
    await expect(page.getByRole('heading', { name: 'FIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTEENTH heading「SIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTEENTH');
    await expect(page.getByRole('heading', { name: 'SIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTEENTH heading「SEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4073 桌面分析页未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics heading「处置统计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '处置统计' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「多维资产趋势、分类结构与运营指标分析」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '多维资产趋势、分类结构与运营指标分析' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「数据范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '数据范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4074 桌面报表构建器未用角色', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「保存报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '保存报表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「已选字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '已选字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTH heading「EIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4075 桌面报表构建器未用配置 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder heading「报表名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '报表名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「报表类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '报表类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「图表类型」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '图表类型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4076 桌面报表构建器未用图表类型 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「柱状图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '柱状图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「折线图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '折线图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「饼图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '饼图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4077 桌面报表构建器未用字段 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「表格」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '表格' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「资产编码」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '资产编码' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「资产名称」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '资产名称' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4078 桌面报表构建器未用资产字段 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「原值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '原值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '净值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「购入日期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '购入日期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4079 桌面报表构建器未用运维字段 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「工单数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '工单数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「保养次数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '保养次数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「故障次数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '故障次数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4080 桌面报表构建器未用财务字段 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「折旧金额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '折旧金额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「维保费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '维保费用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「能耗费用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '能耗费用' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4081 桌面导入导出未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「总成本」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '总成本' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export heading「将 .xlsx 文件拖到此处，或点击选择文件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByRole('heading', { name: '将 .xlsx 文件拖到此处，或点击选择文件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export heading「支持 .xlsx 格式，文件大小不超过 10MB」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/import-export');
    await expect(page.getByRole('heading', { name: '支持 .xlsx 格式，文件大小不超过 10MB' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4082 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETEENTH heading「NINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETEENTH');
    await expect(page.getByRole('heading', { name: 'NINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTIETH heading「TWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTIETH');
    await expect(page.getByRole('heading', { name: 'TWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYFIRST heading「TWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4083 桌面报表构建器未用说明与字段 button', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/report-builder button「使用部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '使用部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「通过拖拽选择字段，快速创建自定义报表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '通过拖拽选择字段，快速创建自定义报表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「资产分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '资产分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4084 桌面分析 KPI 未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics heading「在用 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '在用 0' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「共 0 次」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '共 0 次' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「已退役 0」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '已退役 0' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4085 桌面分析/报表构建器未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics heading「净值¥0万」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '净值¥0万' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「运维」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '运维', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder heading「财务」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('heading', { name: '财务', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4086 桌面部门管理未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts heading「请选择一个部门查看详情」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '请选择一个部门查看详情' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts heading「点击左侧组织架构树中的节点即可查看」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '点击左侧组织架构树中的节点即可查看' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts heading「支持展开/收起子级」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '支持展开/收起子级' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4087 桌面部门/分析未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/depts heading「支持搜索过滤」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '支持搜索过滤' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「0 项」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '0 项', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics heading「¥0总值万」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '¥0总值万' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4088 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWENTYSECOND heading「TWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYTHIRD heading「TWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYFOURTH heading「TWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4089 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWENTYFIFTH heading「TWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYSIXTH heading「TWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYSEVENTH heading「TWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4090 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWENTYEIGHTH heading「TWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWENTYNINTH heading「TWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTIETH heading「THIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTIETH');
    await expect(page.getByRole('heading', { name: 'THIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4091 桌面位置管理未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/locations heading「资产存放位置的层级管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('heading', { name: '资产存放位置的层级管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations heading「总位置数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('heading', { name: '总位置数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/locations heading「顶级位置」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/locations');
    await expect(page.getByRole('heading', { name: '顶级位置' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4092 桌面供应商未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors heading「合作供应商信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '合作供应商信息维护' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors heading「全部供应商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '全部供应商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendors heading「合作中」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '合作中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4093 桌面供应商/报表构建器未用角色', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/vendors heading「已停用」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/vendors');
    await expect(page.getByRole('heading', { name: '已停用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「状态」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '状态', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/report-builder button「位置」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/report-builder');
    await expect(page.getByRole('button', { name: '位置', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4094 桌面位置工作台未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「暂无根位置。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '暂无根位置。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYFIRST heading「THIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'THIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYSECOND heading「THIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'THIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4095 桌面部门工作台未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「暂无根部门。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '暂无根部门。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYTHIRD heading「THIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYFOURTH heading「THIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4096 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THIRTYFIFTH heading「THIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYSIXTH heading「THIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYSEVENTH heading「THIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4097 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THIRTYEIGHTH heading「THIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THIRTYNINTH heading「THIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'THIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTIETH heading「FORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTIETH');
    await expect(page.getByRole('heading', { name: 'FORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4098 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/FORTYFIRST heading「FORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYFIRST');
    await expect(page.getByRole('heading', { name: 'FORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYSECOND heading「FORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYSECOND');
    await expect(page.getByRole('heading', { name: 'FORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYTHIRD heading「FORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'FORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4099 桌面制造商/许可证/合同未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers heading「设备制造商信息维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '设备制造商信息维护' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses heading「许可证席位跟踪与到期管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '许可证席位跟踪与到期管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts heading「合同信息维护与到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '合同信息维护与到期预警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4100 桌面分类/型号/邮件模板未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories heading「管理资产分类层级结构」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '管理资产分类层级结构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models heading「管理资产品类的标准模板与规格定义」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByRole('heading', { name: '管理资产品类的标准模板与规格定义' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「请选择一个模板查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '请选择一个模板查看详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4101 桌面字段集/邮件日志/岗位未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「请选择一个字段集查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '请选择一个字段集查看详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「请选择一条日志查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '请选择一条日志查看脱敏详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「请选择一条岗位元数据查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: '请选择一条岗位元数据查看详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4102 桌面邮件网关未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「请选择一条邮件网关元数据查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '请选择一条邮件网关元数据查看脱敏详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYFOURTH heading「FORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'FORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYFIFTH heading「FORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'FORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4103 桌面自定义字段未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「请选择一个字段定义查看详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '请选择一个字段定义查看详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYSIXTH heading「FORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'FORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYSEVENTH heading「FORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'FORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4104 桌面审计日志未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「请选择一条审计日志查看脱敏详情。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '请选择一条审计日志查看脱敏详情。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYEIGHTH heading「FORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'FORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FORTYNINTH heading「FORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FORTYNINTH');
    await expect(page.getByRole('heading', { name: 'FORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4105 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/FIFTIETH heading「FIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTIETH');
    await expect(page.getByRole('heading', { name: 'FIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYFIRST heading「FIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'FIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYSECOND heading「FIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'FIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4106 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/FIFTYTHIRD heading「FIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'FIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYFOURTH heading「FIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'FIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYFIFTH heading「FIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'FIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4107 桌面 SLA 未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「暂无 SLA 策略。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '暂无 SLA 策略。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「暂无超时记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '暂无超时记录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYSIXTH heading「FIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'FIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4108 桌面审批规则未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「暂无审批规则。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '暂无审批规则。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「暂无冲突检测结果。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '暂无冲突检测结果。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYSEVENTH heading「FIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'FIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4109 桌面流程定义未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「暂无可展示的流程摘要。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '暂无可展示的流程摘要。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYEIGHTH heading「FIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'FIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/FIFTYNINTH heading「FIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/FIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'FIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4110 桌面待办字段未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「暂无待办字段配置。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '暂无待办字段配置。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTIETH heading「SIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTIETH');
    await expect(page.getByRole('heading', { name: 'SIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYFIRST heading「SIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'SIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4111 桌面表单存储未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「暂无表单实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '暂无表单实例。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYSECOND heading「SIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'SIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYTHIRD heading「SIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4112 桌面角色权限未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「暂无权限库存。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '暂无权限库存。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYFOURTH heading「SIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYFIFTH heading「SIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4113 桌面运行监控未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「暂无节点耗时异常。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '暂无节点耗时异常。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「暂无异常轨迹。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '暂无异常轨迹。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYSIXTH heading「SIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4114 桌面分类/设计器/表单配置未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「暂无分类树节点。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '暂无分类树节点。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYSEVENTH heading「SIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYEIGHTH heading「SIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4115 桌面流程设计器未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「暂无发布/回滚审计版本。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '暂无发布/回滚审计版本。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXTYNINTH heading「SIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'SIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTIETH heading「SEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4116 桌面表单配置未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「暂无发布/停用/回滚审计版本。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '暂无发布/停用/回滚审计版本。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYFIRST heading「SEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYSECOND heading「SEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4117 桌面邮件/通知模板未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「暂无邮件模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '暂无邮件模板。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates heading「暂无通知模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('heading', { name: '暂无通知模板。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYTHIRD heading「SEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4118 桌面同步规则/接口未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sync-rules heading「暂无同步规则。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByRole('heading', { name: '暂无同步规则。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYFOURTH heading「SEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYFIFTH heading「SEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4119 桌面接口管理未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces heading「暂无接口，请通过 V3 接口创建。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByRole('heading', { name: '暂无接口，请通过 V3 接口创建。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYSIXTH heading「SEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYSEVENTH heading「SEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4120 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENTYEIGHTH heading「SEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENTYNINTH heading「SEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTIETH heading「EIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTIETH');
    await expect(page.getByRole('heading', { name: 'EIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4121 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/EIGHTYFIRST heading「EIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'EIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYSECOND heading「EIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'EIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYTHIRD heading「EIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'EIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4122 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/EIGHTYFOURTH heading「EIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYFIFTH heading「EIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYSIXTH heading「EIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4123 桌面通知偏好未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences heading「暂无通知偏好。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('heading', { name: '暂无通知偏好。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYSEVENTH heading「EIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYEIGHTH heading「EIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4124 桌面编号规则未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules heading「暂无编号规则；空态不代表序列分配、并发唯一或创建链路闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('heading', { name: '暂无编号规则；空态不代表序列分配、并发唯一或创建链路闭环。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTYNINTH heading「EIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'EIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETIETH heading「NINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETIETH');
    await expect(page.getByRole('heading', { name: 'NINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4125 桌面 Webhook 未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-webhook-config heading「暂无 Webhook 配置，请通过 V3 创建配置。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('heading', { name: '暂无 Webhook 配置，请通过 V3 创建配置。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYFIRST heading「NINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYFIRST');
    await expect(page.getByRole('heading', { name: 'NINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYSECOND heading「NINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYSECOND');
    await expect(page.getByRole('heading', { name: 'NINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4126 桌面通知渠道未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-channels heading「暂无通知渠道配置；空态不代表真实发送链闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('heading', { name: '暂无通知渠道配置；空态不代表真实发送链闭环。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYTHIRD heading「NINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'NINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYFOURTH heading「NINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'NINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4127 桌面外部系统未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems heading「暂无外部系统，请通过 V3 创建目录项。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('heading', { name: '暂无外部系统，请通过 V3 创建目录项。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYFIFTH heading「NINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'NINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYSIXTH heading「NINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'NINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4128 桌面字段映射未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping heading「暂无字段映射。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByRole('heading', { name: '暂无字段映射。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYSEVENTH heading「NINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'NINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYEIGHTH heading「NINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'NINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4129 桌面缓存管理未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management heading「暂无可管理缓存命名空间。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByRole('heading', { name: '暂无可管理缓存命名空间。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETYNINTH heading「NINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETYNINTH');
    await expect(page.getByRole('heading', { name: 'NINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTH heading「HUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4130 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFIRST heading「HUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSECOND heading「HUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRD heading「HUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4131 桌面许可证未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses heading「总许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '总许可证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses heading「有效许可证」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '有效许可证' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/licenses heading「即将到期(30天)」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '即将到期(30天)' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4132 桌面许可证/合同未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/licenses heading「到期预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/licenses');
    await expect(page.getByRole('heading', { name: '到期预警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts heading「全部合同」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '全部合同' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts heading「生效中」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '生效中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4133 桌面合同/制造商未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/contracts heading「即将到期」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '即将到期', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/contracts heading「30天内到期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/contracts');
    await expect(page.getByRole('heading', { name: '30天内到期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers heading「全部制造商」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '全部制造商' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4134 桌面制造商未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/manufacturers heading「正常」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '正常', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers heading「停用」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '停用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/manufacturers heading「有官网」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/manufacturers');
    await expect(page.getByRole('heading', { name: '有官网' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4135 桌面自定义字段未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「暂无自定义字段定义。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '暂无自定义字段定义。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFOURTH heading「HUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTH heading「HUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4136 桌面字段集/基础参数/分类未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「暂无字段集。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '暂无字段集。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「暂无 SYSTEM 基础参数，请通过 V3 新增。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: '暂无 SYSTEM 基础参数，请通过 V3 新增。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「暂无资产分类数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '暂无资产分类数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4137 桌面邮件日志/岗位/网关未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「暂无可展示的邮件日志；这不代表日志采集链路已完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '暂无可展示的邮件日志；这不代表日志采集链路已完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「暂无可展示的岗位元数据；这不代表组织权限组完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: '暂无可展示的岗位元数据；这不代表组织权限组完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「暂无可展示的邮件网关元数据；这不代表邮件子系统完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '暂无可展示的邮件网关元数据；这不代表邮件子系统完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4138 桌面流程通知开关未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch heading「暂无流程通知开关；空态不代表通知发送链或流程平台闭环。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('heading', { name: '暂无流程通知开关；空态不代表通知发送链或流程平台闭环。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTH heading「HUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTH heading「HUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4139 桌面资产分类未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories heading「总分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '总分类数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '根分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「子分类数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '子分类数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4140 桌面分类/安全检查未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/categories heading「当前选中」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '当前选中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config heading「全部模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '全部模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history heading「全部执行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '全部执行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4141 桌面安全检查/风险评估未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history heading「已完成」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '已完成', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「全部评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '全部评估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「重大风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '重大风险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4142 桌面流程控制台未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「暂无符合条件的流程模板。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '暂无符合条件的流程模板。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「暂无符合条件的运行实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '暂无符合条件的运行实例。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「暂无 SLA 超时记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '暂无 SLA 超时记录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4143 桌面运行监控未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「暂无审批实例。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '暂无审批实例。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「待处理数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '待处理数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「总实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '总实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4144 桌面审计日志未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「暂无审计日志。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '暂无审计日志。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTH heading「HUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINTH heading「HUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4145 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFIFTEENTH heading「HUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTENTH heading「HUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDELEVENTH heading「HUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4146 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTWELFTH heading「HUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTEENTH heading「HUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFOURTEENTH heading「HUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4147 桌面流程控制台未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「流程模板总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '流程模板总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「已配置模板」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '已配置模板' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「运行实例总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '运行实例总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4148 桌面流程控制台未用 SLA heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「SLA 风险」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: 'SLA 风险', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「SLA 策略」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: 'SLA 策略', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTEENTH heading「HUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4149 桌面运行监控未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「当前页实例」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '当前页实例' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「SLA 超时」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: 'SLA 超时', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「SLA 预警」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: 'SLA 预警', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4150 桌面运行监控未用轨迹 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「高风险轨迹」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '高风险轨迹' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTEENTH heading「HUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTEENTH heading「HUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4151 桌面安全检查未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/config heading「已启用」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '已启用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config heading「已禁用」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '已禁用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/history heading「通过」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '通过', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4152 桌面安全检查/风险评估未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history heading「不通过」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '不通过', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「高危风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '高危风险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「中/低危」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '中/低危' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4153 桌面审计日志未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「命中日志」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '命中日志' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「统计总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '统计总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「操作类型」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '操作类型', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4154 桌面 SLA 配置未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「策略总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '策略总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「启用策略」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '启用策略' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「运行风险」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '运行风险' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4155 桌面角色权限未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「角色数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '角色数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「权限库存」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '权限库存', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「角色-权限绑定数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '角色-权限绑定数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4156 桌面数据权限未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「角色总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '角色总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「全部数据（ALL）」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '全部数据（ALL）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「已收紧范围」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '已收紧范围' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4157 桌面文件存储未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「附件总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '附件总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「总容量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '总容量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「业务类型」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '业务类型', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4158 桌面租户管理未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「租户总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '租户总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「正常租户」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '正常租户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「当前过滤结果」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '当前过滤结果' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4159 桌面技术支持/文档中心未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「工单总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '工单总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「待处理/处理中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '待处理/处理中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「文档总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '文档总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4160 桌面文档/导入导出未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「已发布」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '已发布', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '任务总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「失败任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '失败任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4161 桌面交接/流程邮件未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「交接任务总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '交接任务总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「待处理/进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '待处理/进行中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail heading「配置总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('heading', { name: '配置总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4162 桌面角色/数据权限/文件存储余项 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「未绑定权限」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '未绑定权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「自定义（CUSTOM）」', async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '自定义（CUSTOM）' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「文件类型」exact', async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '文件类型', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4163 桌面菜单权限未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「权限域数量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '权限域数量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「权限编码库存」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '权限编码库存' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「已绑定权限」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '已绑定权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4164 桌面 SAM 未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「总许可数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '总许可数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「合规」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '合规', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「超用」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '超用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4165 桌面 SAM/采购未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「闲置」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '闲置', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「已过期」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '已过期', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders heading「总采购单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('heading', { name: '总采购单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4166 桌面采购/型号/数据权限未用 heading', () => {
  test('/purchase-orders heading「已审批」exact', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('heading', { name: '已审批', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-models heading「全部模型」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-models');
    await expect(page.getByRole('heading', { name: '全部模型' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「边界提示」', async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '边界提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4167 桌面租户/表单存储未用 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「只读提示」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '只读提示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「字段摘要」', async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '字段摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「附件摘要」', async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '附件摘要' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4168 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDNINETEENTH heading「HUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTIETH heading「HUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYFIRST heading「HUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4169 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTWENTYSECOND heading「HUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYTHIRD heading「HUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYFOURTH heading「HUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4170 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTWENTYFIFTH heading「HUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYSIXTH heading「HUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYSEVENTH heading「HUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4171 桌面重要设备未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/equipment heading「总设备数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '总设备数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「维保中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '维保中' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「正常运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '正常运行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4172 桌面重要设备/表单存储未用 heading', () => {
  test('/equipment heading「设备状态:」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '设备状态:' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「维保状态:」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '维保状态:' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「暂无字段摘要。」', async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '暂无字段摘要。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4173 桌面表单存储未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「暂无附件引用。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '暂无附件引用。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYEIGHTH heading「HUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTWENTYNINTH heading「HUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4174 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTHIRTIETH heading「HUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYFIRST heading「HUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYSECOND heading「HUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4175 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTHIRTYTHIRD heading「HUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYFOURTH heading「HUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYFIFTH heading「HUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4176 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTHIRTYSIXTH heading「HUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYSEVENTH heading「HUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDTHIRTYEIGHTH heading「HUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4177 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDTHIRTYNINTH heading「HUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTIETH heading「HUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYFIRST heading「HUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4178 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFORTYSECOND heading「HUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYTHIRD heading「HUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYFOURTH heading「HUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4179 桌面闲置资产未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle heading「闲置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '闲置总量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle heading「已发布公告」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '已发布公告' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle heading「待审批认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '待审批认领' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4180 桌面闲置/安全检查未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle heading「已完成认领」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '已完成认领' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/idle heading「闲置资产公告发布与认领流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '闲置资产公告发布与认领流程管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/config heading「配置检查表模板与检查项清单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/config');
    await expect(page.getByRole('heading', { name: '配置检查表模板与检查项清单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4181 桌面安全历史/风险/设备未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/safety-checklists/history heading「查看安全检查执行记录与结果明细」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/history');
    await expect(page.getByRole('heading', { name: '查看安全检查执行记录与结果明细' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments heading「5x5 风险评估热力图与评估记录管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-assessments');
    await expect(page.getByRole('heading', { name: '5x5 风险评估热力图与评估记录管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「关键生产设备维保追踪与状态监控」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '关键生产设备维保追踪与状态监控' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4182 桌面 SAM/采购/GIS 未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「软件许可合规扫描与审计仪表盘」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '软件许可合规扫描与审计仪表盘' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/purchase-orders heading「采购订单创建、审批与收货全流程管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/purchase-orders');
    await expect(page.getByRole('heading', { name: '采购订单创建、审批与收货全流程管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/gis heading「资产地理位置分布可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: '资产地理位置分布可视化' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4183 桌面平面图/表单配置未用 heading', () => {
  test('/floorplans heading「资产位置可视化 · 空间联动」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '资产位置可视化 · 空间联动' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「双列布局模拟 PC 工作台表单。」', async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '双列布局模拟 PC 工作台表单。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「单列移动端布局，敏感字段默认遮罩。」', async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '单列移动端布局，敏感字段默认遮罩。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4184 桌面通知中心未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/notifications heading「共 0 条通知」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/notifications');
    await expect(page.getByRole('heading', { name: '共 0 条通知' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYFIFTH heading「HUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYSIXTH heading「HUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4185 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFORTYSEVENTH heading「HUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYEIGHTH heading「HUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFORTYNINTH heading「HUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4186 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFIFTIETH heading「HUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYFIRST heading「HUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYSECOND heading「HUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4187 桌面验收/维保计划/定时报表未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/intake heading「管理资产入库验收全流程」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('heading', { name: '管理资产入库验收全流程' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance/plans heading「周期性维保计划配置与管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance/plans');
    await expect(page.getByRole('heading', { name: '周期性维保计划配置与管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports/scheduled heading「管理定时报表调度和邮件推送」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports/scheduled');
    await expect(page.getByRole('heading', { name: '管理定时报表调度和邮件推送' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4188 桌面健康评分未用副文案/KPI heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health heading「多维度资产健康度评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '多维度资产健康度评估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「平均健康分」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '平均健康分' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「健康资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '健康资产', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4189 桌面健康评分 KPI/资产健康未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/analytics/health heading「警告资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '警告资产', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health heading「危险资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '危险资产', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/asset-health heading「基于年龄、维修频率、故障率、利用率、折旧进度的多维度评估」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/asset-health');
    await expect(page.getByRole('heading', { name: '基于年龄、维修频率、故障率、利用率、折旧进度的多维度评估' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4190 桌面审计/风险矩阵/流程中心未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/audit heading「按最近 7 天汇总操作趋势、风险事件和操作人分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '按最近 7 天汇总操作趋势、风险事件和操作人分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-matrix heading「管理风险评估的概率维度、严重度维度和等级映射规则」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/risk-matrix');
    await expect(page.getByRole('heading', { name: '管理风险评估的概率维度、严重度维度和等级映射规则' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflows heading「集中维护审批流程、发布状态、版本快照和业务入口。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '集中维护审批流程、发布状态、版本快照和业务入口。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4191 桌面盘点周期/退役/报表中心未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles heading「管理库存盘点计划与执行周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '管理库存盘点计划与执行周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「管理资产生命周期终止、处置流程和退役记录。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '管理资产生命周期终止、处置流程和退役记录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/reports heading「预定义报表查看与数据可视化」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '预定义报表查看与数据可视化' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4192 桌面检验列表/记录未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections heading「管理设备检验记录与年检计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('heading', { name: '管理设备检验记录与年检计划' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records heading「创建、编辑和追踪设备检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('heading', { name: '创建、编辑和追踪设备检验记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYTHIRD heading「HUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4193 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFIFTYFOURTH heading「HUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYFIFTH heading「HUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYSIXTH heading「HUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4194 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDFIFTYSEVENTH heading「HUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYEIGHTH heading「HUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDFIFTYNINTH heading「HUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4195 桌面履历/表单配置/租户未用副文案 heading', () => {
  test('/assets/1/timeline heading「该资产暂无任何履历事件」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/assets/1/timeline');
    await expect(page.getByRole('heading', { name: '该资产暂无任何履历事件' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-form-config heading「表单对象、字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动。」', async ({ page }) => {
    await page.route('**/api/**', mockFormDefinitionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-config');
    await expect(page.getByRole('heading', { name: '表单对象、字段列表、桌面预览、钉钉 H5 预览和字段属性同屏联动。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「只读展示 /tenants 与 /tenants/meta 返回的租户主数据、套餐与状态。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '只读展示 /tenants 与 /tenants/meta 返回的租户主数据、套餐与状态。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4196 桌面技术支持/导入导出/交接未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「只读展示技术支持工单、优先级、处理状态与诊断包脱敏标记。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '只读展示技术支持工单、优先级、处理状态与诊断包脱敏标记。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「只读展示导入导出任务历史、错误报告摘要与只读边界。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '只读展示导入导出任务历史、错误报告摘要与只读边界。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「只读展示交接任务摘要、状态记录与未闭环风险提示。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '只读展示交接任务摘要、状态记录与未闭环风险提示。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4197 桌面文档/流程邮件/资产分类未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「只读展示文档标题、分类、版本、发布状态与附件计数。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '只读展示文档标题、分类、版本、发布状态与附件计数。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail heading「只读展示流程节点邮件配置、触发事件、模板与收件人范围。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('heading', { name: '只读展示流程节点邮件配置、触发事件、模板与收件人范围。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-asset-category heading「只读展示分类列表与分类树，搜索仅调用 /categories/list 查询能力。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-asset-category');
    await expect(page.getByRole('heading', { name: '只读展示分类列表与分类树，搜索仅调用 /categories/list 查询能力。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4198 桌面文件存储/角色/菜单未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「只读展示 /system/file-storage/attachments/catalog 返回的附件元数据、统计摘要与筛选结果。」', async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '只读展示 /system/file-storage/attachments/catalog 返回的附件元数据、统计摘要与筛选结果。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录、权限库存与绑定数量。」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '只读展示 /system/role-permissions/catalog 返回的角色-权限绑定目录、权限库存与绑定数量。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「只读展示 /system/role-permissions/catalog 的权限编码库存，按权限域聚合为菜单权限覆盖视图。」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '只读展示 /system/role-permissions/catalog 的权限编码库存，按权限域聚合为菜单权限覆盖视图。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4199 桌面数据权限/缓存/字段映射未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「展示并收紧角色 dataScope；CUSTOM 部门规则仍未接入。」', async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '展示并收紧角色 dataScope；CUSTOM 部门规则仍未接入。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-cache-management heading「只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告为普通成功。」', async ({ page }) => {
    await page.route('**/api/**', mockCacheNamespacesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-cache-management');
    await expect(page.getByRole('heading', { name: '只管理应用内白名单 CacheManager 命名空间，空缓存不会被报告为普通成功。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-field-mapping heading「转换表达式仅允许 trim(value)、upper(value)、lower(value)，预览不写库、不外呼。」', async ({ page }) => {
    await page.route('**/api/**', mockSystemFieldMappingsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-field-mapping');
    await expect(page.getByRole('heading', { name: '转换表达式仅允许 trim(value)、upper(value)、lower(value)，预览不写库、不外呼。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4200 桌面 Webhook/接口/外部系统未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-webhook-config heading「只加载 /system/webhook-configs 配置，配置校验仅做 config-only 验证，未触发真实外部调用。」', async ({ page }) => {
    await page.route('**/api/**', mockWebhookConfigsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-webhook-config');
    await expect(page.getByRole('heading', { name: '只加载 /system/webhook-configs 配置，配置校验仅做 config-only 验证，未触发真实外部调用。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-interfaces heading「只加载接口管理数据，配置校验仅验证元数据，未触发真实外部调用。」', async ({ page }) => {
    await page.route('**/api/**', mockSystemInterfacesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-interfaces');
    await expect(page.getByRole('heading', { name: '只加载接口管理数据，配置校验仅验证元数据，未触发真实外部调用。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-external-systems heading「真实调用 /system/external-systems；只展示目录、状态、认证掩码和 config-only 校验结果，未触发真实外部调用。」', async ({ page }) => {
    await page.route('**/api/**', mockExternalSystemsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-external-systems');
    await expect(page.getByRole('heading', { name: '真实调用 /system/external-systems；只展示目录、状态、认证掩码和 config-only 校验结果，未触发真实外部调用。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4201 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSIXTIETH heading「HUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYFIRST heading「HUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYSECOND heading「HUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4202 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSIXTYTHIRD heading「HUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYFOURTH heading「HUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYFIFTH heading「HUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4203 桌面运行监控/SLA/审计未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-runtime-monitor heading「只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records 返回的审批实例、SLA 风险摘要和超时记录。」', async ({ page }) => {
    await page.route('**/api/**', mockRuntimeMonitorEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-runtime-monitor');
    await expect(page.getByRole('heading', { name: '只读展示 /approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records 返回的审批实例、SLA 风险摘要和超时记录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-sla-config heading「真实调用 /sla-config 策略列表、详情、更新、启停、simulate、runtime-summary、timeout-records 与 export 接口。」', async ({ page }) => {
    await page.route('**/api/**', mockSlaConfigEmptyCatalog);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sla-config');
    await expect(page.getByRole('heading', { name: '真实调用 /sla-config 策略列表、详情、更新、启停、simulate、runtime-summary、timeout-records 与 export 接口。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-audit-log heading「真实调用 /audit-logs、/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta。」', async ({ page }) => {
    await page.route('**/api/**', mockAuditLogsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-audit-log');
    await expect(page.getByRole('heading', { name: '真实调用 /audit-logs、/audit-logs/{id}、/audit-logs/stats、/audit-logs/trends、/audit-logs/action-type-distribution、/audit-logs/operator-ranking 与 /audit-logs/meta。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4204 桌面同步规则/审批规则/流程定义未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-sync-rules heading「dryRun 默认 true；只保留 dry-run、单条日志重试、只读队列摘要，批处理队列消费入口不开放。」', async ({ page }) => {
    await page.route('**/api/**', mockSystemSyncRulesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-sync-rules');
    await expect(page.getByRole('heading', { name: 'dryRun 默认 true；只保留 dry-run、单条日志重试、只读队列摘要，批处理队列消费入口不开放。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-approval-rules heading「真实调用 /approval-rules 列表、详情、创建、更新、启用、停用、模拟与冲突检测接口。」', async ({ page }) => {
    await page.route('**/api/**', mockApprovalRulesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-approval-rules');
    await expect(page.getByRole('heading', { name: '真实调用 /approval-rules 列表、详情、创建、更新、启用、停用、模拟与冲突检测接口。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「只读展示 /workflows 与 /workflows/{businessType} 返回的流程模板、版本状态和节点摘要。」', async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '只读展示 /workflows 与 /workflows/{businessType} 返回的流程模板、版本状态和节点摘要。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4205 桌面待办字段/位置/部门未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-todo-fields heading「真实调用 /todo-fields 列表、保存、排序、角色覆盖、默认恢复与预览接口。」', async ({ page }) => {
    await page.route('**/api/**', mockTodoFieldsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-todo-fields');
    await expect(page.getByRole('heading', { name: '真实调用 /todo-fields 列表、保存、排序、角色覆盖、默认恢复与预览接口。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选在前端结果内完成。」', async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '只读展示 /locations/list 与 /locations/root 返回的位置，搜索与状态筛选在前端结果内完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成。」', async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '只读展示 /depts/list 与 /depts/tree 返回的组织部门，搜索调用列表接口，状态筛选在前端结果内完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4206 桌面用户/供应商/控制台未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-user-management heading「只读展示 /user-management/list 返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('heading', { name: '只读展示 /user-management/list 返回的用户，搜索调用后端 keyword，状态筛选在前端结果内完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management heading「只读展示 /vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成。」', async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('heading', { name: '只读展示 /vendors/list 返回的供应商，搜索与状态筛选在前端结果内完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-settings-command-center heading「只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records，展示流程模板、运行实例、SLA 风险摘要与超时记录。」', async ({ page }) => {
    await page.route('**/api/**', mockCommandCenterEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-settings-command-center');
    await expect(page.getByRole('heading', { name: '只读聚合 /workflows、/approvals/list、/approvals/pending/count、/sla-config/runtime-summary 与 /sla-config/timeout-records，展示流程模板、运行实例、SLA 风险摘要与超时记录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4207 桌面表单存储/设计器/安全策略未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-form-storage heading「真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕、附件引用与导出脱敏接口。」', async ({ page }) => {
    await page.route('**/api/**', mockFormStorageEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-form-storage');
    await expect(page.getByRole('heading', { name: '真实调用 /form-storage 列表、详情、创建、更新、归档、删除留痕、附件引用与导出脱敏接口。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-designer heading「真实调用 /workflows、/workflows/{businessType}/designer/draft、/designer/validate、/publish 与 /versions/{version}/rollback。」', async ({ page }) => {
    await page.route('**/api/**', mockFlowDesignerEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-designer');
    await expect(page.getByRole('heading', { name: '真实调用 /workflows、/workflows/{businessType}/designer/draft、/designer/validate、/publish 与 /versions/{version}/rollback。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-security-policy heading「真实调用 /system-config/security 与 /system-config/security/preview；本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖，也不是 Workbench V3 全量完成。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-security-policy');
    await expect(page.getByRole('heading', { name: '真实调用 /system-config/security 与 /system-config/security/preview；本页只展示配置态、预览态和审计摘要，仍非 44 项全量覆盖，也不是 Workbench V3 全量完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4208 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSIXTYSIXTH heading「HUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYSEVENTH heading「HUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSIXTYEIGHTH heading「HUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4209 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSIXTYNINTH heading「HUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTIETH heading「HUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYFIRST heading「HUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4210 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSEVENTYSECOND heading「HUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYTHIRD heading「HUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYFOURTH heading「HUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4211 桌面邮件网关/日志/通知开关未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-mail-gateway heading「真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-gateway');
    await expect(page.getByRole('heading', { name: '真实调用 /system/mail-gateways、/system/mail-gateways/{id}、/system/mail-gateways/meta 与 /system/mail-gateways/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-logs heading「真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta；页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-logs');
    await expect(page.getByRole('heading', { name: '真实调用 /mail-logs/list、/mail-logs/{id}、/mail-logs/biz 与 /mail-logs/meta；页面不触发重试、导出、发送、SMTP/mail gateway、workflow mail 或 notification pipeline。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch heading「真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockNotificationSwitchesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-notification-switch');
    await expect(page.getByRole('heading', { name: '真实调用 /notification-switches/list、/notification-switches/biz-type/{bizType}、/notification-switches/meta 与 /notification-switches/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4212 桌面自定义字段/岗位/字段集未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-custom-fields heading「真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockCustomFieldsEmptyCatalog);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-fields');
    await expect(page.getByRole('heading', { name: '真实调用 /system/custom-fields、/system/custom-fields/all、/system/custom-fields/{id}、/system/custom-fields/meta 与 /system/custom-fields/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-post-management heading「真实调用 /system/posts、/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-post-management');
    await expect(page.getByRole('heading', { name: '真实调用 /system/posts、/system/posts/all、/system/posts/{id}、/system/posts/meta 与 /system/posts/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-custom-field-sets heading「真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-custom-field-sets');
    await expect(page.getByRole('heading', { name: '真实调用 /system/custom-fieldsets、/system/custom-fieldsets/all、/system/custom-fieldsets/{id}、/system/custom-fieldsets/{id}/fields、/system/custom-fieldsets/by-category/{categoryId}、/system/custom-fieldsets/meta 与 /system/custom-fieldsets/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4213 桌面渠道/偏好/编号规则未用副文案 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-notification-channels heading「真实调用 /system/channel-configs、/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockNotificationChannelsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-channels');
    await expect(page.getByRole('heading', { name: '真实调用 /system/channel-configs、/system/channel-configs/{id}、/system/channel-configs/meta 与 /system/channel-configs/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-preferences heading「真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockNotificationPreferencesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-preferences');
    await expect(page.getByRole('heading', { name: '真实调用 /notification-preferences、/notification-preferences/{category}、/notification-preferences/meta 与 /notification-preferences/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-numbering-rules heading「真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.route('**/numbering-rules*', mockNumberingRulesEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-numbering-rules');
    await expect(page.getByRole('heading', { name: '真实调用 /numbering-rules、/numbering-rules/{ruleKey}、/numbering-rules/meta 与 /numbering-rules/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4214 桌面邮件模板/通知模板/基础参数未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-mail-templates heading「真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-mail-templates');
    await expect(page.getByRole('heading', { name: '真实调用 /mail-templates/list、/mail-templates/{id}、/mail-templates/code/{code}、/mail-templates/meta 与 /mail-templates/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-notification-templates heading「真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-notification-templates');
    await expect(page.getByRole('heading', { name: '真实调用 /notification-templates/list、/notification-templates/{id}、/notification-templates/code/{code}、/notification-templates/meta 与 /notification-templates/preview。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-base-params heading「真实调用 /system-config/system 与 /system/configs；仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-base-params');
    await expect(page.getByRole('heading', { name: '真实调用 /system-config/system 与 /system/configs；仅覆盖 SYSTEM 分组基础参数，仍非 44 项全量覆盖，也不代表 Workbench V3 全量完成。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4215 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSEVENTYFIFTH heading「HUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYSIXTH heading「HUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYSEVENTH heading「HUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4216 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDSEVENTYEIGHTH heading「HUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDSEVENTYNINTH heading「HUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTIETH heading「HUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4217 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDEIGHTYFIRST heading「HUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYSECOND heading「HUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYTHIRD heading「HUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4218 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDEIGHTYFOURTH heading「HUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYFIFTH heading「HUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYSIXTH heading「HUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4219 桌面保险未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/insurances heading「保单总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '保单总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances heading「总保费」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '总保费' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances heading「保单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '保单', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4220 桌面维保未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance heading「设备维护」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '设备维护' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance heading「计划中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '计划中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/maintenance heading「执行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '执行中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4221 桌面维保/折旧未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance heading「逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '逾期', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation heading「本月折旧总额」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '本月折旧总额' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation heading「待计算资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '待计算资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4222 桌面借用/领用未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows heading「借用总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '借用总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows heading「借用管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '借用管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「总记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '总记录', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4223 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDEIGHTYSEVENTH heading「HUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYEIGHTH heading「HUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDEIGHTYNINTH heading「HUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4224 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDNINETIETH heading「HUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYFIRST heading「HUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYSECOND heading「HUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4225 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDNINETYTHIRD heading「HUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYFOURTH heading「HUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYFIFTH heading「HUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4226 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDNINETYSIXTH heading「HUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYSEVENTH heading「HUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/HUNDREDNINETYEIGHTH heading「HUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4227 桌面备件未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts heading「备件」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '备件', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts heading「备件总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '备件总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts heading「库存告警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '库存告警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4228 桌面备件/处置/故障未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/spare-parts heading「库存总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '库存总价值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals heading「全周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '全周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「故障树」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '故障树' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4229 桌面部门/领用/折旧未用 heading', () => {
  test('/system/depts heading「组织架构」', async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '组织架构' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「已签收」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '已签收', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation heading「折旧」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '折旧', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4230 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/HUNDREDNINETYNINTH heading「HUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/HUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'HUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTH heading「TWOHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIRST heading「TWOHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4231 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSECOND heading「TWOHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRD heading「TWOHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFOURTH heading「TWOHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4232 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFIFTH heading「TWOHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTH heading「TWOHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTH heading「TWOHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4233 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDEIGHTH heading「TWOHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINTH heading「TWOHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTENTH heading「TWOHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4234 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDELEVENTH heading「TWOHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWELFTH heading「TWOHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTEENTH heading「TWOHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4235 桌面重估未用徽标统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations heading「价值调整」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '价值调整' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「已通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '已通过', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「已拒绝」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '已拒绝', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4236 桌面盘点未用徽标统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory heading「RFID」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: 'RFID', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory heading「实时同步」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '实时同步' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory heading「平均进度」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '平均进度' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4237 桌面预算未用徽标统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets heading「资产预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '资产预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「总预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '总预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「已使用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '已使用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4238 桌面审批未用徽标统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/approvals heading「审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '审批', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals heading「我发起的」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '我发起的' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals heading「已驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '已驳回', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4239 桌面故障未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fault-codes heading「节点总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '节点总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「故障现象」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '故障现象' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「故障原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '故障原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4240 桌面处置领用未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals heading「本月处置总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '本月处置总量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals heading「资产回收价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '资产回收价值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「领用」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '领用', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4241 桌面盘点故障未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory heading「已盘资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '已盘资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inventory heading「盘亏预警」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '盘亏预警' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fault-codes heading「解决措施」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fault-codes');
    await expect(page.getByRole('heading', { name: '解决措施' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4242 桌面预算资产未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/budgets heading「已承诺」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '已承诺', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「执行率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '执行率', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets heading「资产总净值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '资产总净值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4243 桌面资产未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets heading「待处理维修」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '待处理维修' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets heading「闲置率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '闲置率', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets heading「累计折旧」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '累计折旧' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4244 桌面资产领用未用 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/assets heading「资产管理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '资产管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「待处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '待处理', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「已归还」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '已归还', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4245 桌面部门未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
  });

  test('/system/depts heading「部门总数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '部门总数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts heading「正常部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '正常部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts heading「顶级部门」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '顶级部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4246 桌面岗位用户未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/posts heading「岗位总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位总量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users heading「用户总量」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '用户总量' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users heading「角色数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '角色数', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4247 桌面角色用户未用统计 heading', () => {
  test('/system/roles heading「当前页」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '当前页', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles heading「总页数」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '总页数', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users heading「岗位数」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '岗位数', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4248 桌面检验未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections heading「总检验数」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('heading', { name: '总检验数' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records heading「待检验」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('heading', { name: '待检验', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspection-records heading「已过期/不通过」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspection-records');
    await expect(page.getByRole('heading', { name: '已过期/不通过' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4249 桌面菜单未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
  });

  test('/system/menus heading「目录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '目录', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus heading「菜单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '菜单', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus heading「按钮」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '按钮', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4250 桌面盘点周期未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/stocktaking-cycles heading「总周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '总周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles heading「进行中」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '进行中', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles heading「已计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '已计划', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4251 桌面菜单角色未用徽标 heading', () => {
  test('/system/menus heading「总计」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '总计', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus heading「树形」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '树形', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles heading「RBAC」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: 'RBAC' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4252 桌面岗位用户重估未用 heading', () => {
  test('/system/posts heading「岗位」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users heading「用户」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '用户', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「减值/重估合计」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '减值/重估合计' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4253 桌面重估未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations heading「需及时处理」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '需及时处理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「本期已审批」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '本期已审批' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「本期驳回」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '本期驳回' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4254 桌面处置预算未用副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals heading「本月已结案」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '本月已结案' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals heading「本月合计」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '本月合计' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「合同锁定」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '合同锁定' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4255 桌面借用预算未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/borrows heading「已借出」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '已借出', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows heading「已逾期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '已逾期', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「0 项预算」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '0 项预算' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4256 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFOURTEENTH heading「TWOHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTEENTH heading「TWOHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTEENTH heading「TWOHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4257 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSEVENTEENTH heading「TWOHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTEENTH heading「TWOHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETEENTH heading「TWOHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4258 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTWENTIETH heading「TWOHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYFIRST heading「TWOHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYSECOND heading「TWOHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4259 桌面预算重估菜单未用列表 heading', () => {
  test('/budgets heading「占比 0.0%」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '占比 0.0%' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations heading「减值/重估列表」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '减值/重估列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus heading「菜单列表」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '菜单列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4260 桌面预算岗位角色未用列表 heading', () => {
  test('/budgets heading「预算列表」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '预算列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts heading「岗位列表」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles heading「角色列表」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '角色列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4261 桌面盘点领用备件未用列表 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inventory heading「盘点任务」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '盘点任务' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments heading「领用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments');
    await expect(page.getByRole('heading', { name: '领用列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts heading「备件列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '备件列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4262 桌面折旧借用保险未用列表 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/depreciation heading「折旧计划」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '折旧计划' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows heading「借用列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '借用列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances heading「保单列表」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '保单列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4263 桌面维保用户菜单未用 heading', () => {
  test('/maintenance heading「维保列表」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '维保列表' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/users heading「账号与权限管理」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '账号与权限管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/menus heading「系统菜单目录与按钮权限」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '系统菜单目录与按钮权限' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4264 桌面岗位角色未用分区 heading', () => {
  test('/system/posts heading「岗位信息维护」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '岗位信息维护' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles heading「角色与权限管理」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '角色与权限管理' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYTHIRD heading「TWOHUNDREDTWENTYTHIRD」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4265 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTWENTYFOURTH heading「TWOHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYFIFTH heading「TWOHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYSIXTH heading「TWOHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4266 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTWENTYSEVENTH heading「TWOHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYEIGHTH heading「TWOHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTWENTYNINTH heading「TWOHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4267 桌面处置未用列表 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/disposals heading「资产清退」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '资产清退', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYSECOND heading「TWOHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTIETH heading「TWOHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4268 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYFIRST heading「TWOHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYSECOND heading「TWOHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYTHIRD heading「TWOHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4269 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYFOURTH heading「TWOHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYFIFTH heading「TWOHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYSIXTH heading「TWOHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4270 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYSEVENTH heading「TWOHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYEIGHTH heading「TWOHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDTHIRTYNINTH heading「TWOHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4271 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFORTIETH heading「TWOHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYFIRST heading「TWOHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYSECOND heading「TWOHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4272 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFORTYTHIRD heading「TWOHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYFOURTH heading「TWOHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYFIFTH heading「TWOHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4273 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFORTYSIXTH heading「TWOHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYSEVENTH heading「TWOHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFORTYEIGHTH heading「TWOHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4274 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFORTYNINTH heading「TWOHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTIETH heading「TWOHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYFIRST heading「TWOHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4275 桌面重估预算盘点未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/revaluations heading「暂无减值/重估记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/revaluations');
    await expect(page.getByRole('heading', { name: '暂无减值/重估记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/budgets heading「暂无预算记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets');
    await expect(page.getByRole('heading', { name: '暂无预算记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/stocktaking-cycles heading「暂无盘点周期」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByRole('heading', { name: '暂无盘点周期' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4276 桌面检验折旧处置未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/inspections heading「暂无检验记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections');
    await expect(page.getByRole('heading', { name: '暂无检验记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/depreciation heading「暂无折旧计划数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/depreciation');
    await expect(page.getByRole('heading', { name: '暂无折旧计划数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals heading「暂无资产清退记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '暂无资产清退记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4277 桌面维保保险借用未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/maintenance heading「暂无维保记录，点击「新增」开始创建」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '暂无维保记录，点击「新增」开始创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances heading「暂无保险数据，点击「新增保险」开始录入」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances');
    await expect(page.getByRole('heading', { name: '暂无保险数据，点击「新增保险」开始录入' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows heading「暂无借用记录，点击「新建借用单」开始申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows');
    await expect(page.getByRole('heading', { name: '暂无借用记录，点击「新建借用单」开始申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4278 桌面角色岗位备件未用空态 heading', () => {
  test('/system/roles heading「暂无角色数据，点击「新增角色」创建」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await expect(page.getByRole('heading', { name: '暂无角色数据，点击「新增角色」创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/posts heading「暂无岗位数据，点击「新增岗位」创建」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/posts');
    await expect(page.getByRole('heading', { name: '暂无岗位数据，点击「新增岗位」创建' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts heading「暂无备件数据，点击「新增备件」开始录入」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/spare-parts');
    await expect(page.getByRole('heading', { name: '暂无备件数据，点击「新增备件」开始录入' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4279 桌面用户审批未用空态 heading', () => {
  test('/system/users heading「暂无用户数据，可调整筛选或新建用户」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '暂无用户数据，可调整筛选或新建用户' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/approvals heading「暂无审批数据」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '暂无审批数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYTHIRD heading「TWOHUNDREDFIFTYTHIRD」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4280 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFIFTYFOURTH heading「TWOHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYFIFTH heading「TWOHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYSIXTH heading「TWOHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4281 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDFIFTYSEVENTH heading「TWOHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYEIGHTH heading「TWOHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDFIFTYNINTH heading「TWOHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4282 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSIXTIETH heading「TWOHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYFIRST heading「TWOHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYSECOND heading「TWOHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4283 桌面退役验收设备未用空态 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement heading「暂无退役申请记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '暂无退役申请记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake heading「暂无验收单」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/intake');
    await expect(page.getByRole('heading', { name: '暂无验收单' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「暂无设备数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '暂无设备数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4284 桌面SAM设备部门未用空态 heading', () => {
  test('/sam heading「暂无扫描历史」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '暂无扫描历史' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/equipment heading「暂无维保记录」exact', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment');
    await expect(page.getByRole('heading', { name: '暂无维保记录', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/depts heading「暂无部门数据」', async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '暂无部门数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4285 桌面菜单空态与流程表单未用编码 heading', () => {
  test('/system/menus heading「暂无菜单数据，请通过 DDL 初始化种子数据」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/menus');
    await expect(page.getByRole('heading', { name: '暂无菜单数据，请通过 DDL 初始化种子数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYTHIRD heading「TWOHUNDREDSIXTYTHIRD」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYFOURTH heading「TWOHUNDREDSIXTYFOURTH」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4286 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSIXTYFIFTH heading「TWOHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYSIXTH heading「TWOHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYSEVENTH heading「TWOHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4287 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSIXTYEIGHTH heading「TWOHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSIXTYNINTH heading「TWOHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTIETH heading「TWOHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4288 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYFIRST heading「TWOHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYSECOND heading「TWOHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYTHIRD heading「TWOHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4289 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYFOURTH heading「TWOHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYFIFTH heading「TWOHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYSIXTH heading「TWOHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4290 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYSEVENTH heading「TWOHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYEIGHTH heading「TWOHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDSEVENTYNINTH heading「TWOHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4291 桌面自定义字段资产平面图空态描述 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/system/custom-fields heading「没有找到匹配的字段」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/system/custom-fields');
    await expect(page.getByRole('heading', { name: '没有找到匹配的字段' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets heading「未找到符合条件的资产记录，请调整筛选条件或新建资产」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/assets');
    await expect(page.getByRole('heading', { name: '未找到符合条件的资产记录，请调整筛选条件或新建资产' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/floorplans heading「请新建平面图」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/floorplans');
    await expect(page.getByRole('heading', { name: '请新建平面图' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4292 桌面SAM空态描述 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/sam heading「执行合规扫描后显示许可类型分布」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '执行合规扫描后显示许可类型分布' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「执行合规扫描后显示席位使用率」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '执行合规扫描后显示席位使用率' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/sam heading「所有许可合规运行」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/sam');
    await expect(page.getByRole('heading', { name: '所有许可合规运行' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4293 桌面分类与默认空态描述 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/idle heading「当前没有可显示的数据」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/idle');
    await expect(page.getByRole('heading', { name: '当前没有可显示的数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「点击上方按钮添加根分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '点击上方按钮添加根分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/categories heading「请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/categories');
    await expect(page.getByRole('heading', { name: '请在左侧树中选择一个分类查看详情，或点击上方按钮添加新分类' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4294 桌面部门空态提示与流程表单 heading', () => {
  test('/system/depts heading「点击下方按钮创建第一个部门」', async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/depts');
    await expect(page.getByRole('heading', { name: '点击下方按钮创建第一个部门' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTIETH heading「TWOHUNDREDEIGHTIETH」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYFIRST heading「TWOHUNDREDEIGHTYFIRST」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4295 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYSECOND heading「TWOHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYTHIRD heading「TWOHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYFOURTH heading「TWOHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4296 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYFIFTH heading「TWOHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYSIXTH heading「TWOHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYSEVENTH heading「TWOHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4297 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYEIGHTH heading「TWOHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDEIGHTYNINTH heading「TWOHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETIETH heading「TWOHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4298 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDNINETYFIRST heading「TWOHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYSECOND heading「TWOHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYTHIRD heading「TWOHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4299 桌面退役未用统计 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement heading「退役总价值」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '退役总价值' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「残值回收」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '残值回收' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「主要退役原因」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '主要退役原因' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4300 桌面退役未用空态副文案 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement heading「暂无待审核」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '暂无待审核' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「0 项申请」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '0 项申请' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「共 0 条记录」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '共 0 条记录' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4301 桌面退役未用占比与待审核 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/retirement heading「回收率 0%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '回收率 0%' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「占全部退役的 0%」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '占全部退役的 0%' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement heading「待审核」exact', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement');
    await expect(page.getByRole('heading', { name: '待审核', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4302 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDNINETYFOURTH heading「TWOHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYFIFTH heading「TWOHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYSIXTH heading「TWOHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4303 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TWOHUNDREDNINETYSEVENTH heading「TWOHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYEIGHTH heading「TWOHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TWOHUNDREDNINETYNINTH heading「TWOHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TWOHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TWOHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4304 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTH heading「THREEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIRST heading「THREEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSECOND heading「THREEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4305 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTHIRD heading「THREEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFOURTH heading「THREEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTH heading「THREEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4306 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSIXTH heading「THREEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTH heading「THREEHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTH heading「THREEHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4307 桌面GIS能耗与角色菜单空态描述 heading', () => {
  test('/gis heading「没有已定位的资产可在地图上显示」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: '没有已定位的资产可在地图上显示' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/energy heading「尚未采集到能耗数据」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/energy');
    await expect(page.getByRole('heading', { name: '尚未采集到能耗数据' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/system/roles 点菜单权限 heading「暂无菜单数据」exact', async ({ page }) => {
    await page.route('**/api/**', async (apiRoute) => {
      const path = new URL(apiRoute.request().url()).pathname.replace(/^\/api/, '');
      if (path === '/roles/list') {
        return fulfill(apiRoute, {
          records: [{ id: 1, roleName: 'E2E角色', roleCode: 'E2E', dataScope: 1, description: '' }],
          total: 1,
          size: 10,
          current: 1,
          pages: 1,
        });
      }
      return mockApi(apiRoute);
    });
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/system/roles');
    await page.getByRole('button', { name: '菜单权限' }).first().click();
    await expect(page.getByRole('heading', { name: '暂无菜单数据', exact: true }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('DialogTitle'))).toEqual([]);
  });

});

test.describe('Q4308 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDNINTH heading「THREEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTENTH heading「THREEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDELEVENTH heading「THREEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4309 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTWELFTH heading「THREEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTEENTH heading「THREEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFOURTEENTH heading「THREEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4310 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFIFTEENTH heading「THREEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTEENTH heading「THREEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTEENTH heading「THREEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4311 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDEIGHTEENTH heading「THREEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDNINETEENTH heading「THREEHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTIETH heading「THREEHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4312 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTWENTYFIRST heading「THREEHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYSECOND heading「THREEHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYTHIRD heading「THREEHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4313 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTWENTYFOURTH heading「THREEHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYFIFTH heading「THREEHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYSIXTH heading「THREEHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4314 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTWENTYSEVENTH heading「THREEHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYEIGHTH heading「THREEHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTWENTYNINTH heading「THREEHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4315 桌面工作台用户供应商位置空表 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-user-management heading「暂无用户数据。」', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-user-management');
    await expect(page.getByRole('heading', { name: '暂无用户数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-vendor-management heading「暂无供应商数据。」', async ({ page }) => {
    await page.route('**/api/**', mockVendorsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-vendor-management');
    await expect(page.getByRole('heading', { name: '暂无供应商数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-location-management heading「暂无位置数据。」', async ({ page }) => {
    await page.route('**/api/**', mockLocationsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-location-management');
    await expect(page.getByRole('heading', { name: '暂无位置数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4316 桌面工作台部门附件流程定义空表 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-dept-org heading「暂无部门组织数据。」', async ({ page }) => {
    await page.route('**/api/**', mockDeptsListEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-dept-org');
    await expect(page.getByRole('heading', { name: '暂无部门组织数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-file-storage heading「暂无附件元数据。」', async ({ page }) => {
    await page.route('**/api/**', mockFileStorageCatalogEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-file-storage');
    await expect(page.getByRole('heading', { name: '暂无附件元数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-flow-definition heading「暂无流程定义模板。」', async ({ page }) => {
    await page.route('**/api/**', mockWorkflowDefinitionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-flow-definition');
    await expect(page.getByRole('heading', { name: '暂无流程定义模板。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4317 桌面工作台租户文档技术支持空表 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-tenant-management heading「暂无租户数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tenant-management');
    await expect(page.getByRole('heading', { name: '暂无租户数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-doc-center heading「暂无文档数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-doc-center');
    await expect(page.getByRole('heading', { name: '暂无文档数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-tech-support heading「暂无技术支持工单数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-tech-support');
    await expect(page.getByRole('heading', { name: '暂无技术支持工单数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4318 桌面工作台交接导入流程邮件空表 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/fixed-assets/workbenchv3?menu=system-handover heading「暂无交接任务数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-handover');
    await expect(page.getByRole('heading', { name: '暂无交接任务数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-import-export heading「暂无导入导出任务数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-import-export');
    await expect(page.getByRole('heading', { name: '暂无导入导出任务数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-workflow-mail heading「暂无流程邮件配置数据。」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-workflow-mail');
    await expect(page.getByRole('heading', { name: '暂无流程邮件配置数据。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4319 桌面工作台角色数据菜单权限空表 heading', () => {
  test('/fixed-assets/workbenchv3?menu=system-role-permissions heading「暂无角色权限绑定目录。」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-role-permissions');
    await expect(page.getByRole('heading', { name: '暂无角色权限绑定目录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-data-permissions heading「暂无角色数据范围。」', async ({ page }) => {
    await page.route('**/api/**', mockDataPermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-data-permissions');
    await expect(page.getByRole('heading', { name: '暂无角色数据范围。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/fixed-assets/workbenchv3?menu=system-menu-permissions heading「暂无菜单权限编码目录。」', async ({ page }) => {
    await page.route('**/api/**', mockRolePermissionsEmpty);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/fixed-assets/workbenchv3?menu=system-menu-permissions');
    await expect(page.getByRole('heading', { name: '暂无菜单权限编码目录。' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4320 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTHIRTIETH heading「THREEHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYFIRST heading「THREEHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYSECOND heading「THREEHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4321 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYTHIRD heading「THREEHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYFOURTH heading「THREEHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYFIFTH heading「THREEHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4322 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYSIXTH heading「THREEHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYSEVENTH heading「THREEHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYEIGHTH heading「THREEHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4323 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDTHIRTYNINTH heading「THREEHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTIETH heading「THREEHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYFIRST heading「THREEHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4324 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFORTYSECOND heading「THREEHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYTHIRD heading「THREEHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYFOURTH heading「THREEHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4325 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFORTYFIFTH heading「THREEHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYSIXTH heading「THREEHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYSEVENTH heading「THREEHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4326 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFORTYEIGHTH heading「THREEHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFORTYNINTH heading「THREEHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTIETH heading「THREEHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4327 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFIFTYFIRST heading「THREEHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYSECOND heading「THREEHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYTHIRD heading「THREEHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4328 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFIFTYFOURTH heading「THREEHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYFIFTH heading「THREEHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYSIXTH heading「THREEHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4329 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDFIFTYSEVENTH heading「THREEHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYEIGHTH heading「THREEHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDFIFTYNINTH heading「THREEHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4330 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSIXTIETH heading「THREEHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYFIRST heading「THREEHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYSECOND heading「THREEHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4331 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSIXTYTHIRD heading「THREEHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYFOURTH heading「THREEHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYFIFTH heading「THREEHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4332 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSIXTYSIXTH heading「THREEHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYSEVENTH heading「THREEHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSIXTYEIGHTH heading「THREEHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4333 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSIXTYNINTH heading「THREEHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTIETH heading「THREEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYFIRST heading「THREEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4334 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYSECOND heading「THREEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYTHIRD heading「THREEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYFOURTH heading「THREEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4335 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYFIFTH heading「THREEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYSIXTH heading「THREEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYSEVENTH heading「THREEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4336 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYEIGHTH heading「THREEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDSEVENTYNINTH heading「THREEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTIETH heading「THREEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4337 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYFIRST heading「THREEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYSECOND heading「THREEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYTHIRD heading「THREEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4338 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYFOURTH heading「THREEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYFIFTH heading「THREEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/THREEHUNDREDEIGHTYSIXTH heading「THREEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/THREEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'THREEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4339 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/THREEHUNDRE
桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDSIXTYNINTH heading「SIXHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTIETH heading「SIXHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYFIRST heading「SIXHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4434 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYSECOND heading「SIXHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYTHIRD heading「SIXHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYFOURTH heading「SIXHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4435 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYFIFTH heading「SIXHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYSIXTH heading「SIXHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYSEVENTH heading「SIXHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4436 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYEIGHTH heading「SIXHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDSEVENTYNINTH heading「SIXHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTIETH heading「SIXHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4437 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYFIRST heading「SIXHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYSECOND heading「SIXHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYTHIRD heading「SIXHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4438 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYFOURTH heading「SIXHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYFIFTH heading「SIXHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYSIXTH heading「SIXHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4439 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYSEVENTH heading「SIXHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYEIGHTH heading「SIXHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDEIGHTYNINTH heading「SIXHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4440 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDNINETIETH heading「SIXHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYFIRST heading「SIXHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYSECOND heading「SIXHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4441 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDNINETYTHIRD heading「SIXHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYFOURTH heading「SIXHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYFIFTH heading「SIXHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4442 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDNINETYSIXTH heading「SIXHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYSEVENTH heading「SIXHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SIXHUNDREDNINETYEIGHTH heading「SIXHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4443 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SIXHUNDREDNINETYNINTH heading「SIXHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SIXHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'SIXHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTH heading「SEVENHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIRST heading「SEVENHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4444 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDSECOND heading「SEVENHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRD heading「SEVENHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFOURTH heading「SEVENHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4445 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFIFTH heading「SEVENHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSIXTH heading「SEVENHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSEVENTH heading「SEVENHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4446 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDEIGHTH heading「SEVENHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDNINTH heading「SEVENHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTENTH heading「SEVENHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4447 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDELEVENTH heading「SEVENHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWELFTH heading「SEVENHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTEENTH heading「SEVENHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4448 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFOURTEENTH heading「SEVENHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTEENTH heading「SEVENHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSIXTEENTH heading「SEVENHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4449 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDSEVENTEENTH heading「SEVENHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDEIGHTEENTH heading「SEVENHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDNINETEENTH heading「SEVENHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4450 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTWENTIETH heading「SEVENHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYFIRST heading「SEVENHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYSECOND heading「SEVENHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4451 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYTHIRD heading「SEVENHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYFOURTH heading「SEVENHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYFIFTH heading「SEVENHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4452 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYSIXTH heading「SEVENHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYSEVENTH heading「SEVENHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYEIGHTH heading「SEVENHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4453 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTWENTYNINTH heading「SEVENHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTIETH heading「SEVENHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYFIRST heading「SEVENHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4454 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYSECOND heading「SEVENHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYTHIRD heading「SEVENHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYFOURTH heading「SEVENHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4455 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYFIFTH heading「SEVENHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYSIXTH heading「SEVENHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYSEVENTH heading「SEVENHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4456 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYEIGHTH heading「SEVENHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDTHIRTYNINTH heading「SEVENHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTIETH heading「SEVENHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4457 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFORTYFIRST heading「SEVENHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYSECOND heading「SEVENHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYTHIRD heading「SEVENHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4458 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFORTYFOURTH heading「SEVENHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYFIFTH heading「SEVENHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYSIXTH heading「SEVENHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4459 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFORTYSEVENTH heading「SEVENHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYEIGHTH heading「SEVENHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFORTYNINTH heading「SEVENHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4460 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFIFTIETH heading「SEVENHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYFIRST heading「SEVENHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYSECOND heading「SEVENHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4461 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYTHIRD heading「SEVENHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYFOURTH heading「SEVENHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYFIFTH heading「SEVENHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4462 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYSIXTH heading「SEVENHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYSEVENTH heading「SEVENHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYEIGHTH heading「SEVENHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4463 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDFIFTYNINTH heading「SEVENHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSIXTIETH heading「SEVENHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSIXTYFIRST heading「SEVENHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4464 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/SEVENHUNDREDSIXTYSECOND heading「SEVENHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDREDSIXTYTHIRD heading「SEVENHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/SEVENHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'SEVENHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/SEVENHUNDRED
OUSANDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFIFTYFIFTH heading「ONETHOUSANDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4562 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFIFTYSIXTH heading「ONETHOUSANDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFIFTYSEVENTH heading「ONETHOUSANDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFIFTYEIGHTH heading「ONETHOUSANDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4563 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFIFTYNINTH heading「ONETHOUSANDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTIETH heading「ONETHOUSANDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYFIRST heading「ONETHOUSANDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4564 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXTYSECOND heading「ONETHOUSANDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYTHIRD heading「ONETHOUSANDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYFOURTH heading「ONETHOUSANDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4565 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXTYFIFTH heading「ONETHOUSANDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYSIXTH heading「ONETHOUSANDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYSEVENTH heading「ONETHOUSANDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4566 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXTYEIGHTH heading「ONETHOUSANDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXTYNINTH heading「ONETHOUSANDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTIETH heading「ONETHOUSANDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4567 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYFIRST heading「ONETHOUSANDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYSECOND heading「ONETHOUSANDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYTHIRD heading「ONETHOUSANDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4568 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYFOURTH heading「ONETHOUSANDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYFIFTH heading「ONETHOUSANDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYSIXTH heading「ONETHOUSANDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4569 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYSEVENTH heading「ONETHOUSANDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYEIGHTH heading「ONETHOUSANDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENTYNINTH heading「ONETHOUSANDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4570 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDEIGHTIETH heading「ONETHOUSANDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYFIRST heading「ONETHOUSANDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYSECOND heading「ONETHOUSANDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4571 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYTHIRD heading「ONETHOUSANDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYFOURTH heading「ONETHOUSANDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYFIFTH heading「ONETHOUSANDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4572 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYSIXTH heading「ONETHOUSANDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYSEVENTH heading「ONETHOUSANDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYEIGHTH heading「ONETHOUSANDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4573 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDEIGHTYNINTH heading「ONETHOUSANDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETIETH heading「ONETHOUSANDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYFIRST heading「ONETHOUSANDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4574 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDNINETYSECOND heading「ONETHOUSANDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYTHIRD heading「ONETHOUSANDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYFOURTH heading「ONETHOUSANDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4575 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDNINETYFIFTH heading「ONETHOUSANDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYSIXTH heading「ONETHOUSANDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYSEVENTH heading「ONETHOUSANDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4576 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDNINETYEIGHTH heading「ONETHOUSANDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDNINETYNINTH heading「ONETHOUSANDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTH heading「ONETHOUSANDONEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4577 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIRST heading「ONETHOUSANDONEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSECOND heading「ONETHOUSANDONEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRD heading「ONETHOUSANDONEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4578 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFOURTH heading「ONETHOUSANDONEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTH heading「ONETHOUSANDONEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTH heading「ONETHOUSANDONEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4579 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTH heading「ONETHOUSANDONEHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTH heading「ONETHOUSANDONEHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINTH heading「ONETHOUSANDONEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4580 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTENTH heading「ONETHOUSANDONEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDELEVENTH heading「ONETHOUSANDONEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWELFTH heading「ONETHOUSANDONEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4581 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTEENTH heading「ONETHOUSANDONEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFOURTEENTH heading「ONETHOUSANDONEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTEENTH heading「ONETHOUSANDONEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4582 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTEENTH heading「ONETHOUSANDONEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTEENTH heading「ONETHOUSANDONEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTEENTH heading「ONETHOUSANDONEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4583 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETEENTH heading「ONETHOUSANDONEHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTIETH heading「ONETHOUSANDONEHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFIRST heading「ONETHOUSANDONEHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4584 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSECOND heading「ONETHOUSANDONEHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYTHIRD heading「ONETHOUSANDONEHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFOURTH heading「ONETHOUSANDONEHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4585 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFIFTH heading「ONETHOUSANDONEHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSIXTH heading「ONETHOUSANDONEHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSEVENTH heading「ONETHOUSANDONEHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4586 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYEIGHTH heading「ONETHOUSANDONEHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYNINTH heading「ONETHOUSANDONEHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTIETH heading「ONETHOUSANDONEHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4587 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFIRST heading「ONETHOUSANDONEHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSECOND heading「ONETHOUSANDONEHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYTHIRD heading「ONETHOUSANDONEHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4588 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFOURTH heading「ONETHOUSANDONEHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFIFTH heading「ONETHOUSANDONEHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSIXTH heading「ONETHOUSANDONEHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4589 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSEVENTH heading「ONETHOUSANDONEHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYEIGHTH heading「ONETHOUSANDONEHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYNINTH heading「ONETHOUSANDONEHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4590 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTIETH heading「ONETHOUSANDONEHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFIRST heading「ONETHOUSANDONEHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSECOND heading「ONETHOUSANDONEHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4591 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYTHIRD heading「ONETHOUSANDONEHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFOURTH heading「ONETHOUSANDONEHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFIFTH heading「ONETHOUSANDONEHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4592 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSIXTH heading「ONETHOUSANDONEHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSEVENTH heading「ONETHOUSANDONEHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYEIGHTH heading「ONETHOUSANDONEHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4593 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFORTYNINTH heading「ONETHOUSANDONEHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTIETH heading「ONETHOUSANDONEHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFIRST heading「ONETHOUSANDONEHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4594 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSECOND heading「ONETHOUSANDONEHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYTHIRD heading「ONETHOUSANDONEHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFOURTH heading「ONETHOUSANDONEHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4595 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFIFTH heading「ONETHOUSANDONEHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSIXTH heading「ONETHOUSANDONEHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSEVENTH heading「ONETHOUSANDONEHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4596 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYEIGHTH heading「ONETHOUSANDONEHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYNINTH heading「ONETHOUSANDONEHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTIETH heading「ONETHOUSANDONEHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4597 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFIRST heading「ONETHOUSANDONEHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSECOND heading「ONETHOUSANDONEHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYTHIRD heading「ONETHOUSANDONEHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4598 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFOURTH heading「ONETHOUSANDONEHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFIFTH heading「ONETHOUSANDONEHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSIXTH heading「ONETHOUSANDONEHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4599 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSEVENTH heading「ONETHOUSANDONEHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYEIGHTH heading「ONETHOUSANDONEHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYNINTH heading「ONETHOUSANDONEHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4600 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTIETH heading「ONETHOUSANDONEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFIRST heading「ONETHOUSANDONEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSECOND heading「ONETHOUSANDONEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4601 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYTHIRD heading「ONETHOUSANDONEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFOURTH heading「ONETHOUSANDONEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFIFTH heading「ONETHOUSANDONEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4602 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSIXTH heading「ONETHOUSANDONEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSEVENTH heading「ONETHOUSANDONEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYEIGHTH heading「ONETHOUSANDONEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4603 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYNINTH heading「ONETHOUSANDONEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTIETH heading「ONETHOUSANDONEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFIRST heading「ONETHOUSANDONEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4604 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSECOND heading「ONETHOUSANDONEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYTHIRD heading「ONETHOUSANDONEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFOURTH heading「ONETHOUSANDONEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4605 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFIFTH heading「ONETHOUSANDONEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSIXTH heading「ONETHOUSANDONEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSEVENTH heading「ONETHOUSANDONEHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4606 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYEIGHTH heading「ONETHOUSANDONEHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYNINTH heading「ONETHOUSANDONEHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETIETH heading「ONETHOUSANDONEHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4607 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFIRST heading「ONETHOUSANDONEHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSECOND heading「ONETHOUSANDONEHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYTHIRD heading「ONETHOUSANDONEHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4608 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFOURTH heading「ONETHOUSANDONEHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFIFTH heading「ONETHOUSANDONEHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSIXTH heading「ONETHOUSANDONEHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4609 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSEVENTH heading「ONETHOUSANDONEHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYEIGHTH heading「ONETHOUSANDONEHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDONEHUNDREDNINETYNINTH heading「ONETHOUSANDONEHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDONEHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDONEHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4610 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTH heading「ONETHOUSANDTWOHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIRST heading「ONETHOUSANDTWOHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSECOND heading「ONETHOUSANDTWOHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4611 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRD heading「ONETHOUSANDTWOHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFOURTH heading「ONETHOUSANDTWOHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTH heading「ONETHOUSANDTWOHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4612 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTH heading「ONETHOUSANDTWOHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTH heading「ONETHOUSANDTWOHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTH heading「ONETHOUSANDTWOHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4613 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINTH heading「ONETHOUSANDTWOHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTENTH heading「ONETHOUSANDTWOHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDELEVENTH heading「ONETHOUSANDTWOHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4614 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWELFTH heading「ONETHOUSANDTWOHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTEENTH heading「ONETHOUSANDTWOHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFOURTEENTH heading「ONETHOUSANDTWOHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4615 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTEENTH heading「ONETHOUSANDTWOHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTEENTH heading「ONETHOUSANDTWOHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTEENTH heading「ONETHOUSANDTWOHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4616 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTEENTH heading「ONETHOUSANDTWOHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETEENTH heading「ONETHOUSANDTWOHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTIETH heading「ONETHOUSANDTWOHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4617 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFIRST heading「ONETHOUSANDTWOHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSECOND heading「ONETHOUSANDTWOHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYTHIRD heading「ONETHOUSANDTWOHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4618 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFOURTH heading「ONETHOUSANDTWOHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFIFTH heading「ONETHOUSANDTWOHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSIXTH heading「ONETHOUSANDTWOHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4619 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSEVENTH heading「ONETHOUSANDTWOHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYEIGHTH heading「ONETHOUSANDTWOHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYNINTH heading「ONETHOUSANDTWOHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4620 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTIETH heading「ONETHOUSANDTWOHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFIRST heading「ONETHOUSANDTWOHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSECOND heading「ONETHOUSANDTWOHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4621 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYTHIRD heading「ONETHOUSANDTWOHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFOURTH heading「ONETHOUSANDTWOHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFIFTH heading「ONETHOUSANDTWOHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4622 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSIXTH heading「ONETHOUSANDTWOHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSEVENTH heading「ONETHOUSANDTWOHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYEIGHTH heading「ONETHOUSANDTWOHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4623 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYNINTH heading「ONETHOUSANDTWOHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTIETH heading「ONETHOUSANDTWOHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFIRST heading「ONETHOUSANDTWOHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4624 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSECOND heading「ONETHOUSANDTWOHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYTHIRD heading「ONETHOUSANDTWOHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFOURTH heading「ONETHOUSANDTWOHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4625 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFIFTH heading「ONETHOUSANDTWOHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSIXTH heading「ONETHOUSANDTWOHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSEVENTH heading「ONETHOUSANDTWOHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4626 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYEIGHTH heading「ONETHOUSANDTWOHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYNINTH heading「ONETHOUSANDTWOHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTIETH heading「ONETHOUSANDTWOHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4627 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFIRST heading「ONETHOUSANDTWOHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSECOND heading「ONETHOUSANDTWOHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYTHIRD heading「ONETHOUSANDTWOHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4628 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFOURTH heading「ONETHOUSANDTWOHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFIFTH heading「ONETHOUSANDTWOHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSIXTH heading「ONETHOUSANDTWOHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4629 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSEVENTH heading「ONETHOUSANDTWOHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYEIGHTH heading「ONETHOUSANDTWOHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYNINTH heading「ONETHOUSANDTWOHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4630 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTIETH heading「ONETHOUSANDTWOHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFIRST heading「ONETHOUSANDTWOHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSECOND heading「ONETHOUSANDTWOHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4631 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYTHIRD heading「ONETHOUSANDTWOHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFOURTH heading「ONETHOUSANDTWOHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFIFTH heading「ONETHOUSANDTWOHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4632 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSIXTH heading「ONETHOUSANDTWOHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSEVENTH heading「ONETHOUSANDTWOHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYEIGHTH heading「ONETHOUSANDTWOHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4633 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYNINTH heading「ONETHOUSANDTWOHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTIETH heading「ONETHOUSANDTWOHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFIRST heading「ONETHOUSANDTWOHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4634 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSECOND heading「ONETHOUSANDTWOHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYTHIRD heading「ONETHOUSANDTWOHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFOURTH heading「ONETHOUSANDTWOHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4635 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFIFTH heading「ONETHOUSANDTWOHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSIXTH heading「ONETHOUSANDTWOHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSEVENTH heading「ONETHOUSANDTWOHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4636 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYEIGHTH heading「ONETHOUSANDTWOHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYNINTH heading「ONETHOUSANDTWOHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTIETH heading「ONETHOUSANDTWOHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4637 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFIRST heading「ONETHOUSANDTWOHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSECOND heading「ONETHOUSANDTWOHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYTHIRD heading「ONETHOUSANDTWOHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4638 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFOURTH heading「ONETHOUSANDTWOHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFIFTH heading「ONETHOUSANDTWOHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSIXTH heading「ONETHOUSANDTWOHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4639 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSEVENTH heading「ONETHOUSANDTWOHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYEIGHTH heading「ONETHOUSANDTWOHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYNINTH heading「ONETHOUSANDTWOHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4640 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETIETH heading「ONETHOUSANDTWOHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFIRST heading「ONETHOUSANDTWOHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSECOND heading「ONETHOUSANDTWOHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4641 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYTHIRD heading「ONETHOUSANDTWOHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFOURTH heading「ONETHOUSANDTWOHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFIFTH heading「ONETHOUSANDTWOHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4642 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSIXTH heading「ONETHOUSANDTWOHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSEVENTH heading「ONETHOUSANDTWOHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYEIGHTH heading「ONETHOUSANDTWOHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4643 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYNINTH heading「ONETHOUSANDTWOHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTWOHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTWOHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTH heading「ONETHOUSANDTHREEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIRST heading「ONETHOUSANDTHREEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4644 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSECOND heading「ONETHOUSANDTHREEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRD heading「ONETHOUSANDTHREEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFOURTH heading「ONETHOUSANDTHREEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4645 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTH heading「ONETHOUSANDTHREEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTH heading「ONETHOUSANDTHREEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTH heading「ONETHOUSANDTHREEHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4646 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTH heading「ONETHOUSANDTHREEHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINTH heading「ONETHOUSANDTHREEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTENTH heading「ONETHOUSANDTHREEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4647 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDELEVENTH heading「ONETHOUSANDTHREEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWELFTH heading「ONETHOUSANDTHREEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTEENTH heading「ONETHOUSANDTHREEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4648 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFOURTEENTH heading「ONETHOUSANDTHREEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTEENTH heading「ONETHOUSANDTHREEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTEENTH heading「ONETHOUSANDTHREEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4649 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTEENTH heading「ONETHOUSANDTHREEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTEENTH heading「ONETHOUSANDTHREEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETEENTH heading「ONETHOUSANDTHREEHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4650 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTIETH heading「ONETHOUSANDTHREEHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFIRST heading「ONETHOUSANDTHREEHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSECOND heading「ONETHOUSANDTHREEHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4651 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYTHIRD heading「ONETHOUSANDTHREEHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFOURTH heading「ONETHOUSANDTHREEHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFIFTH heading「ONETHOUSANDTHREEHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4652 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSIXTH heading「ONETHOUSANDTHREEHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4653 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYNINTH heading「ONETHOUSANDTHREEHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTIETH heading「ONETHOUSANDTHREEHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFIRST heading「ONETHOUSANDTHREEHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4654 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSECOND heading「ONETHOUSANDTHREEHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYTHIRD heading「ONETHOUSANDTHREEHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFOURTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4655 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFIFTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSIXTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4656 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYNINTH heading「ONETHOUSANDTHREEHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTIETH heading「ONETHOUSANDTHREEHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4657 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFIRST heading「ONETHOUSANDTHREEHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSECOND heading「ONETHOUSANDTHREEHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYTHIRD heading「ONETHOUSANDTHREEHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4658 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFOURTH heading「ONETHOUSANDTHREEHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFIFTH heading「ONETHOUSANDTHREEHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSIXTH heading「ONETHOUSANDTHREEHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4659 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYNINTH heading「ONETHOUSANDTHREEHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4660 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTIETH heading「ONETHOUSANDTHREEHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFIRST heading「ONETHOUSANDTHREEHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSECOND heading「ONETHOUSANDTHREEHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4661 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYTHIRD heading「ONETHOUSANDTHREEHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFOURTH heading「ONETHOUSANDTHREEHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFIFTH heading「ONETHOUSANDTHREEHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4662 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSIXTH heading「ONETHOUSANDTHREEHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4663 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYNINTH heading「ONETHOUSANDTHREEHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTIETH heading「ONETHOUSANDTHREEHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFIRST heading「ONETHOUSANDTHREEHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4664 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSECOND heading「ONETHOUSANDTHREEHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYTHIRD heading「ONETHOUSANDTHREEHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFOURTH heading「ONETHOUSANDTHREEHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4665 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFIFTH heading「ONETHOUSANDTHREEHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSIXTH heading「ONETHOUSANDTHREEHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4666 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYNINTH heading「ONETHOUSANDTHREEHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTIETH heading「ONETHOUSANDTHREEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4667 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFIRST heading「ONETHOUSANDTHREEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSECOND heading「ONETHOUSANDTHREEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYTHIRD heading「ONETHOUSANDTHREEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4668 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFOURTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFIFTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSIXTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4669 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYNINTH heading「ONETHOUSANDTHREEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4670 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTIETH heading「ONETHOUSANDTHREEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFIRST heading「ONETHOUSANDTHREEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSECOND heading「ONETHOUSANDTHREEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4671 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYTHIRD heading「ONETHOUSANDTHREEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFOURTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFIFTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4672 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSIXTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSEVENTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYEIGHTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4673 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYNINTH heading「ONETHOUSANDTHREEHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETIETH heading「ONETHOUSANDTHREEHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFIRST heading「ONETHOUSANDTHREEHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4674 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSECOND heading「ONETHOUSANDTHREEHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYTHIRD heading「ONETHOUSANDTHREEHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFOURTH heading「ONETHOUSANDTHREEHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4675 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFIFTH heading「ONETHOUSANDTHREEHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSIXTH heading「ONETHOUSANDTHREEHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSEVENTH heading「ONETHOUSANDTHREEHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4676 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYEIGHTH heading「ONETHOUSANDTHREEHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYNINTH heading「ONETHOUSANDTHREEHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDTHREEHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDTHREEHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTH heading「ONETHOUSANDFOURHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4677 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIRST heading「ONETHOUSANDFOURHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSECOND heading「ONETHOUSANDFOURHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRD heading「ONETHOUSANDFOURHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4678 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFOURTH heading「ONETHOUSANDFOURHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTH heading「ONETHOUSANDFOURHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTH heading「ONETHOUSANDFOURHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4679 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTH heading「ONETHOUSANDFOURHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTH heading「ONETHOUSANDFOURHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDNINTH heading「ONETHOUSANDFOURHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4680 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTENTH heading「ONETHOUSANDFOURHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDELEVENTH heading「ONETHOUSANDFOURHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWELFTH heading「ONETHOUSANDFOURHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4681 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTEENTH heading「ONETHOUSANDFOURHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFOURTEENTH heading「ONETHOUSANDFOURHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTEENTH heading「ONETHOUSANDFOURHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4682 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTEENTH heading「ONETHOUSANDFOURHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTEENTH heading「ONETHOUSANDFOURHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTEENTH heading「ONETHOUSANDFOURHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4683 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDNINETEENTH heading「ONETHOUSANDFOURHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTIETH heading「ONETHOUSANDFOURHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFIRST heading「ONETHOUSANDFOURHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4684 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSECOND heading「ONETHOUSANDFOURHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYTHIRD heading「ONETHOUSANDFOURHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFOURTH heading「ONETHOUSANDFOURHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4685 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFIFTH heading「ONETHOUSANDFOURHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSIXTH heading「ONETHOUSANDFOURHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSEVENTH heading「ONETHOUSANDFOURHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4686 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYEIGHTH heading「ONETHOUSANDFOURHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYNINTH heading「ONETHOUSANDFOURHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTIETH heading「ONETHOUSANDFOURHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4687 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFIRST heading「ONETHOUSANDFOURHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSECOND heading「ONETHOUSANDFOURHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYTHIRD heading「ONETHOUSANDFOURHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4688 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFOURTH heading「ONETHOUSANDFOURHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFIFTH heading「ONETHOUSANDFOURHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSIXTH heading「ONETHOUSANDFOURHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4689 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSEVENTH heading「ONETHOUSANDFOURHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYEIGHTH heading「ONETHOUSANDFOURHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYNINTH heading「ONETHOUSANDFOURHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4690 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTIETH heading「ONETHOUSANDFOURHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFIRST heading「ONETHOUSANDFOURHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSECOND heading「ONETHOUSANDFOURHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4691 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYTHIRD heading「ONETHOUSANDFOURHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFOURTH heading「ONETHOUSANDFOURHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFIFTH heading「ONETHOUSANDFOURHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4692 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSIXTH heading「ONETHOUSANDFOURHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSEVENTH heading「ONETHOUSANDFOURHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYEIGHTH heading「ONETHOUSANDFOURHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4693 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYNINTH heading「ONETHOUSANDFOURHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTIETH heading「ONETHOUSANDFOURHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFIRST heading「ONETHOUSANDFOURHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4694 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSECOND heading「ONETHOUSANDFOURHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYTHIRD heading「ONETHOUSANDFOURHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFOURTH heading「ONETHOUSANDFOURHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4695 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFIFTH heading「ONETHOUSANDFOURHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSIXTH heading「ONETHOUSANDFOURHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSEVENTH heading「ONETHOUSANDFOURHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4696 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYEIGHTH heading「ONETHOUSANDFOURHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYNINTH heading「ONETHOUSANDFOURHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTIETH heading「ONETHOUSANDFOURHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4697 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFIRST heading「ONETHOUSANDFOURHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSECOND heading「ONETHOUSANDFOURHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYTHIRD heading「ONETHOUSANDFOURHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4698 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFOURTH heading「ONETHOUSANDFOURHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFIFTH heading「ONETHOUSANDFOURHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSIXTH heading「ONETHOUSANDFOURHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4699 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSEVENTH heading「ONETHOUSANDFOURHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYEIGHTH heading「ONETHOUSANDFOURHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYNINTH heading「ONETHOUSANDFOURHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4700 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTIETH heading「ONETHOUSANDFOURHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFIRST heading「ONETHOUSANDFOURHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSECOND heading「ONETHOUSANDFOURHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4701 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYTHIRD heading「ONETHOUSANDFOURHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFOURTH heading「ONETHOUSANDFOURHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFIFTH heading「ONETHOUSANDFOURHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4702 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSIXTH heading「ONETHOUSANDFOURHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSEVENTH heading「ONETHOUSANDFOURHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYEIGHTH heading「ONETHOUSANDFOURHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4703 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYNINTH heading「ONETHOUSANDFOURHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTIETH heading「ONETHOUSANDFOURHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYFIRST heading「ONETHOUSANDFOURHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4704 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYSECOND heading「ONETHOUSANDFOURHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYTHIRD heading「ONETHOUSANDFOURHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYFOURTH heading「ONETHOUSANDFOURHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDFOURHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDFOURHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q
OUSANDSIXHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFOURTH heading「ONETHOUSANDSIXHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4745 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTH heading「ONETHOUSANDSIXHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTH heading「ONETHOUSANDSIXHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTH heading「ONETHOUSANDSIXHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4746 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTH heading「ONETHOUSANDSIXHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINTH heading「ONETHOUSANDSIXHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTENTH heading「ONETHOUSANDSIXHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4747 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDELEVENTH heading「ONETHOUSANDSIXHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWELFTH heading「ONETHOUSANDSIXHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTEENTH heading「ONETHOUSANDSIXHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4748 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFOURTEENTH heading「ONETHOUSANDSIXHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTEENTH heading「ONETHOUSANDSIXHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTEENTH heading「ONETHOUSANDSIXHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4749 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTEENTH heading「ONETHOUSANDSIXHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTEENTH heading「ONETHOUSANDSIXHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETEENTH heading「ONETHOUSANDSIXHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4750 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTIETH heading「ONETHOUSANDSIXHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFIRST heading「ONETHOUSANDSIXHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSECOND heading「ONETHOUSANDSIXHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4751 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYTHIRD heading「ONETHOUSANDSIXHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFOURTH heading「ONETHOUSANDSIXHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFIFTH heading「ONETHOUSANDSIXHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4752 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSIXTH heading「ONETHOUSANDSIXHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSEVENTH heading「ONETHOUSANDSIXHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYEIGHTH heading「ONETHOUSANDSIXHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4753 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYNINTH heading「ONETHOUSANDSIXHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTIETH heading「ONETHOUSANDSIXHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFIRST heading「ONETHOUSANDSIXHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4754 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSECOND heading「ONETHOUSANDSIXHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYTHIRD heading「ONETHOUSANDSIXHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFOURTH heading「ONETHOUSANDSIXHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4755 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFIFTH heading「ONETHOUSANDSIXHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSIXTH heading「ONETHOUSANDSIXHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSEVENTH heading「ONETHOUSANDSIXHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4756 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYEIGHTH heading「ONETHOUSANDSIXHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYNINTH heading「ONETHOUSANDSIXHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTIETH heading「ONETHOUSANDSIXHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4757 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFIRST heading「ONETHOUSANDSIXHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSECOND heading「ONETHOUSANDSIXHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYTHIRD heading「ONETHOUSANDSIXHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4758 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFOURTH heading「ONETHOUSANDSIXHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFIFTH heading「ONETHOUSANDSIXHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSIXTH heading「ONETHOUSANDSIXHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4759 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSEVENTH heading「ONETHOUSANDSIXHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYEIGHTH heading「ONETHOUSANDSIXHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYNINTH heading「ONETHOUSANDSIXHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4760 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTIETH heading「ONETHOUSANDSIXHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFIRST heading「ONETHOUSANDSIXHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSECOND heading「ONETHOUSANDSIXHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4761 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYTHIRD heading「ONETHOUSANDSIXHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFOURTH heading「ONETHOUSANDSIXHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFIFTH heading「ONETHOUSANDSIXHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4762 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSIXTH heading「ONETHOUSANDSIXHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSEVENTH heading「ONETHOUSANDSIXHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYEIGHTH heading「ONETHOUSANDSIXHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4763 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYNINTH heading「ONETHOUSANDSIXHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTIETH heading「ONETHOUSANDSIXHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFIRST heading「ONETHOUSANDSIXHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4764 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSECOND heading「ONETHOUSANDSIXHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYTHIRD heading「ONETHOUSANDSIXHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFOURTH heading「ONETHOUSANDSIXHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4765 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFIFTH heading「ONETHOUSANDSIXHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSIXTH heading「ONETHOUSANDSIXHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSEVENTH heading「ONETHOUSANDSIXHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4766 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYEIGHTH heading「ONETHOUSANDSIXHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYNINTH heading「ONETHOUSANDSIXHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTIETH heading「ONETHOUSANDSIXHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4767 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFIRST heading「ONETHOUSANDSIXHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSECOND heading「ONETHOUSANDSIXHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYTHIRD heading「ONETHOUSANDSIXHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4768 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFOURTH heading「ONETHOUSANDSIXHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFIFTH heading「ONETHOUSANDSIXHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSIXTH heading「ONETHOUSANDSIXHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4769 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSEVENTH heading「ONETHOUSANDSIXHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYEIGHTH heading「ONETHOUSANDSIXHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYNINTH heading「ONETHOUSANDSIXHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4770 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTIETH heading「ONETHOUSANDSIXHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFIRST heading「ONETHOUSANDSIXHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSECOND heading「ONETHOUSANDSIXHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4771 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYTHIRD heading「ONETHOUSANDSIXHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFOURTH heading「ONETHOUSANDSIXHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFIFTH heading「ONETHOUSANDSIXHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4772 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSIXTH heading「ONETHOUSANDSIXHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSEVENTH heading「ONETHOUSANDSIXHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYEIGHTH heading「ONETHOUSANDSIXHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4773 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYNINTH heading「ONETHOUSANDSIXHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETIETH heading「ONETHOUSANDSIXHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFIRST heading「ONETHOUSANDSIXHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4774 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSECOND heading「ONETHOUSANDSIXHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYTHIRD heading「ONETHOUSANDSIXHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFOURTH heading「ONETHOUSANDSIXHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4775 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFIFTH heading「ONETHOUSANDSIXHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSIXTH heading「ONETHOUSANDSIXHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSEVENTH heading「ONETHOUSANDSIXHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4776 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYEIGHTH heading「ONETHOUSANDSIXHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYNINTH heading「ONETHOUSANDSIXHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSIXHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSIXHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDTH heading「ONETHOUSANDSEVENHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4777 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDFIRST heading「ONETHOUSANDSEVENHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDSECOND heading「ONETHOUSANDSEVENHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDTHIRD heading「ONETHOUSANDSEVENHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4778 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDFOURTH heading「ONETHOUSANDSEVENHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDFIFTH heading「ONETHOUSANDSEVENHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDSIXTH heading「ONETHOUSANDSEVENHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4779 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDSEVENTH heading「ONETHOUSANDSEVENHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDEIGHTH heading「ONETHOUSANDSEVENHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDNINTH heading「ONETHOUSANDSEVENHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4780 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDTENTH heading「ONETHOUSANDSEVENHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDELEVENTH heading「ONETHOUSANDSEVENHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDTWELFTH heading「ONETHOUSANDSEVENHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q4781 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDREDTHIRTEENTH heading「ONETHOUSANDSEVENHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/ONETHOUSANDSEVENHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'ONETHOUSANDSEVENHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ONETHOUSANDSEVENHUNDRED
OUSANDNINEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFIRST heading「NINETHOUSANDNINEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7534 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSECOND heading「NINETHOUSANDNINEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYTHIRD heading「NINETHOUSANDNINEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFOURTH heading「NINETHOUSANDNINEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7535 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFIFTH heading「NINETHOUSANDNINEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSIXTH heading「NINETHOUSANDNINEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSEVENTH heading「NINETHOUSANDNINEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7536 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYEIGHTH heading「NINETHOUSANDNINEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYNINTH heading「NINETHOUSANDNINEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTIETH heading「NINETHOUSANDNINEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7537 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFIRST heading「NINETHOUSANDNINEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSECOND heading「NINETHOUSANDNINEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYTHIRD heading「NINETHOUSANDNINEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7538 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFOURTH heading「NINETHOUSANDNINEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFIFTH heading「NINETHOUSANDNINEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSIXTH heading「NINETHOUSANDNINEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7539 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSEVENTH heading「NINETHOUSANDNINEHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYEIGHTH heading「NINETHOUSANDNINEHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYNINTH heading「NINETHOUSANDNINEHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7540 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETIETH heading「NINETHOUSANDNINEHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFIRST heading「NINETHOUSANDNINEHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSECOND heading「NINETHOUSANDNINEHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7541 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYTHIRD heading「NINETHOUSANDNINEHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFOURTH heading「NINETHOUSANDNINEHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFIFTH heading「NINETHOUSANDNINEHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7542 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSIXTH heading「NINETHOUSANDNINEHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSEVENTH heading「NINETHOUSANDNINEHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYEIGHTH heading「NINETHOUSANDNINEHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7543 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYNINTH heading「NINETHOUSANDNINEHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/NINETHOUSANDNINEHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'NINETHOUSANDNINEHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTH heading「TENTHOUSANDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIRST heading「TENTHOUSANDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7544 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSECOND heading「TENTHOUSANDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRD heading「TENTHOUSANDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURTH heading「TENTHOUSANDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7545 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIFTH heading「TENTHOUSANDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTH heading「TENTHOUSANDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENTH heading「TENTHOUSANDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7546 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTH heading「TENTHOUSANDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINTH heading「TENTHOUSANDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTENTH heading「TENTHOUSANDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7547 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDELEVENTH heading「TENTHOUSANDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWELFTH heading「TENTHOUSANDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTEENTH heading「TENTHOUSANDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7548 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURTEENTH heading「TENTHOUSANDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTEENTH heading「TENTHOUSANDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTEENTH heading「TENTHOUSANDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7549 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENTEENTH heading「TENTHOUSANDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTEENTH heading「TENTHOUSANDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINETEENTH heading「TENTHOUSANDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7550 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTWENTIETH heading「TENTHOUSANDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYFIRST heading「TENTHOUSANDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYSECOND heading「TENTHOUSANDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7551 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTWENTYTHIRD heading「TENTHOUSANDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYFOURTH heading「TENTHOUSANDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYFIFTH heading「TENTHOUSANDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7552 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTWENTYSIXTH heading「TENTHOUSANDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYSEVENTH heading「TENTHOUSANDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTWENTYEIGHTH heading「TENTHOUSANDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7553 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTWENTYNINTH heading「TENTHOUSANDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTIETH heading「TENTHOUSANDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYFIRST heading「TENTHOUSANDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7554 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYSECOND heading「TENTHOUSANDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYTHIRD heading「TENTHOUSANDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYFOURTH heading「TENTHOUSANDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7555 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYFIFTH heading「TENTHOUSANDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYSIXTH heading「TENTHOUSANDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYSEVENTH heading「TENTHOUSANDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7556 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYEIGHTH heading「TENTHOUSANDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHIRTYNINTH heading「TENTHOUSANDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTIETH heading「TENTHOUSANDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7557 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFORTYFIRST heading「TENTHOUSANDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYSECOND heading「TENTHOUSANDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYTHIRD heading「TENTHOUSANDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7558 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFORTYFOURTH heading「TENTHOUSANDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYFIFTH heading「TENTHOUSANDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYSIXTH heading「TENTHOUSANDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7559 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFORTYSEVENTH heading「TENTHOUSANDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYEIGHTH heading「TENTHOUSANDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFORTYNINTH heading「TENTHOUSANDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7560 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIFTIETH heading「TENTHOUSANDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYFIRST heading「TENTHOUSANDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYSECOND heading「TENTHOUSANDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7561 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIFTYTHIRD heading「TENTHOUSANDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYFOURTH heading「TENTHOUSANDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYFIFTH heading「TENTHOUSANDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7562 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIFTYSIXTH heading「TENTHOUSANDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYSEVENTH heading「TENTHOUSANDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIFTYEIGHTH heading「TENTHOUSANDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7563 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIFTYNINTH heading「TENTHOUSANDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTIETH heading「TENTHOUSANDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYFIRST heading「TENTHOUSANDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7564 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXTYSECOND heading「TENTHOUSANDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYTHIRD heading「TENTHOUSANDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYFOURTH heading「TENTHOUSANDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7565 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXTYFIFTH heading「TENTHOUSANDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYSIXTH heading「TENTHOUSANDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYSEVENTH heading「TENTHOUSANDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7566 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXTYEIGHTH heading「TENTHOUSANDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXTYNINTH heading「TENTHOUSANDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENTIETH heading「TENTHOUSANDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7567 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEV
HOUSANDTHREEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTENTH heading「TENTHOUSANDTHREEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7647 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDELEVENTH heading「TENTHOUSANDTHREEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWELFTH heading「TENTHOUSANDTHREEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTEENTH heading「TENTHOUSANDTHREEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7648 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFOURTEENTH heading「TENTHOUSANDTHREEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTEENTH heading「TENTHOUSANDTHREEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTEENTH heading「TENTHOUSANDTHREEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7649 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTEENTH heading「TENTHOUSANDTHREEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTEENTH heading「TENTHOUSANDTHREEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETEENTH heading「TENTHOUSANDTHREEHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7650 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTIETH heading「TENTHOUSANDTHREEHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFIRST heading「TENTHOUSANDTHREEHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSECOND heading「TENTHOUSANDTHREEHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7651 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYTHIRD heading「TENTHOUSANDTHREEHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFOURTH heading「TENTHOUSANDTHREEHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFIFTH heading「TENTHOUSANDTHREEHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7652 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSIXTH heading「TENTHOUSANDTHREEHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7653 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYNINTH heading「TENTHOUSANDTHREEHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTIETH heading「TENTHOUSANDTHREEHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFIRST heading「TENTHOUSANDTHREEHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7654 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSECOND heading「TENTHOUSANDTHREEHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYTHIRD heading「TENTHOUSANDTHREEHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFOURTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7655 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFIFTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSIXTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7656 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYNINTH heading「TENTHOUSANDTHREEHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTIETH heading「TENTHOUSANDTHREEHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7657 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFIRST heading「TENTHOUSANDTHREEHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSECOND heading「TENTHOUSANDTHREEHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYTHIRD heading「TENTHOUSANDTHREEHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7658 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFOURTH heading「TENTHOUSANDTHREEHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFIFTH heading「TENTHOUSANDTHREEHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSIXTH heading「TENTHOUSANDTHREEHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7659 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYNINTH heading「TENTHOUSANDTHREEHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7660 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTIETH heading「TENTHOUSANDTHREEHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFIRST heading「TENTHOUSANDTHREEHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSECOND heading「TENTHOUSANDTHREEHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7661 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYTHIRD heading「TENTHOUSANDTHREEHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFOURTH heading「TENTHOUSANDTHREEHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFIFTH heading「TENTHOUSANDTHREEHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7662 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSIXTH heading「TENTHOUSANDTHREEHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7663 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYNINTH heading「TENTHOUSANDTHREEHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTIETH heading「TENTHOUSANDTHREEHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFIRST heading「TENTHOUSANDTHREEHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7664 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSECOND heading「TENTHOUSANDTHREEHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYTHIRD heading「TENTHOUSANDTHREEHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFOURTH heading「TENTHOUSANDTHREEHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7665 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFIFTH heading「TENTHOUSANDTHREEHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSIXTH heading「TENTHOUSANDTHREEHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7666 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYNINTH heading「TENTHOUSANDTHREEHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTIETH heading「TENTHOUSANDTHREEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7667 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFIRST heading「TENTHOUSANDTHREEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSECOND heading「TENTHOUSANDTHREEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYTHIRD heading「TENTHOUSANDTHREEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7668 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFOURTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFIFTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSIXTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7669 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYNINTH heading「TENTHOUSANDTHREEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7670 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTIETH heading「TENTHOUSANDTHREEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFIRST heading「TENTHOUSANDTHREEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSECOND heading「TENTHOUSANDTHREEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7671 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYTHIRD heading「TENTHOUSANDTHREEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFOURTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFIFTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7672 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSIXTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSEVENTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYEIGHTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7673 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYNINTH heading「TENTHOUSANDTHREEHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETIETH heading「TENTHOUSANDTHREEHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFIRST heading「TENTHOUSANDTHREEHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7674 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSECOND heading「TENTHOUSANDTHREEHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYTHIRD heading「TENTHOUSANDTHREEHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFOURTH heading「TENTHOUSANDTHREEHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7675 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFIFTH heading「TENTHOUSANDTHREEHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSIXTH heading「TENTHOUSANDTHREEHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSEVENTH heading「TENTHOUSANDTHREEHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7676 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYEIGHTH heading「TENTHOUSANDTHREEHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYNINTH heading「TENTHOUSANDTHREEHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDTHREEHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDTHREEHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTH heading「TENTHOUSANDFOURHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7677 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIRST heading「TENTHOUSANDFOURHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSECOND heading「TENTHOUSANDFOURHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRD heading「TENTHOUSANDFOURHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7678 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFOURTH heading「TENTHOUSANDFOURHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTH heading「TENTHOUSANDFOURHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTH heading「TENTHOUSANDFOURHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7679 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTH heading「TENTHOUSANDFOURHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTH heading「TENTHOUSANDFOURHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINTH heading「TENTHOUSANDFOURHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7680 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTENTH heading「TENTHOUSANDFOURHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDELEVENTH heading「TENTHOUSANDFOURHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWELFTH heading「TENTHOUSANDFOURHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7681 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTEENTH heading「TENTHOUSANDFOURHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFOURTEENTH heading「TENTHOUSANDFOURHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTEENTH heading「TENTHOUSANDFOURHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7682 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTEENTH heading「TENTHOUSANDFOURHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTEENTH heading「TENTHOUSANDFOURHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTEENTH heading「TENTHOUSANDFOURHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7683 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETEENTH heading「TENTHOUSANDFOURHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTIETH heading「TENTHOUSANDFOURHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFIRST heading「TENTHOUSANDFOURHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7684 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSECOND heading「TENTHOUSANDFOURHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYTHIRD heading「TENTHOUSANDFOURHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFOURTH heading「TENTHOUSANDFOURHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7685 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFIFTH heading「TENTHOUSANDFOURHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSIXTH heading「TENTHOUSANDFOURHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSEVENTH heading「TENTHOUSANDFOURHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7686 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYEIGHTH heading「TENTHOUSANDFOURHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYNINTH heading「TENTHOUSANDFOURHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTIETH heading「TENTHOUSANDFOURHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7687 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFIRST heading「TENTHOUSANDFOURHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSECOND heading「TENTHOUSANDFOURHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYTHIRD heading「TENTHOUSANDFOURHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7688 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFOURTH heading「TENTHOUSANDFOURHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFIFTH heading「TENTHOUSANDFOURHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSIXTH heading「TENTHOUSANDFOURHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7689 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSEVENTH heading「TENTHOUSANDFOURHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYEIGHTH heading「TENTHOUSANDFOURHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYNINTH heading「TENTHOUSANDFOURHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7690 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTIETH heading「TENTHOUSANDFOURHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFIRST heading「TENTHOUSANDFOURHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSECOND heading「TENTHOUSANDFOURHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7691 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYTHIRD heading「TENTHOUSANDFOURHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFOURTH heading「TENTHOUSANDFOURHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFIFTH heading「TENTHOUSANDFOURHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7692 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSIXTH heading「TENTHOUSANDFOURHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSEVENTH heading「TENTHOUSANDFOURHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYEIGHTH heading「TENTHOUSANDFOURHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7693 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYNINTH heading「TENTHOUSANDFOURHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTIETH heading「TENTHOUSANDFOURHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFIRST heading「TENTHOUSANDFOURHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7694 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSECOND heading「TENTHOUSANDFOURHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYTHIRD heading「TENTHOUSANDFOURHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFOURTH heading「TENTHOUSANDFOURHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7695 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFIFTH heading「TENTHOUSANDFOURHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSIXTH heading「TENTHOUSANDFOURHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSEVENTH heading「TENTHOUSANDFOURHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7696 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYEIGHTH heading「TENTHOUSANDFOURHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYNINTH heading「TENTHOUSANDFOURHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTIETH heading「TENTHOUSANDFOURHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7697 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFIRST heading「TENTHOUSANDFOURHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSECOND heading「TENTHOUSANDFOURHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYTHIRD heading「TENTHOUSANDFOURHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7698 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFOURTH heading「TENTHOUSANDFOURHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFIFTH heading「TENTHOUSANDFOURHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSIXTH heading「TENTHOUSANDFOURHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7699 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSEVENTH heading「TENTHOUSANDFOURHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYEIGHTH heading「TENTHOUSANDFOURHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYNINTH heading「TENTHOUSANDFOURHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7700 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTIETH heading「TENTHOUSANDFOURHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFIRST heading「TENTHOUSANDFOURHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSECOND heading「TENTHOUSANDFOURHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7701 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYTHIRD heading「TENTHOUSANDFOURHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFOURTH heading「TENTHOUSANDFOURHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFIFTH heading「TENTHOUSANDFOURHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7702 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSIXTH heading「TENTHOUSANDFOURHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSEVENTH heading「TENTHOUSANDFOURHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYEIGHTH heading「TENTHOUSANDFOURHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7703 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYNINTH heading「TENTHOUSANDFOURHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTIETH heading「TENTHOUSANDFOURHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFIRST heading「TENTHOUSANDFOURHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7704 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSECOND heading「TENTHOUSANDFOURHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYTHIRD heading「TENTHOUSANDFOURHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFOURTH heading「TENTHOUSANDFOURHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7705 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFIFTH heading「TENTHOUSANDFOURHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSIXTH heading「TENTHOUSANDFOURHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSEVENTH heading「TENTHOUSANDFOURHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7706 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYEIGHTH heading「TENTHOUSANDFOURHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYNINTH heading「TENTHOUSANDFOURHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETIETH heading「TENTHOUSANDFOURHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7707 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFIRST heading「TENTHOUSANDFOURHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSECOND heading「TENTHOUSANDFOURHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYTHIRD heading「TENTHOUSANDFOURHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7708 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFOURTH heading「TENTHOUSANDFOURHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFIFTH heading「TENTHOUSANDFOURHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSIXTH heading「TENTHOUSANDFOURHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7709 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSEVENTH heading「TENTHOUSANDFOURHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYEIGHTH heading「TENTHOUSANDFOURHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYNINTH heading「TENTHOUSANDFOURHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFOURHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFOURHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7710 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTH heading「TENTHOUSANDFIVEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIRST heading「TENTHOUSANDFIVEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSECOND heading「TENTHOUSANDFIVEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7711 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRD heading「TENTHOUSANDFIVEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFOURTH heading「TENTHOUSANDFIVEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTH heading「TENTHOUSANDFIVEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7712 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTH heading「TENTHOUSANDFIVEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTH heading「TENTHOUSANDFIVEHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTH heading「TENTHOUSANDFIVEHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7713 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINTH heading「TENTHOUSANDFIVEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTENTH heading「TENTHOUSANDFIVEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDELEVENTH heading「TENTHOUSANDFIVEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7714 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWELFTH heading「TENTHOUSANDFIVEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTEENTH heading「TENTHOUSANDFIVEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFOURTEENTH heading「TENTHOUSANDFIVEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7715 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTEENTH heading「TENTHOUSANDFIVEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTEENTH heading「TENTHOUSANDFIVEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTEENTH heading「TENTHOUSANDFIVEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7716 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTEENTH heading「TENTHOUSANDFIVEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETEENTH heading「TENTHOUSANDFIVEHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTIETH heading「TENTHOUSANDFIVEHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7717 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFIRST heading「TENTHOUSANDFIVEHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSECOND heading「TENTHOUSANDFIVEHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYTHIRD heading「TENTHOUSANDFIVEHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7718 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFOURTH heading「TENTHOUSANDFIVEHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFIFTH heading「TENTHOUSANDFIVEHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSIXTH heading「TENTHOUSANDFIVEHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7719 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYNINTH heading「TENTHOUSANDFIVEHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7720 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTIETH heading「TENTHOUSANDFIVEHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFIRST heading「TENTHOUSANDFIVEHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSECOND heading「TENTHOUSANDFIVEHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7721 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYTHIRD heading「TENTHOUSANDFIVEHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFOURTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFIFTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7722 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSIXTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7723 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYNINTH heading「TENTHOUSANDFIVEHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTIETH heading「TENTHOUSANDFIVEHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFIRST heading「TENTHOUSANDFIVEHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7724 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSECOND heading「TENTHOUSANDFIVEHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYTHIRD heading「TENTHOUSANDFIVEHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFOURTH heading「TENTHOUSANDFIVEHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7725 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFIFTH heading「TENTHOUSANDFIVEHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSIXTH heading「TENTHOUSANDFIVEHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7726 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYNINTH heading「TENTHOUSANDFIVEHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTIETH heading「TENTHOUSANDFIVEHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7727 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFIRST heading「TENTHOUSANDFIVEHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSECOND heading「TENTHOUSANDFIVEHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYTHIRD heading「TENTHOUSANDFIVEHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7728 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFOURTH heading「TENTHOUSANDFIVEHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFIFTH heading「TENTHOUSANDFIVEHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSIXTH heading「TENTHOUSANDFIVEHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7729 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYNINTH heading「TENTHOUSANDFIVEHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7730 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTIETH heading「TENTHOUSANDFIVEHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFIRST heading「TENTHOUSANDFIVEHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSECOND heading「TENTHOUSANDFIVEHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7731 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYTHIRD heading「TENTHOUSANDFIVEHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFOURTH heading「TENTHOUSANDFIVEHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFIFTH heading「TENTHOUSANDFIVEHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7732 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSIXTH heading「TENTHOUSANDFIVEHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7733 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYNINTH heading「TENTHOUSANDFIVEHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTIETH heading「TENTHOUSANDFIVEHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFIRST heading「TENTHOUSANDFIVEHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7734 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSECOND heading「TENTHOUSANDFIVEHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYTHIRD heading「TENTHOUSANDFIVEHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFOURTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7735 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFIFTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSIXTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7736 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYNINTH heading「TENTHOUSANDFIVEHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTIETH heading「TENTHOUSANDFIVEHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7737 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFIRST heading「TENTHOUSANDFIVEHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSECOND heading「TENTHOUSANDFIVEHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYTHIRD heading「TENTHOUSANDFIVEHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7738 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFOURTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFIFTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSIXTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7739 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSEVENTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYEIGHTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYNINTH heading「TENTHOUSANDFIVEHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7740 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETIETH heading「TENTHOUSANDFIVEHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFIRST heading「TENTHOUSANDFIVEHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSECOND heading「TENTHOUSANDFIVEHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7741 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYTHIRD heading「TENTHOUSANDFIVEHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFOURTH heading「TENTHOUSANDFIVEHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFIFTH heading「TENTHOUSANDFIVEHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7742 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSIXTH heading「TENTHOUSANDFIVEHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSEVENTH heading「TENTHOUSANDFIVEHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYEIGHTH heading「TENTHOUSANDFIVEHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7743 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYNINTH heading「TENTHOUSANDFIVEHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDFIVEHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDFIVEHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDTH heading「TENTHOUSANDSIXHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDFIRST heading「TENTHOUSANDSIXHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7744 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDSECOND heading「TENTHOUSANDSIXHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDTHIRD heading「TENTHOUSANDSIXHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDFOURTH heading「TENTHOUSANDSIXHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7745 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDFIFTH heading「TENTHOUSANDSIXHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDSIXTH heading「TENTHOUSANDSIXHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDSEVENTH heading「TENTHOUSANDSIXHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSIXHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSIXHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7746 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSIXHUNDREDEIGH
HOUSANDSEVENHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYFOURTH heading「TENTHOUSANDSEVENHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYFIFTH heading「TENTHOUSANDSEVENHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7792 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYSIXTH heading「TENTHOUSANDSEVENHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYSEVENTH heading「TENTHOUSANDSEVENHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYEIGHTH heading「TENTHOUSANDSEVENHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7793 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYNINTH heading「TENTHOUSANDSEVENHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTIETH heading「TENTHOUSANDSEVENHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFIRST heading「TENTHOUSANDSEVENHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7794 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSECOND heading「TENTHOUSANDSEVENHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYTHIRD heading「TENTHOUSANDSEVENHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFOURTH heading「TENTHOUSANDSEVENHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7795 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFIFTH heading「TENTHOUSANDSEVENHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSIXTH heading「TENTHOUSANDSEVENHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSEVENTH heading「TENTHOUSANDSEVENHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7796 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYEIGHTH heading「TENTHOUSANDSEVENHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYNINTH heading「TENTHOUSANDSEVENHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTIETH heading「TENTHOUSANDSEVENHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7797 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFIRST heading「TENTHOUSANDSEVENHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSECOND heading「TENTHOUSANDSEVENHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYTHIRD heading「TENTHOUSANDSEVENHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7798 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFOURTH heading「TENTHOUSANDSEVENHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFIFTH heading「TENTHOUSANDSEVENHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSIXTH heading「TENTHOUSANDSEVENHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7799 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSEVENTH heading「TENTHOUSANDSEVENHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYEIGHTH heading「TENTHOUSANDSEVENHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYNINTH heading「TENTHOUSANDSEVENHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7800 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTIETH heading「TENTHOUSANDSEVENHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFIRST heading「TENTHOUSANDSEVENHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSECOND heading「TENTHOUSANDSEVENHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7801 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYTHIRD heading「TENTHOUSANDSEVENHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFOURTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFIFTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7802 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSIXTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSEVENTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYEIGHTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7803 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYNINTH heading「TENTHOUSANDSEVENHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTIETH heading「TENTHOUSANDSEVENHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFIRST heading「TENTHOUSANDSEVENHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7804 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSECOND heading「TENTHOUSANDSEVENHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYTHIRD heading「TENTHOUSANDSEVENHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFOURTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7805 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFIFTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSIXTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSEVENTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7806 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYEIGHTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYNINTH heading「TENTHOUSANDSEVENHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETIETH heading「TENTHOUSANDSEVENHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7807 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFIRST heading「TENTHOUSANDSEVENHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSECOND heading「TENTHOUSANDSEVENHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYTHIRD heading「TENTHOUSANDSEVENHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7808 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFOURTH heading「TENTHOUSANDSEVENHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFIFTH heading「TENTHOUSANDSEVENHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSIXTH heading「TENTHOUSANDSEVENHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7809 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSEVENTH heading「TENTHOUSANDSEVENHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYEIGHTH heading「TENTHOUSANDSEVENHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYNINTH heading「TENTHOUSANDSEVENHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDSEVENHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDSEVENHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7810 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTH heading「TENTHOUSANDEIGHTHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIRST heading「TENTHOUSANDEIGHTHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSECOND heading「TENTHOUSANDEIGHTHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7811 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRD heading「TENTHOUSANDEIGHTHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFOURTH heading「TENTHOUSANDEIGHTHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTH heading「TENTHOUSANDEIGHTHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7812 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTH heading「TENTHOUSANDEIGHTHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7813 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINTH heading「TENTHOUSANDEIGHTHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTENTH heading「TENTHOUSANDEIGHTHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDELEVENTH heading「TENTHOUSANDEIGHTHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7814 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWELFTH heading「TENTHOUSANDEIGHTHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTEENTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFOURTEENTH heading「TENTHOUSANDEIGHTHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7815 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTEENTH heading「TENTHOUSANDEIGHTHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTEENTH heading「TENTHOUSANDEIGHTHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTEENTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7816 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTEENTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETEENTH heading「TENTHOUSANDEIGHTHUNDREDNINETEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTIETH heading「TENTHOUSANDEIGHTHUNDREDTWENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7817 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFIRST heading「TENTHOUSANDEIGHTHUNDREDTWENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSECOND heading「TENTHOUSANDEIGHTHUNDREDTWENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDTWENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7818 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7819 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYNINTH heading「TENTHOUSANDEIGHTHUNDREDTWENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTWENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTWENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7820 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTIETH heading「TENTHOUSANDEIGHTHUNDREDTHIRTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFIRST heading「TENTHOUSANDEIGHTHUNDREDTHIRTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSECOND heading「TENTHOUSANDEIGHTHUNDREDTHIRTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7821 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDTHIRTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7822 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7823 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYNINTH heading「TENTHOUSANDEIGHTHUNDREDTHIRTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDTHIRTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDTHIRTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTIETH heading「TENTHOUSANDEIGHTHUNDREDFORTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFIRST heading「TENTHOUSANDEIGHTHUNDREDFORTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7824 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSECOND heading「TENTHOUSANDEIGHTHUNDREDFORTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDFORTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDFORTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7825 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDFORTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDFORTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDFORTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7826 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDFORTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYNINTH heading「TENTHOUSANDEIGHTHUNDREDFORTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFORTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFORTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTIETH heading「TENTHOUSANDEIGHTHUNDREDFIFTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7827 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFIRST heading「TENTHOUSANDEIGHTHUNDREDFIFTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSECOND heading「TENTHOUSANDEIGHTHUNDREDFIFTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDFIFTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7828 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7829 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYNINTH heading「TENTHOUSANDEIGHTHUNDREDFIFTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDFIFTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDFIFTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7830 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTIETH heading「TENTHOUSANDEIGHTHUNDREDSIXTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFIRST heading「TENTHOUSANDEIGHTHUNDREDSIXTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSECOND heading「TENTHOUSANDEIGHTHUNDREDSIXTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7831 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDSIXTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7832 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7833 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYNINTH heading「TENTHOUSANDEIGHTHUNDREDSIXTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSIXTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSIXTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTIETH heading「TENTHOUSANDEIGHTHUNDREDSEVENTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFIRST heading「TENTHOUSANDEIGHTHUNDREDSEVENTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7834 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSECOND heading「TENTHOUSANDEIGHTHUNDREDSEVENTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDSEVENTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7835 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7836 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYNINTH heading「TENTHOUSANDEIGHTHUNDREDSEVENTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDSEVENTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDSEVENTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTIETH heading「TENTHOUSANDEIGHTHUNDREDEIGHTIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7837 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFIRST heading「TENTHOUSANDEIGHTHUNDREDEIGHTYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSECOND heading「TENTHOUSANDEIGHTHUNDREDEIGHTYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYTHIRD heading「TENTHOUSANDEIGHTHUNDREDEIGHTYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7838 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFOURTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFIFTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSIXTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7839 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYNINTH heading「TENTHOUSANDEIGHTHUNDREDEIGHTYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDEIGHTYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDEIGHTYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7840 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETIETH heading「TENTHOUSANDEIGHTHUNDREDNINETIETH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETIETH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETIETH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFIRST heading「TENTHOUSANDEIGHTHUNDREDNINETYFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSECOND heading「TENTHOUSANDEIGHTHUNDREDNINETYSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7841 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYTHIRD heading「TENTHOUSANDEIGHTHUNDREDNINETYTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFOURTH heading「TENTHOUSANDEIGHTHUNDREDNINETYFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFIFTH heading「TENTHOUSANDEIGHTHUNDREDNINETYFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7842 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSIXTH heading「TENTHOUSANDEIGHTHUNDREDNINETYSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSEVENTH heading「TENTHOUSANDEIGHTHUNDREDNINETYSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYEIGHTH heading「TENTHOUSANDEIGHTHUNDREDNINETYEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7843 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYNINTH heading「TENTHOUSANDEIGHTHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDEIGHTHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDEIGHTHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDTH heading「TENTHOUSANDNINEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDFIRST heading「TENTHOUSANDNINEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7844 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDSECOND heading「TENTHOUSANDNINEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDTHIRD heading「TENTHOUSANDNINEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDFOURTH heading「TENTHOUSANDNINEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7845 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDFIFTH heading「TENTHOUSANDNINEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDSIXTH heading「TENTHOUSANDNINEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDSEVENTH heading「TENTHOUSANDNINEHUNDREDSEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDSEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDSEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7846 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDEIGHTH heading「TENTHOUSANDNINEHUNDREDEIGHTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDEIGHTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDEIGHTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDNINTH heading「TENTHOUSANDNINEHUNDREDNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDNINTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDTENTH heading「TENTHOUSANDNINEHUNDREDTENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDTENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDTENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7847 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDELEVENTH heading「TENTHOUSANDNINEHUNDREDELEVENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDELEVENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDELEVENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDTWELFTH heading「TENTHOUSANDNINEHUNDREDTWELFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDTWELFTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDTWELFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDTHIRTEENTH heading「TENTHOUSANDNINEHUNDREDTHIRTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDTHIRTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDTHIRTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7848 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDFOURTEENTH heading「TENTHOUSANDNINEHUNDREDFOURTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDFOURTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDFOURTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDFIFTEENTH heading「TENTHOUSANDNINEHUNDREDFIFTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDFIFTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDFIFTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDSIXTEENTH heading「TENTHOUSANDNINEHUNDREDSIXTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDSIXTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDSIXTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q7849 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDSEVENTEENTH heading「TENTHOUSANDNINEHUNDREDSEVENTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDSEVENTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDSEVENTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDEIGHTEENTH heading「TENTHOUSANDNINEHUNDREDEIGHTEENTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/TENTHOUSANDNINEHUNDREDEIGHTEENTH');
    await expect(page.getByRole('heading', { name: 'TENTHOUSANDNINEHUNDREDEIGHTEENTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/TENTHOUSANDNINEHUNDREDNIN
H' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFOURHUNDREDNINETYNINTH heading「EIGHTEENTHOUSANDFOURHUNDREDNINETYNINTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFOURHUNDREDNINETYNINTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFOURHUNDREDNINETYNINTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDTH heading「EIGHTEENTHOUSANDFIVEHUNDREDTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q10377 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFIRST heading「EIGHTEENTHOUSANDFIVEHUNDREDFIRST」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFIRST');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDFIRST' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDSECOND heading「EIGHTEENTHOUSANDFIVEHUNDREDSECOND」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDSECOND');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDSECOND' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDTHIRD heading「EIGHTEENTHOUSANDFIVEHUNDREDTHIRD」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDTHIRD');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDTHIRD' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

test.describe('Q10378 桌面流程表单未用业务编码 heading', () => {
  test.beforeEach(async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFOURTH heading「EIGHTEENTHOUSANDFIVEHUNDREDFOURTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFOURTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDFOURTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFIFTH heading「EIGHTEENTHOUSANDFIVEHUNDREDFIFTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDFIFTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDFIFTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDSIXTH heading「EIGHTEENTHOUSANDFIVEHUNDREDSIXTH」', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/EIGHTEENTHOUSANDFIVEHUNDREDSIXTH');
    await expect(page.getByRole('heading', { name: 'EIGHTEENTHOUSANDFIVEHUNDREDSIXTH' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

});

const errorPages: Array<{ path: string; failPath: string; error?: string }> = [
  { path: '/energy', failPath: '/energy/dashboard' },
  { path: '/gis', failPath: '/gis/assets' },
  { path: '/categories', failPath: '/categories/tree' },
  { path: '/sam', failPath: '/sam/dashboard' },
  { path: '/assets', failPath: '/assets' },
  { path: '/contracts', failPath: '/contracts' },
  { path: '/floorplans', failPath: '/floor-plans' },
  { path: '/m/index', failPath: '/mobile/dashboard' },
  { path: '/m/assets', failPath: '/mobile/assets' },
  { path: '/assets/1/timeline', failPath: '/assets/1/history' },
  { path: '/assets/2/timeline', failPath: '/assets/2/history' },
  { path: '/m/work-orders', failPath: '/mobile/work-orders' },
  { path: '/m/notifications', failPath: '/mobile/notifications' },
  { path: '/reports', failPath: '/reports/by-category' },
  { path: '/approvals', failPath: '/approvals/list' },
  { path: '/disposals/1', failPath: '/retirement/1' },
  { path: '/audit/1', failPath: '/audit-logs/1', error: '未找到该审计记录' },
  { path: '/assets/1', failPath: '/assets/1', error: '获取数据失败' },
  { path: '/approvals/1', failPath: '/approvals/1', error: '工单不存在或已删除' },
  { path: '/retirement/1', failPath: '/retirement/1', error: '未找到该退役申请记录' },
  { path: '/budgets/1', failPath: '/budgets/1', error: '未找到预算信息' },
  { path: '/insurances/1', failPath: '/insurance/1', error: '未找到保险记录' },
  { path: '/analytics', failPath: '/dashboard/trends', error: '趋势数据加载失败' },
  { path: '/analytics', failPath: '/dashboard/dept-distribution', error: '部门数据加载失败' },
  { path: '/analytics', failPath: '/reports/by-category', error: '分类数据加载失败' },
  { path: '/analytics', failPath: '/reports/summary', error: '汇总数据加载失败' },
  { path: '/analytics', failPath: '/dashboard/stats', error: '统计数据加载失败' },
  { path: '/borrows/1', failPath: '/borrows/1', error: '借用单不存在' },
  { path: '/assignments/1', failPath: '/assignments/1', error: '领用单不存在' },
  { path: '/intake/1', failPath: '/intake-orders/1', error: '验收单不存在' },
  { path: '/inspections/1', failPath: '/inspections/1', error: '检验记录不存在' },
  { path: '/spare-parts/1', failPath: '/spare-parts/1', error: '备件不存在' },
  { path: '/m/stocktaking-tasks/1', failPath: '/stocktaking/tasks/1', error: '获取任务失败' },
  { path: '/borrows/999', failPath: '/borrows/999', error: '借用单不存在' },
  { path: '/assignments/999', failPath: '/assignments/999', error: '领用单不存在' },
  { path: '/intake/999', failPath: '/intake-orders/999', error: '验收单不存在' },
  { path: '/inspections/999', failPath: '/inspections/999', error: '检验记录不存在' },
  { path: '/spare-parts/999', failPath: '/spare-parts/999', error: '备件不存在' },
  { path: '/budgets/999', failPath: '/budgets/999', error: '未找到预算信息' },
  { path: '/insurances/999', failPath: '/insurance/999', error: '未找到保险记录' },
  { path: '/audit/999', failPath: '/audit-logs/999', error: '未找到该审计记录' },
  { path: '/retirement/999', failPath: '/retirement/999', error: '未找到该退役申请记录' },
  { path: '/approvals/999', failPath: '/approvals/999', error: '工单不存在或已删除' },
  { path: '/disposals/999', failPath: '/retirement/999', error: '加载失败' },
  { path: '/assets/999', failPath: '/assets/999', error: '获取数据失败' },
  { path: '/workorders/1/acceptance', failPath: '/workorders/1', error: '工单不存在' },
  { path: '/workorders/999/acceptance', failPath: '/workorders/999', error: '工单不存在' },
  { path: '/m/stocktaking-tasks/999', failPath: '/stocktaking/tasks/999', error: '获取任务失败' },
  { path: '/m/stocktaking-tasks/abc', failPath: '/stocktaking/tasks/abc', error: '获取任务失败' },
  { path: '/intake/2', failPath: '/intake-orders/2', error: '验收单不存在' },
  { path: '/borrows/2', failPath: '/borrows/2', error: '借用单不存在' },
  { path: '/assignments/2', failPath: '/assignments/2', error: '领用单不存在' },
  { path: '/inspections/2', failPath: '/inspections/2', error: '检验记录不存在' },
  { path: '/spare-parts/2', failPath: '/spare-parts/2', error: '备件不存在' },
  { path: '/budgets/2', failPath: '/budgets/2', error: '未找到预算信息' },
  { path: '/retirement/2', failPath: '/retirement/2', error: '未找到该退役申请记录' },
  { path: '/workorders/2/acceptance', failPath: '/workorders/2', error: '工单不存在' },
  { path: '/audit/2', failPath: '/audit-logs/2', error: '未找到该审计记录' },
  { path: '/approvals/2', failPath: '/approvals/2', error: '工单不存在或已删除' },
  { path: '/disposals/2', failPath: '/retirement/2', error: '加载失败' },
  { path: '/assets/3', failPath: '/assets/3', error: '获取数据失败' },
  { path: '/insurances/2', failPath: '/insurance/2', error: '未找到保险记录' },
  { path: '/m/assets/3', failPath: '/mobile/assets/3', error: '资产加载失败' },
  { path: '/assets/3/timeline', failPath: '/assets/3/history' },
  { path: '/m/assets/4', failPath: '/mobile/assets/4', error: '资产加载失败' },
  { path: '/assets/4', failPath: '/assets/4', error: '获取数据失败' },
  { path: '/audit/3', failPath: '/audit-logs/3', error: '未找到该审计记录' },
  { path: '/approvals/3', failPath: '/approvals/3', error: '工单不存在或已删除' },
  { path: '/disposals/3', failPath: '/retirement/3', error: '加载失败' },
  { path: '/workorders/3/acceptance', failPath: '/workorders/3', error: '工单不存在' },
  { path: '/retirement/3', failPath: '/retirement/3', error: '未找到该退役申请记录' },
  { path: '/intake/3', failPath: '/intake-orders/3', error: '验收单不存在' },
  { path: '/borrows/3', failPath: '/borrows/3', error: '借用单不存在' },
  { path: '/assignments/3', failPath: '/assignments/3', error: '领用单不存在' },
  { path: '/inspections/3', failPath: '/inspections/3', error: '检验记录不存在' },
  { path: '/spare-parts/3', failPath: '/spare-parts/3', error: '备件不存在' },
  { path: '/budgets/3', failPath: '/budgets/3', error: '未找到预算信息' },
  { path: '/insurances/3', failPath: '/insurance/3', error: '未找到保险记录' },
  { path: '/workorders/4/acceptance', failPath: '/workorders/4', error: '工单不存在' },
  { path: '/retirement/4', failPath: '/retirement/4', error: '未找到该退役申请记录' },
  { path: '/intake/4', failPath: '/intake-orders/4', error: '验收单不存在' },
  { path: '/borrows/4', failPath: '/borrows/4', error: '借用单不存在' },
  { path: '/assignments/4', failPath: '/assignments/4', error: '领用单不存在' },
  { path: '/inspections/4', failPath: '/inspections/4', error: '检验记录不存在' },
  { path: '/spare-parts/4', failPath: '/spare-parts/4', error: '备件不存在' },
  { path: '/budgets/4', failPath: '/budgets/4', error: '未找到预算信息' },
  { path: '/insurances/4', failPath: '/insurance/4', error: '未找到保险记录' },
  { path: '/audit/4', failPath: '/audit-logs/4', error: '未找到该审计记录' },
  { path: '/approvals/4', failPath: '/approvals/4', error: '工单不存在或已删除' },
  { path: '/disposals/4', failPath: '/retirement/4', error: '加载失败' },
  { path: '/m/assets/5', failPath: '/mobile/assets/5', error: '资产加载失败' },
  { path: '/workorders/5/acceptance', failPath: '/workorders/5', error: '工单不存在' },
  { path: '/retirement/5', failPath: '/retirement/5', error: '未找到该退役申请记录' },
  { path: '/intake/5', failPath: '/intake-orders/5', error: '验收单不存在' },
  { path: '/borrows/5', failPath: '/borrows/5', error: '借用单不存在' },
  { path: '/assignments/5', failPath: '/assignments/5', error: '领用单不存在' },
  { path: '/inspections/5', failPath: '/inspections/5', error: '检验记录不存在' },
  { path: '/spare-parts/5', failPath: '/spare-parts/5', error: '备件不存在' },
  { path: '/budgets/5', failPath: '/budgets/5', error: '未找到预算信息' },
  { path: '/insurances/5', failPath: '/insurance/5', error: '未找到保险记录' },
  { path: '/audit/5', failPath: '/audit-logs/5', error: '未找到该审计记录' },
  { path: '/approvals/5', failPath: '/approvals/5', error: '工单不存在或已删除' },
  { path: '/disposals/5', failPath: '/retirement/5', error: '加载失败' },
  { path: '/assets/5', failPath: '/assets/5', error: '获取数据失败' },
  { path: '/m/stocktaking-tasks/5', failPath: '/stocktaking/tasks/5', error: '获取任务失败' },
  { path: '/m/assets/2', failPath: '/mobile/assets/2', error: '资产加载失败' },
];

test.describe('API 错误态', () => {
  for (const route of errorPages) {
    test(`${route.path} 失败显示「${route.error ?? '加载失败'}」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, route.failPath));
      await seedSession(page, adminUser);
      await page.goto(route.path);
      await expect(page.getByText(route.error ?? '加载失败').first()).toBeVisible({ timeout: 15_000 });
    });
  }

  for (const id of Array.from({ length: 795 }, (_, i) => i + 6)) {
    test(`/borrows/${id} 失败显示「借用单不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/borrows/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/borrows/${id}`);
      await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/assignments/${id} 失败显示「领用单不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/assignments/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/assignments/${id}`);
      await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/intake/${id} 失败显示「验收单不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/intake-orders/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/intake/${id}`);
      await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/retirement/${id} 失败显示「未找到该退役申请记录」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/retirement/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/retirement/${id}`);
      await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/workorders/${id}/acceptance 失败显示「工单不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/workorders/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/workorders/${id}/acceptance`);
      await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/budgets/${id} 失败显示「未找到预算信息」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/budgets/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/budgets/${id}`);
      await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/inspections/${id} 失败显示「检验记录不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/inspections/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/inspections/${id}`);
      await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/spare-parts/${id} 失败显示「备件不存在」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/spare-parts/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/spare-parts/${id}`);
      await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/insurances/${id} 失败显示「未找到保险记录」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/insurance/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/insurances/${id}`);
      await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/audit/${id} 失败显示「未找到该审计记录」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/audit-logs/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/audit/${id}`);
      await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/approvals/${id} 失败显示「工单不存在或已删除」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/approvals/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/approvals/${id}`);
      await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/disposals/${id} 失败显示「加载失败」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/retirement/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/disposals/${id}`);
      await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/assets/${id} 失败显示「获取数据失败」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/assets/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/assets/${id}`);
      await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/m/assets/${id} 失败显示「资产加载失败」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/mobile/assets/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/m/assets/${id}`);
      await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
    });
    test(`/m/stocktaking-tasks/${id} 失败显示「获取任务失败」`, async ({ page }) => {
      await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, `/stocktaking/tasks/${id}`));
      await seedSession(page, adminUser);
      await page.goto(`/m/stocktaking-tasks/${id}`);
      await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
    });
  }

  test('/m/scan 查询失败显示「查询失败」', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/scan'));
    await seedSession(page, adminUser);
    await page.goto('/m/scan');
    await page.getByPlaceholder('手动输入条码或 RFID 编号').fill('RFID-FAIL');
    await page.getByRole('button', { name: '查 询' }).click();
    await expect(page.getByText('查询失败').first()).toBeVisible({ timeout: 15_000 });
  });
});

test.describe('未测路由 / 权限 / 404', () => {
  test('/m 重定向到 /m/index', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/m');
    await expect(page).toHaveURL(/\/m\/index$/, { timeout: 15_000 });
  });

  test('未知路径落到 /404', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/e2e-missing-page-xyz');
    await expect(page).toHaveURL(/\/404$/, { timeout: 15_000 });
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证 /system/tenants/1 落到 /404', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/system/tenants/1');
    await expect(page).toHaveURL(/\/404/, { timeout: 15_000 });
  });

  test('未挂载 /system/tenants 落到 /404', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/system/tenants');
    await expect(page).toHaveURL(/\/404$/, { timeout: 15_000 });
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证 /settings/numbering 重定向工作台编号规则', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/numbering');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-numbering-rules/, { timeout: 15_000 });
  });

  test('已认证 /settings/sysconfig 重定向工作台基础参数', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/sysconfig');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings/webhook 重定向工作台 Webhook', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/webhook');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-webhook-config/, { timeout: 15_000 });
  });

  test('已认证 /settings/notif-pref 重定向工作台通知偏好', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/notif-pref');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-preferences/, { timeout: 15_000 });
  });

  test('已认证 /settings/mail-template 重定向工作台邮件模板', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/mail-template');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-mail-templates/, { timeout: 15_000 });
  });

  test('已认证 /settings/sla-config 重定向工作台 SLA', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/sla-config');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-sla-config/, { timeout: 15_000 });
  });

  test('已认证 /settings 重定向 sysconfig 再进工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings/system 重定向工作台基础参数', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/system');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings/users 重定向用户管理', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/users');
    await expect(page).toHaveURL(/\/system\/users$/, { timeout: 15_000 });
  });

  test('已认证 /settings/departments 重定向部门管理', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/departments');
    await expect(page).toHaveURL(/\/system\/depts$/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2 重定向邮件模板工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-mail-templates/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/webhook 重定向 Webhook 工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/webhook');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-webhook-config/, { timeout: 15_000 });
  });

  test('已认证 /workorders 重定向处置列表', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/workorders');
    await expect(page).toHaveURL(/\/disposals$/, { timeout: 15_000 });
  });

  test('已认证 / 重定向工作台首页', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=home/, { timeout: 15_000 });
  });

  test('未登录访问 / 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('已认证 /settings/notif-channel 重定向通知渠道', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/notif-channel');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-channels/, { timeout: 15_000 });
  });

  test('已认证 /settings/notif-switch 重定向流程通知开关', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/notif-switch');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-workflow-notification-switch/, { timeout: 15_000 });
  });

  test('已认证 /settings/notif-template 重定向通知模板', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/notif-template');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-templates/, { timeout: 15_000 });
  });

  test('已认证 /settings/unknown-tab 回落到基础参数', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/unknown-tab');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/unknown-tab 回落到基础参数', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/unknown-tab');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings/mail-log 重定向邮件日志', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings/mail-log');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-mail-logs/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/notif-channel 重定向通知渠道', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/notif-channel');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-channels/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/notif-switch 重定向流程通知开关', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/notif-switch');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-workflow-notification-switch/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/sla-config 重定向 SLA', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/sla-config');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-sla-config/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/sysconfig 重定向基础参数', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/sysconfig');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-base-params/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/notif-pref 重定向通知偏好', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/notif-pref');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-preferences/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/notif-template 重定向通知模板', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/notif-template');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-notification-templates/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/mail-template 重定向邮件模板', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/mail-template');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-mail-templates/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/mail-log 重定向邮件日志', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/mail-log');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-mail-logs/, { timeout: 15_000 });
  });

  test('已认证 /settings-v2/numbering 重定向编号规则', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/settings-v2/numbering');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=system-numbering-rules/, { timeout: 15_000 });
  });

  test('已认证访问 /login 仍可见欢迎回来', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/login');
    await expect(page.getByRole('heading', { name: '欢迎回来' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('/sso-callback 仅 token 可见 Token 缺失', async ({ page }) => {
    await page.goto('/sso-callback?token=e2e-token');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/sso-callback 仅 username 可见 Token 缺失', async ({ page }) => {
    await page.goto('/sso-callback?username=admin');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/sso-callback 完整参数跳转工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/sso-callback?token=e2e-sso&userId=1&username=admin&realName=管理员&roles=ADMIN');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=home/, { timeout: 15_000 });
  });

  test('/sso-callback 无 userId 仍跳工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/sso-callback?token=e2e-sso&username=admin');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=home/, { timeout: 15_000 });
  });

  test('/sso-callback USER 角色跳转工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/sso-callback?token=e2e-sso&userId=2&username=viewer&realName=只读&roles=USER');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench\?menu=home/, { timeout: 15_000 });
  });

  for (const path of ['/login2', '/login3', '/login4', '/login5']) {
    test(`已认证访问 ${path}?from=e2e 停留登录变体`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, adminUser);
      await page.goto(`${path}?from=e2e`);
      await expect(page).toHaveURL(new RegExp(`${path.replace('/', '\\/')}\\?`));
    });
  }

  for (const path of ['/login2', '/login3', '/login4', '/login5']) {
    test(`已认证访问 ${path} 停留登录变体`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, adminUser);
      await page.goto(path);
      await expect(page).toHaveURL(new RegExp(`${path.replace('/', '\\/')}(\\?|$)`));
    });
  }

  test('已认证 /sso-callback 无参可见 Token 缺失', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/sso-callback');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/login2?expired=1 停留登录变体', async ({ page }) => {
    await page.goto('/login2?expired=1');
    await expect(page).toHaveURL(/\/login2\?/);
  });

  test('/login3?expired=1 停留登录变体', async ({ page }) => {
    await page.goto('/login3?expired=1');
    await expect(page).toHaveURL(/\/login3\?/);
  });

  test('/login4?expired=1 停留登录变体', async ({ page }) => {
    await page.goto('/login4?expired=1');
    await expect(page).toHaveURL(/\/login4\?/);
  });

  test('/login5?expired=1 停留登录变体', async ({ page }) => {
    await page.goto('/login5?expired=1');
    await expect(page).toHaveURL(/\/login5\?/);
  });

  test('/sso-callback 空 token 可见 Token 缺失', async ({ page }) => {
    await page.goto('/sso-callback?token=&username=admin');
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证访问 /forbidden 可见无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/forbidden');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证访问 /workspace-preview 可见设计稿', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/workspace-preview');
    await expect(page.getByText('固定资产工作台设计').first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证访问 /vendor-portal 可见供应商门户', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/vendor-portal');
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /forbidden 可见无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/forbidden');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of [
    '/gis/1', '/energy/1', '/sam/1', '/licenses/1',
    '/notifications/1', '/depreciation/1', '/floorplans/1', '/maintenance/1',
    '/idle/1', '/dashboard/1', '/reports/1', '/analytics/1',
    '/inventory/1', '/categories/1', '/test-results/1', '/abc/1',
    '/locations/1', '/vendors/1', '/fault-codes/1', '/manufacturers/1',
    '/asset-models/1', '/contracts/1', '/purchase-orders/1', '/risk-matrix/1',
    '/report-builder/1', '/workflow-designer/1', '/bigscreen/1', '/bigscreen-3d/1',
    '/system/custom-fields/1', '/system/custom-fieldsets/1', '/reports/scheduled/1',
    '/inspection-templates/1', '/inspection-records/1', '/safety-checklists/config/1',
    '/system/users/1', '/system/roles/1', '/system/depts/1', '/system/posts/1', '/system/menus/1',
    '/profile/1', '/workflows/1', '/risk-assessments/1', '/safety-checklists/history/1',
    '/inventory/abc-classification/1', '/inventory/cycle-count/1', '/asset-health/1',
    '/analytics/tco/1', '/analytics/health/1', '/analytics/reliability/1',
    '/m/unknown-xyz', '/m/work-orders/1', '/m/notifications/1', '/m/scan/1', '/m/profile/1',
    '/retirement/1/edit', '/approvals/1/edit', '/disposals/1/edit', '/intake/1/edit',
    '/compensation/1/edit', '/spare-parts/1/edit',
    '/workflows-v2/1', '/fixed-assets/foo', '/vendor-portal/1',
    '/login/foo', '/forbidden/1', '/sso-callback/1', '/workspace-preview/1',
    '/stocktaking-cycles/1/edit', '/revaluations/1/edit', '/depreciation/1/edit',
    '/licenses/1/edit', '/fault-codes/1/edit', '/vendors/1/edit',
    '/workorders/1/edit', '/inventory/1/edit', '/inventory/tasks/1/edit',
    '/m/index/foo', '/assets/import-export/1',
    '/fixed-assets/workbenchv3/1', '/compensation/new/edit',
    '/safety-checklists/foo', '/risk-assessments/foo',
  ]) {
    test(`已认证 ${path} 未挂载子路由落到 /404`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, adminUser);
      await page.goto(path);
      await expect(page).toHaveURL(/\/404/, { timeout: 15_000 });
    });
  }

  test('USER 访问 /bigscreen 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/bigscreen');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /system/users 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/system/users');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /analytics 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/analytics');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /energy 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/energy');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /inventory 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/inventory');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /disposals 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/disposals');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /approvals 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/approvals');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /gis 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/gis');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /reports 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/reports');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /audit 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/audit');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /settings 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/settings');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /workflows 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/workflows');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('/m/assets/999 显示资产不存在', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/m/assets/999');
    await expect(page.getByText('未找到对应资产').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/m/assets/1 API 失败显示资产加载失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/1'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets/1');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of [
    '/equipment', '/categories', '/vendors', '/spare-parts', '/insurances', '/inspections',
    '/borrows', '/assignments', '/idle', '/depreciation', '/licenses', '/sam',
    '/fault-codes', '/budgets', '/notifications', '/locations', '/floorplans', '/risk-assessments',
    '/intake', '/purchase-orders', '/contracts', '/compensation', '/revaluations', '/stocktaking-cycles',
    '/asset-models', '/manufacturers', '/system/roles', '/system/depts', '/bigscreen-3d',
    '/workorders', '/system/posts', '/system/menus', '/safety-checklists/config', '/report-builder',
    '/workflow-designer', '/inspection-records', '/risk-matrix', '/reports/scheduled',
    '/dashboard', '/analytics/tco', '/asset-health', '/system/custom-fields',
    '/workflow-form/ASSET_TRANSFER', '/test-results',
    '/analytics/reliability', '/analytics/health', '/system/custom-fieldsets',
    '/inventory/abc-classification', '/inventory/cycle-count', '/safety-checklists/history',
    '/settings/notif-pref', '/settings/mail-template', '/settings/webhook', '/settings/sla-config',
    '/safety-checklists/execute',
    '/settings/notif-template', '/settings/notif-channel', '/settings/notif-switch', '/settings/mail-log',
    '/settings-v2', '/settings/sysconfig', '/inspection-templates', '/disposals/transfer/new', '/abc',
    '/disposals/clearance/new', '/disposals/scrap/new', '/settings-v2/mail-template', '/maintenance/plans',
    '/workflow-form/ASSET_CLEARANCE', '/workflow-form/ASSET_SCRAP', '/compensation/new',
    '/inventory/smart-report', '/borrows/new', '/assignments/new',
    '/inspections/new', '/budgets/new', '/intake/new', '/insurances/new',
    '/revaluations/new', '/stocktaking-cycles/new', '/workflow-form/ASSET_RETIREMENT',
    '/audit/1', '/inventory/tasks/1',
    '/risk-assessments/new', '/spare-parts/new', '/workflow-form/ASSET_COMPENSATION',
    '/inventory/scan/RFID-1', '/safety-checklists/execute/1',
    '/equipment/1', '/locations/1', '/vendors/1', '/fault-codes/1',
    '/spare-parts/1', '/insurances/1', '/borrows/1', '/assignments/1', '/budgets/1', '/intake/1',
    '/inspections/1', '/revaluations/1', '/stocktaking-cycles/1', '/depreciation/1', '/notifications/1',
    '/compensation/1', '/workorders/1', '/workorders/new', '/maintenance/1', '/gis/1',
    '/floorplans/1', '/energy/1', '/sam/1', '/licenses/1', '/report-builder/1',
    '/bigscreen/1', '/analytics/1', '/inventory/1',
    '/approvals/1', '/disposals/1', '/idle/1',
    '/inspections/1/edit', '/inspections/1/upload', '/assignments/1/edit',
    '/borrows/1/edit', '/workorders/1/acceptance', '/risk-assessments/1/edit',
    '/inventory/smart-report/INV-001', '/settings/system', '/settings/users',
    '/settings/departments',
    '/system/users/1', '/categories/1', '/test-results/1', '/settings-v2/notif-pref',
    '/system/posts/1', '/system/depts/1', '/system/menus/1', '/system/roles/1',
    '/settings-v2/webhook', '/settings-v2/mail-log', '/settings-v2/notif-channel',
    '/settings-v2/notif-switch', '/settings-v2/sla-config',
    '/settings-v2/sysconfig', '/abc/1',
    '/disposals/1/edit', '/approvals/1/edit',
    '/intake/1/edit', '/budgets/1/edit', '/compensation/1/edit',
    '/revaluations/1/edit', '/insurances/1/edit', '/spare-parts/1/edit',
    '/stocktaking-cycles/1/edit', '/depreciation/1/edit', '/licenses/1/edit',
    '/fault-codes/1/edit', '/vendors/1/edit', '/locations/1/edit',
    '/manufacturers/1/edit', '/asset-models/1/edit', '/equipment/1/edit',
    '/categories/1/edit', '/contracts/1/edit', '/purchase-orders/1/edit',
    '/notifications/1/edit', '/idle/1/edit', '/gis/1/edit',
    '/energy/1/edit', '/sam/1/edit', '/floorplans/1/edit',
    '/maintenance/1/edit', '/audit/1/edit', '/report-builder/1/edit',
    '/test-results/1/edit', '/bigscreen/1/edit', '/analytics/1/edit',
    '/workorders/1/edit', '/inventory/1/edit', '/inventory/tasks/1/edit',
    '/risk-matrix/1/edit', '/inspection-templates/1/edit', '/inspection-records/1/edit',
    '/safety-checklists/config/1', '/reports/scheduled/1', '/system/custom-fields/1',
    '/system/custom-fieldsets/1', '/workflow-designer/1',
    '/analytics/tco/1', '/analytics/health/1', '/analytics/reliability/1',
    '/reports/1', '/bigscreen-3d/1', '/inventory/abc-classification/1',
    '/inventory/cycle-count/1', '/settings/notif-pref/1', '/settings/webhook/1',
    '/settings/mail-template/1', '/settings/sla-config/1', '/settings/mail-log/1',
    '/settings/notif-channel/1', '/settings/notif-switch/1', '/settings/notif-template/1',
    '/safety-checklists/history/1', '/safety-checklists/execute/2',
    '/settings-v2/mail-template/1', '/abc/1/edit', '/workflow-form/FOO',
    '/dashboard/1', '/gis/foo', '/energy/foo',
    '/sam/foo', '/floorplans/foo', '/licenses/foo',
    '/locations/foo', '/vendors/foo', '/fault-codes/foo',
    '/categories/foo', '/manufacturers/foo', '/asset-models/foo',
    '/equipment/foo', '/idle/foo', '/borrows/foo',
    '/assignments/foo', '/inspections/foo', '/spare-parts/foo',
    '/insurances/foo', '/intake/foo', '/budgets/foo',
    '/contracts/foo', '/purchase-orders/foo', '/compensation/foo',
    '/revaluations/foo', '/depreciation/foo', '/notifications/foo',
    '/workorders/foo', '/inventory/foo', '/disposals/foo',
    '/approvals/foo', '/audit/foo', '/reports/foo',
    '/maintenance/foo', '/stocktaking-cycles/foo', '/risk-assessments/foo',
    '/settings/foo', '/system/users/foo', '/system/roles/foo',
    '/system/depts/foo', '/system/posts/foo', '/system/menus/foo',
    '/system/custom-fields/foo', '/bigscreen/foo', '/abc/foo',
    '/test-results/foo', '/report-builder/foo', '/workflow-designer/foo',
    '/analytics/foo', '/bigscreen-3d/foo', '/risk-matrix/foo',
    '/inspection-templates/foo', '/inspection-records/foo', '/reports/scheduled/foo',
    '/workflow-form/BAR', '/settings-v2/foo', '/system/custom-fieldsets/foo',
    '/licenses/bar', '/gis/bar', '/energy/bar',
    '/sam/bar', '/floorplans/bar', '/locations/bar',
    '/vendors/bar', '/fault-codes/bar',
    '/categories/bar',
    '/settings/numbering', '/settings/numbering/1',
    '/notifications/new', '/report-builder/new', '/licenses/new',
    '/manufacturers/new', '/asset-models/new', '/vendors/new',
    '/locations/new', '/fault-codes/new', '/idle/new',
    '/purchase-orders/new', '/contracts/new', '/licenses/new/edit',
    '/manufacturers/new/edit', '/asset-models/new/edit',
    '/settings-v2/numbering',
    '/inventory/smart-report/FOO', '/inventory/scan/FOO',
    '/safety-checklists/execute/FOO', '/workflow-form/UNKNOWN_TYPE',
    '/settings-v2/notif-template', '/disposals/2', '/purchase-orders/1',
    '/inventory/tasks/2', '/inventory/tasks/3', '/approvals/2',
    '/borrows/2', '/assignments/2', '/intake/2', '/budgets/2',
    '/inspections/2', '/spare-parts/2', '/insurances/2',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }

  for (const id of Array.from({ length: 20 }, (_, i) => i + 6)) {
    test(`USER 访问 /borrows/${id} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(`/borrows/${id}`);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
    test(`USER 访问 /assignments/${id} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(`/assignments/${id}`);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
    test(`USER 访问 /intake/${id} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(`/intake/${id}`);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
    test(`USER 访问 /inspections/${id} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(`/inspections/${id}`);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
    test(`USER 访问 /budgets/${id} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(`/budgets/${id}`);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }

  test('USER 有台账权限时可打开 /assets 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets');
    await expect(page.getByText('暂无资产').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 无规则路径 /retirement 可见空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/retirement');
    await expect(page.getByText('暂无退役申请记录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /maintenance 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/maintenance');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 有台账权限可打开工作台', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/fixed-assets/workbench');
    await expect(page).toHaveURL(/\/fixed-assets\/workbench/, { timeout: 15_000 });
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  for (const path of ['/equipment', '/analytics', '/gis']) {
    test(`未登录访问 ${path} 跳转 /login`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await page.goto(path);
      await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
    });
  }

  test('未登录访问 /inventory 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/inventory');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录访问 /disposals 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/disposals');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录访问 /approvals 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/approvals');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('USER 有查询权限可打开 /assets/import-export', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/import-export');
    await expect(page).toHaveURL(/\/assets\/import-export/, { timeout: 15_000 });
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 有台账权限可打开 /assets/import-export/1', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/import-export/1');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 无精确规则可打开 /asset-health/1', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/asset-health/1');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('未登录访问 /retirement 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/retirement');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录访问 /dashboard 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/dashboard');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('USER 无创建权限访问 /assets/new 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/new');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of ['/m/scan', '/m/assets', '/m/work-orders', '/m/notifications']) {
    test(`未登录访问 ${path} 跳转 /login`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await page.goto(path);
      await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
    });
  }

  test('未登录访问 /m/profile 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/m/profile');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录访问 /m 跳转 /login', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/m');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('/login?expired=1 可见会话过期提示', async ({ page }) => {
    await page.goto('/login?expired=1');
    await expect(page.getByText('您的会话已过期，请重新登录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证 /login?expired=1 可见会话过期提示', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/login?expired=1');
    await expect(page.getByText('您的会话已过期，请重新登录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /login?redirect=/settings/numbering 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/settings/numbering');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/m/scan 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/m/scan');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/vendor-portal 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/vendor-portal');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/analytics 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/analytics');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/system/users 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/system/users');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/ 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/bigscreen 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/bigscreen');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/energy 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/energy');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/gis 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/gis');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/reports 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/reports');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/contracts 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/contracts');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/maintenance 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/maintenance');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/idle 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/idle');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/categories 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/categories');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/vendors 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/vendors');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/locations 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/locations');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/licenses 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/licenses');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/sam 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/sam');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/notifications 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/notifications');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/audit 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/audit');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/workflows-v2 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/workflows-v2');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?expired=1&redirect=/m/scan 可见会话过期提示', async ({ page }) => {
    await page.goto('/login?expired=1&redirect=/m/scan');
    await expect(page.getByText('您的会话已过期，请重新登录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/login?expired=1&redirect=/assets 可见会话过期提示', async ({ page }) => {
    await page.goto('/login?expired=1&redirect=/assets');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
    await expect(page.getByText('您的会话已过期，请重新登录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /login 可见欢迎回来', async ({ page }) => {
    await page.goto('/login');
    await expect(page.getByRole('heading', { name: '欢迎回来' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录未知路径跳转 /login', async ({ page }) => {
    await page.goto('/e2e-anon-missing-xyz');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('ADMIN 访问 /403 可见无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/403');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('ADMIN 访问 /404 可见页面不存在', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/404');
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /login?redirect=/sso-callback 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/sso-callback');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('已认证访问 /403 可见无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/403');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('已认证访问 /404 可见页面不存在', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/404');
    await expect(page.getByText('404 — 页面不存在').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /404 跳转 /login', async ({ page }) => {
    await page.goto('/404');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /forbidden 可见无访问权限', async ({ page }) => {
    await page.goto('/forbidden');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /workspace-preview 不跳登录', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/workspace-preview');
    await expect(page).not.toHaveURL(/\/login/, { timeout: 15_000 });
    await expect(page.getByText('固定资产工作台设计').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /vendor-portal 可见供应商门户', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await page.goto('/vendor-portal');
    await expect(page).not.toHaveURL(/\/login/, { timeout: 15_000 });
    await expect(page.getByText('供应商门户').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /sso-callback 可见 Token 缺失', async ({ page }) => {
    await page.goto('/sso-callback');
    await expect(page).not.toHaveURL(/\/login/, { timeout: 15_000 });
    await expect(page.getByText('Token 缺失，SSO 登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of ['/login2', '/login3', '/login4', '/login5']) {
    test(`未登录访问 ${path} 停留登录变体`, async ({ page }) => {
      await page.goto(path);
      await expect(page).toHaveURL(new RegExp(`${path.replace('/', '\\/')}(\\?|$)`));
    });
  }

  for (const path of ['/login2', '/login3', '/login4', '/login5']) {
    test(`未登录访问 ${path}?from=e2e 停留登录变体`, async ({ page }) => {
      await page.goto(`${path}?from=e2e`);
      await expect(page).toHaveURL(new RegExp(`${path.replace('/', '\\/')}\\?`));
    });
  }

  test('USER 可打开 /m/assets 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/assets');
    await expect(page.getByText('暂无资产数据').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/scan 查询资产', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/scan');
    await expect(page.getByText('查询资产').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/work-orders 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/work-orders');
    await expect(page.getByText('暂无待办工单').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/index 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/index');
    await expect(page.getByText('暂无待办事项').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /assets/3', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/3');
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /assets/2', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/2');
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /assets/1', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/1');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 有台账权限可打开 /assets/2/edit', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/2/edit');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 有台账权限可打开 /assets/1/edit', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/1/edit');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 有台账权限可打开工作台 assets section', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/fixed-assets/workbench/assets?menu=asset');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('ADMIN 可打开 /m/profile', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/m/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/profile', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/profile');
    await expect(page.getByText('只读用户').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/notifications 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/notifications');
    await expect(page.getByText('暂无未读通知').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 无规则可打开 /retirement/new', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/retirement/new');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 无规则可打开 /retirement/1 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/retirement/1');
    await expect(page.getByText('暂无审批记录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 无规则可打开 /retirement/1/edit', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/retirement/1/edit');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER 有台账权限可打开 /assets/2/timeline 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/2/timeline');
    await expect(page.getByText('暂无履历记录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 有台账权限可打开 /assets/1/timeline 空态', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assets/1/timeline');
    await expect(page.getByText('暂无履历记录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('ADMIN 可打开 /bigscreen', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/bigscreen');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('ADMIN 可打开 /bigscreen-3d', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/bigscreen-3d');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('ADMIN /profile 可见真实姓名', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 无规则路径 /profile 可见真实姓名', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/profile');
    await expect(page.getByText('只读用户').first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of ['/m/unknown-xyz', '/m/work-orders/1', '/profile/1', '/workflows-v2/1', '/retirement/1/edit']) {
    test(`USER 访问未挂载 ${path} 落到 /404`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page).toHaveURL(/\/404/, { timeout: 15_000 });
    });
  }

  test('ADMIN 可打开 /m/assets/2 详情', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/m/assets/2');
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/borrows/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/borrows/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/assignments/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/assignments/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/intake/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/intake/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/workorders/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workorders/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/approvals/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/approvals/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/disposals/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/disposals/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/audit/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/audit/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/retirement/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/retirement/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/budgets/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/budgets/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/inspections/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/inspections/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/compensation/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/compensation/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/m/assets/abc 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/m/assets/abc');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute/999 不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/safety-checklists/execute/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/workflow-form/UNKNOWN_TYPE 不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/workflow-form/UNKNOWN_TYPE');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/equipment/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/equipment/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/insurances/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/insurances/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('/inventory/tasks/999 无详情不崩溃', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    const errors = collectBrowserErrors(page);
    await page.goto('/inventory/tasks/999');
    await page.waitForLoadState('domcontentloaded');
    expect(errors).toEqual([]);
  });

  test('ADMIN 可打开 /assets/2 详情', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/assets/2');
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/assets/2 详情', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/assets/2');
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/assets/1 详情', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/assets/1');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
    await expect(page.getByText('E2E-ASSET').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /fixed-assets/workbenchv3', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page).toHaveURL(/\/fixed-assets\/workbenchv3/, { timeout: 15_000 });
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('USER /retirement/999 失败显示未找到', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/999'));
    await seedSession(page, limitedUser);
    await page.goto('/retirement/999');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER /assets/999 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/999'));
    await seedSession(page, limitedUser);
    await page.goto('/assets/999');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 可打开 /m/stocktaking-tasks/1 失败态', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/tasks/1'));
    await seedSession(page, limitedUser);
    await page.goto('/m/stocktaking-tasks/1');
    await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 无规则可打开 /workflows-v2', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/workflows-v2');
    await expect(page.getByRole('heading', { name: '无访问权限' })).toHaveCount(0);
  });

  test('/vendor-portal 错误密码显示登录失败', async ({ page }) => {
    await page.addInitScript(() => {
      window.localStorage.removeItem('vendor_token');
      window.localStorage.removeItem('vendor_id');
      window.localStorage.removeItem('vendor_name');
    });
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/vendor-portal/login'));
    await page.goto('/vendor-portal');
    await page.getByPlaceholder('供应商编码').fill('V001');
    await page.getByPlaceholder('密码').fill('bad');
    await page.getByRole('button', { name: '登录' }).click();
    await expect(page.getByText('登录失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/vendor-portal 登录后可见暂无合同数据', async ({ page }) => {
    await page.addInitScript(() => {
      window.localStorage.removeItem('vendor_token');
      window.localStorage.removeItem('vendor_id');
      window.localStorage.removeItem('vendor_name');
    });
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/vendor-portal/login') {
        return fulfill(route, { token: 'vendor-token', vendorId: 1, vendorName: 'E2E供应商' });
      }
      if (path.startsWith('/vendor-portal/contracts')) {
        return fulfill(route, []);
      }
      return mockApi(route);
    });
    await page.goto('/vendor-portal');
    await page.getByPlaceholder('供应商编码').fill('V001');
    await page.getByPlaceholder('密码').fill('ok');
    await page.getByRole('button', { name: '登录' }).click();
    await expect(page.getByText('暂无合同数据').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER /m/assets/999 显示资产不存在', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/m/assets/999');
    await expect(page.getByText('未找到对应资产').first()).toBeVisible({ timeout: 15_000 });
  });

  test('未登录 /login?redirect=/assets 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/assets');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/m/assets/1 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/m/assets/1');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/m/stocktaking-tasks/1 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/m/stocktaking-tasks/1');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/m/index 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/m/index');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/profile 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/profile');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/retirement 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/retirement');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/fixed-assets/workbench 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/fixed-assets/workbench');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/403 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/403');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('未登录 /login?redirect=/404 停留登录页', async ({ page }) => {
    await page.goto('/login?redirect=/404');
    await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
  });

  test('/vendor-portal 登录后合同加载失败', async ({ page }) => {
    await page.addInitScript(() => {
      window.localStorage.removeItem('vendor_token');
      window.localStorage.removeItem('vendor_id');
      window.localStorage.removeItem('vendor_name');
    });
    await page.route('**/api/**', async (route) => {
      const url = new URL(route.request().url());
      const path = url.pathname.replace(/^\/api/, '');
      if (path === '/vendor-portal/login') {
        return fulfill(route, { token: 'vendor-token', vendorId: 1, vendorName: 'E2E供应商' });
      }
      if (path.startsWith('/vendor-portal/contracts')) {
        return fulfill(route, null, 500, '获取数据失败');
      }
      return mockApi(route);
    });
    await page.goto('/vendor-portal');
    await page.getByPlaceholder('供应商编码').fill('V001');
    await page.getByPlaceholder('密码').fill('ok');
    await page.getByRole('button', { name: '登录' }).click();
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER /m/assets/3 API 失败显示资产加载失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/3'));
    await seedSession(page, limitedUser);
    await page.goto('/m/assets/3');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER /m/assets/1 API 失败显示资产加载失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/1'));
    await seedSession(page, limitedUser);
    await page.goto('/m/assets/1');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/test-results 失败显示加载测试结果失败', async ({ page }) => {
    await page.route('**/test-reports/data.json', async (route) => {
      await route.fulfill({ status: 500, contentType: 'text/plain', body: 'fail' });
    });
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/test-results');
    await expect(page.getByText('加载测试结果失败').first()).toBeVisible({ timeout: 15_000 });
  });

  for (const path of [
    '/assets', '/profile', '/energy', '/contracts', '/system/users', '/reports', '/audit', '/settings', '/workflows', '/bigscreen',
    '/maintenance', '/borrows', '/idle', '/locations', '/vendors', '/categories', '/fault-codes', '/notifications',
    '/spare-parts', '/insurances', '/inspections', '/assignments', '/depreciation', '/licenses', '/intake', '/purchase-orders', '/m/index',
    '/sam',
    '/revaluations', '/stocktaking-cycles', '/asset-models', '/manufacturers', '/system/roles', '/system/depts',
    '/floorplans', '/risk-matrix', '/test-results', '/fixed-assets/workbench', '/compensation', '/workorders', '/budgets',
    '/system/posts', '/system/menus', '/risk-assessments', '/report-builder', '/workflow-designer',
    '/inspection-records', '/reports/scheduled', '/analytics/tco', '/asset-health', '/bigscreen-3d',
    '/safety-checklists/config', '/inspection-templates', '/assets/new', '/m/assets/1',
    '/403', '/analytics/health', '/analytics/reliability', '/system/custom-fields', '/m/stocktaking-tasks/1',
    '/system/custom-fieldsets', '/assets/1', '/assets/1/edit', '/retirement/new',
    '/404', '/equipment/1', '/borrows/new', '/inspections/new', '/spare-parts/new', '/compensation/new',
    '/workflow-form/ASSET_TRANSFER', '/maintenance/plans', '/intake/new', '/budgets/new',
    '/insurances/new', '/revaluations/new', '/stocktaking-cycles/new', '/inventory/smart-report',
    '/risk-assessments/new', '/assignments/new', '/safety-checklists/execute', '/disposals/transfer/new',
    '/workflow-form/ASSET_CLEARANCE', '/inventory/tasks/1', '/audit/1', '/spare-parts/1',
    '/disposals/clearance/new', '/disposals/scrap/new', '/workflow-form/ASSET_SCRAP',
    '/workflow-form/ASSET_RETIREMENT', '/workflow-form/ASSET_COMPENSATION', '/inventory/scan/RFID-1',
    '/safety-checklists/execute/1', '/safety-checklists/history', '/settings/sysconfig', '/settings-v2', '/abc',
    '/locations/1', '/vendors/1', '/fault-codes/1', '/insurances/1', '/borrows/1', '/inspections/1',
    '/settings/notif-pref', '/settings/mail-template', '/settings/webhook',
    '/settings/notif-channel', '/settings/notif-switch', '/settings/mail-log',
    '/compensation/1', '/workorders/1', '/workorders/new', '/maintenance/1', '/gis/1',
    '/floorplans/1', '/energy/1', '/sam/1', '/licenses/1',
    '/report-builder/1', '/notifications/1', '/depreciation/1', '/revaluations/1',
    '/stocktaking-cycles/1', '/assignments/1', '/budgets/1', '/intake/1', '/assets/1/timeline',
    '/workflows-v2', '/bigscreen/1', '/analytics/1', '/fixed-assets/workbench/assets',
    '/approvals/1', '/disposals/1', '/idle/1', '/inventory/1',
    '/test-results/1', '/categories/1', '/system/users/1', '/m/assets/2',
    '/inventory/abc-classification', '/inventory/cycle-count', '/settings/notif-template',
    '/settings/sla-config', '/inspections/1/edit', '/inspections/1/upload',
    '/assignments/1/edit', '/borrows/1/edit', '/workorders/1/acceptance',
    '/risk-assessments/1/edit', '/inventory/smart-report/INV-001',
    '/settings/system', '/settings/users', '/settings/departments', '/fixed-assets/workbenchv3',
    '/settings-v2/notif-pref', '/system/roles/1', '/m/work-orders/1', '/fixed-assets/workbench/home',
    '/system/posts/1', '/system/depts/1', '/system/menus/1', '/settings-v2/webhook',
    '/settings-v2/mail-log', '/settings-v2/notif-channel', '/settings-v2/sla-config',
    '/settings-v2/notif-switch', '/fixed-assets/workbench/approvals', '/m/notifications/1',
    '/settings-v2/sysconfig', '/fixed-assets/workbench/orders', '/m/profile/1',
    '/fixed-assets/workbench/maintenance', '/fixed-assets/workbench/inventory', '/m/scan/1', '/abc/1',
    '/fixed-assets/workbench/energy', '/fixed-assets/workbench/reports', '/fixed-assets/workbench/settings',
    '/fixed-assets/workbench/gis', '/fixed-assets/workbench/audit', '/fixed-assets/workbench/workflow',
    '/fixed-assets/workbench/dashboard', '/disposals/1/edit', '/approvals/1/edit',
    '/intake/1/edit', '/budgets/1/edit', '/compensation/1/edit',
    '/revaluations/1/edit', '/insurances/1/edit', '/spare-parts/1/edit',
    '/stocktaking-cycles/1/edit', '/depreciation/1/edit', '/licenses/1/edit',
    '/fault-codes/1/edit', '/vendors/1/edit', '/locations/1/edit',
    '/manufacturers/1/edit', '/asset-models/1/edit', '/equipment/1/edit',
    '/categories/1/edit', '/contracts/1/edit', '/purchase-orders/1/edit',
    '/notifications/1/edit', '/idle/1/edit', '/gis/1/edit',
    '/energy/1/edit', '/sam/1/edit', '/floorplans/1/edit',
    '/maintenance/1/edit', '/audit/1/edit', '/report-builder/1/edit',
    '/test-results/1/edit', '/bigscreen/1/edit', '/analytics/1/edit',
    '/workorders/1/edit', '/inventory/1/edit', '/inventory/tasks/1/edit',
    '/risk-matrix/1/edit', '/inspection-templates/1/edit', '/inspection-records/1/edit',
    '/safety-checklists/config/1', '/reports/scheduled/1', '/system/custom-fields/1',
    '/system/custom-fieldsets/1', '/asset-health/1', '/workflow-designer/1',
    '/analytics/tco/1', '/analytics/health/1', '/analytics/reliability/1',
    '/reports/1', '/bigscreen-3d/1', '/inventory/abc-classification/1',
    '/inventory/cycle-count/1', '/settings/notif-pref/1', '/settings/webhook/1',
    '/settings/mail-template/1', '/settings/sla-config/1', '/settings/mail-log/1',
    '/settings/notif-channel/1', '/settings/notif-switch/1', '/settings/notif-template/1',
    '/safety-checklists/history/1', '/safety-checklists/execute/2',
    '/settings-v2/mail-template/1', '/abc/1/edit', '/workflow-form/FOO',
    '/fixed-assets/workbench/profile', '/m/index/foo', '/assets/import-export/1',
    '/fixed-assets/workbench/scan', '/m/assets/foo', '/retirement/1/edit',
    '/dashboard/1', '/profile/1',
    '/m/work-orders/foo', '/m/notifications/foo', '/m/scan/foo',
    '/system/tenants/1', '/unknown-e2e-zzz',
    '/sam/foo', '/floorplans/foo', '/licenses/foo',
    '/locations/foo', '/vendors/foo', '/fault-codes/foo',
    '/categories/foo', '/manufacturers/foo', '/asset-models/foo',
    '/equipment/foo', '/idle/foo', '/borrows/foo',
    '/assignments/foo', '/inspections/foo', '/spare-parts/foo',
    '/insurances/foo', '/intake/foo', '/budgets/foo',
    '/contracts/foo', '/purchase-orders/foo', '/compensation/foo',
    '/revaluations/foo', '/depreciation/foo', '/notifications/foo',
    '/workorders/foo', '/inventory/foo', '/disposals/foo',
    '/approvals/foo', '/audit/foo', '/reports/foo',
    '/maintenance/foo', '/stocktaking-cycles/foo', '/risk-assessments/foo',
    '/settings/foo', '/system/users/foo', '/system/roles/foo',
    '/system/depts/foo', '/system/posts/foo', '/system/menus/foo',
    '/system/custom-fields/foo', '/bigscreen/foo', '/abc/foo',
    '/test-results/foo', '/report-builder/foo', '/workflow-designer/foo',
    '/analytics/foo', '/bigscreen-3d/foo', '/risk-matrix/foo',
    '/inspection-templates/foo', '/inspection-records/foo', '/reports/scheduled/foo',
    '/workflow-form/BAR', '/settings-v2/foo', '/system/custom-fieldsets/foo',
    '/licenses/bar', '/gis/bar', '/energy/bar',
    '/sam/bar', '/floorplans/bar', '/locations/bar',
    '/vendors/bar', '/fault-codes/bar',
    '/categories/bar',
    '/settings/numbering', '/settings/numbering/1',
    '/notifications/new', '/report-builder/new', '/licenses/new',
    '/manufacturers/new', '/asset-models/new', '/vendors/new',
    '/locations/new', '/fault-codes/new', '/idle/new',
    '/m/stocktaking-tasks/foo', '/fixed-assets/workbench/foo',
    '/purchase-orders/new', '/contracts/new',
    '/settings-v2/numbering',
    '/inventory/smart-report/FOO', '/inventory/scan/FOO',
    '/safety-checklists/execute/FOO', '/workflow-form/UNKNOWN_TYPE',
    '/assets/import-export', '/retirement/1', '/settings-v2/mail-template',
    '/settings-v2/notif-template', '/m/stocktaking-tasks/2', '/disposals/2',
    '/fixed-assets/workbench/todo', '/assets/3/timeline',
    '/inventory/tasks/2', '/approvals/2', '/borrows/2', '/assignments/2',
    '/intake/2', '/budgets/2', '/inspections/2', '/spare-parts/2',
  ]) {
    test(`未登录访问 ${path} 跳转 /login`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await page.goto(path);
      await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
    });
  }
});

test.describe('Q120 API 失败态独立', () => {
  test('/workflows 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflows');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/asset-health 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/asset-health/unhealthy'));
    await seedSession(page, adminUser);
    await page.goto('/asset-health');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/audit 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs'));
    await seedSession(page, adminUser);
    await page.goto('/audit');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/dashboard 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/dashboard/stats'));
    await seedSession(page, adminUser);
    await page.goto('/dashboard');
    await expect(page.getByText('运营首页').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/vendor-portal 已登录合同非数组不崩溃', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.addInitScript(() => {
      window.localStorage.setItem('vendor_token', 'vendor-token');
      window.localStorage.setItem('vendor_id', '1');
      window.localStorage.setItem('vendor_name', 'E2E供应商');
    });
    await page.route('**/api/**', mockApi);
    await page.goto('/vendor-portal');
    await expect(page.getByText('暂无合同数据').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q121 API 失败态独立', () => {
  test('/bigscreen 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/bigscreen/stats'));
    await seedSession(page, adminUser);
    await page.goto('/bigscreen');
    await expect(page.getByText('资产运营分析平台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/bigscreen-3d 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/bigscreen/stats'));
    await seedSession(page, adminUser);
    await page.goto('/bigscreen-3d');
    await expect(page.getByText('固定资产智慧运营大屏').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/health 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/asset-health/unhealthy'));
    await seedSession(page, adminUser);
    await page.goto('/analytics/health');
    await expect(page.getByRole('heading', { name: '资产健康评分' }).first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/reliability 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/reliability/trend'));
    await seedSession(page, adminUser);
    await page.goto('/analytics/reliability');
    await expect(page.getByText('可靠性分析').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/analytics/tco 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/tco'));
    await seedSession(page, adminUser);
    await page.goto('/analytics/tco');
    await expect(page.getByText('TCO 全生命周期成本').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q122 新建表单失败态独立', () => {
  test('/assets/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/categories/tree'));
    await seedSession(page, adminUser);
    await page.goto('/assets/new');
    await expect(page.getByText('新建资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/new');
    await expect(page.getByText('新建借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/new');
    await expect(page.getByText('新建领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/new');
    await expect(page.getByText('新增检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/intake/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/intake/new');
    await expect(page.getByText('新建验收单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q123 新建表单失败态独立', () => {
  test('/budgets/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/depts/tree'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/new');
    await expect(page.getByText('新增预算').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/insurances/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/new');
    await expect(page.getByText('新增保险').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/retirement/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/new');
    await expect(page.getByText('资产退役申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/spare-parts/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/new');
    await expect(page.getByText('备件申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/compensation/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/depts/tree'));
    await seedSession(page, adminUser);
    await page.goto('/compensation/new');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q124 新建表单失败态独立', () => {
  test('/disposals/scrap/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/scrap/new');
    await expect(page.getByText('资产报废申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/clearance/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/clearance/new');
    await expect(page.getByText('资产清退申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/disposals/transfer/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/transfer/new');
    await expect(page.getByText('资产转移申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workorders/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/new');
    await expect(page.getByText('新建工单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/revaluations/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/revaluations/new');
    await expect(page.getByText('新增减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q125 USER 403 / 未登录补测', () => {
  for (const path of [
    '/workflow-form',
    '/disposals/transfer',
    '/disposals/clearance',
    '/disposals/scrap',
    '/settings-v2/numbering/1',
    '/maintenance/plans/1',
    '/workflow-form/ASSET_BORROW',
    '/workflow-form/ASSET_ASSIGNMENT',
    '/workflow-form/ASSET_INTAKE',
    '/abc/new',
    '/test-results/new',
    '/gis/map',
    '/inventory/scan/RFID-2',
    '/assignments/2/edit',
    '/borrows/2/edit',
    '/safety-checklists/execute/3',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }

  for (const path of [
    '/fixed-assets/workbench/device',
    '/fixed-assets/workbench/inspection',
    '/fixed-assets/workbench/spares',
    '/fixed-assets/workbench/alarm',
    '/fixed-assets/workbench/policy',
    '/m/stocktaking-tasks/3',
    '/m/stocktaking-tasks/4',
    '/workflow-form',
    '/disposals/transfer',
    '/disposals/clearance',
    '/disposals/scrap',
    '/settings-v2/numbering/1',
    '/maintenance/plans/1',
    '/workflow-form/ASSET_BORROW',
    '/gis/map',
    '/abc/new',
  ]) {
    test(`未登录访问 ${path} 跳转 /login`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await page.goto(path);
      await expect(page).toHaveURL(/\/login/, { timeout: 15_000 });
    });
  }
});

test.describe('Q126 新建/编辑失败态独立', () => {
  test('/stocktaking-cycles/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/cycles'));
    await seedSession(page, adminUser);
    await page.goto('/stocktaking-cycles/new');
    await expect(page.getByText('新建盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assets/import-export 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/assets/import-export');
    await expect(page.getByText('资产批量导入导出').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/risk-assessments/new 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/risk-assessments'));
    await seedSession(page, adminUser);
    await page.goto('/risk-assessments/new');
    await expect(page.getByText('新增风险评估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/assignments/1/edit 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/1'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/1/edit');
    await expect(page.getByText('编辑领用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/borrows/1/edit 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/1'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/1/edit');
    await expect(page.getByText('编辑借用单').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q127 编辑/配置失败态独立', () => {
  test('/assets/1/edit 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/1'));
    await seedSession(page, adminUser);
    await page.goto('/assets/1/edit');
    await expect(page.getByText('编辑资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/inspections/1/edit 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/1'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/1/edit');
    await expect(page.getByText('编辑检验记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/workflow-form/ASSET_TRANSFER 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_TRANSFER');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });

  test('/safety-checklists/execute/1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/safety-checklists'));
    await seedSession(page, adminUser);
    await page.goto('/safety-checklists/execute/1');
    await expect(page.getByText('安全检查执行').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inventory/cycle-count 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/cycle-count'));
    await seedSession(page, adminUser);
    await page.goto('/inventory/cycle-count');
    await expect(page.getByText('循环盘点规则配置（ABC分类）').first()).toBeVisible({ timeout: 15_000 });
    expect(errors).toEqual([]);
  });
});

test.describe('Q128 流程表单/报表失败态独立', () => {
  test('/workflow-form/ASSET_CLEARANCE 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_CLEARANCE');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/workflow-form/ASSET_SCRAP 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_SCRAP');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/workflow-form/ASSET_COMPENSATION 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_COMPENSATION');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/inventory/abc-classification 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/abc'));
    await seedSession(page, adminUser);
    await page.goto('/inventory/abc-classification');
    await expect(page.getByText('ABC 分类管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/report-builder 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/reports'));
    await seedSession(page, adminUser);
    await page.goto('/report-builder');
    await expect(page.getByText('自定义报表构建器').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q129 配置页失败态独立', () => {
  test('/workflow-form/RETIREMENT 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/RETIREMENT');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/inspection-templates 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspection-templates'));
    await seedSession(page, adminUser);
    await page.goto('/inspection-templates');
    await expect(page.getByText('检验模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/risk-assessments 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/risk-assessments'));
    await seedSession(page, adminUser);
    await page.goto('/risk-assessments');
    await expect(page.getByText('风险矩阵').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/safety-checklists/config 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/safety-checklists'));
    await seedSession(page, adminUser);
    await page.goto('/safety-checklists/config');
    await expect(page.getByText('安全检查表模板').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/workflow-designer 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-designer');
    await expect(page.getByText('资产转移流程').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q130 列表失败态独立', () => {
  test('/profile 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/users/current'));
    await seedSession(page, adminUser);
    await page.goto('/profile');
    await expect(page.getByRole('heading').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/notifications 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/notifications'));
    await seedSession(page, adminUser);
    await page.goto('/notifications');
    await expect(page.getByText('通知中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/fault-codes 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/fault-codes'));
    await seedSession(page, adminUser);
    await page.goto('/fault-codes');
    await expect(page.getByText('故障代码管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/idle 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/idle'));
    await seedSession(page, adminUser);
    await page.goto('/idle');
    await expect(page.getByText('闲置资产管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/depreciation 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/depreciation'));
    await seedSession(page, adminUser);
    await page.goto('/depreciation');
    await expect(page.getByText('折旧管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q131 主数据失败态独立', () => {
  test('/licenses 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/licenses'));
    await seedSession(page, adminUser);
    await page.goto('/licenses');
    await expect(page.getByText('软件许可证管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/sam 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/sam/dashboard'));
    await seedSession(page, adminUser);
    await page.goto('/sam');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/manufacturers 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/manufacturers'));
    await seedSession(page, adminUser);
    await page.goto('/manufacturers');
    await expect(page.getByText('制造商管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/locations 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/locations'));
    await seedSession(page, adminUser);
    await page.goto('/locations');
    await expect(page.getByText('位置管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/asset-models 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/asset-models'));
    await seedSession(page, adminUser);
    await page.goto('/asset-models');
    await expect(page.getByText('资产模型管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q132 系统页失败态独立', () => {
  test('/system/users 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/users'));
    await seedSession(page, adminUser);
    await page.goto('/system/users');
    await expect(page.getByText('用户管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/system/roles 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/roles'));
    await seedSession(page, adminUser);
    await page.goto('/system/roles');
    await expect(page.getByText('角色管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/system/depts 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/depts/tree'));
    await seedSession(page, adminUser);
    await page.goto('/system/depts');
    await expect(page.getByText('部门管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/system/posts 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/posts'));
    await seedSession(page, adminUser);
    await page.goto('/system/posts');
    await expect(page.getByText('岗位管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/system/menus 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/menus/admin/tree'));
    await seedSession(page, adminUser);
    await page.goto('/system/menus');
    await expect(page.getByText('菜单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q133 字段/采购/维保失败态独立', () => {
  test('/system/custom-fields 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/system/custom-fields'));
    await seedSession(page, adminUser);
    await page.goto('/system/custom-fields');
    await expect(page.getByText('自定义字段管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/system/custom-fieldsets 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/system/custom-fieldsets'));
    await seedSession(page, adminUser);
    await page.goto('/system/custom-fieldsets');
    await expect(page.getByText('自定义字段集管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/purchase-orders 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/purchase-orders'));
    await seedSession(page, adminUser);
    await page.goto('/purchase-orders');
    await expect(page.getByText('采购订单管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/maintenance 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/maintenance'));
    await seedSession(page, adminUser);
    await page.goto('/maintenance');
    await expect(page.getByText('维保管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/maintenance/plans 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/maintenance/plans'));
    await seedSession(page, adminUser);
    await page.goto('/maintenance/plans');
    await expect(page.getByText('维保计划管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q134 设备/主数据失败态独立', () => {
  test('/equipment 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/equipment'));
    await seedSession(page, adminUser);
    await page.goto('/equipment');
    await expect(page.getByText('重要设备管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/vendors 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/vendors'));
    await seedSession(page, adminUser);
    await page.goto('/vendors');
    await expect(page.getByText('供应商管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/categories 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/categories/tree'));
    await seedSession(page, adminUser);
    await page.goto('/categories');
    await expect(page.getByText('资产分类管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/floorplans 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/floor-plans'));
    await seedSession(page, adminUser);
    await page.goto('/floorplans');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/gis 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/gis/assets'));
    await seedSession(page, adminUser);
    await page.goto('/gis');
    await expect(page.getByText('GIS 资产地图').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q135 列表失败态独立', () => {
  test('/energy 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/energy/dashboard'));
    await seedSession(page, adminUser);
    await page.goto('/energy');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/contracts 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/contracts'));
    await seedSession(page, adminUser);
    await page.goto('/contracts');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/reports 失败显示数据加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/reports/by-category'));
    await seedSession(page, adminUser);
    await page.goto('/reports');
    await expect(page.getByText('数据加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inventory 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/tasks'));
    await seedSession(page, adminUser);
    await page.goto('/inventory');
    await expect(page.getByText('盘点管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/assets 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/assets');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q136 列表失败态独立', () => {
  test('/disposals 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/list'));
    await seedSession(page, adminUser);
    await page.goto('/disposals');
    await expect(page.getByText('资产处置管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/approvals 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/list'));
    await seedSession(page, adminUser);
    await page.goto('/approvals');
    await expect(page.getByText('审批中心').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/retirement 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/list'));
    await seedSession(page, adminUser);
    await page.goto('/retirement');
    await expect(page.getByText('资产退役管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inspections 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/list'));
    await seedSession(page, adminUser);
    await page.goto('/inspections');
    await expect(page.getByText('检验/年检管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/borrows 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows'));
    await seedSession(page, adminUser);
    await page.goto('/borrows');
    await expect(page.getByText('资产借用').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q137 列表失败态独立', () => {
  test('/assignments 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments'));
    await seedSession(page, adminUser);
    await page.goto('/assignments');
    await expect(page.getByText('资产领用归还').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/intake 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders'));
    await seedSession(page, adminUser);
    await page.goto('/intake');
    await expect(page.getByText('入库验收').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/budgets 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets'));
    await seedSession(page, adminUser);
    await page.goto('/budgets');
    await expect(page.getByText('预算管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/insurances 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance'));
    await seedSession(page, adminUser);
    await page.goto('/insurances');
    await expect(page.getByText('保险管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/revaluations 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/revaluations'));
    await seedSession(page, adminUser);
    await page.goto('/revaluations');
    await expect(page.getByText('资产减值/重估').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q138 列表失败态独立', () => {
  test('/stocktaking-cycles 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/cycles'));
    await seedSession(page, adminUser);
    await page.goto('/stocktaking-cycles');
    await expect(page.getByText('循环盘点周期').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取盘点周期列表失败'))).toEqual([]);
  });

  test('/spare-parts 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts');
    await expect(page.getByText('备品备件管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inspection-records 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/list'));
    await seedSession(page, adminUser);
    await page.goto('/inspection-records');
    await expect(page.getByText('检验记录管理').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/safety-checklists/history 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/safety-checklists'));
    await seedSession(page, adminUser);
    await page.goto('/safety-checklists/history');
    await expect(page.getByText('安全检查历史').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/risk-matrix 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/risk-assessments/matrix'));
    await seedSession(page, adminUser);
    await page.goto('/risk-matrix');
    await expect(page.getByText('风险矩阵配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q139 列表/移动失败态独立', () => {
  test('/reports/scheduled 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/scheduled-reports'));
    await seedSession(page, adminUser);
    await page.goto('/reports/scheduled');
    await expect(page.getByText('定时报表配置').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/compensation 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets'));
    await seedSession(page, adminUser);
    await page.goto('/compensation');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/index 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/dashboard'));
    await seedSession(page, adminUser);
    await page.goto('/m/index');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/assets 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/work-orders 失败显示待办加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/work-orders'));
    await seedSession(page, adminUser);
    await page.goto('/m/work-orders');
    await expect(page.getByText('待办加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });
});

test.describe('Q140 移动/测试结果失败态独立', () => {
  test('/m/notifications 失败显示通知加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/notifications'));
    await seedSession(page, adminUser);
    await page.goto('/m/notifications');
    await expect(page.getByText('通知加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/scan 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/scan'));
    await seedSession(page, adminUser);
    await page.goto('/m/scan');
    await expect(page.getByText('查询资产').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/profile 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/users/current'));
    await seedSession(page, adminUser);
    await page.goto('/m/profile');
    await expect(page.getByText('系统管理员').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/test-results JSON 失败显示加载测试结果失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/test-reports/data.json', (apiRoute) =>
      apiRoute.fulfill({ status: 500, contentType: 'application/json', body: '{"error":"fail"}' }),
    );
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/test-results');
    await expect(page.getByText('加载测试结果失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('USER 访问 /inventory/smart-report/INV-002 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/inventory/smart-report/INV-002');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });
});

test.describe('Q141 USER 403 独立', () => {
  for (const path of [
    '/inspections/2/edit',
    '/inspections/2/upload',
    '/workorders/2/acceptance',
    '/risk-assessments/2/edit',
    '/inventory/scan/RFID-3',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q142 USER 403 独立', () => {
  for (const path of [
    '/assignments/3/edit',
    '/borrows/3/edit',
    '/inspections/3/edit',
    '/workorders/3/acceptance',
    '/safety-checklists/execute/4',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q143 USER 403 独立', () => {
  for (const path of [
    '/assignments/4/edit',
    '/borrows/4/edit',
    '/inspections/4/edit',
    '/inspections/3/upload',
    '/inventory/scan/RFID-4',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q144 详情失败态独立', () => {
  test('/inventory/smart-report 未指定任务 ID', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/inventory/smart-report');
    await expect(page.getByText('未指定盘点任务 ID').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inventory/smart-report/INV-001 非数组 summary 不崩溃', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', mockApi);
    await seedSession(page, adminUser);
    await page.goto('/inventory/smart-report/INV-001');
    await expect(page.getByText('盘点报告').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('Received NaN'))).toEqual([]);
  });

  test('/inspections/1/upload 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/1'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/1/upload');
    await expect(page.getByText('检验照片上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/workorders/1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/1'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/1');
    await expect(page.getByText('工单详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/m/stocktaking-tasks/2 失败显示获取任务失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/tasks/2'));
    await seedSession(page, adminUser);
    await page.goto('/m/stocktaking-tasks/2');
    await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取任务失败'))).toEqual([]);
  });
});

test.describe('Q145 USER 403 独立', () => {
  for (const path of [
    '/assignments/5/edit',
    '/borrows/5/edit',
    '/inspections/5/edit',
    '/inspections/4/upload',
    '/workorders/4/acceptance',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q146 USER 403 独立', () => {
  for (const path of [
    '/risk-assessments/3/edit',
    '/safety-checklists/execute/5',
    '/inventory/scan/RFID-5',
    '/inspections/5/upload',
    '/workorders/5/acceptance',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q147 详情/预览失败态独立', () => {
  test('/inventory/scan/RFID-1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/tasks/RFID-1'));
    await seedSession(page, adminUser);
    await page.goto('/inventory/scan/RFID-1');
    await expect(page.getByText(/RFID扫描任务|盘点/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inventory/tasks/1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/tasks/1'));
    await seedSession(page, adminUser);
    await page.goto('/inventory/tasks/1');
    await expect(page.getByText(/办公室盘点|盘点详情/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/disposals/1 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/1'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/1');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/workflows-v2 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflows-v2');
    await expect(page.getByTitle('流程定义 2').or(page.getByText('流程定义 2')).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/fixed-assets/workbenchv3 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/menus/current'));
    await seedSession(page, adminUser);
    await page.goto('/fixed-assets/workbenchv3');
    await expect(page.getByText('系统管理 V3 工作台').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q148 USER 403 独立', () => {
  for (const path of [
    '/assignments/6/edit',
    '/borrows/6/edit',
    '/inspections/6/edit',
    '/risk-assessments/4/edit',
    '/safety-checklists/execute/6',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q149 详情失败态独立', () => {
  test('/approvals/1 失败显示工单不存在或已删除', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/1'));
    await seedSession(page, adminUser);
    await page.goto('/approvals/1');
    await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/audit/1 失败显示未找到该审计记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs/1'));
    await seedSession(page, adminUser);
    await page.goto('/audit/1');
    await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/spare-parts/1 失败显示备件不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts/1'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/1');
    await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/insurances/1 失败显示未找到保险记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance/1'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/1');
    await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/budgets/1 失败显示未找到预算信息', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/1'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/1');
    await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q150 USER 403 独立', () => {
  for (const path of [
    '/inventory/scan/RFID-6',
    '/assignments/7/edit',
    '/borrows/7/edit',
    '/inspections/7/edit',
    '/inspections/6/upload',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q151 详情失败态独立', () => {
  test('/retirement/1 失败显示未找到该退役申请记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/1'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/1');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/intake/1 失败显示验收单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/1'));
    await seedSession(page, adminUser);
    await page.goto('/intake/1');
    await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/borrows/1 失败显示借用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/1'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/1');
    await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assignments/1 失败显示领用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/1'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/1');
    await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inspections/1 失败显示检验记录不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/1'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/1');
    await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q152 USER 403 独立', () => {
  for (const path of [
    '/workorders/6/acceptance',
    '/risk-assessments/5/edit',
    '/safety-checklists/execute/7',
    '/inventory/scan/RFID-7',
    '/inspections/7/upload',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q153 详情失败态独立', () => {
  test('/assets/1 失败显示获取数据失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/1'));
    await seedSession(page, adminUser);
    await page.goto('/assets/1');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/assets/1 失败显示资产加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/1'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets/1');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/workorders/1/acceptance 失败显示工单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/1'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/1/acceptance');
    await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/stocktaking-cycles/1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/cycles/1'));
    await seedSession(page, adminUser);
    await page.goto('/stocktaking-cycles/1');
    await expect(page.getByText('盘点周期不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取盘点周期'))).toEqual([]);
  });

  test('/compensation/1 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/compensation/1'));
    await seedSession(page, adminUser);
    await page.goto('/compensation/1');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q154 USER 403 独立', () => {
  for (const path of [
    '/assignments/8/edit',
    '/borrows/8/edit',
    '/inspections/8/edit',
    '/workorders/7/acceptance',
    '/risk-assessments/6/edit',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q155 失败态/403 独立', () => {
  test('/assets/1/timeline 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/1/history'));
    await seedSession(page, adminUser);
    await page.goto('/assets/1/timeline');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/stocktaking-tasks/3 失败显示获取任务失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/tasks/3'));
    await seedSession(page, adminUser);
    await page.goto('/m/stocktaking-tasks/3');
    await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取任务失败'))).toEqual([]);
  });

  test('USER 访问 /inventory/smart-report/INV-003 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/inventory/smart-report/INV-003');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /assignments/9/edit 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/assignments/9/edit');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });

  test('USER 访问 /borrows/9/edit 显示无访问权限', async ({ page }) => {
    await page.route('**/api/**', mockApi);
    await seedSession(page, limitedUser);
    await page.goto('/borrows/9/edit');
    await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
  });
});

test.describe('Q156 USER 403 独立', () => {
  for (const path of [
    '/inspections/9/edit',
    '/inspections/8/upload',
    '/workorders/8/acceptance',
    '/safety-checklists/execute/8',
    '/inventory/scan/RFID-8',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q157 USER 403 独立', () => {
  for (const path of [
    '/assignments/10/edit',
    '/borrows/10/edit',
    '/inspections/10/edit',
    '/risk-assessments/7/edit',
    '/inventory/smart-report/INV-004',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q158 流程表单失败态独立', () => {
  test('/workflow-form/ASSET_INTAKE 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_INTAKE');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/workflow-form/ASSET_BORROW 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_BORROW');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/workflow-form/ASSET_ASSIGNMENT 失败显示获取数据失败', async ({ page }) => {
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workflows'));
    await seedSession(page, adminUser);
    await page.goto('/workflow-form/ASSET_ASSIGNMENT');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
  });

  test('/assets/2/timeline 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/2/history'));
    await seedSession(page, adminUser);
    await page.goto('/assets/2/timeline');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/stocktaking-tasks/4 失败显示获取任务失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/tasks/4'));
    await seedSession(page, adminUser);
    await page.goto('/m/stocktaking-tasks/4');
    await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取任务失败'))).toEqual([]);
  });
});

test.describe('Q159 详情失败态独立', () => {
  test('/spare-parts/2 失败显示备件不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts/2'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/2');
    await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/insurances/2 失败显示未找到保险记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance/2'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/2');
    await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/budgets/2 失败显示未找到预算信息', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/2'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/2');
    await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/audit/2 失败显示未找到该审计记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs/2'));
    await seedSession(page, adminUser);
    await page.goto('/audit/2');
    await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/retirement/2 失败显示未找到该退役申请记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/2'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/2');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q160 USER 403 独立', () => {
  for (const path of [
    '/assignments/11/edit',
    '/borrows/11/edit',
    '/inspections/11/edit',
    '/workorders/10/acceptance',
    '/safety-checklists/execute/10',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q161 详情失败态独立', () => {
  test('/approvals/2 失败显示工单不存在或已删除', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/2'));
    await seedSession(page, adminUser);
    await page.goto('/approvals/2');
    await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/disposals/2 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/2'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/2');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/intake/2 失败显示验收单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/2'));
    await seedSession(page, adminUser);
    await page.goto('/intake/2');
    await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/borrows/2 失败显示借用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/2'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/2');
    await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assignments/2 失败显示领用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/2'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/2');
    await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q162 USER 403 独立', () => {
  for (const path of [
    '/inventory/scan/RFID-10',
    '/inspections/12/edit',
    '/inspections/10/upload',
    '/workorders/11/acceptance',
    '/risk-assessments/8/edit',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q163 详情失败态独立', () => {
  test('/inspections/2 失败显示检验记录不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/2'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/2');
    await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/spare-parts/3 失败显示备件不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts/3'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/3');
    await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/workorders/2/acceptance 失败显示工单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/2'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/2/acceptance');
    await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assets/3 失败显示获取数据失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/3'));
    await seedSession(page, adminUser);
    await page.goto('/assets/3');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/assets/3 失败显示资产加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/3'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets/3');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q164 USER 403 独立', () => {
  for (const path of [
    '/assignments/12/edit',
    '/borrows/12/edit',
    '/inspections/13/edit',
    '/inventory/scan/RFID-11',
    '/safety-checklists/execute/11',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q165 详情失败态独立', () => {
  test('/budgets/3 失败显示未找到预算信息', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/3'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/3');
    await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/insurances/3 失败显示未找到保险记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance/3'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/3');
    await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/audit/3 失败显示未找到该审计记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs/3'));
    await seedSession(page, adminUser);
    await page.goto('/audit/3');
    await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/retirement/3 失败显示未找到该退役申请记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/3'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/3');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/approvals/3 失败显示工单不存在或已删除', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/3'));
    await seedSession(page, adminUser);
    await page.goto('/approvals/3');
    await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q166 USER 403 独立', () => {
  for (const path of [
    '/assignments/13/edit',
    '/borrows/13/edit',
    '/inspections/11/upload',
    '/workorders/12/acceptance',
    '/inventory/smart-report/INV-005',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q167 详情失败态独立', () => {
  test('/disposals/3 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/3'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/3');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/intake/3 失败显示验收单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/3'));
    await seedSession(page, adminUser);
    await page.goto('/intake/3');
    await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/borrows/3 失败显示借用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/3'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/3');
    await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assignments/3 失败显示领用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/3'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/3');
    await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inspections/3 失败显示检验记录不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/3'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/3');
    await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q168 USER 403 独立', () => {
  for (const path of [
    '/assignments/14/edit',
    '/borrows/14/edit',
    '/inspections/14/edit',
    '/inventory/scan/RFID-12',
    '/safety-checklists/execute/12',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q169 详情失败态独立', () => {
  test('/workorders/3/acceptance 失败显示工单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/3'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/3/acceptance');
    await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assets/4 失败显示获取数据失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/4'));
    await seedSession(page, adminUser);
    await page.goto('/assets/4');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/assets/4 失败显示资产加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/4'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets/4');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/spare-parts/4 失败显示备件不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts/4'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/4');
    await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/budgets/4 失败显示未找到预算信息', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/4'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/4');
    await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q170 详情失败态独立', () => {
  test('/compensation/2 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/compensation/2'));
    await seedSession(page, adminUser);
    await page.goto('/compensation/2');
    await expect(page.getByText('资产赔偿申请').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/stocktaking-cycles/2 失败显示盘点周期不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/cycles/2'));
    await seedSession(page, adminUser);
    await page.goto('/stocktaking-cycles/2');
    await expect(page.getByText('盘点周期不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取盘点周期'))).toEqual([]);
  });

  test('/workorders/2 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/2'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/2');
    await expect(page.getByText('工单详情').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inspections/3/upload 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/3'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/3/upload');
    await expect(page.getByText('检验照片上传').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => item !== '获取数据失败')).toEqual([]);
  });

  test('/inventory/tasks/3 失败无 pageerror', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inventory/tasks/3'));
    await seedSession(page, adminUser);
    await page.goto('/inventory/tasks/3');
    await expect(page.getByText(/盘点详情|盘点/).first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q171 USER 403 独立', () => {
  for (const path of [
    '/assignments/15/edit',
    '/borrows/15/edit',
    '/inspections/15/edit',
    '/inventory/scan/RFID-13',
    '/safety-checklists/execute/13',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q172 详情失败态独立', () => {
  test('/insurances/4 失败显示未找到保险记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance/4'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/4');
    await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/audit/4 失败显示未找到该审计记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs/4'));
    await seedSession(page, adminUser);
    await page.goto('/audit/4');
    await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/retirement/4 失败显示未找到该退役申请记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/4'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/4');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/approvals/4 失败显示工单不存在或已删除', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/4'));
    await seedSession(page, adminUser);
    await page.goto('/approvals/4');
    await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/disposals/4 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/4'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/4');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q173 USER 403 独立', () => {
  for (const path of [
    '/assignments/16/edit',
    '/borrows/16/edit',
    '/inspections/16/edit',
    '/inventory/scan/RFID-14',
    '/safety-checklists/execute/14',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q174 详情失败态独立', () => {
  test('/intake/4 失败显示验收单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/4'));
    await seedSession(page, adminUser);
    await page.goto('/intake/4');
    await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/borrows/4 失败显示借用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/4'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/4');
    await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assignments/4 失败显示领用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/4'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/4');
    await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inspections/4 失败显示检验记录不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/4'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/4');
    await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/workorders/4/acceptance 失败显示工单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/4'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/4/acceptance');
    await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q175 USER 403 独立', () => {
  for (const path of [
    '/assignments/17/edit',
    '/borrows/17/edit',
    '/inspections/17/edit',
    '/inventory/scan/RFID-15',
    '/safety-checklists/execute/15',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q176 详情失败态独立', () => {
  test('/assets/5 失败显示获取数据失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assets/5'));
    await seedSession(page, adminUser);
    await page.goto('/assets/5');
    await expect(page.getByText('获取数据失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/assets/5 失败显示资产加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/mobile/assets/5'));
    await seedSession(page, adminUser);
    await page.goto('/m/assets/5');
    await expect(page.getByText('资产加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/spare-parts/5 失败显示备件不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/spare-parts/5'));
    await seedSession(page, adminUser);
    await page.goto('/spare-parts/5');
    await expect(page.getByText('备件不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/budgets/5 失败显示未找到预算信息', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/budgets/5'));
    await seedSession(page, adminUser);
    await page.goto('/budgets/5');
    await expect(page.getByText('未找到预算信息').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/insurances/5 失败显示未找到保险记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/insurance/5'));
    await seedSession(page, adminUser);
    await page.goto('/insurances/5');
    await expect(page.getByText('未找到保险记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q177 USER 403 独立', () => {
  for (const path of [
    '/assignments/18/edit',
    '/borrows/18/edit',
    '/inspections/18/edit',
    '/inventory/scan/RFID-16',
    '/safety-checklists/execute/16',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q178 详情失败态独立', () => {
  test('/audit/5 失败显示未找到该审计记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/audit-logs/5'));
    await seedSession(page, adminUser);
    await page.goto('/audit/5');
    await expect(page.getByText('未找到该审计记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/retirement/5 失败显示未找到该退役申请记录', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/5'));
    await seedSession(page, adminUser);
    await page.goto('/retirement/5');
    await expect(page.getByText('未找到该退役申请记录').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/approvals/5 失败显示工单不存在或已删除', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/approvals/5'));
    await seedSession(page, adminUser);
    await page.goto('/approvals/5');
    await expect(page.getByText('工单不存在或已删除').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/disposals/5 失败显示加载失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/retirement/5'));
    await seedSession(page, adminUser);
    await page.goto('/disposals/5');
    await expect(page.getByText('加载失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/intake/5 失败显示验收单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/intake-orders/5'));
    await seedSession(page, adminUser);
    await page.goto('/intake/5');
    await expect(page.getByText('验收单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });
});

test.describe('Q179 USER 403 独立', () => {
  for (const path of [
    '/assignments/19/edit',
    '/borrows/19/edit',
    '/inspections/19/edit',
    '/inventory/scan/RFID-17',
    '/safety-checklists/execute/17',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q180 详情失败态独立', () => {
  test('/borrows/5 失败显示借用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/borrows/5'));
    await seedSession(page, adminUser);
    await page.goto('/borrows/5');
    await expect(page.getByText('借用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/assignments/5 失败显示领用单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/assignments/5'));
    await seedSession(page, adminUser);
    await page.goto('/assignments/5');
    await expect(page.getByText('领用单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/inspections/5 失败显示检验记录不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/inspections/5'));
    await seedSession(page, adminUser);
    await page.goto('/inspections/5');
    await expect(page.getByText('检验记录不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/workorders/5/acceptance 失败显示工单不存在', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/workorders/5'));
    await seedSession(page, adminUser);
    await page.goto('/workorders/5/acceptance');
    await expect(page.getByText('工单不存在').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败'))).toEqual([]);
  });

  test('/m/stocktaking-tasks/5 失败显示获取任务失败', async ({ page }) => {
    const errors = collectBrowserErrors(page);
    await page.route('**/api/**', (apiRoute) => mockApiWithFailure(apiRoute, '/stocktaking/tasks/5'));
    await seedSession(page, adminUser);
    await page.goto('/m/stocktaking-tasks/5');
    await expect(page.getByText('获取任务失败').first()).toBeVisible({ timeout: 15_000 });
    expect(errors.filter((item) => !item.includes('获取数据失败') && !item.includes('获取任务失败'))).toEqual([]);
  });
});

test.describe('Q181 USER 403 独立', () => {
  for (const path of [
    '/assignments/20/edit',
    '/borrows/20/edit',
    '/inspections/20/edit',
    '/inventory/scan/RFID-18',
    '/safety-checklists/execute/18',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q182 USER 403 独立', () => {
  for (const path of [
    '/assignments/21/edit',
    '/workorders/13/acceptance',
    '/risk-assessments/9/edit',
    '/inspections/12/upload',
    '/inventory/smart-report/INV-006',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q183 USER 403 独立', () => {
  for (const path of [
    '/borrows/21/edit',
    '/inspections/21/edit',
    '/inventory/scan/RFID-19',
    '/safety-checklists/execute/19',
    '/workorders/14/acceptance',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q184 USER 403 独立', () => {
  for (const path of [
    '/assignments/22/edit',
    '/borrows/22/edit',
    '/inspections/22/edit',
    '/inventory/scan/RFID-20',
    '/safety-checklists/execute/20',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q185 USER 403 独立', () => {
  for (const path of [
    '/assignments/23/edit',
    '/borrows/23/edit',
    '/inspections/23/edit',
    '/inventory/scan/RFID-21',
    '/safety-checklists/execute/21',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q186 USER 403 独立', () => {
  for (const path of [
    '/assignments/24/edit',
    '/borrows/24/edit',
    '/inspections/24/edit',
    '/inventory/scan/RFID-22',
    '/safety-checklists/execute/22',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q187 USER 403 独立', () => {
  for (const path of [
    '/assignments/25/edit',
    '/borrows/25/edit',
    '/inspections/25/edit',
    '/inventory/scan/RFID-23',
    '/safety-checklists/execute/23',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q188 USER 403 独立', () => {
  for (const path of [
    '/assignments/26/edit',
    '/borrows/26/edit',
    '/inspections/26/edit',
    '/inventory/scan/RFID-24',
    '/safety-checklists/execute/24',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q189 USER 403 独立', () => {
  for (const path of [
    '/assignments/27/edit',
    '/borrows/27/edit',
    '/inspections/27/edit',
    '/inventory/scan/RFID-25',
    '/safety-checklists/execute/25',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q190 USER 403 独立', () => {
  for (const path of [
    '/assignments/28/edit',
    '/borrows/28/edit',
    '/inspections/28/edit',
    '/inventory/scan/RFID-26',
    '/safety-checklists/execute/26',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q191 USER 403 独立', () => {
  for (const path of [
    '/assignments/29/edit',
    '/borrows/29/edit',
    '/inspections/29/edit',
    '/inventory/scan/RFID-27',
    '/safety-checklists/execute/27',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q192 USER 403 独立', () => {
  for (const path of [
    '/assignments/30/edit',
    '/borrows/30/edit',
    '/inspections/30/edit',
    '/inventory/scan/RFID-28',
    '/safety-checklists/execute/28',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q193 USER 403 独立', () => {
  for (const path of [
    '/assignments/31/edit',
    '/borrows/31/edit',
    '/inspections/31/edit',
    '/inventory/scan/RFID-29',
    '/safety-checklists/execute/29',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q194 USER 403 独立', () => {
  for (const path of [
    '/assignments/32/edit',
    '/borrows/32/edit',
    '/inspections/32/edit',
    '/inventory/scan/RFID-30',
    '/safety-checklists/execute/30',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q195 USER 403 独立', () => {
  for (const path of [
    '/assignments/33/edit',
    '/borrows/33/edit',
    '/inspections/33/edit',
    '/inventory/scan/RFID-31',
    '/safety-checklists/execute/31',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q196 USER 403 独立', () => {
  for (const path of [
    '/assignments/34/edit',
    '/borrows/34/edit',
    '/inspections/34/edit',
    '/inventory/scan/RFID-32',
    '/safety-checklists/execute/32',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q197 USER 403 独立', () => {
  for (const path of [
    '/assignments/35/edit',
    '/borrows/35/edit',
    '/inspections/35/edit',
    '/inventory/scan/RFID-33',
    '/safety-checklists/execute/33',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q198 USER 403 独立', () => {
  for (const path of [
    '/assignments/36/edit',
    '/borrows/36/edit',
    '/inspections/36/edit',
    '/inventory/scan/RFID-34',
    '/safety-checklists/execute/34',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q199 USER 403 独立', () => {
  for (const path of [
    '/assignments/37/edit',
    '/borrows/37/edit',
    '/inspections/37/edit',
    '/inventory/scan/RFID-35',
    '/safety-checklists/execute/35',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q200 USER 403 独立', () => {
  for (const path of [
    '/assignments/38/edit',
    '/borrows/38/edit',
    '/inspections/38/edit',
    '/inventory/scan/RFID-36',
    '/safety-checklists/execute/36',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q201 USER 403 独立', () => {
  for (const path of [
    '/assignments/39/edit',
    '/borrows/39/edit',
    '/inspections/39/edit',
    '/inventory/scan/RFID-37',
    '/safety-checklists/execute/37',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q202 USER 403 独立', () => {
  for (const path of [
    '/assignments/40/edit',
    '/borrows/40/edit',
    '/inspections/40/edit',
    '/inventory/scan/RFID-38',
    '/safety-checklists/execute/38',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q203 USER 403 独立', () => {
  for (const path of [
    '/assignments/41/edit',
    '/borrows/41/edit',
    '/inspections/41/edit',
    '/inventory/scan/RFID-39',
    '/safety-checklists/execute/39',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q204 USER 403 独立', () => {
  for (const path of [
    '/assignments/42/edit',
    '/borrows/42/edit',
    '/inspections/42/edit',
    '/inventory/scan/RFID-40',
    '/safety-checklists/execute/40',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q205 USER 403 独立', () => {
  for (const path of [
    '/assignments/43/edit',
    '/borrows/43/edit',
    '/inspections/43/edit',
    '/inventory/scan/RFID-41',
    '/safety-checklists/execute/41',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q206 USER 403 独立', () => {
  for (const path of [
    '/assignments/44/edit',
    '/borrows/44/edit',
    '/inspections/44/edit',
    '/inventory/scan/RFID-42',
    '/safety-checklists/execute/42',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q207 USER 403 独立', () => {
  for (const path of [
    '/assignments/45/edit',
    '/borrows/45/edit',
    '/inspections/45/edit',
    '/inventory/scan/RFID-43',
    '/safety-checklists/execute/43',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q208 USER 403 独立', () => {
  for (const path of [
    '/assignments/46/edit',
    '/borrows/46/edit',
    '/inspections/46/edit',
    '/inventory/scan/RFID-44',
    '/safety-checklists/execute/44',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q209 USER 403 独立', () => {
  for (const path of [
    '/assignments/47/edit',
    '/borrows/47/edit',
    '/inspections/47/edit',
    '/inventory/scan/RFID-45',
    '/safety-checklists/execute/45',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q210 USER 403 独立', () => {
  for (const path of [
    '/assignments/48/edit',
    '/borrows/48/edit',
    '/inspections/48/edit',
    '/inventory/scan/RFID-46',
    '/safety-checklists/execute/46',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q211 USER 403 独立', () => {
  for (const path of [
    '/assignments/49/edit',
    '/borrows/49/edit',
    '/inspections/49/edit',
    '/inventory/scan/RFID-47',
    '/safety-checklists/execute/47',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q212 USER 403 独立', () => {
  for (const path of [
    '/assignments/50/edit',
    '/borrows/50/edit',
    '/inspections/50/edit',
    '/inventory/scan/RFID-48',
    '/safety-checklists/execute/48',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q213 USER 403 独立', () => {
  for (const path of [
    '/assignments/51/edit',
    '/borrows/51/edit',
    '/inspections/51/edit',
    '/inventory/scan/RFID-49',
    '/safety-checklists/execute/49',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q214 USER 403 独立', () => {
  for (const path of [
    '/assignments/52/edit',
    '/borrows/52/edit',
    '/inspections/52/edit',
    '/inventory/scan/RFID-50',
    '/safety-checklists/execute/50',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q215 USER 403 独立', () => {
  for (const path of [
    '/assignments/53/edit',
    '/borrows/53/edit',
    '/inspections/53/edit',
    '/inventory/scan/RFID-51',
    '/safety-checklists/execute/51',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q216 USER 403 独立', () => {
  for (const path of [
    '/assignments/54/edit',
    '/borrows/54/edit',
    '/inspections/54/edit',
    '/inventory/scan/RFID-52',
    '/safety-checklists/execute/52',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q217 USER 403 独立', () => {
  for (const path of [
    '/assignments/55/edit',
    '/borrows/55/edit',
    '/inspections/55/edit',
    '/inventory/scan/RFID-53',
    '/safety-checklists/execute/53',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q218 USER 403 独立', () => {
  for (const path of [
    '/assignments/56/edit',
    '/borrows/56/edit',
    '/inspections/56/edit',
    '/inventory/scan/RFID-54',
    '/safety-checklists/execute/54',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q219 USER 403 独立', () => {
  for (const path of [
    '/assignments/57/edit',
    '/borrows/57/edit',
    '/inspections/57/edit',
    '/inventory/scan/RFID-55',
    '/safety-checklists/execute/55',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q220 USER 403 独立', () => {
  for (const path of [
    '/assignments/58/edit',
    '/borrows/58/edit',
    '/inspections/58/edit',
    '/inventory/scan/RFID-56',
    '/safety-checklists/execute/56',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q221 USER 403 独立', () => {
  for (const path of [
    '/assignments/59/edit',
    '/borrows/59/edit',
    '/inspections/59/edit',
    '/inventory/scan/RFID-57',
    '/safety-checklists/execute/57',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q222 USER 403 独立', () => {
  for (const path of [
    '/assignments/60/edit',
    '/borrows/60/edit',
    '/inspections/60/edit',
    '/inventory/scan/RFID-58',
    '/safety-checklists/execute/58',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q223 USER 403 独立', () => {
  for (const path of [
    '/assignments/61/edit',
    '/borrows/61/edit',
    '/inspections/61/edit',
    '/inventory/scan/RFID-59',
    '/safety-checklists/execute/59',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q224 USER 403 独立', () => {
  for (const path of [
    '/assignments/62/edit',
    '/borrows/62/edit',
    '/inspections/62/edit',
    '/inventory/scan/RFID-60',
    '/safety-checklists/execute/60',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q225 USER 403 独立', () => {
  for (const path of [
    '/assignments/63/edit',
    '/borrows/63/edit',
    '/inspections/63/edit',
    '/inventory/scan/RFID-61',
    '/safety-checklists/execute/61',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q226 USER 403 独立', () => {
  for (const path of [
    '/assignments/64/edit',
    '/borrows/64/edit',
    '/inspections/64/edit',
    '/inventory/scan/RFID-62',
    '/safety-checklists/execute/62',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q227 USER 403 独立', () => {
  for (const path of [
    '/assignments/65/edit',
    '/borrows/65/edit',
    '/inspections/65/edit',
    '/inventory/scan/RFID-63',
    '/safety-checklists/execute/63',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q228 USER 403 独立', () => {
  for (const path of [
    '/assignments/66/edit',
    '/borrows/66/edit',
    '/inspections/66/edit',
    '/inventory/scan/RFID-64',
    '/safety-checklists/execute/64',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q229 USER 403 独立', () => {
  for (const path of [
    '/assignments/67/edit',
    '/borrows/67/edit',
    '/inspections/67/edit',
    '/inventory/scan/RFID-65',
    '/safety-checklists/execute/65',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q230 USER 403 独立', () => {
  for (const path of [
    '/assignments/68/edit',
    '/borrows/68/edit',
    '/inspections/68/edit',
    '/inventory/scan/RFID-66',
    '/safety-checklists/execute/66',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q231 USER 403 独立', () => {
  for (const path of [
    '/assignments/69/edit',
    '/borrows/69/edit',
    '/inspections/69/edit',
    '/inventory/scan/RFID-67',
    '/safety-checklists/execute/67',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q232 USER 403 独立', () => {
  for (const path of [
    '/assignments/70/edit',
    '/borrows/70/edit',
    '/inspections/70/edit',
    '/inventory/scan/RFID-68',
    '/safety-checklists/execute/68',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q233 USER 403 独立', () => {
  for (const path of [
    '/assignments/71/edit',
    '/borrows/71/edit',
    '/inspections/71/edit',
    '/inventory/scan/RFID-69',
    '/safety-checklists/execute/69',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q234 USER 403 独立', () => {
  for (const path of [
    '/assignments/72/edit',
    '/borrows/72/edit',
    '/inspections/72/edit',
    '/inventory/scan/RFID-70',
    '/safety-checklists/execute/70',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q235 USER 403 独立', () => {
  for (const path of [
    '/assignments/73/edit',
    '/borrows/73/edit',
    '/inspections/73/edit',
    '/inventory/scan/RFID-71',
    '/safety-checklists/execute/71',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q236 USER 403 独立', () => {
  for (const path of [
    '/assignments/74/edit',
    '/borrows/74/edit',
    '/inspections/74/edit',
    '/inventory/scan/RFID-72',
    '/safety-checklists/execute/72',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q237 USER 403 独立', () => {
  for (const path of [
    '/assignments/75/edit',
    '/borrows/75/edit',
    '/inspections/75/edit',
    '/inventory/scan/RFID-73',
    '/safety-checklists/execute/73',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q238 USER 403 独立', () => {
  for (const path of [
    '/assignments/76/edit',
    '/borrows/76/edit',
    '/inspections/76/edit',
    '/inventory/scan/RFID-74',
    '/safety-checklists/execute/74',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q239 USER 403 独立', () => {
  for (const path of [
    '/assignments/77/edit',
    '/borrows/77/edit',
    '/inspections/77/edit',
    '/inventory/scan/RFID-75',
    '/safety-checklists/execute/75',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q240 USER 403 独立', () => {
  for (const path of [
    '/assignments/78/edit',
    '/borrows/78/edit',
    '/inspections/78/edit',
    '/inventory/scan/RFID-76',
    '/safety-checklists/execute/76',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q241 USER 403 独立', () => {
  for (const path of [
    '/assignments/79/edit',
    '/borrows/79/edit',
    '/inspections/79/edit',
    '/inventory/scan/RFID-77',
    '/safety-checklists/execute/77',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q242 USER 403 独立', () => {
  for (const path of [
    '/assignments/80/edit',
    '/borrows/80/edit',
    '/inspections/80/edit',
    '/inventory/scan/RFID-78',
    '/safety-checklists/execute/78',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q243 USER 403 独立', () => {
  for (const path of [
    '/assignments/81/edit',
    '/borrows/81/edit',
    '/inspections/81/edit',
    '/inventory/scan/RFID-79',
    '/safety-checklists/execute/79',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q244 USER 403 独立', () => {
  for (const path of [
    '/assignments/82/edit',
    '/borrows/82/edit',
    '/inspections/82/edit',
    '/inventory/scan/RFID-80',
    '/safety-checklists/execute/80',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q245 USER 403 独立', () => {
  for (const path of [
    '/assignments/83/edit',
    '/borrows/83/edit',
    '/inspections/83/edit',
    '/inventory/scan/RFID-81',
    '/safety-checklists/execute/81',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q246 USER 403 独立', () => {
  for (const path of [
    '/assignments/84/edit',
    '/borrows/84/edit',
    '/inspections/84/edit',
    '/inventory/scan/RFID-82',
    '/safety-checklists/execute/82',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q247 USER 403 独立', () => {
  for (const path of [
    '/assignments/85/edit',
    '/borrows/85/edit',
    '/inspections/85/edit',
    '/inventory/scan/RFID-83',
    '/safety-checklists/execute/83',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q248 USER 403 独立', () => {
  for (const path of [
    '/assignments/86/edit',
    '/borrows/86/edit',
    '/inspections/86/edit',
    '/inventory/scan/RFID-84',
    '/safety-checklists/execute/84',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q249 USER 403 独立', () => {
  for (const path of [
    '/assignments/87/edit',
    '/borrows/87/edit',
    '/inspections/87/edit',
    '/inventory/scan/RFID-85',
    '/safety-checklists/execute/85',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q250 USER 403 独立', () => {
  for (const path of [
    '/assignments/88/edit',
    '/borrows/88/edit',
    '/inspections/88/edit',
    '/inventory/scan/RFID-86',
    '/safety-checklists/execute/86',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q251 USER 403 独立', () => {
  for (const path of [
    '/assignments/89/edit',
    '/borrows/89/edit',
    '/inspections/89/edit',
    '/inventory/scan/RFID-87',
    '/safety-checklists/execute/87',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q252 USER 403 独立', () => {
  for (const path of [
    '/assignments/90/edit',
    '/borrows/90/edit',
    '/inspections/90/edit',
    '/inventory/scan/RFID-88',
    '/safety-checklists/execute/88',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q253 USER 403 独立', () => {
  for (const path of [
    '/assignments/91/edit',
    '/borrows/91/edit',
    '/inspections/91/edit',
    '/inventory/scan/RFID-89',
    '/safety-checklists/execute/89',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q254 USER 403 独立', () => {
  for (const path of [
    '/assignments/92/edit',
    '/borrows/92/edit',
    '/inspections/92/edit',
    '/inventory/scan/RFID-90',
    '/safety-checklists/execute/90',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q255 USER 403 独立', () => {
  for (const path of [
    '/assignments/93/edit',
    '/borrows/93/edit',
    '/inspections/93/edit',
    '/inventory/scan/RFID-91',
    '/safety-checklists/execute/91',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q256 USER 403 独立', () => {
  for (const path of [
    '/assignments/94/edit',
    '/borrows/94/edit',
    '/inspections/94/edit',
    '/inventory/scan/RFID-92',
    '/safety-checklists/execute/92',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q257 USER 403 独立', () => {
  for (const path of [
    '/assignments/95/edit',
    '/borrows/95/edit',
    '/inspections/95/edit',
    '/inventory/scan/RFID-93',
    '/safety-checklists/execute/93',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q258 USER 403 独立', () => {
  for (const path of [
    '/assignments/96/edit',
    '/borrows/96/edit',
    '/inspections/96/edit',
    '/inventory/scan/RFID-94',
    '/safety-checklists/execute/94',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q259 USER 403 独立', () => {
  for (const path of [
    '/assignments/97/edit',
    '/borrows/97/edit',
    '/inspections/97/edit',
    '/inventory/scan/RFID-95',
    '/safety-checklists/execute/95',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q260 USER 403 独立', () => {
  for (const path of [
    '/assignments/98/edit',
    '/borrows/98/edit',
    '/inspections/98/edit',
    '/inventory/scan/RFID-96',
    '/safety-checklists/execute/96',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q261 USER 403 独立', () => {
  for (const path of [
    '/assignments/99/edit',
    '/borrows/99/edit',
    '/inspections/99/edit',
    '/inventory/scan/RFID-97',
    '/safety-checklists/execute/97',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q262 USER 403 独立', () => {
  for (const path of [
    '/assignments/100/edit',
    '/borrows/100/edit',
    '/inspections/100/edit',
    '/inventory/scan/RFID-98',
    '/safety-checklists/execute/98',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q263 USER 403 独立', () => {
  for (const path of [
    '/assignments/101/edit',
    '/borrows/101/edit',
    '/inspections/101/edit',
    '/inventory/scan/RFID-99',
    '/safety-checklists/execute/99',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q264 USER 403 独立', () => {
  for (const path of [
    '/assignments/102/edit',
    '/borrows/102/edit',
    '/inspections/102/edit',
    '/inventory/scan/RFID-100',
    '/safety-checklists/execute/100',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q265 USER 403 独立', () => {
  for (const path of [
    '/assignments/103/edit',
    '/borrows/103/edit',
    '/inspections/103/edit',
    '/inventory/scan/RFID-101',
    '/safety-checklists/execute/101',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q266 USER 403 独立', () => {
  for (const path of [
    '/assignments/104/edit',
    '/borrows/104/edit',
    '/inspections/104/edit',
    '/inventory/scan/RFID-102',
    '/safety-checklists/execute/102',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q267 USER 403 独立', () => {
  for (const path of [
    '/assignments/105/edit',
    '/borrows/105/edit',
    '/inspections/105/edit',
    '/inventory/scan/RFID-103',
    '/safety-checklists/execute/103',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q268 USER 403 独立', () => {
  for (const path of [
    '/assignments/106/edit',
    '/borrows/106/edit',
    '/inspections/106/edit',
    '/inventory/scan/RFID-104',
    '/safety-checklists/execute/104',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q269 USER 403 独立', () => {
  for (const path of [
    '/assignments/107/edit',
    '/borrows/107/edit',
    '/inspections/107/edit',
    '/inventory/scan/RFID-105',
    '/safety-checklists/execute/105',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q270 USER 403 独立', () => {
  for (const path of [
    '/assignments/108/edit',
    '/borrows/108/edit',
    '/inspections/108/edit',
    '/inventory/scan/RFID-106',
    '/safety-checklists/execute/106',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q271 USER 403 独立', () => {
  for (const path of [
    '/assignments/109/edit',
    '/borrows/109/edit',
    '/inspections/109/edit',
    '/inventory/scan/RFID-107',
    '/safety-checklists/execute/107',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q272 USER 403 独立', () => {
  for (const path of [
    '/assignments/110/edit',
    '/borrows/110/edit',
    '/inspections/110/edit',
    '/inventory/scan/RFID-108',
    '/safety-checklists/execute/108',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q273 USER 403 独立', () => {
  for (const path of [
    '/assignments/111/edit',
    '/borrows/111/edit',
    '/inspections/111/edit',
    '/inventory/scan/RFID-109',
    '/safety-checklists/execute/109',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q274 USER 403 独立', () => {
  for (const path of [
    '/assignments/112/edit',
    '/borrows/112/edit',
    '/inspections/112/edit',
    '/inventory/scan/RFID-110',
    '/safety-checklists/execute/110',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q275 USER 403 独立', () => {
  for (const path of [
    '/assignments/113/edit',
    '/borrows/113/edit',
    '/inspections/113/edit',
    '/inventory/scan/RFID-111',
    '/safety-checklists/execute/111',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q276 USER 403 独立', () => {
  for (const path of [
    '/assignments/114/edit',
    '/borrows/114/edit',
    '/inspections/114/edit',
    '/inventory/scan/RFID-112',
    '/safety-checklists/execute/112',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q277 USER 403 独立', () => {
  for (const path of [
    '/assignments/115/edit',
    '/borrows/115/edit',
    '/inspections/115/edit',
    '/inventory/scan/RFID-113',
    '/safety-checklists/execute/113',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q278 USER 403 独立', () => {
  for (const path of [
    '/assignments/116/edit',
    '/borrows/116/edit',
    '/inspections/116/edit',
    '/inventory/scan/RFID-114',
    '/safety-checklists/execute/114',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q279 USER 403 独立', () => {
  for (const path of [
    '/assignments/117/edit',
    '/borrows/117/edit',
    '/inspections/117/edit',
    '/inventory/scan/RFID-115',
    '/safety-checklists/execute/115',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q280 USER 403 独立', () => {
  for (const path of [
    '/assignments/118/edit',
    '/borrows/118/edit',
    '/inspections/118/edit',
    '/inventory/scan/RFID-116',
    '/safety-checklists/execute/116',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q281 USER 403 独立', () => {
  for (const path of [
    '/assignments/119/edit',
    '/borrows/119/edit',
    '/inspections/119/edit',
    '/inventory/scan/RFID-117',
    '/safety-checklists/execute/117',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q282 USER 403 独立', () => {
  for (const path of [
    '/assignments/120/edit',
    '/borrows/120/edit',
    '/inspections/120/edit',
    '/inventory/scan/RFID-118',
    '/safety-checklists/execute/118',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q283 USER 403 独立', () => {
  for (const path of [
    '/assignments/121/edit',
    '/borrows/121/edit',
    '/inspections/121/edit',
    '/inventory/scan/RFID-119',
    '/safety-checklists/execute/119',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q284 USER 403 独立', () => {
  for (const path of [
    '/assignments/122/edit',
    '/borrows/122/edit',
    '/inspections/122/edit',
    '/inventory/scan/RFID-120',
    '/safety-checklists/execute/120',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q285 USER 403 独立', () => {
  for (const path of [
    '/assignments/123/edit',
    '/borrows/123/edit',
    '/inspections/123/edit',
    '/inventory/scan/RFID-121',
    '/safety-checklists/execute/121',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

test.describe('Q286 USER 403 独立', () => {
  for (const path of [
    '/assignments/124/edit',
    '/borrows/124/edit',
    '/inspections/124/edit',
    '/inventory/scan/RFID-122',
    '/safety-checklists/execute/122',
  ]) {
    test(`USER 访问 ${path} 显示无访问权限`, async ({ page }) => {
      await page.route('**/api/**', mockApi);
      await seedSession(page, limitedUser);
      await page.goto(path);
      await expect(page.getByRole('heading', { name: '无访问权限' }).first()).toBeVisible({ timeout: 15_000 });
    });
  }
});

function collectBrowserErrors(page: Page) {
  const errors: string[] = [];
  const ignored = [
    'downloadable font: download failed',
    'Image corrupt or truncated.',
    'Failed to load resource',
    'ResizeObserver loop',
    'Encountered two children with the same key',
    '[antd:',
    'Error creating WebGL context',
    '[ErrorBoundary]',
  ];
  const shouldIgnore = (text: string) => ignored.some((message) => text.includes(message));
  page.on('console', (message) => {
    if (message.type() === 'error' && !shouldIgnore(message.text())) {
      errors.push(message.text());
    }
  });
  page.on('pageerror', (error) => {
    if (!shouldIgnore(error.message)) {
      errors.push(error.message);
    }
  });
  return errors;
}

async function seedSession(page: Page, user: AuthUser) {
  await page.addInitScript(({ authUser }) => {
    window.localStorage.setItem('auth_token', 'empty-error-smoke-token');
    window.localStorage.setItem('user_info', JSON.stringify(authUser));
    window.sessionStorage.setItem('auth_token', 'empty-error-smoke-token');
    window.sessionStorage.setItem('user_info', JSON.stringify(authUser));
  }, { authUser: user });
}

async function mockApiWithFailure(route: Route, failPath: string) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api/, '');
  if (path === failPath || path.startsWith(`${failPath}/`) || (failPath === '/categories/tree' && path.startsWith('/categories'))) {
    return fulfill(route, null, 500, '获取数据失败');
  }
  return mockApi(route);
}

async function mockNotificationPreferencesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/notification-preferences' || path === '/notification-preferences/') {
    return fulfill(route, []);
  }
  if (path === '/notification-preferences/meta') {
    return fulfill(route, { categories: [], channelTypes: [], nonGoals: [] });
  }
  return mockApi(route);
}

async function mockWebhookConfigsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/webhook-configs' || path === '/system/webhook-configs/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockNotificationSwitchesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/notification-switches/list' || path.startsWith('/notification-switches/biz-type/')) {
    return fulfill(route, []);
  }
  if (path === '/notification-switches/meta') {
    return fulfill(route, { bizTypes: [], events: [], channelTypes: [] });
  }
  return mockApi(route);
}

async function mockSystemInterfacesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/interfaces' || path === '/system/interfaces/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockSystemFieldMappingsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/field-mappings' || path === '/system/field-mappings/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockSystemSyncRulesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/sync-rules' || path === '/system/sync-rules/') {
    return fulfill(route, []);
  }
  if (path === '/system/sync-rules/queue/summary') {
    return fulfill(route, {
      pending: 0,
      running: 0,
      failed: 0,
      nextRetry: 0,
      queueConsumptionEnabled: false,
      mode: 'disabled',
    });
  }
  return mockApi(route);
}

async function mockCacheNamespacesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/cache/namespaces' || path === '/system/cache/namespaces/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockAuditLogsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/audit-logs/operator-ranking') {
    return fulfill(route, []);
  }
  if (path === '/audit-logs/trends') {
    return fulfill(route, { granularity: 'daily', data: [] });
  }
  if (path === '/audit-logs/action-type-distribution') {
    return fulfill(route, { distribution: [] });
  }
  if (path === '/audit-logs/meta') {
    return fulfill(route, { operationTypes: ['CREATE', 'UPDATE', 'DELETE', 'LOGIN'], resourceTypes: ['ASSET', 'WORKFLOW', 'SYSTEM_CONFIG'] });
  }
  if (path === '/audit-logs/stats') {
    return fulfill(route, { totalCount: 0 });
  }
  return mockApi(route);
}

async function mockWorkflowDefinitionsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/workflows' || path === '/workflows/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockExternalSystemsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/external-systems' || path === '/system/external-systems/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockApprovalRulesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/approval-rules' || path === '/approval-rules/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockRolePermissionsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/role-permissions/catalog') {
    return fulfill(route, {
      roles: [],
      permissions: [],
      summary: {
        roleCount: 0,
        permissionInventoryCount: 0,
        rolePermissionBindingCount: 0,
        boundPermissionCount: 0,
        unboundPermissionCount: 0,
        rolesWithoutPermissionsCount: 0,
      },
      riskTips: ['本页不支持分配/编辑/删除，不代表菜单权限或数据权限闭环'],
      readonlyNotice: '本页不支持分配/编辑/删除，不代表菜单权限或数据权限闭环',
    });
  }
  return mockApi(route);
}

async function mockTodoFieldsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/todo-fields' || path === '/todo-fields/') {
    return fulfill(route, []);
  }
  if (path === '/todo-fields/preview') {
    return fulfill(route, { maskedFields: [], visibleFields: [], totalVisible: 0 });
  }
  return mockApi(route);
}

async function mockFormStorageEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/form-storage' || path === '/form-storage/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockCommandCenterEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/workflows' || path === '/workflows/') {
    return fulfill(route, []);
  }
  return mockRuntimeMonitorEmpty(route);
}

async function mockFormDefinitionsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/form-definitions' || path === '/form-definitions/') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockFlowDesignerEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/workflows' || path === '/workflows/') {
    return fulfill(route, []);
  }
  if (path.endsWith('/versions')) {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockRuntimeMonitorEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/approvals/pending/count') {
    return fulfill(route, 0);
  }
  return mockSlaConfigEmptyCatalog(route);
}

async function mockDataPermissionsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/data-permissions/catalog') {
    return fulfill(route, {
      roles: [],
      summary: { roleCount: 0, allScopeCount: 0, restrictedScopeCount: 0, customScopeCount: 0 },
      riskTips: [],
      readOnlyNotice: '数据权限为只读 catalog。',
    });
  }
  return mockApi(route);
}

async function mockFileStorageCatalogEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/file-storage/attachments/catalog') {
    return fulfill(route, {
      attachments: [],
      summary: {
        totalAttachmentCount: 0,
        totalFileSize: 0,
        businessTypeCount: 0,
        fileTypeCount: 0,
        currentPageAttachmentCount: 0,
      },
      page: { page: 1, pageSize: 20, totalCount: 0, totalPages: 0 },
      businessTypes: [],
      fileTypes: [],
      riskTips: [
        '当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环。',
        '本页不包含导入/导出或执行动作，仅展示附件元数据。',
      ],
      readonlyNotice: '当前仅为 /system/file-storage/attachments/catalog 只读元数据目录，不支持上传/下载/预览/删除，不访问文件系统，不代表文件生命周期闭环。',
    });
  }
  return mockApi(route);
}

async function mockNotificationChannelsEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/channel-configs/meta') {
    return fulfill(route, { channelTypes: [] });
  }
  if (path === '/system/channel-configs') {
    return fulfill(route, { records: [], total: 0, size: 20, current: 1, pages: 0 });
  }
  return mockApi(route);
}

async function mockDeptsListEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/depts/list') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockLocationsListEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/locations/list' || path === '/locations/root') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockVendorsListEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/vendors/list') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockCustomFieldsEmptyCatalog(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/system/custom-fields/all') {
    return fulfill(route, []);
  }
  if (path === '/system/custom-fields/meta') {
    return fulfill(route, { fieldTypes: [], nonGoals: [] });
  }
  return mockApi(route);
}

async function mockSlaConfigEmptyCatalog(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/sla-config' || path === '/sla-config/') {
    return fulfill(route, []);
  }
  if (path === '/sla-config/runtime-summary') {
    return fulfill(route, {
      totalConfigs: 0,
      activeConfigs: 0,
      overdueCount: 0,
      warningCount: 0,
      criticalCount: 0,
      timeoutRecordCount: 0,
      exportMaskingNotice: '导出仅返回脱敏摘要。',
    });
  }
  if (path === '/sla-config/timeout-records') {
    return fulfill(route, []);
  }
  return mockApi(route);
}

async function mockNumberingRulesEmpty(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api(?:\/v1)?/, '');
  if (path === '/numbering-rules' || path === '/numbering-rules/') {
    return fulfill(route, []);
  }
  if (path === '/numbering-rules/meta') {
    return fulfill(route, {
      defaultRules: [],
      allowedVariables: [],
      tenantScoped: true,
      readOnly: true,
    });
  }
  if (path === '/numbering-rules/preview') {
    return fulfill(route, {
      previewValue: '',
      usedVariables: [],
      missingVariables: [],
      rejectedVariables: [],
      tenantScoped: true,
      noPersistence: true,
      noSequenceReserved: true,
      runtimeEffect: false,
      cacheRefreshed: false,
      sequenceAllocated: false,
      persistent: false,
    });
  }
  return fulfill(route, {
    ruleKey: 'numbering.rule.asset',
    name: '资产编号规则',
    template: 'AUTO-{YYYYMMDD}-{SEQ}',
    tenantScoped: true,
    readOnly: true,
  });
}

async function mockApi(route: Route) {
  const url = new URL(route.request().url());
  if (!url.pathname.startsWith('/api/')) {
    return route.fallback();
  }
  const path = url.pathname.replace(/^\/api/, '');

  if (path === '/auth/login' || path === '/user-management/current' || path === '/users/current') {
    return fulfill(route, { token: 'empty-error-smoke-token', ...adminUser });
  }
  if (path === '/menus/current') {
    return fulfill(route, { menus: [], permissions: [], roles: adminUser.roles });
  }
  if (path === '/mobile/dashboard') {
    return fulfill(route, {
      totalAssets: 0,
      inUseAssets: 0,
      idleAssets: 0,
      scrapAssets: 0,
      pendingWorkOrders: 0,
      unreadNotifications: 0,
    });
  }
  if (path === '/mobile/notifications') {
    return fulfill(route, []);
  }
  if (/^\/mobile\/assets\/\d+$/.test(path)) {
    if (path.endsWith('/999')) {
      return fulfill(route, null);
    }
    return fulfill(route, {
      id: 1,
      assetNo: 'E2E-ASSET',
      assetName: 'E2E资产',
      status: 'IN_USE',
    });
  }
  if (/^\/assets\/\d+$/.test(path)) {
    return fulfill(route, {
      id: 1,
      assetNo: 'E2E-ASSET',
      assetName: 'E2E资产',
      status: 'IN_USE',
    });
  }
  if (/^\/stocktaking\/cycles\/\d+$/.test(path)) {
    return fulfill(route, { id: 1, cycleName: 'E2E周期', status: 'PLANNED' });
  }
  if (/^\/stocktaking\/cycles\/\d+\/(tasks|stats)$/.test(path)) {
    return fulfill(route, path.endsWith('/tasks') ? [] : {});
  }
  if (/^\/spare-parts\/\d+\/usages$/.test(path)) {
    return fulfill(route, []);
  }
  if (/^\/spare-parts\/\d+$/.test(path)) {
    return fulfill(route, {
      id: 1,
      partName: 'E2E备件',
      partNo: 'SP-001',
      currentStock: 1,
      safetyStock: 0,
    });
  }
  if (/\/inspections\/\d+\/photos$/.test(path)) {
    return fulfill(route, []);
  }
  if (/\/inspections\/\d+$/.test(path)) {
    return fulfill(route, { id: 1, inspectionNo: 'INSP-001', status: 'PENDING' });
  }
  if (/\/inventory\/tasks\/[^/]+\/summary$/.test(path)) {
    return fulfill(route, {
      surplusCount: 0,
      deficitCount: 0,
      surplusItems: [],
      deficitItems: [],
    });
  }
  if (path.endsWith('/inventory/tasks/INV-001') || path.endsWith('/inventory/tasks/1') || path.endsWith('/inventory/tasks/RFID-1')) {
    return fulfill(route, {
      task: {
        id: path.endsWith('/RFID-1') ? 'RFID-1' : path.endsWith('/1') ? 1 : 'INV-001',
        taskName: path.endsWith('/RFID-1') ? 'RFID扫描任务' : path.endsWith('/1') ? '办公室盘点' : 'E2E盘点任务',
        status: 'IN_PROGRESS',
        totalCount: 1,
        matchedCount: 0,
        createTime: '2026-05-01T00:00:00',
        updateTime: '2026-05-01T00:00:00',
      },
    });
  }
  if (
    path === '/energy/dashboard'
    || path === '/reliability/summary'
    || path === '/gis/stats'
    || path === '/bigscreen/stats'
    || path === '/dashboard/stats'
  ) {
    return fulfill(route, {});
  }
  if (
    path === '/risk-assessments/matrix'
    || path === '/categories/tree'
    || path === '/contracts/expiring'
    || path === '/stocktaking/cycles'
    || path === '/menus/admin/tree'
    || path === '/fault-codes/tree'
    || path === '/gis/assets'
    || path === '/maintenance/upcoming'
    || path === '/menus/admin'
    || path === '/reliability/trend'
    || path === '/reliability/ranking'
    || path === '/asset-health/unhealthy'
    || path === '/asset-health/batch'
    || path === '/manufacturers/options'
    || path === '/system/custom-fieldsets/all'
    || path === '/categories/all'
    || path === '/depts/tree'
    || path === '/roles/all'
    || path === '/posts/all'
    || path === '/locations/tree'
    || path === '/locations/cascade'
    || /\/assets\/[^/]+\/history$/.test(path)
    || /\/assets\/\d+\/(depreciation-schedule|attachments)$/.test(path)
  ) {
    return fulfill(route, []);
  }
  return fulfill(route, { records: [], total: 0, size: 10, current: 1, pages: 1 });
}

async function fulfill(route: Route, data: unknown, code = 200, message = 'OK') {
  await route.fulfill({
    status: 200,
    contentType: 'application/json',
    body: JSON.stringify({ code, message, data }),
  });
}
